# 1.2 技术设计说明（2026-09-04，设计记录）

> 本文保留 1.2 细化前的调查与设计推导。正式可执行真相已经迁入
> [1.2 决策闭合实施合同](docs/development-plan/roadmap/1.2/IMPLEMENTATION_CONTRACT.md)、
> [执行指南](docs/development-plan/roadmap/1.2/EXECUTION_GUIDE.md) 与各节点独立规格；若有冲突，以正式执行包为准。
>
> 本文原本是三个新节点的设计输入：
> `r12-006-input-response-contract`、`r12-007-input-response-delivery`、`r12-045-flow-docx-fidelity`，
> 以及并入 `r12-005-flow-native-authoring-parity` 的 Flow 图形属性修复。
>
> 路线元数据（依赖、写锁、验收门、spec）见 [1.2 正式路线](docs/development-plan/roadmap/1.2/README.md)，本文不重复。
>
> 本文不是合同权威。节点实施时以届时的
> [架构合同](docs/development-plan/ARCHITECTURE_CONTRACT.md) 与正式 Schema 为准；
> 本文给出的形状若与届时源码冲突，按越界停止条件停止并回报 Owner。

---

## 1. 适用范围与已确认约束

| 编号 | 约束（Owner 已确认，不得重开） |
|---|---|
| D2 | 填空题走声明式，不走 Runtime |
| D3 | **不新增**"答案对错"条件类型，判题复用 `course-state.compare` |
| D8 | Native `input` 是第三个 V9 strict discriminator 窄例外，须按架构合同 §8 登记六要素 |
| D11 | **本轮 input 只支持 Slide**，静态导出只对应 PPTX；Flow / Spatial / global 禁止放置 input，由语义校验拒绝并定位；Flow 已支持的作者内容仍必须进入 DOCX |
| D13 | `r13-022` 排序走 Component 载体（本文不涉及，仅记录边界） |

**不改动的既有 wire**：`course-state.set`（`value` 保持必填，不加 `source`）、`INTERACTION_CONDITION_TYPES`、`INTERACTION_ACTION_TYPES`。

---

## 2. 填空题执行模型

### 2.1 为什么不能把"写入答案"做成一条动作

已核实 `src/player/interactions/PublishedInteractionController.ts` 的每个事件处理器（`#handleNodeClick`、`#handleAudioEnded`、`#handleVideoEvent`）都是同一结构：

```text
for (const rule of rules) {
    求 scene.in 条件 → 求 course-state 条件 → 全部通过才 #startRule(rule) 执行动作
}
```

**条件永远在动作之前求值**。若"写入答案"是一条动作，同一规则的条件必然读到旧值；跨规则依赖数组顺序也不可靠，因为动作步骤带 `delayMs`，可异步执行。

### 2.2 采用的模型：写入是触发器的固有语义

```text
input.submit 事件到达
  ① Surface 在真实提交事件中捕获 rawValue（事件主动携带，不回读 DOM）
  ② Controller 按该节点的 Published input 声明归一化
  ③ 在匹配任何规则之前，一次性原子写入本次提交所需的 course-state（值键 + 有效性键）
  ④ 才开始匹配本次事件的规则、求条件、执行动作
```

由此保证：

1. 条件比较的是**本次提交值**（写入发生在匹配之前）；
2. 异步 / `delayMs` 动作**不会**回读已变化的 DOM（值在 ①② 已固化进 state）；
3. 正确 / 错误分支互斥且穷尽（见 §4）；
4. **不新增**条件类型，**不改** `course-state.set`。

### 2.3 Surface Port 契约

**不增加**通用的 `readNodeInputValue()` 或任意 DOM 拉取能力。在 `PublishedInteractionSurfacePort` 增加与既有 `bindVideoEvent` 同构的绑定：

```text
bindInputSubmit(nodeId, listener(rawValue: string)): (() => void) | null
```

依据：既有 `bindVideoEvent(nodeId, kind, listener(seconds))` 就是"事件主动携带数据"的模式，Controller 只在回调里接收，不事后拉取。返回 disposer，与既有 `#disposers` 管理一致。绑定不可用时按既有模式产出 `bind-unavailable` 警告诊断。

**Controller 需要可信的节点描述**：`nodeId → { stateKey, validityKey, answerType, defaultValue }` 必须来自 **Published input 声明**（Published Course V2 payload 中该 layer item 的 input content），**不得**从 DOM `dataset` 或 UI 状态建立第二份业务真相。Controller 在构造期建立该映射，与 `#clickRules` / `#videoRules` 同批。

`input.submit` 的 target 必须解析到真实的 Native `input` 节点；不存在的节点、其他 Native 类型或 Component / Runtime 伪装出的同名 DOM 都按无效引用拒绝，不能触发输入框规则族。

### 2.4 不碰 `InteractionEngine`

已核实 `src/player/InteractionEngine.ts:157-160`：

```ts
case 'course-state.exists':
case 'course-state.compare':
  // Course Project V9 executes these through the Published V2 controller.
  return false
```

且 `new InteractionEngine(` 在 `src/` 下**零实例化**（仅测试使用）。作者态试运行复用 Published V2 Controller。因此**不得**为 input 在 `InteractionEngine` 再造第二套执行语义；本设计不修改该文件。

### 2.5 IME 与画布交互

1. **中文输入法组合期按 Enter 不得提交**：提交监听须在 `compositionstart` / `compositionend` 之间抑制 Enter；`isComposing` 为真时忽略。仅在组合结束后的 Enter 或显式提交按钮触发 `input.submit`。
2. 输入框获得焦点、输入与提交**不得**误触画布选择或拖动：提交事件在输入框元素上处理并阻止冒泡到画布手势层；作者态与运行态都要覆盖。
3. 键盘可达：Tab 可进入输入框，Enter 提交，Esc 放弃本次输入（不触发提交）。

### 2.6 `r12-007` 的 delivery 范围

```text
src/player/interactions/PublishedInteractionController.ts          # input.submit 支持 + 事件时快照 + 批量写入
src/player/interactions/PublishedInteractionSurfacePort.ts         # bindInputSubmit 契约
src/player/interactions/PublishedDomInteractionSurfacePort.ts      # DOM 侧实现
src/player/CourseStateStore.ts                                     # validate-all-then-commit setMany
src/player/surfaces/publishedDynamicHosts.ts                       # whole-course batch session port
src/player/surfaces/slide/publishedSlideInteractionSurfacePort.ts  # Slide 宿主接线
src/player/surfaces/slide/publishedNativeRendering.ts              # input 渲染 + 穷尽分支
src/player/surfaces/slide/SlidePublishedAdapter.ts                 # V9 → Published input 转换
src/renderer/ui/properties/SlideNativePropertiesPanel.tsx          # 输入框属性与答案配置 UI
src/renderer/ui/InteractionEditor.tsx                              # 规则族编译入口与识别
src/renderer/course/v9SlideContentCommands.ts                      # 创建 / 修改命令
src/renderer/store/slices/slideAuthoringSlice.ts
src/renderer/export/course/buildCoursePptx.ts                      # PPTX 可编辑对象映射
src/shared/courseProjectHealth/interaction.ts                      # 诊断
（作者画布的 Native input painter / mount 路径，按届时结构定位）
```

**注意**：`PublishedInteractionController.ts` 当前只支持 `node.click`、`audio.ended` 与三类 video 触发器，未列入者走 `unsupported-trigger` 诊断并跳过规则。`input.submit` 必须在此落实现，否则协议存在而运行时静默不执行。

---

## 3. 协议形状（`r12-006`）

### 3.1 Native `input` 分支

**只存输入特有数据**。已核实 `LayerItemBase`（`course-project-v9/types.ts:52-56`）持有 `layerItemId` 与 `frame`，因此 input content **不得**携带稳定 ID 或几何——重复持有会造第二份真相。

以下是早期字段草案；精确字段、范围和缺省已由正式实施合同 §5.1 固定：

| 字段 | 说明 |
|---|---|
| `answerType` | `'text'` \| `'number'` |
| `stateKey` | 写入归一化答案的 course-state 键；必须是已声明的键，类型与 `answerType` 匹配 |
| `validityKey` | 写入本次提交是否有效的 course-state 键；必须是已声明的 `boolean` 键 |
| `placeholder` | 提示文字（可选） |
| `ruleFamilyRuleIds` | 本输入框管理的规则 ID 列表（见 §4.3） |
| 视觉样式 | 输入框自身外观（边框、字号等），不含几何 |

**共享归一化函数**：已核实 `src/shared/assessmentEvaluators.ts:48` 的 `normalizeShortAnswer` **没有 `export`**。本节点须先把它抽成共享导出纯函数（保持现有行为不变，`evaluateAssessment` 继续复用同一实现），再供 Controller 使用。

### 3.2 双键设计（替代"非法值写 null"）

**为什么不能写 `null`**（两条都已核实）：

1. `CourseStateDeclaration`（`course-state/types.ts:3-7`）每个键固定**一个** `valueType`；声明为 `number` 的键写 `null` 会被 V9 语义校验判类型不匹配；
2. `publishedCourseState.ts:177-178` 的 `exists` 只判 `store.get(key) !== undefined`——写 `null` 后仍算"存在"，区分不出未答。

**采用**：每个输入框声明**两个**显式的 course-state 键。

| 键 | 类型 | 写入规则 |
|---|---|---|
| `stateKey` | 与 `answerType` 匹配（`string` 或 `number`） | 合法提交写归一化值；**非法或空提交写该键声明的 `defaultValue`**（不残留上一次的值） |
| `validityKey` | `boolean` | 合法提交写 `true`；非法或空提交写 `false` |

两个键在规则匹配前**一次性逻辑批量写入**，不产生中间态。

不允许使用未声明的隐藏键，不允许把状态私藏在属性 UI 中。两个键的声明由 UI 在创建输入框时**与节点、规则族同一笔作者事务**写入课程状态声明。

### 3.3 归一化语义

**`answerType: 'text'`**

```text
NFKC → trim → 空白折叠 → 小写      （即抽取后的 normalizeShortAnswer）
空字符串 → validity = false，stateKey 写 defaultValue
否则     → validity = true， stateKey 写归一化字符串
```

**`answerType: 'number'`**

```text
NFKC（覆盖全角数字、全角小数点、全角正负号、全角逗号视为非法）
→ trim
→ 解析为数字
→ 仅接受有限数（Number.isFinite）
```

| 输入 | 结果 |
|---|---|
| 空 / 仅空白 | `validity = false`，值键写 `defaultValue` |
| `NaN` / `Infinity` / `-Infinity` / 无法解析 | `validity = false`，值键写 `defaultValue` |
| `１２３`（全角） | NFKC 后为 `123` → `validity = true`，值键 `123`（**number**，不是字符串） |
| `１．５`（全角小数点） | NFKC 后为 `1.5` → `1.5` |
| `+3` / `-3` / `03` | `3` / `-3` / `3` |
| `1e2` | `100`（与 `100` 写入同一 number） |
| `2.00` | `2`（数值相等，不残留字符串形式） |
| 千分位 `1,000` | 视为非法（`validity = false`）——避免与小数点分隔符的地区差异冲突；若需支持须单独立项 |

**容差不进协议**：由可见 UI 编译成 `gte` / `lte` 条件（见 §4.1）。

### 3.4 悬空引用校验

`stateKey` 与 `validityKey` 必须进入 V9 与 Published 的语义悬空引用校验：

- 键未在课程状态声明中存在 → 诊断定位到该 input 的 `layerItemId` 与字段路径；
- 键存在但类型与 `answerType` 不匹配 → 同上；
- 键被删除后仍被 input 引用 → 同上；
- `ruleFamilyRuleIds` 中的规则 ID 不存在 → 诊断（见 §4.3 的族完整性）。

### 3.5 合同节点必须让全仓可编译

已核实 `nativeType` 出现在 **61 个文件**，其中 `src/player/surfaces/slide/publishedNativeRendering.ts` 与 `src/shared/contracts/course-project-v9/schema.ts` 含穷尽 switch / `never` 检查。新增判别分支会使它们编译失败。

因此 `r12-006` **必须在本节点内**：

1. 让全仓 `npm run typecheck` 通过；
2. 尚未交付的路径（渲染、导出、诊断）显式 **fail loud / unsupported**，不静默降级、不返回空、不当作其他类型处理；
3. 不把编译错误留到 `r12-007`。

### 3.6 Surface 范围限制（D11）

`input` 只允许出现在 Slide scene。Flow / Spatial / global 出现 input 时：

- 由 V9 / Published 语义校验或健康检查**明确拒绝**并定位到 `layerItemId` 与所在 Surface；
- **不得**仅靠"UI 暂时没有按钮"形成隐性限制；
- Published 构建遇到越界 input 时 fail loud，不静默丢弃。

将来若支持 Flow input，再单独定义其 DOCX 映射（本轮不承诺）。

---

## 4. 规则族（canonical rule family）

### 4.1 为什么不能是"正确 / 错误两条规则"

已核实 `src/shared/contracts/interaction-v1/types.ts:88` 注释：

> Different conditions are ANDed. State ids inside one condition are ORed.

条件之间只有 AND，没有 OR。因此 `gte(min) ∧ lte(max)` 的否定 `lt(min) ∨ gt(max)` 塞不进一条规则；多个可接受文本答案同样是 OR 问题。

### 4.2 族的形状

**数值（含容差区间 [min, max]）—— 四条规则**

| 语义分支 | 条件（全部 AND） |
|---|---|
| 非法 / 未答 | `validityKey == false` |
| 正确 | `validityKey == true` ∧ `stateKey >= min` ∧ `stateKey <= max` |
| 过小 | `validityKey == true` ∧ `stateKey < min` |
| 过大 | `validityKey == true` ∧ `stateKey > max` |

精确值答案是 `min == max` 的特例（`gte` ∧ `lte`）。

**文本（可接受答案 A₁…Aₙ，均已归一化）—— n + 2 条规则**

| 语义分支 | 条件（全部 AND） |
|---|---|
| 未答 | `validityKey == false` |
| 正确 ᵢ | `validityKey == true` ∧ `stateKey == Aᵢ` （i = 1…n） |
| 错误 | `validityKey == true` ∧ `stateKey != A₁` ∧ … ∧ `stateKey != Aₙ` |

错误分支用德摩根律把 `¬(A₁ ∨ … ∨ Aₙ)` 展开为 AND 串，符合"条件只 AND"。

当前单条规则最多 16 个条件；错误分支还要占用一个 `validityKey == true`，所以文本可接受答案当前最多 **15 个**。UI 在超过上限时拒绝保存并说明原因，不拆成会重复命中的多条错误规则。

**互斥与穷尽的强制条件**：

- 每个"数值"分支必须带 `validityKey == true` 前提（否则会读到 `defaultValue` 而误判）；
- UI 必须校验 A₁…Aₙ **归一化后两两不同**，否则会有两条正确规则同时命中——重复答案在保存前被拒并定位；
- 区间必须 `min <= max`，否则拒绝。

教师界面只配置“正确反馈”和“错误反馈”两类。未答、格式非法、普通错误、过小和过大都复用教师配置的同一错误反馈；规则族内部可以分成多条互斥规则，但不得因此增加第三类反馈配置。

必须有测试证明：任意一次提交**恰好**命中一个语义分支，不重复执行、不双双落空（含未答与格式非法场景）。

### 4.3 族的识别与教师手改的保护

**不得**通过显示名称猜测族成员。

- 输入框 content 的 `ruleFamilyRuleIds` 保存它管理的规则 ID 列表；UI **只**读写列表内的规则；
- 教师在专业规则编辑器中自建的规则不在列表内，**永不**被 UI 覆盖或删除；
- 保存重开后 UI 按该列表恢复对族的管理；
- **族完整性诊断**：列表内规则缺失、条件形状与预期不符（教师手工改过）、或出现列表外规则也以同一 `input.submit` 触发同一节点时，健康检查产出可定位诊断。UI 显示"该输入框的判题规则已被手工修改"，提供"保留手改"或"按当前配置重建"两个显式选择，**不静默覆盖**。

### 4.4 作者事务边界

以下改动是**一笔**作者事务（一次撤销完整回滚）：

- 创建输入框：节点 + 两个 course-state 声明 + 规则族；
- 修改答案 / 容差：族内规则的条件重编译；
- 切换答案类型：节点类型字段 + 两个键的声明 / 默认值 + 整个规则族同步替换；
- 复制输入框：复制节点视觉与题目配置，但为新副本生成独立的节点 ID、两个状态键和完整规则族；
- 删除输入框：节点 + 族 + （若无其他消费者）状态声明。

以上操作都必须原子完成；失败时不得留下旧类型键、共享规则族、悬空规则或只复制外观的半成品。

**历史边界（重要）**：

- "一次手势一笔历史事务"只针对**教师创作操作**；
- **学生提交产生的是运行期 session state，不进入编辑器 Undo / Redo**；
- 运行态的 course-state 变化由 Player session 持有，与作者历史无关。

---

## 5. Flow DOCX 转换矩阵（`r12-045`）

### 5.1 现状与缺陷

| 事实 | 位置 |
|---|---|
| 浮层被共享常量关闭 | `src/renderer/export/course/flowPrintPlan.ts:14` `FLOW_PRINT_INCLUDES_FLOATING_LAYERS = false as const` |
| DOCX 输出省略提示 | `flowDocx.ts:308` `DOCX 采用正文重排，已省略 N 个页面浮层。` |
| 打印计划同样省略 | `flowPrintPlan.ts:167` |

1.2 的 `r12-005` 让教师在 Flow 中创作原生文字 / 图片 / 图形浮层，创作完导出讲义即丢失，构成内容丢失与产品自相矛盾。

**关键约束**：`flowPrintPlan` 同时服务打印 / PDF / DOCX。**不得**把该常量直接改 `true`——那会未经决定地改变 PDF。本节点必须建立 **DOCX 专用投影**，或对共享投影引入按目标载体分别取值的显式参数；任何 PDF 行为改变需单独立项。

### 5.2 转换矩阵（覆盖当前及 1.2 将出现的全部类型）

| 内容 | DOCX 结果 |
|---|---|
| Native text 浮层 | **可编辑文本框**（anchored drawing + text box）。硬验收 |
| Native shape 浮层 —— 矩形、线条 | **可编辑 Word 图形**（DrawingML preset geometry）。**硬验收** |
| Native shape 浮层 —— 其他类型 | 能映射到 DrawingML preset 的按可编辑图形输出；不能映射的用确定性静态表示（矢量转图片）+ 诊断说明原类型 |
| Native image 浮层 | **带定位信息的 Word 图片**（anchored，保留相对位置与尺寸） |
| Native formula 浮层 | 优先 Word OMML 语义表示；不可行时确定性静态表示（图片 + `accessibleText` 作为替代文字）。**不得消失** |
| Native video 浮层 | 有 poster / staticFallback → 用该帧图片 + 诊断说明"静态帧"；无 → 可见占位框 + 精确诊断 |
| Native table | **本轮 Flow 禁止**；由语义校验和导出 preflight 拒绝并定位 |
| Native chart | **本轮 Flow 禁止**；由语义校验和导出 preflight 拒绝并定位 |
| **Native input** | **本轮 Flow 禁止**（D11）。矩阵结果为"由语义校验拒绝并定位"，不是遗漏 |
| ComponentLayerItem | 有 `staticFallback` → 用该资产 + 诊断；无 → 可见占位框 + 精确诊断（含 package 身份） |
| RuntimeLayerItem | 同上（含 runtime 身份） |
| teacher-controller | **按 `includeInStaticExports` 既有语义**：为 `false` 时排除（与 `buildCoursePptx.ts:293`、`buildCoursePrintArtifacts.ts:262`、`SlidePublishedAdapter.ts:1313` 一致）；作者显式允许静态导出时保留并按 Native 规则输出。**不得一概排除** |
| Flow / global 背景 | 优先 Word 页面 / 节背景或首段落底纹；Word 表达不了的（渐变、多层、图片平铺）用确定性近似 + 诊断说明差异 |
| 运行时目录 UI 等会话界面 | 排除，但诊断必须区分"有意排除的会话界面"与"能力不足省略"，且不与作者创作内容混淆 |

Table / Chart 的正式有效域已经固定为 Slide scene / Slide surface；Flow 中与 input 一样属于明确拒绝，不再留给实施者选择。

### 5.3 Flow 合成合同（七条，必须写成可执行规则）

已核实 `flowDocx.ts:340-345` 的 `buildFlowDocx(surface: PublishedFlowSurface, options)` **只接收单个 surface**，而全局浮层与 location 作用域信息不在 surface 级。要求"以实际 Published Flow 合成结果为准"，就必须扩大输入并把下列规则固定下来：

| # | 规则 | 设计建议（实施时按实际 Published 结构确认） |
|---|---|---|
| 1 | Flow 如何形成 Word 文档 | **一个 Published Flow Surface 对应一份连续 Word 文档**；按该 Surface 的 block / location 声明顺序连续输出，不把每个 `flow-block` location 误建成独立 Word section |
| 2 | location-scoped / global 浮层如何过滤 | location-scoped 只在其所属位置出现一次；global 浮层按 Published 可见性解析，但普通作者浮层在整份文档中只落一次，不因多个 location 可见而复制 |
| 3 | 何时允许重复 | 正式合同已固定唯一例外：global teacher-controller 同时满足 `visibility.mode='all'` 与 `includeInStaticExports=true` 时进入 footer；其余 global / viewport-pinned 内容只出现一次 |
| 4 | paper 定位锚到哪里 | 锚到连续文档中垂直位置最接近的 block 段落，用 anchored drawing + 相对偏移；空 Flow 锚到文档首段 |
| 5 | viewport-pinned 内容如何确定性落位 | 满足第 3 条 teacher-controller 例外时进入 footer；否则映射到文档首锚点并只出现一次。诊断说明由视口定位转为文档定位 |
| 6 | 层级映射 | surface underlay → 正文 → surface overlay → global overlay 的顺序映射为 Word drawing 的 `behindDoc` 与 `relativeHeight`：underlay 用 `behindDoc=true`；overlay 用 `behindDoc=false` 且 `relativeHeight` 递增，global overlay 最高 |
| 7 | 诊断字段 | 每条至少含 `layerItemId`、`scope`（`surface` \| `global`）、`locationId`、`fieldPath`、`disposition`（`preserved` \| `editable-shape` \| `image` \| `static-fallback` \| `excluded`）与 `reason`。**不得只报告数量** |

**实现范围（必查 / 必改，不是"仅当签名变化"）**：

```text
src/renderer/export/course/flowDocx.ts
src/renderer/export/course/flowPrintPlan.ts            # DOCX 专用投影或按载体取值的显式参数
src/renderer/export/course/buildCoursePrintArtifacts.ts
src/renderer/app/useCourseDelivery.ts                  # 调用点需传入更完整的 Published 输入
（必要的 Published Flow composition / helper）
```

### 5.4 验收

1. §5.2 矩阵逐项验证，**无一类静默消失**；
2. **真实 DOCX 验收分层**：1.2 engineering candidate 必须解包验证 editable DrawingML carrier，并在环境已有 Word/LibreOffice 时实际打开；在 Word 中选择、修改并重新保存文本框与图形属于 S1 Owner accepted 的人工证据，不阻断 1.2 rc，也不得由自动化虚假宣称已证明；
3. §5.3 七条规则各有确定性用例，含多 location 的连续文档、普通 global 浮层只出现一次、符合唯一例外的 teacher-controller 通过 footer 重复、空 Flow、viewport-pinned 与层级交叠；
4. 诊断字段齐全，可定位到单个 `layerItemId`；
5. **PDF / 打印不变**：使用固定 fixture 做针对性视觉 / 结构行为比较（不强制字节完全相等——PDF 元数据可能带时间戳等可变字段）。若确需改变 PDF，单独立项并经 Owner 批准；
6. 无 fallback 的 Component / Runtime / 视频在 DOCX 中有**可见**占位且诊断精确。

**越界停止条件**：不得把 `FLOW_PRINT_INCLUDES_FLOATING_LAYERS` 直接改 `true`；不得未经批准改变 PDF / 打印；不得用"已省略 N 个浮层"的提示文字充当交付；不得只做渲染器视觉检查而跳过 Word 内实际编辑验证。

---

## 6. Flow 图形属性修复（并入 `r12-005`）

### 6.1 现状

| 事实 | 位置 |
|---|---|
| Flow 能插入图形 | `src/renderer/composition/crossSurfaceCommands.ts:679` `flow: () => ports.flow.addShapeNode(...)`；`src/renderer/course/flowSharedAuthoringAdapters.ts:610` 调共享 factory |
| 共享 factory 已带默认样式 | `src/renderer/project/nativeNodeFactories.ts:338` `createShapeNode()` |
| Flow 属性面板漏掉图形 | `src/renderer/ui/properties/FlowPropertiesPanel.tsx` 的 `FlowOverlayProperties` 只分支 formula / image / video / component 后 `return null` |
| Slide 侧已有完整实现 | `src/renderer/ui/properties/SlideNativePropertiesPanel.tsx:687` `ShapeProperties` |

因共享 factory 已提供默认填充，**不得**用"给 Flow 再造一套默认样式"掩盖问题。

### 6.2 设计要求

1. **抽取共享 Shape 属性编辑器**：把 `SlideNativePropertiesPanel.tsx:687` 的 `ShapeProperties` 提升为共享组件（落在 `props-shared` 锁覆盖的目录），Slide 与 Flow 各自通过 Surface adapter 提供读写回调。**不复制**一份到 Flow；
2. **Flow 侧 canonical shape patch command**：经 `authoring-flow` 的既有 adapter 写回，每次属性手势一笔历史事务；
3. **同时检查编辑画布的 shape 渲染映射**：共享 factory 已带填充色，若 Flow 画布仍只显示轮廓，说明除属性面板外还有渲染或转换缺陷，必须一并定位——只修面板不算完成；
4. **不建立** Flow 专属的第二套图形样式真相。

### 6.3 验收

1. 至少矩形与线条：图形类型、填充色 / 透明度、边框色 / 透明度 / 宽度 / 线型、圆角、适用箭头属性均可编辑；
2. 修改**即时显示**在编辑画布（同时证伪渲染缺陷）；
3. 撤销 / 重做精确恢复；每次属性手势一笔事务；
4. 保存重开后样式不变；
5. Player 与单 HTML 呈现一致；
6. **新增直接渲染 Flow Shape 属性控件的单元 / 集成测试**——现有 `flowOverlayTransform.test.ts` / `flowUnifiedLayers.test.tsx` 只覆盖变换与图层，不足以证明属性面板可用；
7. **不包含 DOCX**——Shape 的 DOCX 结果由 `r12-045` 在其矩阵中验收（避免与 `r12-045 → r12-005` 依赖成环）。

---

## 7. 验证入口汇总

全部为**现存**文件（校验器会逐条验证路径存在）。

**`r12-006`**

```text
npm run check:contracts
npm run typecheck
npm run test:product -- tests/unit/interactionSchema.test.ts tests/unit/interactionDiscriminatorContract.test.ts
npm run test:product -- tests/unit/assessmentEvaluators.test.ts tests/unit/courseStateStore.test.ts
npm run test:product -- tests/unit/courseProjectCoreContract.test.ts tests/unit/courseProjectRoundTrip.test.ts
```

**`r12-007`**

```text
npm run test:product -- tests/unit/v9SlideContentCommands.test.ts tests/unit/interactionAuthoringCommands.test.ts
npm run test:product -- tests/unit/publishedInteractionController.test.ts tests/unit/publishedCourseState.test.ts
npm run test:product -- tests/unit/courseProjectHealth.test.ts tests/unit/coursePptxExport.test.ts
npm run test:e2e -- tests/e2e/stabilizationCoreUsability.spec.ts
```

**`r12-045`**

```text
npm run test:product -- tests/unit/coursePrintArtifacts.test.ts tests/unit/courseProjectHealth.test.ts
npm run test:product -- tests/integration/courseExportPreflightApp.test.tsx
```

**`r12-005`（含图形修复）**

```text
npm run test:product -- tests/unit/flowOverlayTransform.test.ts tests/unit/flowUnifiedLayers.test.tsx
npm run test:product -- tests/unit/editorTransaction.test.ts
npm run test:e2e -- tests/e2e/stabilizationFlowAuthoring.spec.ts
```

若确需新测试文件，先在该节点的实质 diff 中创建文件并同步更新路线，不得预先把未创建路径写成可执行入口。

**能力索引**：由 `r12-007` 独占 `generated-index` 写锁，在 delivery 完成后运行一次 `npm run generate:ai-capabilities` 并提交生成结果，再使 `npm run check:ai-capabilities` 通过；`r12-006` 不并行生成同一索引。另有一个既有缺陷需在本批修复：能力索引的 `sourceEvidence()` 必须纳入 `src/shared/contracts/**`，确保 interaction 合同变化反映到来源摘要，不接受长期靠人工记得。

---

## 8. 共同越界停止条件

命中任一条立即停止并回报 Owner，不自行扩权：

- 需要修改 V9 既有字段语义、创建 V10、放宽任何 `.strict()` 解析；
- 需要新增第二 Store / History / Session / writer；
- 需要新增交互**条件**类型，或修改 `course-state.set`；
- 需要在 `InteractionEngine` 实现 course-state 执行语义；
- 需要为 input 使用未声明的隐藏 state 键，或把答案配置私藏在属性 UI；
- 需要把 `FLOW_PRINT_INCLUDES_FLOATING_LAYERS` 直接改 `true`，或改变 PDF / 打印结果；
- 需要允许 Slide surface / Flow / Spatial / global 放置 input（D11）；
- 需要为 Flow 建第二套图形样式真相；
- 发现 `input` 无法作为 V9 严格分支表达，或事件时快照语义无法在不改 `course-state.set` 的前提下落地 —— 停止节点，先建立独立追加合同任务。
