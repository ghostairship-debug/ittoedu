# 开发路线优化方案（2026-09-04，第 3 版）

> **状态更新（2026-09-04）**：本方案的路线调整已经进入正式路线；本文现为历史决策记录，不再是执行入口。1.2 后续细化发现 Line、Background 与 input session 必须覆盖额外正式 Owner，因此本文 §5.3 的“四路并行”假设已被正式写锁取代。当前执行只看 [1.2 正式执行包](docs/development-plan/roadmap/1.2/README.md)，其校验结果固定首个安全 frontier 为 `r12-000-native-contract` + `r12-005-flow-native-authoring-parity`。
>
> **本文只改路线元数据**：节点增删、依赖、写锁、验收语义、发布门。
> 1.2 三个新节点的协议与交付设计另见 [1.2 技术设计说明](1.2-技术设计说明-2026-09-04.md)，本文不重复。
>
> 本文是待执行的修改方案，不是新的路线权威。执行完成后权威仍只是
> [开发总纲](COURSEWARE_DEVELOPMENT_PLAN.md)、[架构合同](docs/development-plan/ARCHITECTURE_CONTRACT.md)、
> [工作协议](docs/development-plan/WORKING_PROTOCOL.md)、[路线目录](docs/development-plan/roadmap/README.md)、
> [任务板](docs/development-plan/TASK_BOARD.md)；本文届时移出正文，由 Git 历史保留。
>
> 第 3 版依据 Codex 两轮技术审查（2026-09-04），审查结论已逐条在当前仓库核实。
> 修正对照见 [§8](#8-本版修正对照)。

---

## 0. 已确认的 Owner 决策

以下是 Owner 已作出的决定，**不是待批准事项**，执行者不得重新列为疑问或要求二次确认。

| 编号 | 决策 |
|---|---|
| D1 | 六项优化方向成立：填空题声明式化、拖放非目标、写锁细化、验收门 9→4、依赖解绑、工程侧两项小修。 |
| D2 | 填空题走声明式，不走 Runtime。Runtime 把成本从一次性转为每节课重复（教师改不了答案、每题一段代码需过自动准入、判题归一化每次重写），且 Runtime 内容在静态导出中只能是截图或占位。 |
| D3 | 判题**不新增**"答案对错"条件类型，复用 `course-state.compare`。 |
| D4 | 不向交互协议加入拖放 / 放置触发器。分类、排序用点击等价步骤表达。 |
| D5 | Owner `accepted` 签署点从 9 个合并为 4 个（S1 创作力 / S2 工具与素材 / S3 AI 内核 / S4 AI 产品）。版本 release 串行链保留，源码标签一个不减。 |
| D6 | 合同节点只依赖合同；实现节点依赖真实能力，不依赖无关 Owner 发布门。 |
| D7 | 生成质量属渐进优化项，1.2 不为它让路；不新增"生成基线探针"节点。 |
| D8 | **Native `input` 是第三个 V9 strict discriminator 窄例外**，已获批准。须按架构合同 §8 登记六要素。不构成任意联合类型扩展授权。 |
| D9 | **写锁采用 P1**：按稳定 Owner / 模块建细粒度锁，保持"同锁绝对互斥"。本轮不建设结构化 writeScopes、路径重叠检查等新基础设施。 |
| D10 | **卡 A（Flow 图形属性缺失）并入 `r12-005-flow-native-authoring-parity`**，不建独立修复节点。 |
| D11 | **本轮 Native input 只支持 Slide**，静态导出只对应 PPTX。Flow / Spatial / global 禁止放置 input，由语义校验拒绝并定位。Flow 已支持的作者内容仍必须进入 DOCX。 |
| D12 | **`WorkspaceIdentityV1` 落位选 A**：在 1.5 建共享基础节点，材料域与 AI 会话域分别依赖它。不建 1.6 节点供 1.5 反向依赖。 |
| D13 | **`r13-022` 的排序部分走 Component 载体**（已批准载体），分类部分保持声明式。不为排序扩交互协议。 |

---

## 1. 基线与纪律

| 项目 | 值 |
|---|---|
| 基线 HEAD | `a388aee` |
| 工作树 | 含未提交的 1.1 归档改动与本方案文档，**全部视为用户既有工作** |
| 路线机器真相 | `docs/development-plan/roadmap/manifest.json`，当前 **97** 个节点（1.2–2.0） |
| 校验 | `npm run check:development-roadmap`（当前通过：97 节点，9 份规格） |
| 保全矩阵最大 ID | PM-29（新号从 PM-30 起） |

**工作树纪律**：现有修改、删除与未跟踪文件全部保留，不清理、不回退、不代为提交；不执行无条件 `git pull`；不要求工作树干净才能开工。执行前只需 `git status --short` 与 `npm run check:development-roadmap` 各跑一次，确认改动归属清楚、校验通过。提交时机由 Owner 或集成者决定。

**不改变的边界**：V9 软冻结、Published V2、Runtime API 2/3、Component API 4、载体阶梯、三个动态载体硬门、"自动化最多证明 `engineering candidate`"、不创建 V10、不恢复 V8 导入。

### 1.1 Surface 与静态导出的对应关系

| Surface | 静态载体 | 源码事实 |
|---|---|---|
| Slide | **PPTX** | `buildCoursePptx.ts` |
| Flow | **DOCX** | `useCourseDelivery.ts:358` `throw new Error('DOCX 讲义仅适用于当前课程工程中的流式讲义')`；`:361` 只选 `surface.type === 'flow'` |
| 三 Surface | 单 HTML / Web 包 / PDF | 各自 producer |

**硬约束**：Slide 内容只验收 PPTX 映射，Flow 内容只验收 DOCX 映射。共享 discriminator 不使导出承诺自动跨 Surface。按 D11，本轮 input 只在 Slide，因此只验收 PPTX。

---

## 2. 节点变更

### 2.1 新增 5 个节点（97 → **102**）

| Task ID | Release | 结果 | Dependencies | Write locks |
|---|---|---|---|---|
| `r12-006-input-response-contract` | 1.2 | Native `input` strict 分支、`input.submit` 触发器与 Published 对等窄增量；同节点内使全仓穷尽 switch 可编译并显式 fail loud | `—` | `contracts-schema`, `published-slide`, `published-interaction` |
| `r12-007-input-response-delivery` | 1.2 | Slide 填空题作者 UI、规则族编译、Published 执行、PPTX 映射、诊断与能力索引闭环 | `r12-006-input-response-contract` | `store-slide`, `props-slide`, `authoring-slide`, `authoring-interaction`, `published-slide`, `published-interaction`, `export-pptx`, `diagnostics`, `generated-index` |
| `r12-045-flow-docx-fidelity` | 1.2 | Flow 作者浮层按转换矩阵进入 DOCX，不再静默省略；PDF / 打印行为不变 | `r12-005-flow-native-authoring-parity` | `export-docx-print`, `published-flow`, `app-save-recovery` |
| `r13-055-recipe-closure` | 1.3 | 配方能力统一收口（能力索引、诊断、Published 与导出 preflight） | `r13-010`, `r13-011`, `r13-012`, `r13-020`, `r13-021`, `r13-022`, `r13-030`, `r13-040`, `r13-041`, `r13-050`（全部完整 ID） | `generated-index`, `diagnostics`, `published-producer` |
| `r15-005-workspace-identity` | 1.5 | 共享 `WorkspaceIdentityV1 = projectId + normalizedPath`，供材料域与 AI 会话域共同消费 | `—` | `contracts-schema` |

`r12-006`、`r12-007`、`r12-045` 的完整 Acceptance 与设计见 [1.2 技术设计说明](1.2-技术设计说明-2026-09-04.md)。

`r13-055` Acceptance：能力索引精确声明六个配方与四项生产力能力；诊断定位配方展开后的悬空引用、答案不一致与容量问题；Published 与适用导出对配方展开内容无静默遗漏；展开结果不含配方专用运行时节点；既有基线不退化。
验证：`npm run check:ai-capabilities`；`npm run test:product -- tests/unit/coursewareCaseBuilder.test.ts tests/unit/courseProjectHealth.test.ts tests/unit/assessmentEvaluators.test.ts`

`r15-005` Acceptance：`WorkspaceIdentityV1` 严格解析；同 ID 不同路径互不可见；Save As 产生新身份；该身份是全仓唯一 workspace key，不存在第二套定义；它不承载材料域私有语义。
验证：`npm run check:contracts`；`npm run typecheck`；`npm run test:product -- tests/unit/serializedSessionMount.test.ts tests/unit/courseProjectRoundTrip.test.ts`

> **ID 数字不编码依赖顺序**：`r15-005` 是 `r15-000` 的前置。保留 `r15-000-material-contract` 的 ID 是为减少迁移成本（manifest、1.5 README、`OLD_PLAN_CROSSWALK.md` 映射目标、`r16-020` 依赖四处），**不是**因为技术上不能改名——校验器只要求 crosswalk 映射目标存在，crosswalk 可同步修改。

### 2.2 依赖改写（13 处）

| 节点 | 现在 | 改为 | 依据 |
|---|---|---|---|
| `r12-050-native-closure` | 五个实现节点 | 追加 `r12-007-input-response-delivery`, `r12-045-flow-docx-fidelity` | 新能力须进收口 |
| `r13-060-release` | 十个实现节点 | `r13-055-recipe-closure`, `r12-060-release` | 收口节点接管 |
| `r14-040-builder-v2` | `r12-060-release`, `r13-060-release`, `r14-020…023`, `r14-030` | `r12-050-native-closure`, `r13-055-recipe-closure`, `r14-020…023`, `r14-030` | 需能力落地，非两次签署 |
| `r15-000-material-contract` | `r14-060-release` | `r15-005-workspace-identity` | 纯合同，不需 1.4 能力 |
| `r15-020-material-tools-citations` | `r15-010-material-repository` | `r15-010-material-repository`, `r14-020-slide-tools`, `r14-021-flow-tools` | 它调用正式 Authoring Tools 写入引用，须依赖实际使用的工具节点 |
| `r16-020-local-session-store` | `r15-000-material-contract`, `r16-000-cli-adapter-contract` | `r15-005-workspace-identity`, `r16-000-cli-adapter-contract` | 只消费共享身份，不依赖材料域私有语义 |
| `r17-000-generation-contract` | `r13-060-release`, `r14-060-release`, `r15-060-release`, `r16-040-release` | `r13-000-recipe-contract`, `r14-001-tool-receipt`, `r16-000-cli-adapter-contract` | 合同只依赖合同 |
| `r17-010-staging-workspace` | `r17-000-generation-contract` | 追加 `r16-020-local-session-store` | staging 按 session 隔离 |
| `r17-013-host-candidate-commit` | `r17-000-generation-contract` | 追加 `r13-055-recipe-closure`, `r14-020-slide-tools`, `r14-021-flow-tools`, `r14-022-spatial-tools`, `r14-023-course-global-tools` | 要"逐项映射 canonical command"，须有真实工具 |
| `r17-020-single-page` | `r17-013-host-candidate-commit` | 追加 `r16-030-cli-lifecycle` | 要 CLI 真能跑完整会话 |
| `r17-021-whole-course` | `r17-020-single-page` | 追加 `r15-020-material-tools-citations` | 整课生成消费材料与引用 |
| `r17-023-generated-carriers` | `r17-012`, `r17-020` | 追加 `r14-030-dynamic-code-tools` | 动态载体须经 1.4 前门 |
| `r15-010` / `r15-030` / `r15-040` / `r15-050` | 不变 | 不变 | `r15-030/040/050` 直接依赖 `r14-060-release`，保留；`r15-010` 只依赖 `r15-000`，解绑后不再依赖 1.4 —— **这是有意的，它是纯本地材料仓库，不消费 Authoring Tools** |

**原则**：合同节点可只依赖合同；实现节点必须依赖真实实现能力，不得借合同节点跳过。

**无环验证**（执行后由校验器复核）：`r12-045 → r12-005`、`r12-050 → r12-045`；`r15-020 → r14-020/021`，而 `r14-02x` 不依赖 1.5；`r17-013 → r13-055/r14-02x`，而这些不依赖 1.7；`r14-040 → r12-050/r13-055`，而这些不依赖 1.4。

### 2.3 `r17-013` 职责收紧（消除内部矛盾）

现 Acceptance 一方面要让 Native / Recipe / Existing Component 不等动态准入，一方面写"dynamic manifest 须取得 `r17-011`/`r17-012` 回执后才能提交"。据此若加依赖会重新阻塞简单载体。

改为：

- `r17-013` **只**实现 typed candidate 的 canonical commit，以及"可信 admission receipt"的**消费接口**；
- 它**不**依赖 `r17-011`/`r17-012`；
- Generated Component / Runtime 的真实端到端注册与 gate 汇合由 `r17-023` 完成（`r17-023` 已依赖 `r17-012`）；
- 明确写入：**不得信任 candidate 自报的 admission receipt**，receipt 只能来自宿主自己的准入执行。

---

## 3. 卡 A：Flow 图形属性缺失（并入 `r12-005`）

**现存用户可见缺陷**：Flow 中插入图形后只能看到形状，无颜色与属性编辑。

**已核实证据**：

| 事实 | 位置 |
|---|---|
| Flow 能插入图形 | `crossSurfaceCommands.ts:679` `flow: () => ports.flow.addShapeNode(...)`；`flowSharedAuthoringAdapters.ts:610` 调共享 factory |
| 共享 factory 已带默认样式 | `nativeNodeFactories.ts:338` `createShapeNode()` |
| Flow 属性面板漏掉图形 | `FlowPropertiesPanel.tsx` 的 `FlowOverlayProperties` 只分支 formula / image / video / component 后 `return null` |
| Slide 侧已有完整实现 | `SlideNativePropertiesPanel.tsx:687` `ShapeProperties` |

因共享 factory 已带默认填充，**不得**用"给 Flow 再造一套默认样式"掩盖。

**并入 `r12-005` 后的验收追加**（**不含 DOCX**，避免与 `r12-045` 成环）：

1. 抽取或复用**共享** Shape 属性编辑器，经 Surface adapter 写回；不复制 Slide 逻辑；
2. Flow 侧 canonical shape patch command，每次属性手势一笔历史事务；
3. **同时检查编辑画布的 shape 渲染映射**——共享 factory 已带填充色，若 Flow 仍只显示轮廓，说明除属性面板外还有渲染或转换缺陷，必须一并定位；
4. 至少矩形与线条：图形类型、填充色 / 透明度、边框色 / 透明度 / 宽度 / 线型、圆角、适用箭头属性均可编辑；
5. **即时显示**在编辑画布（同时证伪渲染缺陷）；撤销 / 重做精确恢复；保存重开不变；Player 与单 HTML 一致；
6. **新增直接渲染 Flow Shape 属性控件的单元 / 集成测试**——现有 transform / layer 测试不足以证明属性面板可用。

Shape 的 DOCX 结果由 `r12-045` 在其转换矩阵中验收。

`r12-005` 写锁相应扩为：`store-flow`, `props-flow`, `props-shared`, `authoring-flow`（`props-shared` 因抽取共享编辑器而加入）。

**越界停止条件**：不得为 Flow 建第二套图形样式真相；不得只改属性面板而留渲染缺陷未定位；不得把 DOCX 修复作为本节点完成条件。

---

## 4. 卡 2：分类与排序

**保留 D4**（不加拖放触发器）。分类与排序**不是同一语义**，分别定义：

| 互动 | 指针 | 键盘（必须同结果） |
|---|---|---|
| 分类 | 选中项目 → 选中目标组 | 焦点到项目 → 激活 → 焦点到目标组 → 激活 |
| 排序 | 选中项目 → 选择插入位置（前 / 后目标），或上移 / 下移控制 | 焦点到项目 → 激活 → 选择插入位置或按上移 / 下移 |

### 4.1 排序的真实结果由 Component 承载（D13）

已核实 `INTERACTION_ACTION_TYPES`（20 项：导航、`course-state.set`、音视频、`node.enter/exit`）**没有任何"移动项目 / 改变顺序"动作**。

用预定义 presentation states 表达排序不可行：n 个项目有 n! 种排列（4 个项目 24 个状态），组合爆炸。用 `course-state` 记录位置也不行——没有动作能把状态变成可见的顺序变化。

因此按 D13：

- **分类**保持纯声明式（点击 + `course-state` + `compare` 条件即可完整表达）；
- **排序**的可见重排由**已批准的 Component 载体**承载，Component 把可调参数（项目、正确顺序、反馈）公开为可编辑内容，教师不改代码；
- 不为排序扩交互协议，不新增动作类型；
- `r13-022` 的 Acceptance 必须写明排序走 Component，并证明结果真实可见、可保存重开、Player 一致。

### 4.2 文档改动

1. `roadmap/1.3/README.md` 的 `r13-022-classify-sort-recipe` Acceptance 按上表分别写明分类与排序两条路径与载体分工；其余验收项（缺组、孤立项、重复稳定 ID 被拒且零部分写入）不变；
2. 总纲 §6 追加：*"不在交互协议中引入拖放 / 放置触发器；分类用『选中项目 → 选中目标组』纯声明式表达，排序的可见重排由已批准 Component 载体承载并把参数公开为可编辑内容。确有真实教师反馈要求拖放手势时单独立项。"*
3. **1.3 补停止条款**（1.2 的 `r12-005` 已有、1.3 缺）：§接口与数据合同追加：*"若执行中证明现有声明式层无法表达某配方的验收结果，必须停止该节点并先建立独立追加合同任务；不得在配方实现内私藏状态、不得放宽 Schema、不得把排序偷偷降级成分类。"*

**同步范围（四处，不止前两份）**：总纲 §6、`roadmap/1.3/README.md`、`.agents/skills/build-courseware-project/SKILL.md` 的判题 / 互动映射段、`artifacts/ai-capabilities` 的配方与互动能力声明。

---

## 5. 写锁：P1 细粒度锁（D9）

### 5.1 锁定义（35 把）

按**稳定 Owner / 模块**划分，不按单个任务造锁。同模块内多个任务串行是可接受的；跨 Surface / 跨层的并行是本次要解开的目标。

| # | 锁名 | 覆盖路径（边界以此为准） |
|---|---|---|
| 1 | `none` | 无共享写入 |
| 2 | `contracts-schema` | `src/shared/contracts/**` |
| 3 | `generated-index` | `artifacts/ai-capabilities/**`、`scripts/generate-ai-capabilities.ts` |
| 4 | `legacy-inventory` | `inventories/legacy-consumers.json` |
| 5 | `store-kernel` | `src/renderer/store/editorStore.ts`、`editorStoreKernel.ts`、`history.ts`、`slideBackendPort.ts`、`slideEditorProjection.ts`、`v9LayerMutations.ts` |
| 6 | `store-slide` | `src/renderer/store/slices/slideAuthoringSlice.ts`、`slices/slideOwnedCommands.ts` |
| 7 | `store-flow` | `src/renderer/store/slices/flowAuthoringSlice.ts` |
| 8 | `store-spatial` | `src/renderer/store/slices/spatialAuthoringSlice.ts` |
| 9 | `store-course` | `src/renderer/store/slices/courseLifecycleSlice.ts`、`slices/courseStructureSlice.ts`、`slices/editorShellSlice.ts`、`courseResourceState.ts` |
| 10 | `props-shared` | `src/renderer/ui/properties/PropertyControls.tsx`、`propertiesItemView.ts`、`PropertiesPanelRouter.tsx`、`PropertiesContext*.ts(x)`、`src/renderer/composition/properties/**` |
| 11 | `props-slide` | `SlideNativePropertiesPanel.tsx`、`MultiSelectionPropertiesPanel.tsx`、`RuntimePropertiesPanel.tsx`、`RuntimePropertiesContextBuilder.ts` |
| 12 | `props-flow` | `FlowPropertiesPanel.tsx`、`FlowPropertiesContextBuilder.ts`、`FlowSpatialInteractionUnavailableSection.tsx` |
| 13 | `props-spatial` | `SpatialPropertiesPanel.tsx`、`SpatialPropertiesContextBuilder.ts` |
| 14 | `props-global` | `CourseGlobalPropertiesPanel.tsx`、`EmptyScenePropertiesPanel.tsx` |
| 15 | `workspace-shell` | `src/renderer/ui/Workspace.tsx`、`ui/workspaces/**`、`ui/TopToolbar.tsx`、`ui/RightSidebar.tsx`、`src/renderer/composition/surfaceRouter.ts`、`crossSurfaceCommands.ts` |
| 16 | `authoring-slide` | `src/renderer/course/v9SlideContentCommands.ts`、`slideAuthoringBackend.ts`、`effectiveLayerCommands.ts`、`v9MediaAudioCommands.ts` |
| 17 | `authoring-flow` | `src/renderer/course/flowSharedAuthoringAdapters.ts`、`src/renderer/authoring/flowTextEdit.ts` |
| 18 | `authoring-spatial` | `src/renderer/course/spatialEditorCommands.ts`、`src/renderer/authoring/spatialWorldAuthoring.ts` |
| 19 | `authoring-interaction` | `src/renderer/ui/InteractionEditor.tsx`、`src/renderer/interactions/**`、课程逻辑 authoring 命令 |
| 20 | `authoring-recipe` | 1.3 配方定义目录（首个配方节点创建时确定精确路径并回填本表） |
| 21 | `published-slide` | `src/player/surfaces/slide/**` |
| 22 | `published-flow` | `src/player/surfaces/flow/**` |
| 23 | `published-spatial` | `src/player/surfaces/spatial/**` |
| 24 | `published-interaction` | `src/player/interactions/**`、`src/player/InteractionEngine.ts` |
| 25 | `published-dynamic` | `src/player/surfaces/publishedDynamicHosts.ts`、Component / Runtime registry |
| 26 | `published-producer` | `src/renderer/export/course/buildPublishedCourse.ts`、`buildCoursePackages.ts` |
| 27 | `export-pptx` | `src/renderer/export/course/buildCoursePptx.ts`、`renderPptx*` |
| 28 | `export-docx-print` | `src/renderer/export/course/flowDocx.ts`、`flowPrintPlan.ts`、`buildCoursePrintArtifacts.ts` |
| 29 | `app-save-recovery` | `src/renderer/app/useCourseDelivery.ts`、`src/renderer/project/courseProjectArchive.ts`、保存 / 恢复路径 |
| 30 | `diagnostics` | `src/shared/courseProjectHealth/**`、`src/renderer/diagnostics/**` |
| 31 | `main-preload` | `src/main/**`、`src/preload/**` |
| 32 | `cli-adapters` | 1.6 起的 CLI adapter 与安全启动器（首个节点创建时回填精确路径） |
| 33 | `ai-session` | 1.6 起的本地会话存储、staging、准入（同上） |
| 34 | `mcp-server` | 1.8 起的 MCP Authoring Server 与工具映射（同上） |
| 35 | `chat-ui` | 1.9 起的 Chat shell、timeline、引用选择（同上） |

**规则**：

- **同锁绝对互斥**，不保留人工判断例外（D9）；
- 并行写任务使用隔离 worktree / branch；
- 共享合同、registry、生成器、公共上游继续使用共同锁（`contracts-schema`、`generated-index`、`props-shared`、`store-kernel`、`published-producer`）；
- 每个任务预先列全本节点已知会触达的 Owner 锁；
- 锁 20、32–35 覆盖尚未创建的模块：当前确定的是 Owner 锁名与节点映射，不虚构尚不存在的精确文件路径。首个写入该模块的节点须先回填路径边界并同步校验器，**不得**另造锁名。

**两把锁在 1.2–2.0 映射中未被任何节点使用，但必须保留在清单里**（已用脚本核对：§5.2 的 102 行共用到 33 把，全部已定义，无未定义锁名）：

- `legacy-inventory` —— 校验器的 `validateLegacyInventoryWriterOrder` 依赖它存在，且它是 `inventories/legacy-consumers.json` 的专用锁；1.1 已完成，故本轮无节点持有；
- `published-spatial` —— Spatial 的 Published 宿主在 `v1.1.1` 已完成，1.2–2.0 无改动它的节点；保留是为与 `published-slide` / `published-flow` 对称，并供将来节点使用。

删除这两把锁会使校验器或将来节点失去落点，**不得**因"本轮未用到"而移除。

### 5.2 完整确定性锁映射（102 节点）

**1.2（13 节点）**

| 节点 | 写锁 |
|---|---|
| `r12-000-native-contract` | `contracts-schema` |
| `r12-005-flow-native-authoring-parity` | `store-flow`, `props-flow`, `props-shared`, `props-slide`, `authoring-flow` |
| `r12-006-input-response-contract` | `contracts-schema`, `published-slide`, `published-interaction` |
| `r12-007-input-response-delivery` | `store-slide`, `props-slide`, `authoring-slide`, `authoring-interaction`, `published-slide`, `published-interaction`, `export-pptx`, `diagnostics`, `generated-index` |
| `r12-010-table-core` | `contracts-schema`, `store-slide`, `authoring-slide` |
| `r12-011-table-authoring-delivery` | `props-slide`, `published-slide`, `export-pptx` |
| `r12-020-chart-core` | `contracts-schema`, `store-slide`, `authoring-slide` |
| `r12-021-chart-authoring-delivery` | `props-slide`, `published-slide`, `export-pptx` |
| `r12-030-line-authoring` | `store-slide`, `props-slide`, `authoring-slide` |
| `r12-040-background-authoring` | `store-course`, `props-global`, `published-producer` |
| `r12-045-flow-docx-fidelity` | `export-docx-print`, `published-flow`, `app-save-recovery` |
| `r12-050-native-closure` | `generated-index`, `diagnostics`, `published-producer` |
| `r12-060-release` | `none` |

**1.3（13 节点）**

| 节点 | 写锁 |
|---|---|
| `r13-000-recipe-contract` | `contracts-schema`, `generated-index` |
| `r13-010-cover-recipe` | `authoring-recipe` |
| `r13-011-concept-recipe` | `authoring-recipe` |
| `r13-012-worked-example-recipe` | `authoring-recipe` |
| `r13-020-step-reveal-recipe` | `authoring-recipe`, `authoring-interaction` |
| `r13-021-choice-feedback-recipe` | `authoring-recipe`, `authoring-interaction` |
| `r13-022-classify-sort-recipe` | `authoring-recipe`, `authoring-interaction`, `published-dynamic` |
| `r13-030-reference-clone` | `store-slide`, `authoring-slide`, `workspace-shell` |
| `r13-040-batch-replace` | `store-kernel`, `workspace-shell` |
| `r13-041-token-apply` | `store-kernel`, `props-shared` |
| `r13-050-fast-diagnostics` | `diagnostics`, `workspace-shell` |
| `r13-055-recipe-closure` | `generated-index`, `diagnostics`, `published-producer` |
| `r13-060-release` | `none` |

`r13-022` 增 `published-dynamic`：排序走 Component 载体（D13）。

**1.4（14 节点）**

| 节点 | 写锁 |
|---|---|
| `r14-000-target-wire` | `contracts-schema` |
| `r14-001-tool-receipt` | `contracts-schema`, `store-kernel` |
| `r14-010-component-registry-identity` | `contracts-schema`, `published-dynamic` |
| `r14-011-dynamic-asset-closure` | `published-producer`, `published-dynamic` |
| `r14-012-lifecycle-visible-failure` | `published-dynamic` |
| `r14-020-slide-tools` | `store-slide`, `authoring-slide`, `props-slide` |
| `r14-021-flow-tools` | `store-flow`, `authoring-flow`, `props-flow` |
| `r14-022-spatial-tools` | `store-spatial`, `authoring-spatial`, `props-spatial` |
| `r14-023-course-global-tools` | `store-course`, `props-global` |
| `r14-030-dynamic-code-tools` | `contracts-schema`, `published-dynamic` |
| `r14-040-builder-v2` | `generated-index` |
| `r14-041-builder-v1-compat` | `generated-index` |
| `r14-050-weak-model-vertical` | `generated-index` |
| `r14-060-release` | `none` |

**1.5（10 节点）**

| 节点 | 写锁 |
|---|---|
| `r15-005-workspace-identity` | `contracts-schema` |
| `r15-000-material-contract` | `contracts-schema`, `main-preload` |
| `r15-010-material-repository` | `main-preload` |
| `r15-020-material-tools-citations` | `main-preload`, `store-kernel` |
| `r15-030-pptx-import` | `app-save-recovery`, `store-kernel` |
| `r15-040-style-remix` | `store-kernel`, `generated-index` |
| `r15-050-content-qa` | `diagnostics`, `generated-index` |
| `r15-060-release` | `none` |
| `r15-900-openmaic-review` | `none` |
| `r15-901-openmaic-bridge` | `app-save-recovery` |

**1.6（9 节点）**

| 节点 | 写锁 |
|---|---|
| `r16-000-cli-adapter-contract` | `contracts-schema`, `cli-adapters` |
| `r16-010-secure-process-launcher` | `cli-adapters` |
| `r16-011-codex-adapter` | `cli-adapters` |
| `r16-012-claude-adapter` | `cli-adapters` |
| `r16-013-opencode-adapter` | `cli-adapters` |
| `r16-020-local-session-store` | `ai-session` |
| `r16-030-cli-lifecycle` | `cli-adapters`, `ai-session` |
| `r16-031-hidden-manual-isolation` | `workspace-shell`, `cli-adapters` |
| `r16-040-release` | `none` |

**1.7（12 节点）**

| 节点 | 写锁 |
|---|---|
| `r17-000-generation-contract` | `contracts-schema`, `ai-session` |
| `r17-010-staging-workspace` | `ai-session` |
| `r17-011-static-admission` | `contracts-schema`, `ai-session` |
| `r17-012-host-smoke-admission` | `published-dynamic`, `ai-session` |
| `r17-013-host-candidate-commit` | `contracts-schema`, `store-kernel`, `ai-session` |
| `r17-020-single-page` | `ai-session`, `store-kernel` |
| `r17-021-whole-course` | `ai-session`, `store-kernel`, `generated-index` |
| `r17-022-local-edit` | `ai-session`, `store-kernel` |
| `r17-023-generated-carriers` | `contracts-schema`, `published-dynamic`, `ai-session` |
| `r17-030-single-repair` | `ai-session`, `store-kernel` |
| `r17-040-generation-benchmark` | `ai-session` |
| `r17-060-release` | `none` |

**1.8（13 节点）**

| 节点 | 写锁 |
|---|---|
| `r18-000-mcp-authoring-server` | `contracts-schema`, `mcp-server` |
| `r18-010-context-read-tools` | `mcp-server` |
| `r18-011-write-tool-mapping` | `contracts-schema`, `mcp-server`, `store-kernel` |
| `r18-020-neutral-agent-profile` | `generated-index`, `mcp-server` |
| `r18-021-codex-profile` | `generated-index`, `cli-adapters` |
| `r18-022-claude-profile` | `generated-index`, `cli-adapters` |
| `r18-023-opencode-profile` | `generated-index`, `cli-adapters` |
| `r18-030-course-skills` | `generated-index` |
| `r18-031-editing-craft-skills` | `generated-index` |
| `r18-040-staging-file-boundary` | `ai-session` |
| `r18-041-human-concurrency` | `store-kernel`, `mcp-server` |
| `r18-050-three-cli-benchmark` | `cli-adapters` |
| `r18-060-release` | `none` |

**1.9（8 节点）**

| 节点 | 写锁 |
|---|---|
| `r19-000-chat-shell` | `chat-ui` |
| `r19-010-context-references` | `chat-ui`, `mcp-server` |
| `r19-020-tool-timeline` | `chat-ui`, `ai-session` |
| `r19-021-safe-markdown-formula` | `chat-ui` |
| `r19-030-stop-undo-stale` | `chat-ui`, `store-kernel`, `ai-session` |
| `r19-040-session-persistence-deletion` | `chat-ui`, `ai-session` |
| `r19-050-internal-dogfood` | `chat-ui` |
| `r19-060-release` | `none` |

**2.0（10 节点）**

| 节点 | 写锁 |
|---|---|
| `r20-000-public-governance` | `contracts-schema`, `generated-index`, `workspace-shell` |
| `r20-010-cli-setup-ui` | `workspace-shell`, `cli-adapters` |
| `r20-011-first-use-risk-notice` | `workspace-shell` |
| `r20-020-public-authoring` | `chat-ui`, `store-kernel`, `ai-session` |
| `r20-021-profile-controls` | `generated-index`, `workspace-shell` |
| `r20-022-materials-privacy-controls` | `main-preload`, `workspace-shell` |
| `r20-030-docs-accessibility` | `workspace-shell`, `generated-index` |
| `r20-040-three-cli-acceptance` | `cli-adapters` |
| `r20-050-owner-acceptance` | `none` |
| `r20-060-release` | `none` |

合计 13+13+14+10+9+12+13+8+10 = **102**（已用脚本按表格逐行核对，并与 manifest 的 97 个现有节点比对：无遗漏、新增恰为 5 个）。

**映射修正机制**（不是执行者临场选择）：若节点启动时发现实际触达超出本表，**先更新本表与四份锁副本、跑校验，再执行**；不得在卡里私自增删锁名。

### 5.3 并行度验证（本次目标）

| 组 | 改前 | 改后 |
|---|---|---|
| 1.2 首批 | `r12-005`/`r12-030`/`r12-040` 三者写锁完全相同 → 串行 | 三者两两无交集 → **三路并行**，加 `r12-000` 或 `r12-006` 共 **4 路** |
| 1.4 四组 Surface 工具 | 四者写锁完全相同 → 串行 | `r14-020`/`021`/`022`/`023` 两两无交集 → **四路并行** |
| 1.3 六配方 | 同锁 | 仍同 `authoring-recipe` → 串行（按模块划分的有意结果；配方定义体量小） |
| 1.6 三适配器 | 同 `main-preload` | 仍同 `cli-adapters` → 串行（同上） |

### 5.4 必须同步的四份锁副本

| 文件 | 改什么 |
|---|---|
| `docs/development-plan/TASK_CARD_TEMPLATE.md:15` | 人读锁清单，列全 35 个 |
| `scripts/check-development-roadmap.ts:24-34` | `ROADMAP_WRITE_LOCKS` 数组 |
| `scripts/generate-task-board.ts:14-26` | `writeLockTags` 数组 |
| `tests/unit/developmentRoadmap.test.ts:240-244` | `expect(ROADMAP_WRITE_LOCKS).toEqual([...])` 精确断言 |

另检查 `tests/unit/taskBoardGenerator.test.ts`（12 处锁引用）的 fixture。

`manifest.json` 全部 102 节点与 9 份版本 README 的 Write locks 列按 §5.2 同步。

**验收**：

1. `npm run check:development-roadmap` 通过（逐节点校验 manifest 与 README 的 writeLocks 一致，并拒绝未定义锁名）；
2. `npm run test:product -- tests/unit/developmentRoadmap.test.ts` 与 `npm test -- tests/unit/taskBoardGenerator.test.ts` 均通过；
3. `npm run check:task-board` 通过；
4. 校验器输出的首个并行 frontier 含 1.2 中两个**非 contract** 节点；
5. 抽验 1.2 三节点与 1.4 四节点，两两写锁交集为空。

**越界停止条件**：不得把锁改成 `none` 以图通过；不得保留"同锁人工例外"；不得因拆锁顺带重构 `src/` 实现文件。

---

## 6. 验收门 9 → 4 与标签身份

**保留 D5**。

**标签身份**：

- **候选标签** `vX.Y.Z-rc.N` —— 自动化 + 该版目标测试通过即可创建，**不携带** `accepted` 语义；
- **accepted 标签** `vX.Y.Z` —— 只在 Owner 签署后创建；
- 无后缀版本号**绝不**同时表示"仅自动化通过"与"Owner 已验收"；
- "自动化最多证明 `engineering candidate`"不变。

| 签署点 | 覆盖 | Owner 验什么 | 中间标签 |
|---|---|---|---|
| S1 创作力 | 1.2 + 1.3 | 表格、五种图表、线条、多层背景、Flow 原生图文浮层、**Flow 图形属性**、**Flow DOCX 保真**、Slide 填空题、六种配方、克隆 / 批量替换 / Token / 快速诊断 | `v1.2.0-rc.N` |
| S2 工具与素材 | 1.4 + 1.5 | 三 Surface 与全局写入工具、Builder V2、动态载体三硬门、素材、PPTX 导入、Remix、内容 QA | `v1.4.0-rc.N` |
| S3 AI 内核 | 1.6 + 1.7 + 1.8 | 三种 CLI 会话、生成 / 局部编辑 / 动态载体准入、MCP 与 Skills、人工并发 stale | `v1.6.0-rc.N`、`v1.7.0-rc.N` |
| S4 AI 产品 | 1.9 + 2.0 | 完整内部生产 AI 作者工作流、数据边界、人工回退、固定课例离线 HTML | `v1.9.0-rc.N` |

**保全矩阵晋升时点**统一到签署点；中间候选不晋升。

| 位置 | 改法 |
|---|---|
| 总纲 §2 发布列车 | 候选用 `-rc.N`，accepted 用无后缀；签署点固定 S1–S4 |
| 总纲 §5.4 | "每个版本由 Owner 签署"→"每个签署点由 Owner 签署" |
| `WORKING_PROTOCOL.md:73` 与 §7 | 同步标签 / `accepted` 区分 |
| `roadmap/README.md` §统一验证与发布门 第 4 条 | 区分候选标签节点与签署点节点 |
| `roadmap/README.md` 版本规格表 | 增"Owner 签署"列，标注 S1–S4 与 `-rc.N` |
| `r12-060` / `r14-060` / `r16-040` / `r17-060` / `r19-060` | 去掉"Owner 签署 `accepted`"，改为"自动化与本版目标测试通过后发布 `-rc.N`；保全矩阵晋升随所属签署点"。**其余验收项全部保留** |
| `r13-060` / `r15-060` / `r18-060` / `r20-050`+`r20-060` | 保留 Owner 签署，并把被合并版本的固定课例验收项合并进来 |

**配套**：四份固定验收清单落 `docs/development-plan/acceptance/S1-authoring.md` 等，格式"编号步骤 + 预期结果 + 通过 / 不通过"，目标 30–45 分钟。清单在对应签署点节点执行时创建，本卡只写要求与路径约定，**不预建空文件**（校验器检查失效链接）。

**验收**：校验器通过；总纲 / 工作协议 / roadmap README / 9 份版本 README 对标签区分表述一致；被改为候选的 5 个节点其余验收项逐条仍在；4 个签署点验收项之并集 ⊇ 原 9 个 release 节点的全部人工验收项。

---

## 7. 工程侧两项（优先级低于卡 A / 卡 B）

### 7.1 架构棘轮：逐项映射，不减边界

已核实 `tests/unit/architectureDependencyRatchet.test.ts` 的 19 个用例守 **19 条独立边界**。只替换脆弱证明方式（字符串计数、字符串先后顺序、25 文件手工白名单），**逐项保留边界**。实施前须产出下表并逐行落实：

| 原用例 | 边界 | 新证明方式 |
|---|---|---|
| :146 | Core 身份与事务缝隙独立于具体 Surface / Feature | AST / import graph |
| :161 | Slide 历史缝隙不依赖 App / UI / Media / raw Store | AST / import graph |
| :173 | Player 独立于 renderer Store 模块 | AST / import graph |
| :182 | 图片 use-case 锁定在目标 planner 与单一事务路径 | **行为测试** |
| :206 | 纯资源 planner 与共享 history 不依赖 App / UI / Editor Store | AST / import graph |
| :220 | Flow / Spatial 在资源感知的 frame 缝隙上 | AST / import graph + 行为测试 |
| :235 | Media / Components 锁定在基于 target 的资源事务 | **行为测试** |
| :272 | 异步 App 目标在 Media / Components 包读取前被捕获 | **行为测试**（顺序，import graph 无法证明） |
| :310 | Runtime authoring 在 canonical target planner 上、旧 raw writer 为零 | owner / 符号级检查 |
| :375 | Automation 模板与专业字段在 V9 planner 路径 | owner / 符号级检查 + 行为测试 |
| :422 | session 拥有的全局与可选 Slide 本地控制器在三个 Surface port 上 | **行为测试** |
| :447 | 全局播放动作在 canonical V9 Surface histories | **行为测试** |
| :466 | Surface persist writer 归 slices、清除 V8 名称 | owner / 符号级检查 |
| :487 | `editorStore.ts` 是 Zustand 组合根，不含 planner 或文档变更 | owner / 符号级检查 |
| :522 | slices 与 Feature use case 不依赖 root Store / EditorState / raw zustand | AST / import graph |
| :553 | App hooks / Core kernel / contracts 不依赖 Store、无反向 Feature 边 | AST / import graph |
| :594 | Slide Native painter 与 Course package 分析 / preflight / emitter 在单文件上 | owner / 符号级检查 |
| :643 | 单一 Zustand store、V9 历史深度、fail-loud V8 加载且无迁移代码 | **行为测试**（运行行为） |
| :671 | 清除 teacher-controller Store 环与已知运行时 SCC | AST / SCC 分析 |

规则：依赖方向与环 → AST / import graph；writer 唯一性 → owner / 符号级检查；事务、目标捕获、行为顺序 → **保留或改成针对性行为测试**，不得用 import graph 冒充；重复断言可合并但必须说明由哪条更强证据覆盖；每条结构规则加 red fixture，但 **red fixture 不得替代行为验证**。

范围：`tests/unit/architectureDependencyRatchet.test.ts`；必要时新增纯分析辅助模块（**不得写入 `src/`**）。

**越界停止条件**：不得减少守护边界条数；不得把行为不变量降级为静态检查。

### 7.2 消除测试基线的 3 个既有红

已核实：干净工作树上 `npm run test:product` 有 3 个失败，全部因 `dist-player/player.iife.js` 缺失（`tests/integration/renderHostBenchmark.test.ts` 1 项、`tests/unit/exampleGenerationBoundary.test.ts` 2 项）。测试本身 55 秒跑完 2210 个用例。

**方案（采用固定构建，不做缓存）**：顶层 `test` 与 `test:product` 各自在执行前固定运行一次 `build:player`；`verify` 通过其既有 `npm run test` 链路获得同一前置构建。

理由：`player` 构建耗时相对 55 秒测试是小量；而"过期"若靠 mtime 或文件存在判断都不可靠，靠内容摘要则要引入源码 + 构建配置 + 依赖闭包的摘要计算，复杂度超过它省下的时间。**不接受 skip**——产品证据不得变成跳过。

**验收**：不存在预生成 bundle 时，`npm test`、`npm run test:product` 与 `npm run verify` 都不会再因 `dist-player/player.iife.js` 缺失而失败或跳过；不降低任何断言强度。

---

## 8. 本版修正对照

| Codex 反馈 | 核实 | 本版处理 |
|---|---|---|
| 一.1 不加通用 `readNodeInputValue`，改 `bindInputSubmit(nodeId, listener(rawValue))` | 成立，且符合既有模式（`bindVideoEvent(nodeId, kind, listener(seconds))` 就是事件携带数据） | 见技术设计说明 §2 |
| 一.2 Controller 需可信的 `nodeId → stateKey/answerType` Published 描述 | 成立 | 技术设计说明 §2.3 |
| 一.3 不在 `InteractionEngine` 造第二套语义 | **成立，证据更强**：`InteractionEngine.ts:157-160` 对两个 course-state 条件直接 `return false`，注释写"V9 通过 Published V2 controller 执行"；`new InteractionEngine(` 在 `src/` 下零实例化 | 从 delivery 范围**删除** `InteractionEngine.ts`；改由 `published-interaction` 锁覆盖但不修改 |
| 一.4 补入四个 Published 侧文件 | 成立（四个文件实测全部存在） | 技术设计说明 §2.6 delivery 范围 |
| 一.5 合同节点须使全仓可编译 | 成立：`nativeType` 出现在 61 个文件，`publishedNativeRendering.ts` 与 `course-project-v9/schema.ts` 有穷尽 switch / never | `r12-006` 写锁增 `published-slide`, `published-interaction`；验收含"全仓 typecheck 通过且未交付路径显式 fail loud" |
| 一.6 `normalizeShortAnswer` 不可复用 | 成立：`assessmentEvaluators.ts:48` 无 `export` | 技术设计说明 §3.1 要求先抽共享纯函数 |
| 一.7 IME 组合期不得提前提交 | 成立（产品要求） | 技术设计说明 §2.5 |
| 一.8 学生提交不进编辑器 Undo | 成立 | 技术设计说明 §4.3 明确历史边界 |
| 二 number 写 null 不成立 | **成立，双重错误**：`CourseStateDeclaration` 每 key 固定一个 valueType；`publishedCourseState.ts:177-178` 的 `exists` 只看 `undefined` | 改为"number 值键 + boolean 有效性键"双键设计，见技术设计说明 §3.2 |
| 三 区间否定无法单规则表达 | **成立**：`types.ts:88` 注释"Different conditions are ANDed" | 改为 canonical rule family，见技术设计说明 §4 |
| 四 input Surface 范围冲突 | **成立**（第 2 版 §1.3 与卡 1 自相矛盾） | 按 D11 统一为只 Slide；§1.1 与技术设计说明一致 |
| 五 卡 A 并入后成环 | **成立** | §3 删除卡 A 的 DOCX 验收；Shape 的 DOCX 由 `r12-045` 验收 |
| 六 `flowDocx` 只接单 surface | **成立**：`flowDocx.ts:340-345` | 技术设计说明 §6 给出七条 Flow 合成合同；`r12-045` 范围含 `useCourseDelivery.ts` 与 Published Flow composition |
| 七 DOCX 矩阵补齐 | 成立；`includeInStaticExports` 在 `buildCoursePptx.ts:293`、`buildCoursePrintArtifacts.ts:262`、`SlidePublishedAdapter.ts:1313` 被消费 | 技术设计说明 §5 补齐全部内容类型；teacher-controller 按 `includeInStaticExports` 既有语义而非一概排除 |
| 八 `r15-000` 解绑说明错误 | **成立，第 2 版写反了**：`r15-010` 只依赖 `r15-000`、`r15-020` 只依赖 `r15-010`，解绑后二者彻底脱离 1.4 | §2.2 按真实消费关系处理：`r15-020` 直接依赖 `r14-020`/`r14-021`；`r15-010` 有意不依赖 1.4；`r15-030/040/050` 保留 `r14-060-release` |
| 八 crosswalk 不阻止改 ID | 成立 | §2.1 注明保留 ID 是为减少迁移成本，非技术限制 |
| 八 WorkspaceIdentity 落位 | 采纳选项 A（D12） | 新增 `r15-005-workspace-identity`；取消第 2 版的 `r16-001` |
| 九 `r17-013` 职责矛盾 | 成立 | §2.3 收紧为"只实现 canonical commit 与 receipt 消费接口"，不依赖 `r17-011/012`，不信任 candidate 自报 receipt |
| 十 P1 执行要求 | Owner 决定（D9） | §5 给出 35 把锁定义 + 102 节点完整映射 + 同锁绝对互斥 + 隔离 worktree |
| 十一 节点总数算错 | **成立**：第 2 版漏算 `r13-055` | 全文改为 97 → **102**（含 `r15-005`） |
| 十二 排序无动作可表达 | **成立，且比反馈更严重**：`INTERACTION_ACTION_TYPES` 20 项无顺序动作；presentation states 需 n! 个状态 | 按 D13 排序走 Component 载体；§4.1 写明依据与验收 |
| 十三 bundle 过期无定义 | 成立 | §7.2 采用"每次固定构建一次"，给出理由，不做缓存 |
| 十四 已落地清单 | 确认无误 | 保持 |

**Codex 两处描述需纠正**（不影响结论）：`PublishedInteractionController.ts` 位于 `src/player/interactions/`；`test:product` 对被 exclude 的测试是**静默少跑并显示全绿**（实测 `Test Files 1 passed` / `Tests 25 passed`），而非 `No test files found`——后者更危险。

**第 2 版被撤销的内容**：`r16-001-workspace-identity`（改 A 方案）；`course-state.set` 追加可选 `source`（改事件时快照，完全不改该动作）；`state-delta`（剥离）；number 写 `null`（改双键）；"正确/错误恰好两条规则"（改规则族）；卡 A 的 DOCX 验收（移交 `r12-045`）；`InteractionEngine.ts` 列入 delivery 范围（删除）；"1.5 其余节点保留 r14-060 依赖"（事实错误，已改正）；节点总数 100（改 102）；bundle 缓存式"过期"判断（改固定构建）；"同锁人工例外"规则（P1 不保留）。

---

## 9. 执行顺序

```text
第 0 步  git status --short + npm run check:development-roadmap
         （保留工作树现有改动，不清理、不回退、不代为提交）
  ↓
§5 写锁（35 把定义 + 102 节点映射 + 四份副本）—— 先做，后续节点直接用新锁名
  ↓
§2 节点增删与依赖改写（含 r17-013 职责收紧）
  ↓
§3 卡 A 并入 r12-005 的验收追加
  ↓
§4 分类/排序 + 拖放非目标 + 1.3 停止条款
  ↓
§6 验收门 9→4 + 标签身份
  ↓
§7.1、§7.2 工程侧（独立于路线，任意时点）
```

路线文档改动统一自检：

```text
npm run check:development-roadmap
npm run check:task-board
npm run typecheck
npm run test:product -- tests/unit/developmentRoadmap.test.ts
npm test -- tests/unit/taskBoardGenerator.test.ts
git diff --check
```

**测试命令说明**：`package.json` 的 `test:product` 明确 `--exclude=tests/unit/taskBoardGenerator.test.ts`。把它写在 `test:product` 后面**不报错**，而是静默只跑另一个文件并显示全绿（实测 `Test Files 1 passed` / `Tests 25 passed`）——比报错更危险。因此该测试必须用 `npm test -- …`（实测 12 用例通过）。同样被排除的还有 `tests/unit/repoIndex*.test.ts` 与 `tests/unit/aiCapabilities.test.ts`。

`git diff --check` 当前**是干净的**（已实测）；`.gitattributes` 只对少数生成器输入 / 输出固定 LF，无全仓 CRLF 设置。

**提交建议**（非强制，时机由 Owner / 集成者定）：

```text
docs(roadmap): refine write locks to owner boundaries
docs(roadmap): rebalance dependencies onto real capabilities
docs(roadmap): add slide input response and flow docx nodes
docs(roadmap): split classify and sort semantics
docs(roadmap): consolidate owner acceptance gates
test(architecture): prove boundaries per invariant
test(tooling): remove player bundle baseline failures
```

---

## 10. 明确不做

- 不改 V9 既有字段语义，不创建 V10，不恢复 V8 导入；
- 不新增交互**条件**类型（D3）；**不改 `course-state.set`**；不夹带状态增量能力；
- 不加拖放 / 放置触发器（D4）；不为排序新增动作类型（D13）；
- 不为 Slide 内容宣称 DOCX，不为 Flow 内容宣称 PPTX；本轮不允许 Flow / Spatial / global 放置 input（D11）；
- 不把 `FLOW_PRINT_INCLUDES_FLOATING_LAYERS` 直接改 `true`，不未经批准改变 PDF / 打印；
- 不在 `InteractionEngine` 实现第二套 course-state 执行语义；
- 不减少架构棘轮守护的 19 条边界；不用 skip 充当测试 0 失败；
- 不保留"同锁人工例外"，不建设 writeScopes / 路径重叠检查（D9）；
- 不减少任何版本源码标签，不把 `accepted` 授权给自动化；
- 不按行数拆 `InteractionEditor.tsx`（2838 行）、`SlideLocationWorkspace.tsx`（2624 行）、
  `flowAuthoringSlice.ts`（2076 行）、`spatialAuthoringSlice.ts`（2014 行）——各自单一 owner，
  未命中总纲 §5.5 触发条件；
- 不建设架构评分平台、常设治理流程、并行度看板或验收工时统计；
- 不清理、回退或代为提交工作树中的既有改动。

---

## 11. 自检（全部已在当前仓库核实）

| 断言 | 核对 |
|---|---|
| 条件永远先于动作求值 | `PublishedInteractionController.ts` 的 `#handleNodeClick` / `#handleAudioEnded` / `#handleVideoEvent` 均"求条件 → `#startRule()`"；动作步骤带 `delayMs` |
| 该控制器只支持三类触发器 | 构造期检查，未列入者走 `unsupported-trigger` 并跳过 |
| `InteractionEngine` 不执行 course-state 且未被产品实例化 | `:157-160` 直接 `return false` + 注释；`new InteractionEngine(` 仅在测试 |
| `exists` 只区分 `undefined` | `publishedCourseState.ts:177-178` |
| 每个 state key 固定一个类型 | `course-state/types.ts:3-7` |
| 条件之间只有 AND | `interaction-v1/types.ts:88` 注释 |
| 无顺序 / 移动动作 | `INTERACTION_ACTION_TYPES` 20 项 |
| `course-state.set` 的 `value` 必填 | `interaction-v1/types.ts:168-172` |
| `LayerItemBase` 持有 ID 与几何 | `course-project-v9/types.ts:52-56` |
| `normalizeShortAnswer` 未导出 | `assessmentEvaluators.ts:48` |
| 穷尽 switch 会被新分支波及 | `nativeType` 见于 61 文件；`publishedNativeRendering.ts`、`course-project-v9/schema.ts` 含 never 检查 |
| DOCX 只导出 Flow surface | `useCourseDelivery.ts:358`、`:361`、`:363` |
| `buildFlowDocx` 只接单 surface | `flowDocx.ts:340-345` |
| Flow 浮层被静态导出省略 | `flowPrintPlan.ts:14`、`:167`；`flowDocx.ts:308` |
| `includeInStaticExports` 被多处消费 | `buildCoursePptx.ts:293`、`buildCoursePrintArtifacts.ts:262`、`SlidePublishedAdapter.ts:1313` |
| Flow 能插图形但属性面板漏掉 | `crossSurfaceCommands.ts:679`；`flowSharedAuthoringAdapters.ts:610`；`FlowPropertiesPanel.tsx` 的 `FlowOverlayProperties` |
| Slide 侧有完整 ShapeProperties | `SlideNativePropertiesPanel.tsx:687` |
| 共享 factory 已带默认样式 | `nativeNodeFactories.ts:338` |
| 架构合同只记两个 discriminator 例外 | `ARCHITECTURE_CONTRACT.md:8`、`:133`、`:170` |
| 写锁 9 把且四份副本机器强制 | `check-development-roadmap.ts:24-34` + 三处 |
| 1.5 依赖链 | manifest 实测：`r15-010 → [r15-000]`、`r15-020 → [r15-010]`、`r15-030/040/050 → [r14-060-release]` |
| 棘轮 19 用例守 19 条边界 | 逐 `it()` 枚举（§7.1 表） |
| manifest 当前 97 节点 | `node` 解析，各版本 10/12/14/9/9/12/13/8/10 |
| `test:product` 静默排除 taskBoardGenerator | 实测 `Test Files 1 passed` / `Tests 25 passed` |
| `npm test -- taskBoardGenerator` 可跑 | 实测 12 用例通过 |
| `git diff --check` 干净 | 实测无输出 |
| 测试 55 秒 / 2210 用例 / 3 个既有红 | 实跑，失败均为 `dist-player/player.iife.js` 缺失 |
| PM 当前最大 29 | `PRESERVATION_MATRIX.md` |

**执行时现场确认的事项**（已收窄至三项，均有确定的处理规则）：

1. 锁 20、32–35 覆盖尚未创建的模块，精确路径由首个写入节点回填 §5.1，**不得另造锁名**；
2. `input` content 分支落 `course-project-v9/types.ts` 还是 `native-v1`，由 `r12-006` 按届时结构决定并在卡中写明；无论落哪都不得携带 ID 与几何；
3. `r12-040-background-authoring` 的 `published-producer` 锁是按"背景解析在 producer 层统一"的推断；若启动时发现须改各 Surface published 模块，按 §5.2 的修正机制先更新映射表。
