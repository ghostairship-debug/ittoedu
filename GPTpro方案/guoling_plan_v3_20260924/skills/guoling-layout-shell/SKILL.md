---
name: guoling-layout-shell
description: 在教学与呈现稿基础上生成高质量框架HTML，保留理想表达；完成之前不读取编辑器能力或选择原生/Runtime载体。
compatibility: 未安装的一方方法草案，使用实际授权的文件/HTML工具。
---

# 框架HTML设计

读取当前文稿、已有HTML、用户目标和必要素材。**框架HTML完成前不读取果铃能力索引、Schema、Builder、Runtime限制或shared/shell-contract.md。** 先把理想呈现做清楚，之后由专门呈现策略决定工程化载体。

使用真实上屏文案和有效视觉布局，必要素材/互动可以用有目的的槽表达；不是灰盒，也不要求在策略前完成所有动态机制。使用适合设计的普通HTML/CSS布局，不为了将来能拆原生而简化样式、删除数据或缩减教学。

已完成HTML优先保留，不逆写MD、补齐编辑器标签或重画简化版。当前HTML中的人改文案、布局与槽优先，MD只补未表达背景；冲突单列，不无授权恢复旧稿。

保存和机械标识补足由宿主负责，本阶段不调用要求加载编辑器能力的工程装配步骤，不让模型维护第二份布局源。HTML就绪后交第三份Skill的后置呈现策略，之后才读取相应实现合同。

只核对本次设计的明显缺字/遮挡和用户要求，不固定调用审美Agent或生成三份对照；实际导入与可编辑性由后续宿主验证。交付真实HTML与未完成素材/机制说明，不宣称工程已应用。
