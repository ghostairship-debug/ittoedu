{
  "candidateCarrier": {
    "default": "existing-component"
  },
  "description": "恢复当前组件教师控制台的默认源码。使用当前完整 global update target。参数由 component.configure 修改，源码由 component.package 修改。",
  "examples": [
    {
      "bindings": "values 的  来自当前观察、已读资源或用户要求，值须满足本卡 inputSchema；destination 原样复制当前目标。",
      "code": "const input={\"operation\":\"restore\"}; const receipt=await session.execute(\"component.controller\",input,destination);",
      "operation": "restore"
    }
  ],
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "operation": {
        "const": "restore",
        "type": "string"
      }
    },
    "required": [
      "operation"
    ],
    "type": "object"
  },
  "invocation": "Builder: await session.execute(tool,input,destination)。input 遵循本卡完整 Schema；destination 原样使用当前 observe/createScope 的目标；只以 committed receipt 判定成功。",
  "name": "component.controller",
  "recovery": "invalid-input: 按本卡 Schema 修正字段；revision-conflict: 重新 observe 后复制新 target 和源码基线；tool-failed: 读取 diagnostics 修复所指输入/资源/宿主条件，失败不代表已提交。",
  "support": "conditional-on-input-target-resources-and-host",
  "supportedScopes": [
    "slide:global",
    "flow:global",
    "spatial-2d:global"
  ],
  "variants": [
    {
      "operation": "restore",
      "scopes": [
        "slide:global",
        "flow:global",
        "spatial-2d:global"
      ]
    }
  ],
  "version": 1
}
