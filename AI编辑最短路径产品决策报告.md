# AI 编辑最短路径统一方案

> 2026-09-14 机制接续：常规操作完整输入、依赖组合、同源条件、预检/恢复和结果检查的当前纵切，以及 Codex 快速模式已实现，见[机制方案第 9 节](docs/development-plan/AI_AUTHORING_MECHANISM_IMPLEMENTATION_PLAN.md#9-2026-09-14-开发批次与新增快速模式)和[实测记录](docs/development-plan/reviews/2026-09-14-authoring-mechanism-implementation.md)。三家 CLI 已有自然图形任务首次原生候选证据；先前指定文件/失败续跑样本仍单列。本文保留历史决策，完整后续任务族与 Owner 验收不因本批自动完成。

> 2026-09-14 接续：Owner 明确选择仅是上下文焦点，宿主快捷工具不是能力上限。当前开放修改、CLI 文件兜底、原生权限/代理接线及等待体验按[修复方案](docs/development-plan/R18_USER_EXPERIENCE_REPAIR_PLAN.md)完成工程修复；[真实记录](docs/development-plan/reviews/2026-09-14-native-editing-repair.md)分列首次失败、兜底提交和剩余延迟。下文历史范围规则与此决定不一致时，以本次明确决定和架构合同为准。

> 2026-09-11接续：当晚真实使用证明常规换图、场景导航与默认目标仍未闭环；当前修改方案移至[常规任务三层执行与修改方案](docs/development-plan/AI_COMMON_TASK_EXECUTION_PLAN.md)，明确软件接管确定性工程步骤、10类第二层任务、分批开发及前两层80%以上覆盖目标。2026-09-12经Owner授权完成1.8 B0/B1工程实现与受影响验证，原始Luna样本、后修复及软件应用链分列于[验收记录](docs/development-plan/reviews/2026-09-12-r18-b01-integration.md)；60项未计分，B2–B4及S3仍保留相应边界。下文保留9月9日原决策、已执行批次和证据，不作为并行新执行清单；未被新方案替代的正式边界继续有效。

> 整合与核验日期：2026-09-09。源码基线：`c839c205e594ab61bfa01c22b4a4f28303862d8d`。\
> 本文原位替代《AI 编辑最短路径产品决策报告》，是本轮原方案与八份模型评估整合后的唯一专题方案。评估报告保留为评估时的输入证据，其中的建议、估算和任务编号不再作为并行执行方案。\
> 状态：经用户授权，当前 A/B/C/D 执行包已进入产品实施与真实验证；已集成行为、原始失败和通过证据见[实施记录](docs/development-plan/reviews/2026-09-09-short-path-implementation.md)。本文继续保留决策及原事故事实，不用后续结果改写原样本；未形成新的 S3/S4 或 Owner 验收结论。长期路线仍由[开发总纲](COURSEWARE_DEVELOPMENT_PLAN.md)负责，技术边界服从[架构合同](docs/development-plan/ARCHITECTURE_CONTRACT.md)，实施遵循[工作协议](docs/development-plan/WORKING_PROTOCOL.md)。

## 1. 最终决策

本轮采用**接口优先、AI 自主选择操作、宿主准确执行、依据真实结果结束或继续**的路线。保留 Codex、Claude、OpenCode 的原生模型循环、文件、终端、网络、Skills、子任务和已连接工具；沿用现有候选、私有预演、正式命令和唯一工程事务。

优化同时服务两个目标：老师说“只把这部分改绿”时，准确而及时地完成；老师要求整页重排、批量修改或交互修复时，减少重复读取、全文重写、无效续轮和重复检查。两类任务分别验收，不以其中一类的成绩代替另一类。

最短路径是：

```text
用户要求与本轮真实目标
→ 必要上下文、资产诊断、完整可调用能力
→ 原生 AI 选择操作并形成一份必要粒度的候选
→ 宿主展开身份、私有预演、受影响检查
→ 沿现有应用策略提交，保存真实回执
→ 已完成则结束；确需新观察或后续阶段才继续
```

“接口直达”指 AI 能直接找到并表达正式操作，宿主按候选执行；不意味着给 CLI 增加 live Store、第二写入通道或通用工具 RPC。

| 决定 | 本轮落实 |
|---|---|
| 正确结果优先 | 先关闭错误替代、消息泄漏、配置与发现链路中的实质缺口；最小计时同步补齐 |
| 修正原事故归因 | 用原生记录恢复用量；确认损坏 PNG；缓存失配与模型初始化列为可验证因素，不能宣称已证明全部延迟来源 |
| 常用操作可直接调用 | 当前明确目标的必要卡完整提供；长尾一次发现取得完整调用信息；不只增加字节预算 |
| 输出变化与引用 | 短传输由正式工具生成，宿主展开已知身份；复用已有组件文件级 patch、多步候选与前序结果引用 |
| 明确结束与继续 | 成功后的完成声明须经过实际检查、提交与回执持久化；同步 renderer、main 和本地任务状态 |
| 失败反馈可执行 | 保留结构化错误、目标、资源和实际行为证据；同一失败无新信息不再探索，也不换对象伪造成功 |
| 模型配置保持真实 | 不静默降低用户选择的强度；先固定配置衡量接口与循环变化，再按实测比较其他已授权配置 |
| 复杂能力沿现有基础改进 | 先修已经支持的增量路径和提示矛盾；细粒度源码补丁、资源复用等由实际瓶颈触发 |
| 不新增平台前置 | 不建设应用模型循环、自动路由器、通用断言语言、第二工程或新多 Agent 调度平台 |
| 发布标准保持一致 | 映射现有 Owner 和 1.8–2.0 路线；不恢复历史任务，不用报告的新数字替换既有发布门 |

## 2. 报告核验与取舍

使用六个并行核验分支：Fable 独立核验；GPT6Pro 分为上下文/候选与生命周期两条线；其余六份报告两两组合查增量。高能力报告获得更深入的源码与原始记录核验，最终以证据裁决，不按模型数量投票。

### 2.1 Fable：主要吸收事故取证，修正因果与“一行修复”承诺

| 主张 | 裁决 |
|---|---|
| 原生记录能恢复用量，原方案“无法量化 token”不完整 | **采纳。** 应用记录全部 null，但原生 JSONL 有 27 条用量记录；本轮已独立重算，见第 3 节 |
| requestId 嵌入 outputSchema 导致每轮前缀变化 | **采纳稳定 Schema 的修复方向。** 源码直接成立，官方缓存机制支持这一嫌疑；尚未做本产品受控对照，不能称唯一或已量化根因 |
| 三轮首请求 202,442 未缓存 token 都可通过缓存修复省掉 | **拒绝。** 其中包含第一轮自然冷启动的 35,424；其余也不是已证实可回收量 |
| 线程先用默认模型创建，再切换用户模型，额外注入模型指令 | **采纳配置链修复。** 日志确有 46,246 字符 model-switch 消息；必须处理 harness 先 openSession 后 configure 的顺序，不能只加一行 model |
| outputSchema 必然约束所有中途消息，无法显示自然语言进展 | **修正。** 本例 assistant 消息确为 JSON，但本地原生协议注明约束 final assistant message；只能确认本例格式同化与 adapter 分流缺陷 |
| PNG 是夹具字节损坏 | **采纳。** 本轮独立复现 IDAT CRC 与 zlib 校验双错；先修夹具，另保留坏输入作为负例 |
| 附件解码失败就把所有图片能力标成不可用 | **修正。** 观察附件与图片变换不是同一解码路径，应返回资产及具体操作的适用性，不能以附件失败代替工具预检 |
| 小输出区间中位约 8.4 秒是服务不可压缩的硬下限 | **拒绝硬下限解释。** 它是单次事故中的请求区间样本，含本地与服务等待，不能推导未来最短时间 |
| 上述修复均一行、一天可完成 | **拒绝工期与规模保证。** 用量、配置和终结涉及 strict 合同、持久化与多个 consumer |

### 2.2 GPT6Pro：主要吸收复杂编辑方案，纠正已存在能力和产品范围

| 主张 | 裁决 |
|---|---|
| 减少复杂任务重复阅读、整份重写、碎片候选与重复验证 | **采纳。** 并入第 5、7、9 节 |
| 将改色/字号等普通 AI 修改排除出本轮 | **不采纳。** 这是报告新增前提；当前已批准路线仍有高频 Native 编辑目标 |
| 候选提交后应有条件结束，必要时继续或修复 | **采纳。** 必须贯通 main 任务终态和原生会话回执，不能仅在 renderer break |
| 提出增加墙钟、续跑、修复预算与无进展检测，但未完整盘点已有机制 | **沿现有机制补齐。** harness 已有 20 分钟 deadline、原生执行定时器和无进展/格式修复检查；本轮补覆盖与精确语义，不重建预算系统 |
| 建设组件增量源码能力 | **纠正。** 文件级 patch、shared/instance 已存在；当前观察提示仍要求完整 shared revise，发现域又漏实例模式，应先修这些直接矛盾 |
| 最终态统一验证、资源与静态证据复用 | **分层采纳。** 先合并语义兼容操作；运行过的新代码必须已经准入，行为证据不能仅按源码 hash 复用 |
| 24 个模板、30% 提速、A/B/C 秒数与预算作为首批方案 | **保留模板思想，不采纳为新发布门。** 选择少量有信息增益的代表任务；数字无本产品基线，不作为承诺 |
| 缩小原生 cwd、减少原生 Skills；后续增加直连 API | **修正/暂缓。** 缩小首轮观察，不改变已授权原生能力；API 是需另行产品决定的研究选项 |

本轮另外复现了 GPT6Pro 未指出的两处直接缺口：组件实例 patch 能执行却在 scene 条件下查不到；组件源码观察仍强制提示完整共享修订。它们进入首批，不再为已有能力重建另一套机制。

### 2.3 其余报告只吸收增量

| 报告 | 吸收内容 | 修正或不采纳内容 |
|---|---|---|
| Cursor Grok 4.6 | 必要操作卡不被无关卡挤走；失败恢复不擅自改变操作语义；性能对照固定变量 | “完成前必须即时写进原生历史”改为按 adapter 能力；开发可并行，实验变量仍隔离 |
| Sonnet 5 | 与现有 Owner/路线对齐；说明体积、界面反馈和模型完成时间分开 | 历史 87 KB 是编辑器 Authoring Tools，非不可控原生工具；旧复合 E2E 用时不能证明本事故是统计异常值；未核实的收购/行业断言不作为依据 |
| Kimi | n=1 限制；preview/auto 应用区别；实际配置生效与影响摘要 | 历史 OpenCode issue 的版本和调用方不同，不能当本次根因；图片 copy-on-write 已存在，不写成新缺陷 |
| Muse Spark 1.3 | PNG 隐藏 RGB/alpha 语义；last/total/reasoning 口径；稳定说明与动态对象分层 | 推理 token 不能换算推理耗时；图片→文本同属 Native，单查“跨 carrier”会漏事故；不直接照搬其他 provider 的延迟工具 API |
| Gemini 3.8 Flash | 精简说明、前置诊断与可读展示的方向 | 不按 edit+selection 自动降强度；拒绝 Canvas 通用无损备轨、辅助块根因猜测、固定 8 KB 前缀及 3–8 秒/70%/80% 等无基线收益 |
| DeepSeek V4 | 同条件有限对照；逐 adapter 支持范围；明确继续观察理由 | 119/127 KB 是历史口径；三次不能证明稳定 P95；20% 改善阈值不能归因；W 包是历史建议，不能复活 |

根目录 9 月 2 日综合核验、9 月 4 日路线优化及 1.2 设计说明属于更早阶段的历史材料。其仍有效的约束已由当前总纲/合同承接，本轮不恢复旧版本前提，也不把它们混入八份最短路径评估。

## 3. 重新确认的当前事实

### 3.1 原事故与可恢复用量

事故任务为“帮我把这个颜色改为绿色”，应用策略 auto，配置 Luna + max。第一阶段图片换色被拒；第二阶段删除原图片、创建绿色文本对象并替换；第三阶段回答完成。因此这是**错误结果的失败案例**，不是耗时九分半的成功改色基线。

原生 JSONL 的 27 条 `token_count` 记录已独立重算：各阶段内累计增量逐条等于本次用量，阶段最终累计与下表相符，未见重复累计。输入是多次记录对应输入之和，包含历史反复进入请求的量，不是一次提示词或唯一工程内容的大小；推理 token 包含在输出 token 内，不再加一次。

| 原生阶段 | 输入 token | 缓存输入 | 未缓存输入 | 输出 token | 其中推理 token |
|---|---:|---:|---:|---:|---:|
| 首次候选 | 511,310 | 453,376 | 57,934 | 7,713 | 4,368 |
| 失败后错误替换 | 1,085,998 | 977,408 | 108,590 | 14,851 | 10,364 |
| 提交后回复 | 108,874 | 0 | 108,874 | 209 | 123 |
| 合计 | 1,706,182 | 1,430,784 | 275,398 | 22,773 | 14,855 |

三个原生阶段约 184.5、336.0、16.4 秒，加上阶段间间隔约 568 秒。应用侧计时起点不同，首轮约 186 秒，两者不混写。原方案记录的本地命令耗时合计约 7.1 秒，且部分并行，不能直接相减后把全部剩余时间命名为“推理”。

可得出的结论：

- 不应继续写“所有用量都无法取得”；应写“应用映射缺失，原生记录已能恢复本例用量”。
- 本例可识别出多次原生内部工作，不能把三个宿主阶段当三次底层推理。
- 三轮首用量的缓存输入均为 0；`outputSchema` 两处含变化的 requestId 常量，属于应消除的应用前缀变动。缓存实际收益仍待固定条件验证。
- 日志中额外模型切换指令与初始化链相符；修复应避免首次执行发生不必要的默认模型→所选模型切换。
- 最后 16.4 秒属于提交后的原生回复阶段，说明有可避免续轮；不能据此承诺所有任务都省同样秒数。
- 原生准备、宿主阶段间等待、服务排队、推理和工具区间尚未完全分离。token 计数不能证明各自墙钟占比，也不能证明低强度必然更快完成。

### 3.2 PNG：坏输入与错误恢复是两个问题

事故素材来自 `scripts/build-architecture-baseline-fixtures.ts` 的 `PNG_BYTES`。其 IDAT 末尾 Adler-32 写成 `00 03 00 01`，对应原始像素流的正确值为 `03 01 01 00`；存储的块 CRC 也与当前数据不符。本轮用原始字节重新计算并调用 zlib，复现了失败。

这是关键块 IDAT 和压缩流的损坏，不能用“辅助块宽容”修好。报告中的浏览器实验不是本轮真实编辑器宿主复测；`<img>` 能显示也不足以证明数据有效或变换可用。PNG 规范将 IDAT 列为关键块，CRC 与图像数据解释有明确规则。[PNG 标准](https://www.w3.org/TR/png-3/)

修复须分开验收：

1. 修正生成夹具，重新准备依赖该素材的受影响正例；坏字节保留为独立负例，不抹掉事故。
2. 使用一张有效 PNG 和一张含实际内容/透明度的图片验证变换；不能只靠 1×1 纯色图证明内容保真。
3. 对坏输入给出准确、及时的失败和无提交结果。
4. 换色失败后不得换成绿色文本块、遮盖物或其他内容并宣称完成。修好夹具不会自动解决这条恢复路径。
5. 不批量修写用户已有素材。真实用户文件需要沿已有资产导入/派生机制处理，并保留原资产与可撤销性。

### 3.3 已有能力与真正缺口

| 已有事实 | 真正需要处理的部分 |
|---|---|
| 能力卡已按 ID/操作/类型去重，预算 5,200 bytes | 选择相关性未参与排序；文字先出现可挤走图片变换卡；不是相同文本对象重复装卡 |
| 当前只读执行：文字 edit 卡约 3.56 KB，图片 transform 卡约 3.44 KB | 两卡分别可装入，组合超过现预算；“只改排序”也不能保证全部必要信息可调用 |
| query 可查目录，也可按 ID 读完整卡 | `--help` 实跑退出 1；发现返回目录后还需多次查询，未形成完整单次调用信息 |
| `component.package patch` 支持 shared/instance、changedFiles/deleteFiles、基准绑定 | 发现域只标 global，按 slide/scene 查询实例 patch 得 total:0；观察仍提示完整 shared revise |
| 候选已支持多步、`$result` 与私有预演后一次事务 | 示例、投影和组织方式仍可让 AI 重复身份、重复源码或分成过多轮 |
| 图片变换已经创建派生资产、只更新当前实例，保留未选共享实例 | 失败诊断和上层语义恢复仍有断点；不能把已有 copy-on-write 重列为新建能力 |
| harness 已有 deadline、定时器、格式修复与无进展限制 | 预算须贯通 prepare/apply 等阶段；进展指纹不应依赖候选全文及一段 summary |
| renderer 每次提交/拒绝后继续 captureNext/continue | 缺少可持久化的无总结终结；main 的任务仍依赖后续 answer 才终结 |
| Component 已区分外层属性、public-props 与 full-admission | 不能声称所有参数修改都跑完整准入；成功 smoke 也不能证明任意题目逻辑正确 |
| 回执已有 affected/resources，失败可有 diagnostics/behaviorEvidence | 经过控制器和 local-agent 传输时部分信息被压成 message 或空数组 |
| Codex usage 假测试与 parser 一样使用平面字段 | 应以真实版本嵌套 wire 建 fixture，同步 strict 事件合同、投影、存储与显示 |

源码位置与本轮检查范围见第 11 节。

## 4. 用户行为与保护边界

### 4.1 按任务提供足够信息

| 工作负载 | 首轮需要的信息 | 正确结果的主要判据 |
|---|---|---|
| 精确属性/图片修改 | 选中目标、本轮现值、原资产、必要图像与操作卡 | 指定变化发生，未请求属性及图片内容保留 |
| 整页/跨页批量设计 | 目标页概览、对象关系、关键文案/公式、参考与公共约定 | 阅读顺序、内容和关联正确，无关键遮挡，批量范围准确 |
| Component/Runtime 修改 | 目标实例/共享范围、公开参数、相关源码、依赖、状态和实际故障 | 修改可保存重开，受影响交互真实运行，其他行为不退化 |
| 多阶段结构性改造 | 当前目标与已完成阶段、依赖关系、必要新观察 | 每阶段真实成果可追溯，后续不重复应用，整体目标最终闭合 |

这些是实现与评测分组，不增加四个模式按钮，也不增加固定的“难度分类模型”。是否需要进一步读取、工具组合或运行观察由原生 AI 决定。

### 4.2 焦点、授权与保持条件各有含义

- 选中对象决定信息优先级，不悄悄将用户明确的整页引用改成只读单对象，更不限制 CLI 的 OS 权限。
- 目标引用冻结在本次请求的 canonical target、revision、sessionGeneration/epoch 上；不能将历史 `selection:1` 解释为现在选中的对象。
- “只改颜色”应检查图片类型、原资产派生关系、显示方式、可见形状、alpha、未选区域与未请求字段。只查外框不够，只查 carrier 也不够，因为 image→text 都可属于 Native。
- “重排页面”允许改变位置与尺寸；“把正方形变成翻滚 Runtime”允许授权对象的载体转换。不能建立全局“禁止换载体/禁止改几何”规则。
- 任务保持条件来自用户已明确范围、已选择操作的正式合同和已有结构化约束。AI 的要求摘要是计划说明，不能创造新授权，也不能删掉原要求后宣称完成。
- 宿主按可表达的字段、引用、类型、区域和实际行为检查约束；不新增自然语言授权分类器，也不声称机器能证明所有语义或审美要求。
- 无法机器判定的语义效果保留实际预览与按需观察。已明确授权的修改沿现有 auto/preview 策略执行，不统一追加一轮确认。

对于已开始的图片变换恢复，宿主保留所选操作和失败事实，反馈限定同一操作可行的修复信息；没有新意图或既有授权依据时，不自动将恢复变成对象替换。初次候选同样经过目标/影响校验，不能只给第二轮加保护。

## 5. 上下文、能力发现与短候选

### 5.1 信息分层与预算

首轮分为稳定说明、本轮事实和长尾入口：

- 稳定说明：协议语义、常用调用结构、能力语义版本。保持确定的组织方式，去掉重复口号。
- 本轮事实：目标与现值、活动草稿、引用关系、资源状态、原生已确认配置、必要图像。每轮按当前身份更新。
- 长尾入口：其他能力短目录、可读源码/材料路径和完整发现方法。初始未展开不代表不支持。

保留现行“简单选区初始技术说明 ≤12 KB，原图/材料/源码单列”的目标。必要信息超预算时显示分项及原因，不通过截掉关键 Schema、隐去错误或删除图像来达标。不能把旧 119/127 KB 当成当前输入。

必要操作卡优先于无关遍历项：按选中目标、操作适用性、直接依赖和任务范围排序；同一能力去重。精简卡片冗余 references/示例，但**实际参数、必需条件和一个正确示例必须完整可得**。不把该类型的所有工具都列为无条件热卡，也不无限扩大预算。

### 5.2 一次发现的完成定义

一次发现应返回：能力 ID、operation/mode、适用 Surface/owner/对象类型、真实可用状态、完整输入分支、默认值与限制、相关目标/资产入口、一个可直接用的示例、主要错误和恢复方式。

能力目录、卡片、帮助、示例和模式适用域由正式工具 Owner/生成器产生，不能手写第二份参数真相。必须补以下直接缺口：

1. `--help`、无参数、未知参数与示例的明确输出。
2. `component.package patch mode:instance` 在实际支持的 scene/Flow 正文等域可发现，shared 模式仍显示共享影响。
3. 组件观察删除强制完整 shared revise 的旧指令，优先按实际任务给实例或共享 patch。
4. 资产诊断进入首轮对象摘要。区分“附件未能解码”“此变换预检失败”“格式不支持”“尚未检测”，不把未知写成可用或不可用。
5. 助手与外部 Builder 共用同源卡片与发现行为；044 迁移前不改变当前外部课件 Skill 的分阶段确认路径。

### 5.3 短传输的正式边界

模型主要输出工具、目标短引用和变化参数；宿主掌握的请求身份、完整目标、载体与资源绑定由当前请求映射展开。以下仅是待实现的语义示意，**不是现有可调用协议**：

```json
{
  "version": 1,
  "steps": [
    {
      "id": "recolor",
      "tool": "asset.image.transform",
      "targetRef": "t1",
      "input": {
        "operations": [
          {
            "kind": "replace-color",
            "sourceColor": "#ff0000",
            "targetColor": "#22c55e"
          }
        ]
      }
    }
  ],
  "afterCommit": "finish"
}
```

落地要求：

- 在现有 Generation/LocalAgent Owner 下定义 strict 版本投影；不修改 V9 持久化 Schema。
- 工具参数来自现有正式 Schema，短投影展开后仍经过当前完整候选解析、精确 destination 校验和正式 Facade。
- `t1` 是当前请求映射，不是活动选择查询。跨请求、未知、过期或不匹配引用明确拒绝。
- carrier 从已有工具/目标规则派生，不能由模型省掉必要的能力解释或生成载体准入。
- 保留多步、有序依赖及已有 `$result`；创建 ID 由真实工具结果产生，不让模型猜测。
- 不适合短投影的操作仍可使用完整正式载体；不强制所有工具变成任意 JSON Patch。
- 源色、区域等语义信息缺失时，AI 读取图像/定位结果或澄清；宿主不猜。
- 新旧 adapter 必须按显式能力/版本协商处理，缺失新字段不等于默认完成。

## 6. 原生配置、缓存、用量和聊天分流

### 6.1 Codex 初始化与稳定输出 Schema

先解决可证实的应用变化，不以修改用户模型配置换取表面提速：

1. 将用户选择沿 session 创建链传入，按该版本原生支持的字段初始化模型；当前 harness 的 openSession/configure 顺序须一起调整。
2. 初始化时确认原生已返回的配置；effort 等只能由 turn 参数设置的字段，在原生 settings/配置事件返回后记录实际生效值。不能等待只能由 turn 触发的确认后才允许发出 turn，也不能假定 thread/start 存在顶层 effort 字段。
3. 恢复会话、用户主动换模型及目录默认配置分别处理；不为避免 model-switch 阻止合法切换。
4. `outputSchema` 在同版本、同传输模式内保持稳定，将本轮 requestId 值留在输入/信封并由宿主严格相等校验。当前 candidate/auto 仍有不同 Schema 分支，去掉常量不代表所有模式已经共用同一前缀；两个分支都须保留候选所属请求校验。不能删除身份校验，也不固定过期目标来换缓存。
5. 修后先用同配置、同内容的有限配对比较确认首次/续轮缓存输入和总时间；第一轮冷启动单列。未命中时查看仍有证据的前缀因素，不据此盲目压缩全部上下文。

官方文档确认输出格式/Schema、模型和推理配置都可能影响可复用前缀；这支持稳定应用可控部分，不能证明本例所有缓存失配都由一个字段造成。[Prompt caching](https://developers.openai.com/api/docs/guides/prompt-caching)

### 6.2 用量必须贯通整个事件链

按对应 CLI 版本的真实 wire 解析 Codex `tokenUsage.last/total`，同步 adapter、strict usage 事件、投影、持久化与受影响 UI。当前用错误平面结构造的测试必须换成真实嵌套 fixture。

规范口径：

- 本次用量与累计用量分开保存；累计更新不能逐条累加。
- 保留原生支持的 input、cached input、output、reasoning 及其他实际提供字段；reasoning 若包含于 output，不再合计一次。
- 缺失仍为 unknown/null，不补零，不假造 Claude/OpenCode 同等字段。
- 标明来源、native thread/turn/request 可用标识；只有原生确实暴露时才报告精确模型请求数。
- 计量能力不等于服务时间分解能力；成本只在存在可靠计价与实际服务档时估算。
- 原生 JSONL 本轮用作事故取证；产品常规观测优先消费版本化原生事件，不把读取各 CLI 私有历史格式变成运行前置。
- 回放、重复通知、累计重发和会话恢复要去重；事件重开不能重复计费式累加。

### 6.3 消息在进入聊天前按类型分流

保留现有结构化结果路径作为首批主线，先修正确的消息投影；不为本例全部 JSON 就预设必须迁走 outputSchema。

- 按原生消息 item ID、phase/type 与 turn 生命周期区分公开进展、工具活动、候选数据、最终可读回答和宿主回执。
- 结构化候选片段缓冲到完整、正确身份且有效终态后才交付；中途出现的 edit 信封不提交。
- 对确认属于可读消息的文本正常流式显示；若是结构化 reply，只显示经过安全解析的 reply 内容，不将整个信封先发送给 UI。
- 不以“包含花括号”隐藏普通代码/JSON讨论；不靠提示模型“别输出 JSON”代替类型分流。
- 覆盖逐字符流、多个消息项、中途非最终候选、异常结束、取消迟到与历史重开。
- 结果摘要优先由真实提交回执生成，不能用模型 summary 给错误替换洗成“仅改颜色”。

如果该版本协议在真实测试中仍无法可靠分开进展和候选，再在同一 adapter 的现有 staging/artifact 路径做受控替换验证；保持 strict 宿主解析与有限格式修复，不同时长期维护两条不同完成真相。

### 6.4 OpenCode：分段定位并证明配置生效

不将 `Failed to fetch models.dev` 或旧版本公开 issue 直接认作当前下拉框失败根因。原报告对应版本的缓存/快照/后台刷新解释仍作为定位线索；本轮未重现该 UI 失败，根因继续标为待核实。

沿现有链路记录：进程启动 → initialize → session/new 或恢复 → 配置目录返回 → 选项解析 → UI → set_config_option → 原生完整状态确认 → 下一任务实际配置。

实施要求：

- 并发相同查询合并；使用真实 CLI 版本、工作上下文和非秘密配置身份隔离缓存，失败可刷新。
- 区分加载失败、解析失败、真实空目录和旧缓存；旧目录可显示但不冒充当前已确认可用。
- 消费模型切换返回的完整配置状态，因为强度等选项可能随之变化；兼容协议允许的分组选项。
- 区分待应用偏好与原生已生效值，执行记录只写真实确认值。
- 已有配置系统继续使用；不增加一套模型目录服务，不复制凭据。

ACP 规范要求客户端处理配置状态更新；必须验证下一次运行使用所选模型，不能只证明下拉框可点。[Session Config Options](https://agentclientprotocol.com/protocol/v1/session-config-options)

## 7. 完成、继续、失败与任务状态

### 7.1 完成声明不能只变成 renderer 的 break

候选可表达成功后 `finish` 或 `observe/continue`，继续时给出具体理由，例如受影响交互仍需观察、依赖真实生成资源、还有已声明阶段。字段属于现有会话协议的窄扩展，不建立目标证明引擎或新任务 Store。

| 当前情况 | 行为与可见状态 |
|---|---|
| auto、检查通过、实际提交、回执已持久化、声明 finish、必要证据满足 | 本地任务进入已完成，不再为了总结启动推理；显示回执支持的实际变化 |
| 检查确认目标已经满足，正式返回 unchanged、回执已持久化且声明 finish | 可以结束并显示“无需修改”；不增加 revision 或撤销项，不能把任意空候选当成功 |
| preview 已检查但用户未应用 | 候选就绪/待应用，零工程提交；不能显示已修改 |
| preview 用户应用后满足完成条件 | 保存 committed 或正式 unchanged 回执，再完成，并准确区分已修改与无需修改 |
| 已提交且需要明确新观察/后续阶段 | 保存回执，按受影响范围采集，继续同一任务 |
| 准备或应用失败 | 按结构化诊断决定一次有依据的修复、重新观察或停止；finish 不生效 |
| stale、取消、拒绝或新输入使候选失效 | 当前未提交候选零写入；已提交阶段保留，状态准确 |
| 工程已提交但回执持久化失败 | 在当前进程保留已知 receipt，只重试回执/状态保存，不重复提交；显示已应用但记录未完整 |
| 旧 adapter 缺完成元数据 | 使用现有有预算的兼容流程，不能自动推定任务完成 |

完成链必须覆盖 `GenerationTaskController`、main harness、`AiTask` guard/持久化和 UI 投影。目前 `acceptAiHostResult` 进入 feeding-back，主要依赖下一轮 answer 改为终态；仅在 renderer 返回会留下“界面结束、本地任务仍待续行”的分裂。

已持久化的回执和终态必须支持重开恢复。提交成功但回执尚未落盘就崩溃，当前证据只覆盖崩溃前内存保留与重试，不证明跨崩溃完整恢复。重新进入时先核实当前工程事实，未知部分如实呈现；不得自动重提事务、重复相对修改或补造完成回执。

本地会话先保存真实回执，后续原生会话必须获得该事实：

- Codex 本地抓取的协议含 `thread/inject_items`，官方文档也说明它可不启动新 turn 而追加并持久化历史。先验证当前 adapter/版本的角色、身份、幂等与恢复语义，再用于无推理追加。
- 不支持这一能力的 adapter 在应用会话标记待送回执，下次真正请求前带入；不能伪造“已写进原生历史”。
- 无推理追加不是所有 CLI 的共同完成前置，追加失败也不能重做工程提交。版本能力不足时使用准确的待送状态。[Codex App Server](https://learn.chatgpt.com/docs/app-server)

### 7.2 失败信息全链保留

复用现有 `AuthoringToolFailure`、diagnostics、behaviorEvidence 和 `GenerationCommitReceipt.affected/resources`；需要新增传输字段时在现有 strict LocalAgent/AiHostResult 合同中演进。

至少保留：失败阶段与步骤、正式错误码、目标/资产、实际是否提交、当前 revision、可执行的恢复信息、必要字段路径/截图/状态和已完成部分。恢复选项表示工具真实支持的动作，不是让模型再次搜索整仓的泛化建议。

重点接回当前会丢失的信息：工具失败 → prepare → controller → LocalAgentHostResult → AiHostResult → 下一原生输入；动态 worker/smoke 失败时也应保留已经实际取得的帧与触发步骤，未取得的明确为空。

不能将所有错误压成“验证失败，请修复”，也不能把“意图越界”误用成现有表示引用映射错误的 `replacement-unmappable`。

### 7.3 预算与无进展保护沿现有 Owner 补齐

当前已有 20 分钟任务 deadline 和原生硬定时器，不重建预算系统。补的是：

- 同一任务预算贯穿观察、原生执行、候选准备、必要准入与提交前检查；子阶段/重试不重置。首批保留现有绝对 deadline；awaiting-apply / waiting-input 期间不启动无必要推理，不隐式延长期限。用户等待在性能统计中单列。恢复时检查 deadline、revision 和 epoch；到期候选不得直接应用，用户再次请求继续时经既有新输入/重新观察入口准备，保留已完成事实，不复用旧候选或伪装仍在原执行窗口。
- 明确宿主 continuation、原生 turn、候选、格式修复与内部模型请求的区别。
- 无进展依据错误类别、目标、相关资源/源码实质变化、诊断和实际成果；仅换 candidateId、注释或 summary 不算进展。
- 保留一次有明确诊断的格式修复；无进展计数按当前实现明确为“首个失败建立基线，其后连续两次无实质进展则停止”。随机身份变化不算进展，普通有进展的多阶段创作不当成格式重试。
- 缺资源、认证或目录失败优先定位接入，不自动换更强模型；没有新依据的同一路径停止。
- 超预算停止未提交工作，显示已完成/未完成/可恢复点；不自动撤回此前合法提交，更不吞掉用户后续手改。
- 先报告实际耗时和取消率，再设定工作负载的可配置预算；不直接把 GPT6Pro 的 90/180/360 秒或修复次数作为默认产品限制。

## 8. 图像操作的正确闭环

本轮图片实现优先级是：**有效正例 + 坏输入准确失败 + 及时诊断 + 防错误替代**。

`asset.image.transform` 继续只负责已实现的静态像素换色、裁剪和缩放。保持原有 Native image / Flow media image 载体、当前实例与未选共享实例语义。已有 copy-on-write 和历史事务继续使用。

验收按实际操作：

- 换色：尺寸、alpha、透明像素下 RGB、区域外内容及未请求显示属性保持；验证真实画面。
- 裁剪/缩放：检查合同规定的区域和尺寸变化；不套用换色的“宽高必须不变”条件。
- 原图解码失败：明确具体输入与变换状态，工程无提交，不产生遮盖或替代成功。
- 原图附件失败、变换尚未检测：如实呈现两条路径的状态，允许真正适用的工具预检。
- 生成式人物编辑：当前目录必须显示真实能力状态；没有可用能力时说明缺口。不能用裁剪/换色代替“把人物变成秦始皇”，也不把产物已生成等同语义已正确。

不通过关闭 PNG 校验解决夹具错误，不预设丢弃所有辅助块，也不把 Canvas 重编码称为普遍无损。未来真实有效素材暴露兼容性缺口时，在同一资产 Owner 下按实际失败模式扩展，保持颜色与透明度合同。

## 9. 复杂编辑：先接通增量，再按瓶颈深化

### 9.1 首批直接复用

- Component 优先用已有文件级 patch：基准 package/version、changedFiles/deleteFiles、shared/instance。单实例修改不默认指向 global package，明确共享修改才更新所有相关实例。
- 多项关联变化尽可能进入一份候选，按依赖顺序私有预演并一次事务提交；一次撤销恢复这一批。
- 同一对象语义兼容的配置可合并；不重排创建→引用→替换，不把依赖运行测量的后续步骤提前。
- 小模块或主要内容均改变时允许完整文件；减少全文输出是手段，不把“必须 diff”作为仪式。
- 新生成或修改的 Component/Runtime 源码及影响其运行的资源变化，按既有动态准入与证据复用规则检查；Native、Recipe、Existing Component 承担自身实际适用门。普通图片资源、wrapper-only、关闭 Runtime 等保留现有适用路径，不统一触发完整动态准入。

### 9.2 后续仅在实测说明值得时实施

| 项目 | 启动证据 | 实施边界 |
|---|---|---|
| Runtime/函数级或精确文本补丁 | 大量未变源码输出主导时间或错误 | 明确基准、唯一精确命中、冲突可见；不做模糊替换或任意工程 patch |
| 带基准的增量观察 | 必要续轮重复发送大量未变事实 | 绑定基准与当前 revision；恢复、换模型或丢基准时重新紧凑快照 |
| 静态检查/资源复用 | 编译、Base64、IPC或加载耗时可观 | 缓存不可变资源/编译证据，绑定源码、依赖、合同/规则和环境；不缓存“任务完成” |
| 最终态行为验证合并 | 同候选中间版本被重复完整验证 | 先合并操作，再考虑验证需求合并；任何提前运行的新源码都须先准入 |
| 参数化与可观察状态 | 反复修改同类受控互动底座 | 给高频底座公开数据/状态/更新影响；不自动参数化所有旧 Runtime |
| 预热、Worker、独立准备并行 | 主线程、编解码或启动占主导 | 不复用有状态活动实例，不改变生命周期与清理 |
| 原生子任务并行 | 独立内容生成是主要串行成本 | 使用原生已有能力与私有候选，统一冲突检查和提交；不新增应用调度平台 |

动态 smoke 目前可证明挂载、更新、resize、暂停/恢复、捕获等生命周期，成功证据本身标为 `requires-review`。答题正确/错误/重试、实验重置等语义必须用实际受影响路径验证；静态编译、截图或“没有异常”不能替代。

渐进预览只显示已经闭合且可安全准备的私有候选，不把半段源码或未闭合 JSON 执行到教师 live session。实际正式应用始终走当前事务。

## 10. 实施顺序、并行边界与验收

以下是本专题的实施批次，不是新增任务板或另一套长期路线。当前任务板为 0，089–104 已集成；实施时按当时事实为剩余缺口安排协调，不将它们全部重新启动。W/G 编号只作历史来源索引。

### 10.1 批次与现有 Owner

| 批次 | 可交付结果 | Owner / 当前路线对应 | 开工与汇合条件 |
|---|---|---|---|
| A：正确性与最小观测 | 修正正例夹具、保留坏输入；前置准确诊断；恢复用量；修消息分流和配置初始化；修组件提示/发现域与 help | Assets/Generation；LocalAgent；能力 Owner。对应 090–098、101、104 的当前缺口 | 明确 direct producer/consumer 即可开始；计时体系不阻塞已知正确性修复 |
| B：可直达编辑与完整终态 | 相关完整卡、strict 短投影、条件终结、结构化失败、预算贯通和真实回执衔接 | Generation/LocalAgent/Chat；对应 090、095–097、100–102、104 | 共享合同先由唯一 Owner 定义；独立叶子随后并行，真实 consumer 接线后汇合 |
| C：复杂编辑效率 | 用已有 patch/多步候选完成整页与交互修改；按实测减少重复观察/检查/资源传输 | Components/Runtime/Generation；对应 099，连续课例优化落 1.9-050 | 提示修复与既有 patch 使用可随 A 开始；新缓存/细粒度补丁需要瓶颈证据 |
| D：真实任务与版本汇合 | 准确编辑与复杂编辑的固定配置结果、时间、失败、生命周期和三 adapter 边界 | 当前 103/050，S3；1.9/2.0 既有性能质量节点 | 只补受影响证据；PPTX、三 Surface、导航等既有并列发布门仍保留 |

1.9 的未命名/首存、材料、内置双流程、聊天工作台与长任务，2.0 的全部软件内 QA/修复/导出闭环保持既定落点。本专题不以效率优化绕过手动教学简报→策划→呈现简报→脚本的确认顺序，也不将全部创作流程压入 1.8。

### 10.2 可并行的实际写入安排

性能对照需要固定变量；开发不必因此全部串行。

- 一个集成 Owner 持有共享 Generation/LocalAgent 合同、harness、控制器及共同消息状态写域，先定窄接口，顺序集成。
- 独立叶子可分别负责：PNG/资产诊断；能力生成/帮助/发现域；OpenCode 配置；Chat 展示；组件观察与 patch 提示；目标验证。
- `codexAppServer.ts` 的用量、初始化、Schema 与消息分流必须由同一实际 writer 合并，不能四个分支同时修改后各自声称完成。
- `generationSnapshot.ts` 的焦点、组件提示和诊断同样由一个实际 writer 汇总。独立测试可以并行准备，结果归同一 consumer 交付。
- 多 writer 实现使用隔离工作区、精确非重叠路径和现行写锁；共享实体文件顺序处理。只读核验不写产品文件。

### 10.3 最小充分检查

先运行对应命名局部检查；具备真实接线后再做有限自然语言任务。下表是检查选择，不要求每个叶子全部执行。

| 待证明属性 | 最小有效反例与正例 |
|---|---|
| 能力能发现并调用 | 混合页文字在前但选中图片；scene/Flow 正文实例 patch；help 与一次发现→正式 Schema 解析 |
| 组件增量提示一致 | snapshot→能力卡→instance patch；未改文件保留；shared/instance 影响正确；基准冲突拒绝 |
| 短传输不改事务语义 | 完整/短候选得到同一业务结果；未知/跨请求引用、stale、scope、前序引用；真实提交与一次撤销 |
| 用量与配置准确 | 对应版本嵌套 wire，last/total 去重与 null；创建/恢复/主动换模型，原生确认和下一任务一致 |
| 聊天分流 | 逐字符、多消息项、非最终 edit、正常 JSON讨论、取消迟到与历史重开 |
| finish/observe 状态一致 | auto、preview待应用、committed/unchanged、拒绝、stale、已提交后回执失败；不重提事务；main/renderer与已持久化终态重开一致；未落盘崩溃后的未知状态准确呈现 |
| 失败反馈可行动 | 同一诊断不因换ID而继续；新证据可合法修复；动态失败触发步骤/已有帧确实送回 |
| 图片正确 | 有效 PNG、含内容/透明图片、原坏输入；可见形状、alpha、区域外内容和共享实例；无绿色文本替代 |
| 复杂交互正确 | 一个指定故障的实际触发与修复；暂停/重置/重试等受影响行为；必要保存重开/Player/导出 |
| 取消与手改 | 取消、手动改目标、新输入和迟到准备完成交错；旧候选零提交，已有合法阶段不丢失 |

### 10.4 性能指标与有限真实任务

主指标是**请求到首次正确可用结果出现的时间**，同时报告全任务结束时间、首次成功率、误改、返工、取消与未完成。候选就绪、首字出现、界面流畅和 token 减少分别记录，不能替代正确完成。

最小阶段事件：请求接收、观察准备结束、原生启动/恢复、turn 发出、首个公开输出、工具开始/结束、候选完整、预演/准入开始结束、应用、回执持久化、画面稳定、任务终态。原生未暴露的内部请求边界标为未知。统一关联任务/请求/候选/事务，按依赖关键路径分析，不把并行跨度累加。

preview 另记录候选就绪、等待应用开始和用户决定；澄清/授权等待同样单列。同时报告包含用户等待的端到端时间和扣除明确用户等待的系统处理时间，auto 与 preview 分桶比较，不把用户决策时间算成模型或宿主耗时。

首批任务从以下固定集合按改动选择，不把 24 个模板全部升为局部开工门：

1. 普通文字样式与图片精确变换；坏 PNG 单列失败路径。
2. 同页存在无关对象时编辑选中图片，验证无需研究查询脚本。
3. 保留文案/公式重排一页；授权范围内批量统一若干页。
4. 单实例组件源码修改与明确共享修改各一例。
5. 实际交互故障修复、连续修改与一次必要观察。
6. auto/preview、取消、手改、回执失败、恢复和配置选择的相应反例。

先固定源码/输入副本、CLI版本、实际模型/强度/服务档、工作上下文、任务与测量起终点；冷启动和连续会话分开。保留原事故，不重跑九分半坏任务“建立基线”，也不拿修好的有效 PNG 和原坏 PNG直接计算提速率。

一次只改变要归因的因素；可先比较整批对正确结果的收益，再对有必要的缓存/初始化因素做小范围配对。保留全部失败，有限样本报告原始值和中位数，不能用三次证明 P95或未来100%成功。

保留当前合同的本地目标：上下文提示更新 p95≤500ms，原生事件到 UI p95≤300ms，至少30个确定性本地事件；简单选区技术说明≤12KB且必要材料单列。常见 Native 秒级、代表动态分钟级、标准整课30分钟内是分阶段产品目标，不是本轮已实现事实或新硬承诺。

原 S3 有限三 CLI门继续执行：关键链按现行规格预先固定重复，保留失败并只复核受影响证据；其余有效证据复用。该既有发布门与新增性能实验的样本需求分开，不以“少量实验即可”降低原门，也不把每次文档/局部修复都变成完整付费矩阵。

### 10.5 何时才改变模型或接入路线

完成直接缺陷修复后，若完整信息下短操作仍主要慢在原生请求，可比较用户已授权的模型/强度；任务、服务档和冷/热条件同时记录。强模型可能因少返工更快完成，低强度也可能因错误更慢，按正确结果比较。

Auto、付费服务档、专用代码应用模型、直连 API 或新的服务/凭据管理仅列为后续产品研究选择；本轮不默认增加，也不将官方别的模型速度宣传迁成 Luna 或三 CLI 的保证。

停止扩展的条件：直接失败已关闭、调用与状态一致、受影响生命周期检查充分、真实收益和剩余瓶颈可解释。没有新证据时，不继续建缓存平台、路由器、通用 QA 或全仓重构。

## 11. 证据与边界

### 11.1 关键本地源码索引

行号对应本次核验基线；实施时以符号与实际源码为准。

| 证据 | 位置 |
|---|---|
| 默认 page、选择标记与组件完整共享修订提示 | [CourseChatPanel.tsx](src/renderer/ui/chat/CourseChatPanel.tsx)；[generationSnapshot.ts](src/renderer/authoring/generation/generationSnapshot.ts)，47–77、111–141 |
| 遍历与5,200字节能力预算 | [generationCapabilities.ts](src/renderer/authoring/generation/generationCapabilities.ts)，13–47 |
| 查询、完整卡与生成帮助逻辑 | [courseAgentCapabilities.ts](src/shared/courseAgentCapabilities.ts)，70–94、159–194；[generate-ai-capabilities.ts](scripts/generate-ai-capabilities.ts)，query生成部分 |
| 实例patch与错误发现域 | [componentPackageTool.ts](src/renderer/authoring/tools/componentPackageTool.ts)，19–59；[authoringToolFacade.ts](src/renderer/authoring/tools/authoringToolFacade.ts)，component.package注册 |
| 多步私有预演、完整target与一次提交 | [generationContract.ts](src/shared/generationContract.ts)，54–94；[prepareGenerationCandidate.ts](src/renderer/authoring/generation/prepareGenerationCandidate.ts)，33–39、154–229 |
| Codex动态Schema、初始化、用量和消息 | [codexAppServer.ts](src/main/localAgent/codexAppServer.ts)，58–114、513、597–604、1148–1157 |
| 当前严格用量事件与投影 | [localAgentTaskContract.ts](src/shared/localAgentTaskContract.ts)，usage分支；[localAgentProjection.ts](src/shared/localAgentProjection.ts)，40–44 |
| 任务预算、无进展和终态 | [harness.ts](src/main/localAgent/harness.ts)，268、303–310、372–380、489–490、573–579；[localAgentTaskGuards.ts](src/shared/localAgentTaskGuards.ts)，acceptAiHostResult |
| 固定续行与回执保存 | [generationTaskController.ts](src/renderer/authoring/generation/generationTaskController.ts)，123–170 |
| PNG实现、派生资产与坏夹具 | [imageTransform.ts](src/renderer/project/imageTransform.ts)；[imageTransformTool.ts](src/renderer/authoring/tools/imageTransformTool.ts)；[夹具生成器](scripts/build-architecture-baseline-fixtures.ts)，107–117 |
| OpenCode配置确认 | [opencodeAcp.ts](src/main/localAgent/opencodeAcp.ts)，287–320 |
| 动态准入与实际行为证据 | [dynamicCandidateAdmission.ts](src/renderer/authoring/tools/dynamicCandidateAdmission.ts)；[admissionWorker.ts](src/renderer/authoring/generation/admissionWorker.ts) |
| 原生outputSchema与无推理注入接口 | [本地抓取的Codex v2 Schema](output/r18-codex-protocol/codex_app_server_protocol.v2.schemas.json)，2377、18587、22369–22370 |

### 11.2 本轮已做与未做

已做：完整分工阅读八份报告及原方案；读取当前路线/合同/直接源码与目标测试；独立重算事故原生用量；复现 PNG CRC/zlib 损坏；只读运行能力卡排序、实例 patch 查询和帮助入口；运行三条明确命名的非付费单元用例，验证既有 deadline 保留、无进展候选停止和回执失败不重复提交。三条通过仅证明这些既有行为。

未做：产品代码修复、真实编辑器图片/交互复测、OpenCode 当前 UI 故障复现、新旧缓存/模型/延迟受控对照、完整三 CLI 矩阵、外部框架性能测试。读过的其他测试不冒充本轮执行结果。所有新方案仍须经过第 10 节对应直接验证。

原始事故文件仅用于本地取证，不将原生完整对话、系统指令、认证信息或原始工程材料复制到本方案。可定位来源：

- Codex rollout：2026-09-09，`01a083fe-e8bc-7313-b9f0-0d403238c3bc`，对应 `token_count`、task边界与候选消息。
- 应用 local-agent/v2 会话：`2df4263f-3190-4132-bf5a-cdfc304be66f`，对应 observations、events、tasks 与真实 commit receipt。
- 坏 PNG：上述夹具生成器及事故 mixed-figure 资产。

### 11.3 评估输入与外部参考

| 输入 | 本轮用途 |
|---|---|
| [Claude Fable 5.1](<docs/archive/2026-09-planning/Claude Fable 5.1 最短路径评估.md>) | 主要事故取证线，逐项复算并修正因果口径 |
| [GPT6Pro](docs/archive/2026-09-planning/GPT6PRO最短路径评估.md) | 主要复杂编辑线，核实当前已有机制及真正缺口 |
| [Cursor Grok 4.6](<docs/archive/2026-09-planning/Cursor Grok 4.6 最短路径评估.md>) | 能力预算与失败边界增量 |
| [Sonnet 5](<docs/archive/2026-09-planning/Sonnet 5 最短路径评估-2026-09-09.md>) | 路线与历史基线核对 |
| [Kimi](docs/archive/2026-09-planning/Kimi最短路径评估.md) | 对照实验、应用策略和配置反例 |
| [Muse Spark 1.3](<docs/archive/2026-09-planning/Muse Spark 1.3 最短路径评估.md>) | 像素/透明度、用量与会话边界 |
| [Gemini 3.8 Flash](<docs/archive/2026-09-planning/Gemini 3.8 Flash 最短路径评估.md>) | 可读展示建议与过强提速/容错主张的反证 |
| [DeepSeek V4](docs/archive/2026-09-planning/deepseek-v4-flash-vision-exp-最短路径评估.md) | 逐adapter、统计口径与路线映射增量 |

原方案外部研究中的 tldraw 语义动作、Penpot 成员级文档、Excalidraw 引用、Cloudflare 查询组织、Pi 稳定上下文与终结、Aider 定向读取、Continue 并行准备，只保留其可解释本方案的机制；不引入这些平台或复制 live 编辑模型。RouteLLM、LiteLLM、Cursor 路由/专用编辑也不构成本产品自动路由、额外服务或秒级保证的证据。

这些机制已有本地直接问题与既有 Owner 支撑，本轮无需依赖外部项目的性能、市场份额、最新归属或许可猜测决定开工。外部源码若将来实际引入，按当时固定版本和采用范围再核查许可、依赖与构建；本轮没有引入第三方代码。

本文正文已链接本轮重查的官方缓存、App Server、ACP 与 PNG资料。外部文档说明协议机制，源码与本地直接结果决定本产品当前事实；二者均不能替代教师对实际视觉、互动和可编辑交付的验收。
