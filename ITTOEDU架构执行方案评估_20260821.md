# ITTOEDU_ARCHITECTURE_EXECUTION_PLAN_20260821 详细评估报告

> 版本：v2（详细版，供后续 AI 交叉审阅、评估与整合使用）
> 评估日期：2026-08-21
> 评估对象：`C:\Users\74755\Downloads\ITTOEDU_ARCHITECTURE_EXECUTION_PLAN_20260821.zip`
> 包内容：1 个根目录 `ITTOEDU_ARCHITECTURE_EXECUTION_PLAN_20260821/`，38 份 Markdown（README + 清单 + 7 个分组目录 36 份），合计约 100 KB
> 核对仓库：`C:/Users/74755/Documents/HTML课件编辑器`（= ghostairship-debug/ittoedu）
> 核对时点：HEAD = `690411d4a101b4020134712108262bddf08e0d2e`（与方案声明基线**逐字节一致**，其间 0 个提交）

---

## 0. 报告元信息（后续 AI 请先读本节）

### 0.1 评估方法

1. 通读 zip 内全部 38 份文档（无抽样，全文阅读）；
2. 对方案中每一条**可机器核查**的事实声明，直接在仓库源码中取证（文件存在性、精确字节数、行号、符号名、npm scripts、依赖版本、目录文件数）；
3. 对方案的**设计主张**（不可直接证伪的部分），对照仓库现行治理文件（`AGENTS.md`、`COURSEWARE_DEVELOPMENT_PLAN.md` 12.10、`docs/contracts/`）做一致性分析；
4. 全部核查命令收录于附录 A，可复现。

### 0.2 结论置信度分级（本报告通用）

| 标记 | 含义 |
|---|---|
| `[已证实]` | 在源码中取得直接证据（含路径/行号/字节数） |
| `[部分证实]` | 主体成立，细节有出入或未穷尽核查 |
| `[未证实]` | 无法或来不及核查，列入第 8 节开放问题 |
| `[失实]` | 与源码矛盾（本次评估**未发现任何一条**） |

### 0.3 如何复核本报告

- 事实类：跑附录 A 命令即可逐条重放；
- 行号类：行号基于 HEAD `690411d`，若仓库前进需以符号名重新定位；
- 判断类（第 5、6 节）：属评估者意见，交叉审阅者可持不同结论，但请引用同等级证据。

---

## 1. 总体结论

**这是一份事实底盘极为扎实、技术主张与仓库红线全面兼容、可以采纳的架构治理方案。它的问题集中在"治理衔接程序"而非技术内容。建议：修正 12 项问题清单中的 2 项高优先级 + 3 项中优先级后，作为 Editor 1.0 教师验收完成之后的主线架构计划落地。**

分项评分（供整合时权衡，5 分制）：

| 维度 | 评分 | 一句话依据 |
|---|---|---|
| 事实准确性 | 5.0 | 17 组核查全部命中，含精确到字节的示例数据（见 FACT-13） |
| 与现行治理兼容性 | 4.5 | 技术红线零冲突；程序性衔接缺 2 处（ISSUE-02/03） |
| 技术路线合理性 | 4.5 | 绞杀者式渐进 + 反双写意识强；个别可行性前提未验证（ISSUE-08） |
| 可执行颗粒度 | 3.5 | P0–P4 工作包可直接派发；P5/P6 仅有标题（ISSUE-06） |
| 文档工程质量 | 4.5 | 自带阅读预算控制与权威顺序；有 1 处失效内链（ISSUE-01） |

---

## 2. 文档包结构盘点

```text
ITTOEDU_ARCHITECTURE_EXECUTION_PLAN_20260821/
├── README.md                 总纲：问题定义、关键决策、启动方式、最终形态
├── PACKAGE_MANIFEST.md       37 条目清单（不含自身，见 ISSUE-09）
├── 00-foundation/  (6 份)    阅读矩阵、基线事实、目标/原则/非目标、目标架构、能力模式、Feature Matrix
├── 10-knowledge-system/ (4)  repo-index 决策、数据模型、生成器与新鲜度、查询与 Context Pack
├── 20-modules/     (11 份)   模块地图 + Editor Core / App Shell / Surfaces / Components /
│                             Runtime+Interactions / Media / Player+Export / Diagnostics /
│                             UI 组合 / 当前→目标文件迁移地图
├── 30-execution/   (4 份)    路线图 P0–P6、工作包清单、依赖与并行、Legacy 清理顺序
├── 40-development/ (4 份)    单人+AI 工作流、任务协议与文件防火墙、验证策略、DoD 与交接
├── 50-templates/   (4 份)    任务卡、Feature Manifest、ADR、Handoff 模板
└── 90-appendix/    (3 份)    50 条全局不变量、风险登记（18 条）、术语表（24 条）
```

逐份一句话评注见附录 B。

---

## 3. 事实核查台账（FACT-01 ～ FACT-17）

> 每条格式：方案出处 → 声明 → 证据 → 结论。后续 AI 交叉审阅时，若仓库已前进，请以"符号名 + 语义"重定位，不要依赖行号。

### FACT-01 基线一致性 `[已证实]`

- 出处：`README.md` 头部、`PACKAGE_MANIFEST.md` 头部。
- 声明:方案基线 = ghostairship-debug/ittoedu `main` @ `690411d4a101b4020134712108262bddf08e0d2e`。
- 证据：`git rev-parse HEAD` 输出同一 SHA；`git log 690411d..HEAD` 为 0 条。
- 附注：工作树**不干净**——`artifacts/ai-capabilities/` 下 3 个生成物有未提交修改（`component-catalog.snapshot.json`、`generation-evidence.json`、`index.json`），见 ISSUE-10。

### FACT-02 热点文件清单 `[已证实]`

出处：`00-foundation/01_BASELINE_AND_CURRENT_FACTS.md` §2。方案用**十进制 KB**（字节/1000），全部吻合：

| 文件 | 方案声明 | 实测 | 备注 |
|---|---:|---:|---|
| `src/renderer/store/editorStore.ts` | 352 KB | 352,666 B ≈ 352.7 KB | 9368 行 |
| `src/renderer/ui/Workspace.tsx` | 145 KB | 145.1 KB | |
| `src/renderer/ui/PropertiesTab.tsx` | 128 KB | 127.8 KB | |
| `src/renderer/styles/globals.css` | 109 KB | 109.4 KB | |
| `src/renderer/ui/InteractionEditor.tsx` | 85 KB | 85.3 KB | |
| `src/renderer/App.tsx` | 73 KB | 72.9 KB | |
| `src/renderer/ui/FlowWorkspace.tsx` | 65 KB | 65.2 KB | |
| `tests/e2e/editor.spec.ts` | 144 KB | 143.9 KB | 见 FACT-12 |

### FACT-03 Store 多状态清单（方案 §3 列 10 项）`[已证实，10/10]`

全部在 `src/renderer/store/editorStore.ts` 的 `EditorState`（L1405–1453）取证：

| 方案声明项 | 源码证据（行号 @ 690411d） |
|---|---|
| `project: ProjectDocument`（可写 V8 形状） | L1406 |
| Course Project V9 文档 | `loadCourseProject(project: CourseProjectDocument, ...)` L1464；slideBackend 会话内 present |
| `slideCandidateUi` 等 V8-shaped 投影 | L1435，源码注释原文：`Stable V8-shaped projection so React 19 getSnapshot does not loop` |
| Slide backend/session | `slideBackend: SlideBackend` L1429 |
| Flow session | `flowSession: FlowAuthoringSession \| null` L1451 |
| Spatial session | `spatialSession: SpatialAuthoringSession \| null` L1446 |
| Course authoring session | `courseAuthoringSession` L1453 |
| Slide sidecar past/future | `slideCandidateSidecarPast/Future: CourseAssetSidecar[]` L1440–1441（**全快照数组**，方案 CORE-05/MEDIA-01 的靶子真实存在） |
| Component package past/future | `slideCandidateComponentPackagesPast/Future: Record<string, ComponentPackageData>[]` L1443–1444（注意：带 `slideCandidate` 前缀，方案表述省略了前缀，语义正确） |
| 原 Project history 与各 Surface history | `history: HistoryState` L1419；另有 4 套机制：`v9HistoryToStoreHistory`（L2864 等）、`spatialHistoryToStoreHistory`（L3069 等）、`createFlowEditorHistory`（L2749）、`commitSlideAuthoringHistory`（L3452 等 8 处调用） |

**交叉审阅提示**：投影反写链路的关键函数是 `derivedV8ProjectFromBackend`（L2989）、`derivedV8ProjectFromSpatial`（L1193）、`derivedV8ProjectFromFlow`（L1297）——三个 Surface 会话都被投影回 V8 形状的 `project` 字段。这是 CORE-06/07、SLIDE-04 的核心清理对象。

### FACT-04 能力模式现状 `[已证实]`

- 出处：`00-foundation/01` §4、`00-foundation/04`。
- `export type EditorMode = 'simple' | 'professional'`（editorStore.ts L1343）——现状确无 `'code'`；
- `RightSidebar.tsx` L30–43：`simpleTabs = [元素, 图层, 属性]`；`professionalTabs = [元素, 组件, 图层, 属性, 互动与动画, 开发]`——与方案 §4"简单模式主要显示元素/图层/属性…专业模式增加组件/互动与动画/开发"**逐字一致**；
- 方案提出的 `'code'` 第三模式是本地 UI 偏好（`loadEditorMode()` L3714 从本地读取），不进 persisted Schema，符合 V9 软冻结。

### FACT-05 DeveloperTab 代码能力现状 `[已证实，含一处需澄清的表述]`

- 出处：`00-foundation/01` §4"代码能力"。
- `DeveloperSection = 'runtime' | 'object' | 'rules' | 'component'`（DeveloperTab.tsx L25）、`ComponentDocument = 'manifest' | 'runtime'`（L26）——与方案列举的五项（Runtime 源码/对象 JSON/规则 JSON/Component Manifest/Component Runtime）一一对应；
- **重要细节**：DeveloperTab 当前写入**已经走 store action**（L159–164：`updateNode`、`updateSceneRuntime`、`updateGlobalRuntime`、`setSceneRuntime`、`setGlobalRuntime`、`updateInteractionRule`），并非裸 `setState`。方案 `00-foundation/04` §5"禁止直接 setState 覆盖 Store"是**目标不变量**而非对现状的指控，但表述易误导，见 ISSUE-07。

### FACT-06 App 实时诊断计算 `[已证实]`

- 出处：`00-foundation/01` §6、`20-modules/08` §4。
- `App.tsx` L440–447：`projectHealthDiagnostics = useMemo(() => collectProjectHealth(project, componentPackages), [project, componentPackages])`——依赖整个 `project`，**每次编辑触发全量健康重算**；结果经 L1640 `healthSummary={projectHealthSummary}` 进 TopToolbar。方案 DIAG-03"移除 App 每次变更的完整健康计算"的靶子准确。

### FACT-07 认知索引漂移 `[已证实，量化]`

- 出处：`00-foundation/01` §7、`10-knowledge-system/00` §1。
- `PROJECT_COGNITION_INDEX.md` 共引用 32 条仓库路径，其中 **7 条失效**：
  - `repo-index/README.md`、`repo-index/modules.json`、`repo-index/features.json`、`repo-index/tests.json`、`repo-index/*.json`（目录整体不存在）；
  - `src/renderer/course/CourseStudioApp.tsx`（不存在）；
  - `src/renderer/course/v9SlideVerticalSlice.ts`（不存在）。
- 方案"声明存在但目录不存在 + 部分源码路径已失效"两个说法**都有实锤**。DOC-01 重写时应显式处理这 7 条，建议把此清单写进 DOC-01 任务卡。

### FACT-08 可复用 npm scripts `[已证实]`

出处：`00-foundation/01` §8。`package.json` 实测：

```text
verify                  = check:ai-capabilities && typecheck && test && test:e2e && build:desktop
verify:release          = verify && tsx scripts/verify-release.ts
build:desktop           = build:player && build:renderer && build:electron
generate:ai-capabilities / generate:contracts / validate:course-project / typecheck  均存在
repo:index / repo:context  不存在（正是方案要新建的，无冲突）
```

### FACT-09 复用基础与依赖 `[已证实]`

- `RecoveryWriteCoordinator` 位于 `src/renderer/project/recoveryWriteCoordinator.ts`；
- tests 分层齐备：`tests/{unit, integration, e2e, fixtures, helpers, prototypes}`；
- 关键依赖版本：typescript **7.0.2**（见 ISSUE-08）、tsx 4.23.1、vitest 4.1.10、@playwright/test 1.61.1、zod 4.4.3、zustand 5.0.14、immer 11.1.15、react 19.2.7、electron 43.1.1、phaser 4.2.0；**无 ts-morph**（方案说"不加入 ts-morph"与现状一致）；
- `docs/contracts/` 存在 3 份合同文档：`COURSE_PROJECT_V9.md`、`EDITOR_1_0_ARCHITECTURE_BOUNDARY.md`、`V9_COMPATIBILITY_POLICY.md`（方案未提及，见 ISSUE-11）。

### FACT-10 迁移地图路径 `[已证实]`

出处：`20-modules/10_CURRENT_TO_TARGET_FILE_MAP.md`。抽查全部通配模式：

- `src/renderer/course/flow*` → 6 个文件（flowDocumentModel / flowEditorCommands / flowEditorSlice / flowEditorView / flowOverlayProjection / flowSharedAuthoringAdapters）；
- `spatial*` → 7 个（spatialAuthoringHistory / spatialCameraCommands / spatialEditorCommands / spatialEditorView / spatialPathCommands / spatialRelationCommands / spatialSemanticZoom）；
- `slide*` + `v9Slide*` → 8 个（slideAuthoringBackend / slideEditorCommands / slideEditorView / slideInteractionCommands / slideInteractionView / v9SlideActionCommands / v9SlideClipboard / v9SlideContentCommands）；
- 目录规模：`phaser/` 9、`components/` 6、`project/` 17、`export/` 24、`player/` 50（含 `surfaces/` 16）；
- 历史噪声体量：`docs/tasks/editor-1.0/` 108 个文件、`docs/reviews/` 11 个——支撑 L4 清理与 exclusions 设计。

### FACT-11 模式判断散落度（方案未给数字，本报告量化）`[已证实]`

`editorMode ===` 在 renderer 出现 **39 处 / 7 个文件**：PropertiesTab 10、TopToolbar 8、editorStore 6、ElementsTab 5、RightSidebar 5、SceneStateStrip 4、App 1。方案 UI-01"集中模式可见性配置"的收益对象明确。此数字可作为 UI-01 完成后的回归指标（目标：UI 文件中的散落判断显著下降并集中到组合层）。

### FACT-12 E2E 集中度 `[已证实]`

`tests/e2e/` 仅 3 个 spec：`editor.spec.ts`（143.9 KB，绝对主导）、`componentCatalogMatrix.spec.ts`、`render-host-benchmark.spec.ts`。方案"大多数核心 E2E 集中"属实，"按生命线/Feature 拆分"的迁移方向有据。

### FACT-13 知识系统示例数据为实测数据 `[已证实，强信号]`

`10-knowledge-system/01_DATA_MODEL_AND_FILES.md` §2 的 File 节点示例写着 `"bytes": 26330, "exports": ["ComponentsTab", "ComponentLibraryDialog"]`。实测 `src/renderer/ui/ComponentsTab.tsx` **精确等于 26330 字节**，且顶层导出恰为这两个符号。示例 JSON 不是编造的占位数据，而是对真实仓库的实测——这大幅提升整份知识系统设计文档的可信度。

### FACT-14 Schema 文件为转发桩 `[已证实]`

`src/shared/courseProjectTypes.ts` 与 `courseProjectSchema.ts` 各只有 1 行：`export * from './contracts/course-project-v9/{types,schema}'`。真正的 V9 合同本体在 `src/shared/contracts/course-project-v9/`。影响见 ISSUE-05。

### FACT-15 tsconfig 路径别名 `[已证实]`

`tsconfig.json` 有 `"@/*": ["./src/*"]`。方案 `10-knowledge-system/02` Step 3 已把"tsconfig alias 解析"列入生成算法——必要且不可省略，索引生成器若只做相对路径解析会漏边。

### FACT-16 工作包 ID 无撞名 `[已证实]`

对 `docs/tasks/` 全量正则扫描 `(IDX|FAC|CORE|BOUND|BSL|MAP|DOC|COMP|DIAG|MODE|RUN|INT|SLIDE|FLOW|SPATIAL|WORKSPACE|PROPS|PLAY|EXPORT|CLEAN|FINAL|MEDIA|APP|UI|STYLE|TESTMAP)-\d+`：仅命中小写的 `props-44 / spatial-2 / ui-0`（代码标识符，非任务 ID）。**方案的工作包编号体系与仓库既有任务 ID 不冲突；冲突只发生在阶段号 P0–P6**（见 ISSUE-02）。

### FACT-17 ComponentsTab 职责混合 `[部分证实]`

单文件（26.3 KB）内含 `CatalogThumbnail`、`ComponentDetailsDialog`、`ComponentLibraryDialog`、`ComponentsTab`、`setComponentDragData`，并直接取 `addExternalComponentNode` action——目录浏览、包详情、库对话框、实例创建混在一处，支持 COMP-01 的拆分主张。（"部分"是因为未逐函数映射到方案的四子域，交叉审阅者可通读该文件补全。）

---

## 4. 与现行治理一致性矩阵

> 现行治理 = `AGENTS.md`（工作区强制规则）+ `COURSEWARE_DEVELOPMENT_PLAN.md` 12.10（现行唯一计划）+ `docs/contracts/` 三份合同。

| # | 现行约束 | 方案立场（出处） | 一致性判定 |
|---|---|---|---|
| 1 | Course Project V9 软冻结；additive 可选字段单独合同提交、保持 `.strict()` | "persisted Schema 初期不改"（README §5）；"Schema 任务独立、默认不做"（40-dev/01 §7） | 一致 |
| 2 | 不建 V10、不开 V10 大迁移 | "本轮默认不创建 V10"（不变量 #2、P6 原则） | 一致 |
| 3 | 不得删减 V8 已可用编辑能力；高级能力可收进面板但必须可发现 | G1 能力不降级 + §2.1 保留清单 + "不应删除"清单（30-exec/03 §4）+ 原则 P7 渐进披露 | 一致 |
| 4 | 三种 surface 直接创建入口；界面从 locations/surfaces 推导，不新增持久化 projectMode | 方案未触碰该域；模式（simple/professional/code）为本地 UI 偏好（00-found/04 §1） | 一致（不相交） |
| 5 | 无限画布运行态拖拽/缩放只改会话相机 | 不变量 #26"Spatial 运行态相机默认只改 session" | 一致 |
| 6 | 画布文字可命中；`hitId` 不得代替 `authoringAddress` | 不变量 #6"Stable ID 不用数组下标、DOM ID 或临时 hit ID" | 一致 |
| 7 | 编辑器内无可见 AI；`courseAiHandoff`/`courseAiPatch` 不得新增调用点 | repo-index 全在开发侧，明确"产品运行时零依赖"（10-ks/00 §2）；未提及 courseAi* 接口 | 一致 |
| 8 | 教师工作流不用 Hash/签名/审批状态机/Evidence 清单 | Hash 仅用于开发索引新鲜度（10-ks/02 §3），不进教师工作流 | 一致 |
| 9 | `globalLayerItems`/`surfaceLayerItems` 保留；不启动统一图层 V10 迁移 | Feature Matrix 保留"全局层/共享层""教师控制器"条目，归属 Global Layers / Teacher Controller 模块 | 一致 |
| 10 | 现行计划明言"Store/Workspace 大拆仍属 1.0 之后" | 本方案正是那个"1.0 之后"的大拆计划 | **承接关系成立，但时序衔接未写明**（ISSUE-04） |
| 11 | 长期开发方向只看根目录唯一计划 | 方案是外部包；DOC-01 提出重写 AGENTS.md，但采纳程序未前置（ISSUE-03） | **程序缺口** |
| 12 | 自动化最多证明 engineering candidate；accepted 须教师验收 | 方案 FINAL-03"人工核心流程验收"保留人工环节 | 一致 |

**结论**：12 项中 10 项一致，2 项为程序性缺口（非技术冲突），均有明确修法（见 ISSUE-02/03/04）。

---

## 5. 问题清单（ISSUE-01 ～ ISSUE-12）

> 每条含：严重度 / 类别 / 精确位置 / 证据 / 影响 / **具体修改建议（含替换文本）** / 修复验证 / 交叉审阅提示。
> 严重度定义：高 = 不修会导致误派工或治理冲突；中 = 不修会造成返工或保护失效；低 = 文档质量问题；观察 = 无需行动仅记录。

---

### ISSUE-01（中 / 内链失效）阅读矩阵引用了包内不存在的文件

- **位置**：`00-foundation/00_READING_MATRIX.md` §2 表格"清理 Legacy"行（第 34 行）。
- **证据**：引用 `40-development/02_CODE_CLEANING_POLICY.md`；包内 `40-development/` 实际只有 `00_SINGLE_MAINTAINER_AI_WORKFLOW.md`、`01_TASK_PROTOCOL_AND_FILE_FIREWALL.md`、`02_VALIDATION_STRATEGY.md`、`03_DONE_ROLLBACK_HANDOFF.md`。
- **影响**：后续 AI 按矩阵取读清理类任务文档时会 404；对一份把"路径失效检测"当核心卖点的方案是自反性瑕疵。
- **修改建议**（二选一，推荐 A）：
  - A. 将该行改为：`| 清理 Legacy | 30-execution/03_LEGACY_CLEANUP_SEQUENCE.md、40-development/02_VALIDATION_STRATEGY.md | 相关模块文档 |`（清理政策实际内容在 30-execution/03，验证要求在 40-dev/02）；
  - B. 若原作者本意是单列一份"代码清理政策"，则补写 `40-development/02_CODE_CLEANING_POLICY.md` 并把现 02/03 顺延编号——**不推荐**，会牵连全包内链。
- **修复验证**：对包内全部 `.md` 跑相对链接检查（附录 A 命令 8），零失效。
- **交叉审阅提示**：顺手全包扫一遍内链（本评估扫过一遍只发现这 1 处，欢迎复核）。

---

### ISSUE-02（高 / 编号冲突）阶段号 P0–P6 与仓库既有 P1–P8 车道撞名

- **位置**：`README.md` §4 第三步、`30-execution/00_ROADMAP.md` 全文、`30-execution/01_PHASE_WORK_PACKAGES.md` 各 `# P0..P6` 标题、`30-execution/02_DEPENDENCIES_PARALLELISM.md` §2/§6、`40-development/02_VALIDATION_STRATEGY.md` §4、`50-templates/TASK_CARD_TEMPLATE.md` 的 `Phase: <P0-P6>` 字段。
- **证据**：仓库现行计划 `COURSEWARE_DEVELOPMENT_PLAN.md` L124–131 定义了 **P1–P8 为已合入 main 的教师可见缺陷车道**（P1 = 运行态视频/控制器可拖可点，P8 = 三表面挂 Component API 4 等）；`AGENTS.md` 明文写"T0–T6、P1–P8、Q1–Q8、F1–F3、G0–G3 已合入 main"。同一个"P1"在两套治理文档中含义完全不同（缺陷车道 vs 公共入口阶段）。
- **影响**：本仓库以 AI 为主要执行者、且 AGENTS.md 是每个 Agent 的强制上下文。收到"执行 P1"类指令时存在真实的误派风险；历史检索（对话、commit message、任务卡）也会被污染。
- **修改建议**：
  1. 阶段号全局改名 `P0–P6 → R0–R6`（R = Refactor；或 `A0–A6`，避开现有 T/P/Q/F/G 前缀即可）；
  2. 需同步修改的文件即上面"位置"列出的 6 处 + `90-appendix/01_RISK_REGISTER.md` 若有引用（本评估未发现）+ `00-foundation/01` §9 无引用；
  3. 分支命名建议同步：`refactor/p0-repo-index → refactor/r0-repo-index`（30-exec/02 §6）；
  4. 在 `README.md` 头部加一行显式声明：`> 本方案阶段号 R0–R6 与仓库历史车道编号（T/P/Q/F/G 系列）无关，工作包 ID（IDX/CORE/COMP 等）经核查与既有任务 ID 无冲突。`
  5. **工作包 ID 不需要改**：FACT-16 已证实 IDX-/FAC-/CORE- 等 28 个前缀在 `docs/tasks/` 中无撞名。
- **修复验证**：全包 `rg '\bP[0-6]\b'` 应只剩指向现行计划车道的引用（如有）；`rg '\bR[0-6]\b'` 覆盖全部阶段引用。
- **交叉审阅提示**：改名时注意 `40-development/02_VALIDATION_STRATEGY.md` §4 的"### P0…### P6"小节标题和 `30-exec/02` 依赖图中的裸编号；不要误改方案中引用**现行计划**车道的文句（本评估未发现此类文句，但整合后新增内容可能出现）。

---

### ISSUE-03（高 / 治理衔接）采纳程序未作为前置步骤明确

- **位置**：整包层面；最接近的条目是 `30-execution/01` 的 DOC-01（被排为 P0 内普通文档任务）。
- **证据**：`AGENTS.md` 规定"长期开发方向只看根目录唯一计划（COURSEWARE_DEVELOPMENT_PLAN.md）"。zip 目前是仓库外文档；在采纳动作完成前，任何按现行 AGENTS.md 工作的 Agent **有权且应当**无视 zip 里的任务卡——这会造成"方案已发、无人可执行"的死锁，或更糟的"部分 Agent 执行、部分 Agent 拒绝"的分裂。
- **影响**：治理层死锁 / 分裂风险；也影响审计（执行了未入籍的计划）。
- **修改建议**（给出可直接使用的步骤）：
  1. 新增前置任务 **ADOPT-01**（排在 BSL-01 之前），内容：
     - 把整包并入仓库，建议路径 `docs/architecture-plan/`（保持包内相对结构，改名后的 R0–R6 版本）；
     - 写一条 ADR（用包内 `50-templates/ADR_TEMPLATE.md`），编号建议 ADR-001，标题"采纳 2026-08-21 架构执行方案为 Editor 1.0 后主线计划"，关键字段：
       - 状态：accepted（需仓库维护者确认）；
       - 决策：现行 `COURSEWARE_DEVELOPMENT_PLAN.md` 继续管辖 Editor 1.0 收尾与教师验收；验收 `accepted` 后，长期架构方向由 `docs/architecture-plan/` 接管；两者并存期间冲突以现行计划优先；
       - Supersedes：无（是接续不是替代）；
     - 修改 `AGENTS.md` 中"唯一计划"句，改为双引用（现行计划管 1.0 收尾；架构方案管 1.0 后方向，附 ADR-001 链接）；
     - 修改 `COURSEWARE_DEVELOPMENT_PLAN.md` 头部加一行指针。
  2. DOC-01 保持原内容（重写 AGENTS.md 为短入口、替换 PROJECT_COGNITION_INDEX），但明确它是 ADOPT-01 之后的**第二次**文档修改，不承担采纳职能。
- **修复验证**：任意新开 Agent 会话，仅凭 AGENTS.md 能同时定位两份计划并知道优先级。
- **交叉审阅提示**：ADOPT-01 涉及 AGENTS.md 修改，属于治理变更，**必须由仓库维护者（教师/用户）明确确认后执行**，不应由 AI 自主完成。

---

### ISSUE-04（中 / 时序）与 Editor 1.0 教师验收的关系缺失

- **位置**：`README.md` §4 与 `00-foundation/01_BASELINE_AND_CURRENT_FACTS.md` §9 之间的空白。
- **证据**：现行计划的终点是"P1–P8 真实课例视觉/互动复核 + 教师 accepted"（12.10 §Done Definition），目前**未达成**；现行计划同时明言"Store/Workspace 大拆仍属 1.0 之后"。zip 方案对"何时可以开跑"只字未提。
- **影响**：若在验收期开跑 R2+（触碰 `App.tsx`/UI/Toolbar），可能与验收修复车道抢热点文件，且 UI 变化会使教师复核对象漂移（复核的是改造前还是改造后的界面？）。
- **修改建议**：在 `00-foundation/01` 增补 §10"与 Editor 1.0 收尾的时序关系"，建议文本：

  > R0（知识系统与基线，纯开发侧，不触产品代码）可与教师验收并行。R1（facade，只增文件不改行为）原则上可并行，但须遵守热点文件单卡规则。R2 及之后触碰 `App.tsx`、Toolbar、Sidebar 的任务，须满足以下之一方可开始：(a) 教师验收已 accepted；(b) 维护者明确豁免，且验收期间出现的修复车道**无条件优先**占用热点文件，重构任务让路并 rebase。
- **修复验证**：文本并入后，ADOPT-01 的 ADR 中引用该节。
- **交叉审阅提示**：这一条需要维护者决策（验收前是否放行 R1），AI 只能给默认建议（本报告建议：R0 放行、R1 放行但热点让路、R2+ 等验收）。

---

### ISSUE-05（中 / 防火墙失焦）热点文件清单指向 1 行转发桩

- **位置**：`30-execution/02_DEPENDENCIES_PARALLELISM.md` §4 热点清单。
- **证据**：清单含 `src/shared/courseProjectTypes.ts` 与 `src/shared/courseProjectSchema.ts`，实测两文件各 1 行（`export * from './contracts/course-project-v9/{types,schema}'`）；V9 合同本体在 `src/shared/contracts/course-project-v9/`（FACT-14）。
- **影响**：文件防火墙按路径生效。锁住桩文件不能阻止 Agent 直改 `contracts/course-project-v9/schema.ts`——Schema 软冻结的防护会**整体落空**。
- **修改建议**：§4 清单将两行替换为：

  ```text
  src/shared/contracts/course-project-v9/   （整个目录：schema.ts、types.ts 及同目录合同文件）
  src/shared/courseProjectTypes.ts          （转发桩，防新增旁路导出）
  src/shared/courseProjectSchema.ts         （转发桩，同上）
  ```

  并在 `50-templates/TASK_CARD_TEMPLATE.md` §6 的 Forbidden 默认示例中加入 `src/shared/contracts/course-project-v9/`（除非任务卡显式声明为 Schema 任务）。
- **修复验证**：抽一张非 Schema 任务卡，确认 Forbidden 覆盖合同目录。
- **交叉审阅提示**：顺带检查 `src/shared/contracts/` 下是否还有其他协议目录（published-course、component、runtime、interaction）需要同等待遇——本评估未逐一列举，建议在 BOUND-01 时用索引生成器自动圈定全部 contract 路径。

---

### ISSUE-06（低 / 完备性）R5/R6（原 P5/P6）工作包只有标题

- **位置**：`30-execution/01_PHASE_WORK_PACKAGES.md` 后半部——SLIDE-01～04、FLOW-01～04、SPATIAL-01～04、WORKSPACE-01、PROPS-01、PLAY-01～03、EXPORT-01～03、CLEAN-01～05、FINAL-01～03 共 24 个包，仅一行标题；对比 P0–P4 的包有目标/交付/验证结构。
- **影响**：不可直接派发；且到 R5 时代码库已经过 R1–R4 改造，提前细化的内容也会失真——留白**可以接受**，但整合者需要预算派发前的细化成本。
- **修改建议**：
  1. 不要求现在补全 24 个包，但为每包补**一行 Goal + 一行 Minimal validation**，防止标题歧义。示例（SLIDE-01）：

     > **SLIDE-01 Slide selector/command 入口**
     > Goal：建立 `surfaces/slide/index.ts`，将 `slideEditorCommands/slideEditorView/v9Slide*` 的公共消费面收口为显式导出；不移动实现。
     > Minimal validation：typecheck + 现有 slide command 单测 + 一条 Slide 编辑 E2E。
  2. 在文档头部注明："R5/R6 包须在 R4 收口后、派发前按 TASK_CARD_TEMPLATE 补全，并重新核对热点文件清单。"
- **交叉审阅提示**：细化时最大的坑是 SLIDE-04（删除 Slide 可写 V8 projection）——它依赖 CORE-06/07 的完成质量，细化时必须重读届时的 `derivedV8ProjectFrom*` 存量。

---

### ISSUE-07（低 / 表述精度）"禁止直接 setState"易被误读为现状描述

- **位置**：`00-foundation/04_CAPABILITY_MODES.md` §5"禁止"清单第 1 条；`20-modules/05` §4 同类表述。
- **证据**：FACT-05——DeveloperTab 现状已通过 6 个 store action 写入，不存在裸 setState 旁路。
- **影响**：后续 AI 若把它读成"现状有裸 setState 需要修"，会浪费一轮排查甚至制造无意义改动。真正的差距是：这些 action 各自直写各自的状态域，**缺统一 transaction/history/draft-diff 语义**。
- **修改建议**：§5 开头加一句现状说明：

  > 现状说明：DeveloperTab 已经通过 store action（updateSceneRuntime、updateInteractionRule 等）写入，不存在绕过 Store 的裸写。本节"禁止"清单描述的是目标不变量；当前的差距是缺少统一 draft→validate→diff→command→history 链，而非存在 setState 旁路。
- **交叉审阅提示**：核对其余"禁止"条目是否有同类问题（例如"单独维护 code-mode document"——现状 DeveloperTab 的草稿是组件局部 state，尚未构成第二文档，同样属于"目标性禁止"）。

---

### ISSUE-08（中 / 可行性）TypeScript 7.0.2 的 Compiler API 兼容性未验证

- **位置**：`10-knowledge-system/02_GENERATOR_AND_FRESHNESS.md` §2 Step 2"使用现有 typescript 依赖（Compiler API）"。
- **证据**：仓库 `typescript` 版本为 **7.0.2**（原生化编译器世代）。方案假定可用 `ts.createSourceFile / forEachChild` 一类传统 JS Compiler API 做 AST 扫描；TS7 对该 API 面的兼容性**本评估未验证**（评估环境未实际 require）。
- **影响**：若 TS7 不再完整暴露传统 API，IDX-02 会在第一步卡住。
- **修改建议**：IDX-02 任务卡加一个**先行 spike 步骤**（半小时级）：

  ```bash
  node -e "const ts=require('typescript'); console.log(ts.version); const sf=ts.createSourceFile('a.ts','export const x=1',ts.ScriptTarget.Latest,true); ts.forEachChild(sf,n=>console.log(ts.SyntaxKind[n.kind]))"
  ```

  按结果三选一：(a) 可用 → 按原计划；(b) 部分可用 → 限定使用面（import/export/顶层声明足够，方案本就不做调用图）；(c) 不可用 → 在 devDependencies 以别名安装一个 5.x 系 typescript **仅供索引器使用**（不影响产品构建链），或降级为"正则 + 启发式"扫描 import/export（方案的边类型 V1 需求用启发式也能覆盖 90%）。
- **修复验证**：spike 输出记录进 IDX-02 Handoff。
- **交叉审阅提示**：这是全方案**唯一一个"卡脖子级"未验证技术前提**，建议交叉审阅者最优先实测此项。

---

### ISSUE-09（观察 / 口径）清单计数 37 vs 实际 38

- **位置**：`PACKAGE_MANIFEST.md` 头部"共 37 份 Markdown 文档"。
- **证据**：包内实际 38 份 .md；清单表列 37 条（含 README，不含清单自身）。
- **判定**：口径可辩护（"清单之外 37 份"），无需行动；整合入仓库时若重排目录可顺手改为"38 份（含本清单）"。

---

### ISSUE-10（低 / 基线卫生）BSL-01 缺"工作树必须干净"前置条件

- **位置**：`30-execution/01` BSL-01；`README.md` §4 第一步。
- **证据**：当前工作树有 3 个未提交修改（`artifacts/ai-capabilities/` 下 3 个生成物，FACT-01 附注）。README 第一步只让跑 `git status --short`，未规定处置。
- **影响**：基线记录里混入未提交态会让"基线 SHA"失真（工作树 ≠ SHA 内容）；生成物 diff 还会污染后续每次 `git status`。
- **修改建议**：BSL-01 的"目标"加一条：

  > 前置：工作树必须干净。对生成物类修改（如 `artifacts/ai-capabilities/*`），先重跑对应生成器确认是否为确定性重建差异；是则提交（`chore: refresh generated ai-capabilities`），否则还原。基线记录中写明处置结果。
- **交叉审阅提示**：当前那 3 个文件的 diff 本评估未逐行检查（属产品能力索引生成物），建议 BSL-01 执行者先 `git diff artifacts/` 确认无手工编辑痕迹。

---

### ISSUE-11（低 / 缺口）`docs/contracts/` 未纳入方案文档体系

- **位置**：`10-knowledge-system/01` §2 Contract 节点只列协议名（Course Project V9、Published Course V2、Component API 4、Runtime API 2/3、Interaction API、IPC types），未列合同**文档**路径；`20-modules/10` 迁移地图也未提 `docs/contracts/`。
- **证据**：仓库存在 `docs/contracts/COURSE_PROJECT_V9.md`、`EDITOR_1_0_ARCHITECTURE_BOUNDARY.md`、`V9_COMPATIBILITY_POLICY.md`（FACT-09），且 AGENTS.md 明文"V9 合同说明在 docs/contracts/"。
- **影响**：索引生成器的 `contracts.json` 若只认代码路径，会漏掉人读合同文档；DOC-01 重写入口时也可能漏保留这三份的引用。
- **修改建议**：
  1. `10-ks/01` §2 Contract 节点补充字段 `docs: ["docs/contracts/COURSE_PROJECT_V9.md", ...]`；
  2. `10-ks/02` Step 1 枚举来源加一行"`docs/contracts/**/*.md`"；
  3. DOC-01 交付清单加"保留并链接 docs/contracts/ 三份合同"。
- **交叉审阅提示**：`EDITOR_1_0_ARCHITECTURE_BOUNDARY.md` 本评估未通读，它与方案的模块边界主张是否逐条一致，列为开放问题 UNV-03。

---

### ISSUE-12（低 / 风险登记补全）风险登记缺 2 条已识别风险

- **位置**：`90-appendix/01_RISK_REGISTER.md`（现 18 条）。
- **建议新增**（按其表格格式给出行文本）：

  ```text
  | TS7 Compiler API 不兼容 | 索引生成器依赖传统 ts API | IDX-02 卡死 | IDX-02 前置 spike；备选：别名安装 5.x 仅供索引器 / 启发式扫描 |
  | generated 索引入 Git 的噪声 | 每次重建产生大 diff、并行分支冲突 | 评审噪声、合并冲突 | .gitattributes 标 linguist-generated + merge 策略约定"以重建为准"；PR 评审默认折叠 |
  ```

- **交叉审阅提示**：第二条的 merge 策略（冲突时丢弃双方、重跑 `repo:index`）应写进 `repo-index/README.md`（IDX-01 交付物）。

---

## 6. 设计决策评估（含备选方案对比）

### 6.1 知识系统：静态 JSONL + CLI（10-knowledge-system）

**评估：正确的选型，且给出了明确的"不做清单"防过度设计。**

| 候选方案 | 方案取舍 | 本评估意见 |
|---|---|---|
| 静态 JSONL + CLI Context Pack（选中） | V1 全量重建，<10s 不做增量 | 全仓 TS/TSX 仅 527 个文件（src 299 / tests 213 / scripts 15），10s 预算宽裕 3–10 倍；正确 |
| 常驻图数据库（Neo4j 等） | 明确拒绝 | 同意：单人维护 + 无服务端，运维成本不成比例 |
| 向量检索 / Embedding | 明确拒绝（V1） | 同意：Feature alias + 图扩展对确定性任务定位优于语义检索，且可解释、可离线 |
| tsserver/LSP 常驻索引 | 未讨论 | 可接受的遗漏：LSP 提供精确引用但要常驻进程，与"无 daemon"原则冲突；不必补充 |
| ts-morph | 明确拒绝（除非成本过高） | 同意，且仓库现状无此依赖（FACT-09）；但注意 ISSUE-08 的 TS7 前提 |

**亮点**：
- `generated/` 进 Git 让云端 Agent 免跑生成器——贴合本仓库"多 Agent 派工"的真实工作方式；
- 新鲜度协议（manifest HEAD + 文件 Hash + fresh/partially-stale/stale 三态 + stale 仍可查询但顶部警告）直接回应了 PROJECT_COGNITION_INDEX 手工漂移的前车之鉴（FACT-07）；
- "索引与方案冲突时不允许按索引强行修改源码"（阅读矩阵 §5）——权威顺序写对了；
- Context Pack 输出格式（Start Here / Write Path / Invariants / Do Not Read Unless Needed / Suggested Minimal Validation）是给 AI 的任务上下文最佳实践，字段选择成熟。

**风险**（除 ISSUE-08/12 外）：
- `semantic/features.json` 是新的手工维护点。方案的对冲是"生成器 Step 4 校验 semantic 路径失效即报错"——闭环成立，但**别名（aliases）质量**决定查询命中率，且无法自动校验。建议 MAP-01 验收标准加一条：用 IDX-04 的 4 个基准查询反向检验 alias 覆盖。

### 6.2 执行路线：绞杀者式渐进（30-execution）

**评估：顺序正确，反模式意识强。**

- 依赖链 `BSL/MAP → IDX → FAC/BOUND → (APP/DIAG/COMP-UI ∥ CORE) → …` 把"建入口"放在"动数据真相"之前，把最高风险的 CORE（Editor Core 收口）放在有 facade、有索引、有边界检查之后——风险排序正确；
- 三提交回滚模式（add facade → migrate consumers → remove old path）+ "L2 双写链切换后**立即**删除旧写入"是这类重构最常见失败模式（长期双写）的正解；
- 并行规则可落地：热点文件单卡持有（§4）+ 文件防火墙三段式 + "越界即停不顺手修"（40-dev/01 §8），与仓库现行工人协议精神一致，迁移成本低；
- **保留意见**：P3（R3）阶段"CORE-01～07 + MEDIA-01"体量偏大（7 个子包全是热点串行），实际执行时建议整合者把 CORE-04/05（Slide/Flow/Spatial 写入迁移试点）视为可暂停点——试点失败时整阶段可冻结在"新旧并存、单向流动"的安全态，方案的 CORE-02"先与旧 session 并存"已经暗含此设计，建议在 ROADMAP R3 节把"安全暂停点"写显。

### 6.3 验证策略：V0–V3 分级（40-development/02）

**评估：与仓库现实匹配度高。**

- 实测 `npm run verify` = check:ai-capabilities + typecheck + test + test:e2e + build:desktop 全链（FACT-08），确实不可能每卡都跑；
- "任务到验证映射"表 + "何时必须扩大验证"清单（触碰 canonical/history/Schema/producer/save-open 即扩大）给了 AI 可机械执行的判据；
- flaky 协议（重跑一次判随机、单独立案、不准提高 retries 掩盖）成熟；
- **补充建议**：V2 阶段验证中的"desktop smoke"在 Windows 环境的耗时应在 BSL-01 基线时实测记录一次，作为后续阶段排期依据（方案未给预算数字，不算缺陷，但整合者需要这个数）。

### 6.4 文档工程（00-foundation / 50-templates / 90-appendix）

- 阅读矩阵 + 三档 Token 预算 + "默认不读取"清单：对 AI-driven 仓库是稀缺且正确的设计；
- 50 条全局不变量可判定性强（如 #12"拖拽只在结束时提交"、#35"Try-run 与 Full Preview 共用 producer"），可直接灌入 `invariants.json`；
- 任务卡模板的"模式 × Surface 影响矩阵"（§5）与"Legacy 处理"（§9）栏位优于仓库现有任务卡格式，值得反向移植给日常任务；
- Feature Matrix（00-found/05）30 行覆盖面完整，`core ≠ 必须显示在简单模式`、`advanced ≠ 可删除` 两条使用规则精准防住了"以频率论删除"的常见错误。

---

## 7. 分阶段可行性评估

| 阶段（建议改名后） | 风险 | 前置条件 | 可行性判断 |
|---|---|---|---|
| R0 基线+知识系统 | 低（不触产品代码） | ADOPT-01；ISSUE-08 spike；工作树干净（ISSUE-10） | 高；全部工具链已在仓（tsx/vitest）；可与教师验收并行 |
| R1 公共入口 | 低（纯增量 re-export） | R0 索引可用 | 高；唯一坑是 facade 不得反向依赖旧上帝文件内部状态（模块地图规则 4 已覆盖） |
| R2 低风险解耦 | 中（触 App/Toolbar/Sidebar） | 教师验收关系明确（ISSUE-04） | 高；DIAG-03 收益立竿见影（FACT-06 的实时重算移除） |
| R3 Editor Core 收口 | **最高**（动数据真相与 history） | R1 facade 稳定；热点串行纪律 | 中高；FACT-03 的 10 处状态是明确靶子；建议增设安全暂停点（§6.2） |
| R4 模式/组件/诊断整合 | 中 | R3 的 canonical selectors | 高；四子域拆分有 FACT-17 佐证 |
| R5 Surface 迁移 | 中高（三 Surface 并行诱惑大） | R3/R4；工作包需先细化（ISSUE-06） | 中；纪律依赖"Workspace 最后由一个整合任务接入"（30-exec/02 §2） |
| R6 清理收口 | 中（删除类操作） | 替代路径全部稳定 | 高；删除门槛七问（30-exec/03 §2）+ 清理证据清单完备 |

---

## 8. 未验证项与开放问题（给交叉审阅 AI 的核查指令）

| ID | 内容 | 建议核查方法 | 优先级 |
|---|---|---|---|
| UNV-01 | TypeScript 7.0.2 是否暴露传统 Compiler API（ISSUE-08） | 跑 ISSUE-08 中的 node 单行脚本 | **最高** |
| UNV-02 | 方案 README §2.2"简单/专业/代码模式中重复实现的业务逻辑"的具体实例。本评估证实了 39 处 `editorMode ===` 分支（FACT-11），但"重复业务逻辑"（同一语义两份实现）未定位到具体函数对 | 在 PropertiesTab（10 处分支）与 SimpleEntranceAnimationEditor vs InteractionEditor 之间找同语义双实现；或等 IDX-04 建成后用 `repo:query "简单模式 动画"` | 中 |
| UNV-03 | `docs/contracts/EDITOR_1_0_ARCHITECTURE_BOUNDARY.md` 与方案模块边界主张是否逐条一致（ISSUE-11 关联） | 通读该文档（本评估未读正文），对照 `20-modules/00_MODULE_MAP.md` 依赖表 | 中 |
| UNV-04 | Feature Matrix（00-found/05）30 行的"当前主要入口"列逐行核对。本评估抽查了主要行（App/ScenePanel/Workspace/FlowWorkspace/ComponentsTab/DeveloperTab/informationRelease/visualDensity 等均实在），未逐行验证全部 30 行 | 脚本化：提取表中路径列 → Test-Path；符号列 → rg | 低 |
| UNV-05 | 索引全量生成 <10s 预算的实测（527 文件的 AST 扫描 + JSONL 写盘） | IDX-02 完成后实测记录进 Handoff | 低（预算余量大） |
| UNV-06 | `artifacts/ai-capabilities/` 3 个未提交修改的内容审查（ISSUE-10） | `git diff artifacts/` 人工过目 | 低 |

---

## 9. 采纳与整合路线建议（步骤化）

> 下列步骤有先后依赖，标 ★ 的需要维护者（人）确认，其余可由 AI 执行。

1. **★ 决策采纳**：维护者确认接受本方案作为 1.0 后主线（若否，本报告 ISSUE 清单仍可用于改进方案再议）。
2. **修补文档包**（在 zip 副本上，一个提交）：
   - ISSUE-01 内链修正；
   - ISSUE-02 阶段号 P0–P6 → R0–R6 全局改名（附声明行）；
   - ISSUE-05 热点清单替换为 contracts 目录本体；
   - ISSUE-07 现状说明句插入；
   - ISSUE-11 contracts 文档纳入；
   - ISSUE-12 风险登记补 2 行；
   - ISSUE-04 时序小节插入（含 ★ 维护者对"验收前是否放行 R1"的选择）。
3. **★ ADOPT-01 入籍**（ISSUE-03 的完整步骤）：包并入 `docs/architecture-plan/` + ADR-001 + AGENTS.md 双引用改写 + 现行计划头部指针。此提交独立成 PR 级变更，便于回滚。
4. **ISSUE-08 spike**：TS7 API 实测，结果写入 IDX-02 任务卡（决定走原计划/限定面/别名安装/启发式四选一）。
5. **ISSUE-10 清基线**：处置 artifacts 3 个未提交修改，工作树归零。
6. **开跑 R0**：BSL-01 →（IDX-01 ∥ MAP-01 草稿）→ IDX-02 → IDX-03 → IDX-04 → DOC-01。DOC-01 时把 FACT-07 的 7 条失效路径清单作为已知输入。
7. **R0 收口评审**：用 IDX-04 的 4 个基准查询（组件目录版本更新 / Flow 图片替换保存重开 / Slide 试运行返回编辑 / 代码模式修改互动规则）验收 Context Pack 质量，通过后再议 R1 排期。

**提交切分建议**：步骤 2 一个提交（`docs(architecture-plan): apply review fixes`）、步骤 3 一个提交（`docs: adopt architecture plan via ADR-001`）、步骤 4/5 各一个提交。全部走 origin（ghostairship-debug/ittoedu），不动 upstream。

---

## 10. 一句话结论

方案的事实底盘扎实到罕见——**17 组核查全部命中，连示例 JSON 里的 26330 字节都是实测值**；技术路线与仓库全部红线兼容；真正要修的是 5 处衔接与保护问题（两高三中），全部有具体改法。修完即可入籍开跑 R0。

---

## 附录 A：核查命令清单（可复现）

> 环境：Windows PowerShell，工作目录 = 仓库根。行号基于 HEAD `690411d`。

1. 基线：`git rev-parse HEAD`；`git log --oneline 690411d..HEAD | Measure-Object -Line`；`git status --short`
2. 文件大小：`(Get-Item <path>).Length`（热点清单逐个）；`(Get-Content src/renderer/store/editorStore.ts | Measure-Object -Line).Lines`
3. Store 状态字段：Read `editorStore.ts` L1395–1470；`rg "componentPackages" src/renderer/store/editorStore.ts`
4. 多套 history：`rg "v9HistoryToStoreHistory|spatialHistoryToStoreHistory|createFlowEditorHistory|commitSlideAuthoringHistory" src/renderer/store/editorStore.ts`
5. 模式：`rg "type EditorMode" src/renderer`；`rg -c "editorMode ===" src/renderer`（按文件计数）
6. 认知索引失效路径：提取 `PROJECT_COGNITION_INDEX.md` 中 `src/|scripts/|tests/|docs/|repo-index/|artifacts/` 前缀路径 → 逐条 `Test-Path`（结果：32 引用 / 7 失效）
7. npm scripts 与依赖：`node -e "const p=require('./package.json'); ..."`（scripts 与 deps 键值打印）
8. zip 内链检查：对包内全部 .md 提取相对 .md 引用 → 在包内 `Test-Path`（结果：仅 `02_CODE_CLEANING_POLICY.md` 一处失效）
9. 任务 ID 撞名：`Get-ChildItem docs/tasks -Recurse -Include *.md | Select-String '\b(IDX|FAC|CORE|...)-\d+'`（结果：无大写撞名）
10. 示例数据实测：`(Get-Item src/renderer/ui/ComponentsTab.tsx).Length` → 26330；`rg "^export function" src/renderer/ui/ComponentsTab.tsx`
11. E2E 集中度：`Get-ChildItem tests/e2e -Name`
12. tsconfig 别名：`node -e "console.log(require('./tsconfig.json').compilerOptions.paths)"`

## 附录 B：38 份文档逐份评注

| 文档 | 评注 |
|---|---|
| `README.md` | 总纲清晰；§4 启动方式含基线漂移处理；§5"执行不过度验证"定调正确。受 ISSUE-02 波及（P0–P6 编号） |
| `PACKAGE_MANIFEST.md` | 完整；计数口径见 ISSUE-09 |
| `00/00_READING_MATRIX.md` | 高价值（AI 阅读预算控制）；含唯一失效内链（ISSUE-01）；文档权威顺序正确 |
| `00/01_BASELINE_AND_CURRENT_FACTS.md` | 全包事实密度最高的一份，17 组核查主要出自此文，无失实；建议增补 §10 时序节（ISSUE-04） |
| `00/02_GOALS_PRINCIPLES_NON_GOALS.md` | G1–G6/P1–P9/非目标三层结构完整；删除判据（保留/实验区/可删三档）可判定 |
| `00/03_TARGET_ARCHITECTURE_AND_DIRECTORY.md` | 目标目录合理；依赖方向白/黑名单可直接进 BOUND-01 规则；"文件规模是提示不是门禁"避免了机械拆分 |
| `00/04_CAPABILITY_MODES.md` | 能力暴露矩阵完整；§5 需加现状说明（ISSUE-07）；`editorModeCapabilities` 常量方案克制 |
| `00/05_INITIAL_FEATURE_MATRIX.md` | 30 行覆盖完整；入口列抽查属实（UNV-04 建议全量核对）；使用规则 1/2 精准 |
| `10/00_OVERVIEW_AND_DECISION.md` | 决策论证充分；与 artifacts/ai-capabilities 的分工划分正确（两索引不合并） |
| `10/01_DATA_MODEL_AND_FILES.md` | 数据模型克制（V1 12 种边、不做调用图）；示例数据为实测（FACT-13）；Contract 节点缺 docs 路径（ISSUE-11） |
| `10/02_GENERATOR_AND_FRESHNESS.md` | 生成算法五步合理；新鲜度三态协议成熟；TS7 前提未验证（ISSUE-08）；原子写入（临时目录替换）是正确细节 |
| `10/03_QUERY_AND_CONTEXT_PACK.md` | 评分表 + 预算三档 + 失败处理（禁止回退全仓读取）完整；两个末尾示例（组件库/模式任务）直击知识图谱相对全文搜索的差异价值 |
| `20/00_MODULE_MAP.md` | 模块表"不应承担"列有防越界价值；迁移八规则完整 |
| `20/01_EDITOR_CORE.md` | 全包技术含量最高；CORE-01～07 顺序正确；EditorTransactionResult 设计贴合现有 Immer/Patch 基础 |
| `20/02_APP_SHELL_PERSISTENCE_IPC.md` | hooks 化路线务实（先提取不动底层）；busy 分类替代全局 busy 是好细节 |
| `20/03_SURFACES_SLIDE_FLOW_SPATIAL.md` | "共享什么/不共享什么"清单是防过度统一的关键护栏；切换事务七步可测试 |
| `20/04_COMPONENTS.md` | 四子域（Catalog/Packages/Instances/Authoring）划分与 FACT-17 现状吻合；Catalog unavailable 处理与 AGENTS.md"空目录 ≠ 不能挂组件"一致 |
| `20/05_RUNTIME_INTERACTIONS_AUTOMATION.md` | Runtime/Interactions/Automation UI 三分边界清楚；硬错误 vs 建议的诊断分级正确 |
| `20/06_MEDIA_ASSETS.md` | metadata/bytes/blob URL 三层区分正确；"新增+改当前引用"的默认替换语义是正确的产品决策；GC 不自动删的立场稳妥 |
| `20/07_PLAYER_PREVIEW_EXPORT.md` | 唯一生产链主张与现状（buildPublishedCourse.ts 存在）衔接自然；mount 四阶段 helper 克制（不引状态机库） |
| `20/08_DIAGNOSTICS_ANALYSIS.md` | A/B/C 三类检查划分是全包对现状问题（FACT-06）最直接的解药；revision 缓存策略正确 |
| `20/09_UI_COMPOSITION_AND_MODES.md` | resolvePropertyEditor 简单映射避免插件系统过度设计；"第一阶段不引 Monaco"克制 |
| `20/10_CURRENT_TO_TARGET_FILE_MAP.md` | 全部 25 行路径核实存在（FACT-10）；是 MAP-01 的直接输入 |
| `30/00_ROADMAP.md` | 阶段依赖与退出条件明确；受 ISSUE-02 改名波及最大 |
| `30/01_PHASE_WORK_PACKAGES.md` | P0–P4 包可派发；后 24 包仅标题（ISSUE-06）；工作包规模约束（1–8 文件）合理 |
| `30/02_DEPENDENCIES_PARALLELISM.md` | 热点清单需修（ISSUE-05）；并行/串行划分与 worktree 建议贴合本仓库多 Agent 现实 |
| `30/03_LEGACY_CLEANUP_SEQUENCE.md` | L0–L4 分类 + 删除七问 + 清理证据清单，是全包最可直接复用的纪律文档 |
| `40/00_SINGLE_MAINTAINER_AI_WORKFLOW.md` | 标准闭环与"不必做"清单务实；提交粒度建议与仓库现行风格兼容 |
| `40/01_TASK_PROTOCOL_AND_FILE_FIREWALL.md` | 必填字段完整；"热点修改必须列目标符号"是对 AI 的有效约束 |
| `40/02_VALIDATION_STRATEGY.md` | V0–V3 + 映射表 + 扩大条件，可机械执行；flaky 协议成熟 |
| `40/03_DONE_ROLLBACK_HANDOFF.md` | "不算完成"负面清单（7 条）价值高，直击 AI 交付常见假完成 |
| `50/TASK_CARD_TEMPLATE.md` | 优于仓库现有任务卡格式；建议反向移植 |
| `50/FEATURE_MANIFEST_TEMPLATE.md` | 字段与 10-ks/01 数据模型一致；填写规则防复制源码 |
| `50/ADR_TEMPLATE.md` | 标准；ADOPT-01 直接可用 |
| `50/HANDOFF_TEMPLATE.md` | 含 Index Impact 与 Rollback，闭环完整 |
| `90/00_GLOBAL_INVARIANTS.md` | 50 条分六域，可判定性强；与 AGENTS.md 红线全兼容（第 4 节矩阵） |
| `90/01_RISK_REGISTER.md` | 18 条覆盖主要失败模式；缺 2 条（ISSUE-12） |
| `90/02_GLOSSARY.md` | 24 条术语与正文用法一致；Catalog/Installed Package/Instance/Authoring 四概念定义清楚 |
