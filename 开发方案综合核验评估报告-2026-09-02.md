# ITTOEDU 1.1→2.0 开发方案综合核验评估报告

> 评估对象：`ittoedu-1.1-to-2.0-agent-development-plan-2026-09-02.zip`
> 评估日期：2026-09-02
> ZIP SHA-256：`92CE004B8D81AC9C526CEA4C5F931159A45C59CA3BCD3E6B799F60C893B0192E`
> 当前仓库 HEAD：`018d3df56c0f0e26f0c00b9e2cc16da69be83a05`
> 方案固定基线：`315b373ac1726f406e62b9f7f3ac7f0b0484d426`
> 整合范围：根目录原有四份评估文档全部完整阅读；对冲突结论重新以当前合同、源码、命令、任务图和官方外部来源核验。本报告是唯一保留的最终评估文档。

## 一、最终结论

**最终判定：不批准把原 ZIP 作为当前可直接执行的开发路线，不执行 `NEXT_TASK`；批准将其降级为“经重基、重裁剪和 Owner 决策后可复用的设计素材与候选 backlog”。**

四份原评估报告给出的结论从“91.56/A+、立即执行”到“禁止直接执行”跨度极大。重新核验后，不能通过平均分解决分歧；可复现事实支持较保守的结论：

1. **当前权威没有批准 ZIP 声称的 AI 时间线。** 当前总纲和架构合同仍把编辑器内 AI、聊天、模型与 Provider 延后到 2.0 以后；`r11-000` 试图用任务本身制造新授权。
2. **方案基线已经漂移。** 当前 HEAD 比固定基线前进一个提交，且 `r11-001/r11-002` 的目标至少已被 `018d3df` 实质覆盖；自带 verifier 在 `headMatches: false` 时仍返回 `status: ok` 和退出码 0。
3. **98 个任务不是渐进 DAG，而是深度 98 的严格单链。** 97 条边、1 个根、1 个叶、0 个多前驱节点、最大 fan-out 为 1；任何可选任务都可阻断全部后续价值。
4. **多个合同和执行硬缺口尚未闭合。** 包括 V9 Table/Chart discriminator 授权、presentation override 校验、Authoring target 无损表达、Flow/Spatial 工具覆盖、动态 Carrier 的 registry/素材闭包/生命周期失败，以及生成代码可信提升。
5. **发布和验收不能按规格完成。** 最终任务调用不存在的 `verify:installer`；真实教师验收位于全链末尾；验收矩阵漏掉当前 DOCX 和两种单 HTML 语义；98/98 任务允许执行者自行选择“最小等价测试”。

### 最终使用判定

| 用途 | 判定 | 说明 |
|---|---|---|
| 立即执行 `NEXT_TASK` / `r11-000` | **禁止** | 提案不能自行替代 Owner 当前决定 |
| 作为当前 1.1→2.0 法定路线 | **不通过** | 基线、权威、任务图和发布门均未闭合 |
| 作为长期设计素材库 | **有条件保留** | V9、Carrier、Recipe、事务、安全红线等部分质量较高 |
| 复用 98 份 task spec | **不直接复用** | 需按当前 HEAD、当前六字段协议即时重写 |
| Table/Chart Native 路线 | **等待 Owner 例外与真实需求证据** | 新 discriminator 不是普通 additive optional 变更 |
| 编辑器内 AI 路线 | **等待 Owner 新决定** | 当前正式路线仍明确延后 |
| OpenMAIC | **独立可选探索轨** | 不得阻塞 Builder、内容 QA 或 Provider |

**没有发现由“方案文档存在”造成的当前产品 P0。** 下文的 G0 是执行授权硬门；P1 是“若按方案实施，会阻断教师核心目标或造成错误结果”的路线风险；Security/Legal 是未来公开能力的发布条件，不冒充当前产品不可用。

## 二、四份原评估文档如何裁决

### 2.1 原文档清单与身份

| 原文档 | 行数 | SHA-256 | 原结论 |
|---|---:|---|---|
| `开发方案评估报告.md` | 321 | `55F1BE661C8421711825B541DB59C87FC44145B58A4EF964122A2CA71F1978B6` | 7.0/10，有条件执行 |
| `ITTOEDU_1.1_TO_2.0_DEVELOPMENT_PLAN_EVALUATION_REPORT.md` | 495 | `DCA7F963ADF2F6B951F142D1B891C40623F28F77131D06B6C5D2647F55646787` | 91.56/A+，立即放行 |
| `ITTOEDU_1.1_TO_2.0_PLAN_INDEPENDENT_EVALUATION_2026-09-02.md` | 147 | `64C8B7C1714C109130C7846CCC2103C7881E318B6B9B1CEBDE12DE5BAC017D6F` | 7.7/10，可作底稿但不可开箱执行 |
| `开发方案独立深度评估报告-2026-09-02.md` | 611 | `AFEAFB7F2A044D8C298DC90DDC224D150FD3899474EB794FDFBFD9F9E90E04DC` | 禁止直接执行，降级为设计素材 |

### 2.2 保留与纠正

| 文档 | 保留的高价值内容 | 核验后纠正或剔除 |
|---|---|---|
| `开发方案评估报告.md` | 全量规格统计、任务范围问题、K-12 隐私/成本/内容政策、PPTX 安全、生成合同制品限制 | 把路线冲突标成当前产品 P0 不符合仓库优先级；9–18 个月只是无容量数据的情景推演；`r11-000` 不应被视为默认仍必要 |
| `...DEVELOPMENT_PLAN_EVALUATION_REPORT.md` | Carrier 阶梯、stale 零写入、离线交付、zip bomb 等风险提示可作候选想法 | “A+、立即执行”与事实冲突；S1/S2 比例错误；25–33 净工时和 20% 加速无依据；safeStorage、DOMPurify、严格 iframe/CSP、Staging Draft、exactInverseStep 被写成既定方案但原包并无相应事实，部分还与当前合同冲突 |
| `...PLAN_INDEPENDENT_EVALUATION...md` | 基线漂移、DR-02 未授权、Legacy 双台账、命令/测试 glob、safeStorage 合同冲突 | “1.6–2.0 约 48 个任务”错误，精确值为 36；“路径事实零错误”只适用于有限 verified list，不能覆盖 task spec 中的不存在/拟新增路径 |
| `开发方案独立深度评估报告...md` | 当前权威、严格单链、产品路线、Authoring/Surface/动态 Carrier、发布与数据生命周期的源码交叉核验 | 吸收其核心结论，同时补入其他报告中经核实的未成年人条件合规、成本护栏、PPTX 解析安全、生成 JSON Schema 限制等内容 |

### 2.3 明确不进入最终结论的断言

以下断言经检索或复算不成立，不能继续传播：

- **“91.56/A+、全面通过、立即执行 r11-000”**：与当前权威、任务板和基线漂移相冲突。
- **“98 任务只需 25–33 净工时”或“重排后必然提速 20%–25%”**：方案全包没有人日、吞吐、团队容量或校准数据。
- **“S1=26.5%、S2=73.5%”**：实际为 S1=20、S2=78，即约 20.4%/79.6%。
- **“1.6–2.0 约 48 个任务”**：实际为 6+8+8+7+7=36 个。
- **“方案已规定 iframe `sandbox=allow-scripts`、`connect-src 'none'`、DOMPurify”**：方案 Markdown 中这些精确机制为 0 次命中；当前合同反而说明 opaque-origin iframe 不是永久权限边界，可信 Runtime/Component 可按正式 origin 声明使用网络。
- **“safeStorage 已被架构批准并作为既定保险箱”**：它只是方案候选；当前合同只允许服务端代理、运行时用户输入或短期限域 Token，长期本地 Provider Secret 必须先由 Owner 修改合同。
- **“Staging Draft、exactInverseStep、CAS 已是方案能力”**：这些是某评估文档的新增建议，不是方案或当前源码事实；若采用需另行证明不会形成第二 Store/Session/History 或过度建设。
- **“全部引用路径零错误”**：baseline verified list 可全通过，不等于 98 份规格的抽象、拟新增或不存在路径均可执行。

## 三、重新核验后的事实基线

### 3.1 包与任务图

| 项目 | 核验结果 |
|---|---:|
| ZIP 大小 | 437,792 bytes |
| 包内条目 | 157 |
| task spec | 98 |
| 版本任务数 | 16/15/12/11/8/6/8/8/7/7 |
| 依赖边 | 97 |
| 根 / 叶 | 1 / 1 |
| 最大依赖深度 | **98** |
| 多前驱任务 | **0** |
| 最大 fan-out | **1** |
| S1 / S2 | **20 / 78** |
| 包内 Hash 复算 | 155/155 一致 |
| `check_package.py` | 157 files、98 tasks、0 issues |

包内完整性很好，但“内部自洽”不等于“当前可执行”。自带 `check_package.py` 主要检查 Markdown 围栏、链接、Task ID 和未知依赖，并不证明基线匹配、命令存在、任务图并行度、验收阈值或源码语义。

### 3.2 当前仓库

- 当前 HEAD 为 `018d3df...`，方案基线 `315b373...` 是其祖先。
- 当前 `TASK_BOARD.md` 为 `Tasks: 0`，不存在已批准的 1.1→2.0 active/queued 路线。
- `COURSEWARE_DEVELOPMENT_PLAN.md:91-95` 记录确定性红灯为 0，`018d3df` 已固定五个真实 consumer 制品和 Published Slide 密集栈序测试；剩余工作只有对固定候选执行一次适用门。
- `COURSEWARE_DEVELOPMENT_PLAN.md:130` 与 `ARCHITECTURE_CONTRACT.md:11` 仍明确：编辑器内 AI、聊天、模型、Provider 延后到 2.0 以后。
- 当前 `WORKING_PROTOCOL.md:35,77` 与 `TASK_CARD_TEMPLATE.md:7-24` 已取消 S1/S2，使用六字段和真实 Write locks。
- `package.json` 有 `verify:release`，没有 `verify:installer`。

### 3.3 自带重基线工具的假绿

实际运行 `verify_plan_against_repo.py` 得到：

```json
{
  "expectedBaseline": "315b373ac1726f406e62b9f7f3ac7f0b0484d426",
  "currentHead": "018d3df56c0f0e26f0c00b9e2cc16da69be83a05",
  "headMatches": false,
  "missingPaths": [],
  "status": "ok"
}
```

退出码仍为 0。该工具只能证明抽查路径仍存在，不能证明方案已重基或可执行；应改为 `rebase-required`/非零退出，或要求调用方显式消费 `headMatches`。

## 四、综合关键发现

## G0｜授权与路线硬门

ZIP 的 `r11-000` 声称 Owner 已给出“1.x 建隐藏 AI 底座、2.0 公开”的新路线，并要求改写 AGENTS、总纲和架构合同；但本次用户要求是评估/整合，不是批准路线。方案自己的 `00_BASELINE_AND_AUTHORITY.md` 也承认正式仓库合同高于方案包。

因此：

1. `r11-000` 不能自行制造其前提授权；
2. 1.6–2.0 的 36 个 AI/Provider/Agent/Chat 任务没有合法启动条件；
3. 1.1 Legacy 清理也须满足当前协议的可复现失败、真实 consumer 或正式合同偏差，不能因历史阶段名自动恢复。

**准入条件：** Owner 在包外明确批准、拒绝或部分批准 AI 时间线；随后以当前 HEAD 和当前工作树重建路线。未决前不得执行 `NEXT_TASK`。

## P1-1｜任务图是 98 步单链，可选任务成为全局阻断

所有非根任务恰好一个前驱，任意时刻最多一个 Ready。方案宣称“写域不冲突可并行”，但依赖图没有编码任何并行 frontier。

最明显的错误是 OpenMAIC：任务本身允许因许可证、API 或映射失败停止，却处于材料 → PPTX → Style → OpenMAIC → 内容 QA → Provider → Generation → Agent → Chat → 2.0 的唯一关键路径。一个可选外部桥会阻断全部后续产品价值。

**修订：** 依赖只表达真实产物前置，写锁另行表达。Legacy、Line、Background、Table、Chart、材料、PPTX、Style、OpenMAIC、AI/Chat 分成独立轨；可选轨使用 `implemented | waived-with-evidence` 汇合，不阻断核心纵切。

## P1-2｜产品价值顺序反转，教师证据来得过晚

方案先安排 16 个治理/Legacy 任务，再强制走完整 Native、Recipe、Tools、材料、OpenMAIC、Provider、Generation、Agent、Chat，最终第 98 项才做明确教师签署。当前产品已有人工新建、编辑、保存、重开、Player 与导出流程，当前候选红灯为 0。

这会先承担高回归面的维护迁移，却把“教师能否更快得到正确、可修改、可保存和可交付的课件”推到链尾。

**修订：** 先完成当前候选适用门和真实教师基线；随后用薄 Authoring Tools + 一个高频 Recipe + 快速诊断 + 内容 QA + 外部 Builder 形成最短纵切。每个用户可见纵切完成后立即验证教师完成率、时间、阻断错误、人工修订次数和交付物，不等待 2.0。

## P1-3｜Table/Chart 尚无 V9 例外，现有校验路径也不能直接承载

- 当前 V9 既有字段与 discriminator 软冻结，普通演进只允许 additive optional 字段；新增 Table/Chart discriminator 必须有明确 Owner 例外。
- 当前 Flow 已有 `FlowTableBlock`，仓库也有 editable-table Component 示例；方案没有先用真实课程证明这些载体不足。
- 当前 `createNativeDataSchema` 仍把 Native data 水合成 legacy `SceneNode` 再经 `sceneNodeSchema` 校验；`materializeNativeLayerItem` 返回 `SceneNode`。
- presentation `nativeData` override 合并后仍交给 `sceneNodeSchema`。即使基础 Table/Chart 分支能解析，命名状态仍可能失败；把它们塞进旧 SceneNode 又违反政策。

**修订：** Table/Chart 各自先做真实载体原型和 Owner ADR。若批准 Native，新 variant 必须建立独立 strict schema/materializer，覆盖基础层、命名状态、保存重开、Published、Player/导出和旧 reader fail-loud；Line 与 Background 不依赖该例外，应独立交付。

## P1-4｜Authoring Tool target 会丢失 canonical target 信息

方案的 `AuthoringToolTarget` 缺少 `surfaceType`、`itemId`、`revisionPolicy`，`owner` 是自由字符串，`authoringAddress` 可选；当前 `CourseAuthoringTarget` 则包含严格的工程、revision policy、session generation、Surface、location、state、owner、item 与必填地址。

**风险：** 异步工具无法证明仍针对创建时的原始 Surface/item，可能回退到当前选择或形成第二套 stale/target 真相，造成错页、错元素写入。

**修订：** 更新工具必须对 canonical target 做 1:1 严格 wire 序列化；创建工具单独使用 canonical create-scope target。测试跨页、切工程、重开、generation 与 revision 变化的零写入。

## P1-5｜声称覆盖三 Surface，实际缺 Flow 正文和 Spatial 语义工具

方案工具目录主要是 generic page/native/layer/component/runtime；没有完整的 FlowBlock/FlowComponentBlock CRUD、嵌套/重排，也没有 Spatial camera/path/relation/semanticZoom 工具。现有命令本身也不通用：删除 Flow location 会拒绝，duplicate/move 主要支持 Slide。

**风险：** Builder v2 会宣称 Slide/Flow/Spatial 全覆盖，但无法编辑 Flow 语义正文或 Spatial 路径/镜头/关系；用 generic LayerItem 补洞会违反正式 Carrier 合同。

**修订：** 建立 `Surface × carrier × owner × operation` 矩阵；只对真实 consumer 所需单元实现，并用保存重开和真实 Player 证明。不得用“page CRUD”模糊不同 Surface 语义。

## P1-6｜动态 Component/Runtime 路线跳过三项已知必修门

当前总纲 `COURSEWARE_DEVELOPMENT_PLAN.md:97-103` 已明确，动态 Carrier 一旦进入真实 consumer，必须先闭合：

1. Component registry 身份区分工程、package、version 与 source/content identity；
2. `projectAssetUrl/projectUrl` 直接引用的已保存素材进入 Published asset closure；
3. update/resize/setVisible 等生命周期失败后销毁/隔离旧实例并给出可见静态后备。

方案的 r14/r17 已明确要求 code escape hatch 与真实 Generated Component/Runtime，却没有把三项设为前置。这会产生旧代码缓存、导出漏素材或失败后冻结旧画面仍返回 `ok: true` 的真实结果错误。

**修订：** 只要动态 Carrier 被保留，三项必须成为 Player/HTML 硬门；若不能闭合，公开纵切默认限制为 Native、Recipe 与已审核组件。

## Security-High｜“模型生成代码”不等于“经审核可信代码”

当前合同允许 Runtime/Component 作为经过审核的可信扩展，并明确 opaque iframe 不是永久权限边界。方案却允许模型自动生成、打包、smoke、运行和修复 Component/Runtime，没有定义首次执行前的人类源代码审查或可信提升。

格式校验与 smoke 不能证明未知 JavaScript 不访问文件、Secret、网络或宿主能力；普通教师也不能默认承担 JavaScript 安全审计。

**发布条件：**

```text
生成到待审/隔离制品
→ 依赖、网络、静态分析、资源上限与恶意 fixture 检查
→ 真正受限环境预览
→ 展示来源、diff、声明能力和静态后备
→ 有资格的审核者明确批准
→ 才提升为工程内可信扩展
```

若公开产品无法提供合格审核者，应禁止任意代码生成，而不是把风险转嫁给教师。

## P1-7｜Provider、Secret 与数据生命周期尚未形成统一合同

- 方案把 desktop BYOK/safeStorage 列为候选；当前合同只允许服务端代理、运行时用户输入或短期限域 Token。长期本地 Secret 需要 Owner 明确修改合同，不能由 r16-011 实现任务自行决定。
- 自定义 endpoint 没有 SSRF、loopback、link-local、metadata、DNS rebinding、重定向重校验与 TLS/证书反例。
- 材料、citation、Chat、Agent trace、usage/cost、checkpoint 均被排除出 CourseProject，却没有明确存储 owner、Save As、工程切换、崩溃/重启恢复、保留、删除、卸载和迟到 IPC 行为。
- 材料来源若只存在外部 trace，工程跨机器或另存后可能失去审计链。

**修订：** Provider 前先做一份数据分类、网络与生命周期 ADR；分别裁决 Secret、敏感原材料、可携带 provenance、用户可见 citation、Chat/trace/usage 的存储与删除行为。

## Legal/Privacy｜未成年人问题是条件性发布门，不是当前 P0

原评估文档中“方案没有 K-12 未成年人合规设计”的事实成立：包内没有未成年人、监护人同意、PIPL、COPPA 或专门隐私规则内容，只有泛化的数据保留/删除 ADR。

但不能在没有部署地区、用户年龄、数据来源和处理者角色时直接断言某一法规必然适用。正确结论是：

- 如果在中国处理不满十四周岁未成年人的个人信息，《个人信息保护法》把它列为敏感个人信息，并要求监护人同意和专门处理规则；见[中国人大网正式文本](https://www.npc.gov.cn/WZWSREL25wYy9jMi9jMzA4MzQvMjAyMTA4L3QyMDIxMDgyMF8zMTMwODguaHRtbD9yZWY9aW1i)。
- 如果美国商业网站/在线服务面向 13 岁以下儿童，或实际知道正在收集其个人信息，COPPA 会触发相应义务；见[美国 FTC COPPA Rule](https://www.ftc.gov/legal-library/browse/rules/childrens-online-privacy-protection-rule-coppa)。

**发布条件：** 在任何公开 AI/云 Provider 前，Owner 必须确定地区、数据主体、材料是否含学生个人信息、Provider 数据流、同意/告知、保留/删除和学校/教师角色；随后由合格法律人员确认适用义务。本报告不构成法律意见。

## P1-8｜发布、验收与交付格式无法按原规格闭合

- `r20-060` 要运行不存在且无前置任务创建的 `npm run verify:installer`；当前只有 `verify:release`。
- 方案声称每个版本可独立发布，但 1.6–1.9 又是隐藏能力；1.1–1.9 没有清楚的版本提升、渠道和制品定义。
- 验收矩阵主要列单 HTML、Web、PDF、PPTX，漏掉当前正式 UI 的 Flow DOCX；“单 HTML”又没有区分离线便携与在线轻量。
- 98/98 task spec 都允许通配符不展开时自行选“最小等价集合”，0/98 含当前任务卡真实 Write locks；不同执行者的证据不可比较。
- 多个 phase gate 把已包含 typecheck/test/e2e 的 `verify` 与子命令重复执行，违反同候选、同命令只执行一次的停止条件。
- 最终教师验收没有参与者画像、起始材料、允许帮助、任务时限、阻断错误和修订次数阈值。

**修订：** 先定义产品版本、平台、渠道和制品；固化精确命令；从当前 UI/producer 生成真实 `Surface × 交付格式` 矩阵；每个用户纵切即时教师验收，最终门只汇总已有证据。

## P1-9｜Legacy 路线准备建立第二份真相

当前合同规定 `docs/development-plan/inventories/legacy-consumers.json` 是唯一 Legacy 台账，任务卡只能引用 record ID。方案 r11-010 却拟建立独立 token/path allowlist，且方案全文没有把现有 inventory 作为单一数据源。

裸字符串 `ProjectDocument` 扫描还会把合法 `CourseProjectDocument` 大量误中；当前源码抽查 889 行含该子串，只有约 134 行是独立 token，约 734 行只是子串噪声。

**修订：** 扫描器只能读取、验证和收紧现有 inventory；新增、迁移、清零与台账记录原子更新。优先 AST/import symbol，不用高噪声命中数决定删除或工作量。

## P2｜重要但不应抢占当前核心流程的改进

1. **task spec 范围仍偏宽。** 多项任务同时覆盖多 Surface、多 Carrier、UI/合同/导出或验收+发布；Read-first/Allowed writes 中仍有大量能力名而非精确当前路径。
2. **规格治理字段已过期。** 98/98 含旧 Risk/Hotspot，约 78 项要求 S2 Reviewer，0/98 含当前 Write locks；应按当前六字段即时生成任务卡。
3. **包内检查器证明范围有限。** `check_package.py` 不证明 SHA、环、并行度、命令存在、验收阈值或源码语义；`verify_plan_against_repo.py` 会基线假绿。
4. **生成 JSON Schema 不能证明 Native data。** `generate-contracts.ts` 使用 `unrepresentable: 'any'`，产物中多个 Native `data` 为 `{}`；严格性仍依赖运行时 Zod、类型和端到端 roundtrip。若外部工具消费 JSON Schema，必须补明确限制或可表达 schema。
5. **PPTX 导入缺 staging/batch transaction。** 多元素 document + sidecar、全失败零写入不能靠逐元素普通工具保证；还需解析库选型、解压比/大小上限、禁 XXE、unsupported feature 报告和第 N 项失败回滚测试。
6. **网络声明写工具缺失。** code tools 禁止未声明 origin，但工具目录没有声明/删除 `connectOrigins` 的正式命令；Preview、Electron/CSP、Published 和诊断必须从同一声明派生。
7. **可访问性被放到末尾文档任务。** 该任务无权修改 UI，却要求证明关键 UI 可访问；键盘、焦点、live region、表格语义、对比度和缩放应在功能任务内完成。
8. **成本与容量无门。** 没有模型 token/金额/速率/配额、课程规模、材料/PPTX 大小、stream/session 内存、诊断或导出预算；弱模型 benchmark 也无固定模型和 pass/fail 阈值。
9. **内容 QA 需分学科与人力模型。** 数学 AST/数值检查可确定化，教材版本、学段、知识路径、反馈质量和敏感内容仍需教师/学科专家；不能用 LLM 自审替代。
10. **供应链与发布 runbook 缺失。** 新 Provider SDK/解析器/导入器应统一记录许可证、漏洞差量、包体、锁文件和可重复构建；发布需升级/降级/撤回、数据兼容、凭证/材料状态和制品身份。
11. **当前可见诚实性问题被未来路线遮蔽。** 公式帮助仍写“Project V8”，用户指南导出表未完整体现当前 DOCX；应按用户可见性优先修正，不等待大路线。

## 五、方案值得保留的内容

1. **包内部完整性高。** 文件、Hash、Task ID、INDEX 和依赖引用整理认真。
2. **权威/rebase/obsolete/stop 意识正确。** 问题在于工具和 NEXT_TASK 没有真正落实。
3. **坚持 V9 单一工程、不借机创建 V10。** 单一 Store/Session/History、稳定 target/revision、事务和 sidecar 方向正确。
4. **Carrier 阶梯有产品价值。** Native → Recipe → Existing Component → Generated Component → Runtime 的升级逻辑合理，前提是动态代码信任门闭合。
5. **Recipe 展开为普通 V9。** 不建立第二 DSL，保留教师可编辑性，限制容量并避免无限缩字。
6. **Builder v2 的产品命令方向正确。** 外部 Builder、人工与未来 Agent 应复用同一写语义；应由一个真实 consumer 的薄纵切驱动，而不是先建完整平台。
7. **验证分层正确。** 结构/行为/视觉/内容/教师 accepted 分账，自动化最多证明 candidate。
8. **Secret 与 IPC 基础红线较好。** Secret 不进入 CourseProject、Published、导出、日志；主/预加载职责、取消和脱敏值得保留，但具体存储模式未决。
9. **Prompt Injection 的宿主权限原则正确。** 材料是 data，不得自行提升工具或主进程能力。
10. **OpenMAIC 固定引用准确且态度审慎。** 固定 [commit](https://github.com/THU-MAIC/OpenMAIC/commit/32bfd197eb5a112b63a241d4da02dfcc1558993f) 存在，根 [LICENSE](https://raw.githubusercontent.com/THU-MAIC/OpenMAIC/32bfd197eb5a112b63a241d4da02dfcc1558993f/LICENSE) 为 MIT；仍需按实际复制/链接/依赖做 SBOM 与义务审查。

## 六、建议替代路线

### 阶段 0｜当前候选与 Owner 决策

- 对固定候选 `018d3df` 执行一次适用门并形成持久记录。
- 用真实教师课例建立当前人工流程基线。
- Owner 裁决 AI 时间线、首个正式发布制品、Table/Chart V9 例外、Published V2/V3、Secret 模式与生成代码信任。
- 以当前 HEAD/工作树把 98 任务分类为 `still-valid / obsolete / split / optional / owner-blocked`；`r11-001/r11-002` 不再作为待执行任务。

### 阶段 1｜最短教师价值纵切

```text
薄 Authoring Tools
→ 1 个高频 Recipe
→ 快速诊断 + 内容 QA
→ 现有外部 Builder 真实课例
→ 教师修改
→ 保存/重开
→ Player
→ 适用 HTML/导出
→ 即时教师验收
```

工具只先覆盖真实课例需要的 Surface/Carrier/owner，不宣称全矩阵完成。

### 阶段 2｜独立证据轨

- Line 直接绘制；
- Background authoring；
- Table 原型与 V9/Published ADR；
- Chart 原型与 V9/Published ADR；
- 材料 ingest/read/cite 与 provenance 生命周期；
- PPTX 最小导入与原子事务；
- Style remix；
- OpenMAIC bridge（可 waiver）；
- Legacy record-ID 驱动迁移。

这些轨道各自能交付结果，互不成为无关能力的总前置。

### 阶段 3｜条件性编辑器内 AI

仅在 Owner 改变当前 AI 边界后启动：

```text
Provider/Secret/网络/数据生命周期 ADR
→ 单页或局部编辑入口
→ canonical target + stale 零写入
→ 保存重开/Player/导出/失败恢复
→ 教师 dogfood
→ 再决定是否增加整课、Agent、Skills、Chat
```

任意 Component/Runtime 生成默认关闭，直到可信提升和三项动态 Carrier 门通过。

### 阶段 4｜正式发布

- 明确语义版本、平台、渠道、便携版/安装包/源码制品；
- 完成可访问性、性能、隐私删除、供应链、升级/降级/撤回；
- 固化精确发布命令，不调用不存在脚本；
- 最终 accepted 汇总各纵切已经通过的教师证据。

## 七、重写路线的准入门

| 门 | 必须证明的属性 |
|---|---|
| G0 路线门 | Owner 决定已落库；基线为当前 HEAD；无外部提案自授权 |
| G1 单一真相门 | Legacy 只用现有 inventory；Tool target 是 canonical target 无损适配 |
| G2 Surface 门 | 真实 `Surface × carrier × owner × operation` 覆盖，不用 generic layer 伪装 |
| G3 V9 Native 门 | strict parse、旧 V9、presentation override、保存重开、Published、Player/导出、旧 reader fail-loud |
| G4 动态 Carrier 门 | registry 身份、direct asset closure、生命周期可见失败、生成代码可信提升 |
| G5 网络/导入门 | 单一 origin 声明源；Provider endpoint 反例；PPTX document+sidecar 原子回滚 |
| G6 数据/合规门 | Secret、材料、citation、Chat/trace/usage 的存储、保留、删除与适用法律已裁决 |
| G7 发布门 | 固定候选、精确命令、真实交付物、教师验收；自动化不冒充 accepted |

## 八、必须由 Owner 明确决定

1. 是否推翻当前“编辑器内 AI 延后到 2.0 以后”的路线；批准范围是 Provider、局部生成、整课、Agent、Chat 中的哪些部分。
2. 首个真实发布的版本号、平台、渠道和制品；是否真的承诺安装包。
3. 是否批准 Table/Chart 作为 V9 新 discriminator；首版真实 Surface/导出需求是什么。
4. Published Table/Chart 使用 V2 窄扩展还是 V3，以及独立 reader/长期兼容需求。
5. 是否允许模型生成任意 Runtime/Component；谁审核、审核什么、何时成为可信。
6. Provider 采用服务端代理、运行时输入、短期 Token、BYOK 或组合；若选择长期本地 Secret，是否修改当前合同。
7. 材料、citation、Chat、Agent trace、usage/cost、checkpoint 的存储、恢复、跨机器、保留和删除规则。
8. 产品目标地区、学生/教师数据角色和未成年人个人信息处理范围。
9. PPTX、OpenMAIC、Style remix 是否为已证实需求，还是可选探索。
10. 教师验收参与者、课例、成功阈值和 `accepted` 签署人。

## 九、最终建议

**保留方案思想，重写执行路线；不要修补旧单链后继续推进。**

最先应做的不是 `r11-000`，而是：

1. 完成当前 `018d3df` 候选适用门；
2. 由 Owner 裁决 AI、V9 Native、发布制品与 Secret/数据边界；
3. 把 98 单链重构为结果驱动 DAG，OpenMAIC 等可选项移出关键路径；
4. 只为首个薄纵切生成当前六字段 task cards；
5. 每完成一个教师价值纵切即做真实验收。

这能保留原方案最好的架构纪律，同时避免把未批准决定、维护债、可选实验、流程仪式和未来愿景压成一条无法并行、无法独立交付的 98 步路线。

## 附录 A｜主要本地证据

- 当前路线：`COURSEWARE_DEVELOPMENT_PLAN.md:47-55,85-103,128-134`
- 当前工作协议：`docs/development-plan/WORKING_PROTOCOL.md:13-91`
- 当前架构合同：`docs/development-plan/ARCHITECTURE_CONTRACT.md:7-11,46-51,72-75,120-130`
- V9 兼容政策：`docs/contracts/V9_COMPATIBILITY_POLICY.md:6,16,21-24,39-64`
- 当前任务模板：`docs/development-plan/TASK_CARD_TEMPLATE.md:7-24`
- Authoring target：`src/renderer/authoring/courseAuthoringSession.ts:33-46,106-184`
- V9 Native/override：`src/shared/contracts/course-project-v9/schema.ts:162-249,337-355,552-581`
- V9 Flow/Spatial 类型：`src/shared/contracts/course-project-v9/types.ts:255-459`
- 动态 Carrier 路线门：`COURSEWARE_DEVELOPMENT_PLAN.md:97-103`
- Published producer：`src/renderer/export/course/buildPublishedCourse.ts:242-371`
- Component/Runtime 宿主：`src/player/ComponentRegistry.ts`、`src/player/surfaces/publishedComponentMount.ts`、`src/player/surfaces/runtime/publishedSurfaceRuntimeMount.ts`
- 交付格式：`src/renderer/ui/TopToolbar.tsx:367-473`、`src/renderer/App.tsx:1370-1401`、`docs/USER_GUIDE.md:512-536`
- 生成合同：`scripts/generate-contracts.ts:31-39`、`artifacts/contracts/course-project-v9.schema.json:974-1060`

## 附录 B｜核验方法与限制

- 四份原评估文档逐行完整读取，并记录文件 Hash 后整合。
- 独立复算 98 任务的 ID、版本、依赖、图深度、fan-out、S1/S2、通配/等价检查与 Write locks。
- 重新运行 ZIP 的 package check 与 repo verifier，核对退出码和输出语义。
- 对当前合同、源码、命令、任务板、HEAD、发布脚本和用户指南做定点核验。
- OpenMAIC、PIPL 与 COPPA 仅使用官方仓库或官方机构来源。
- 本次是设计/执行方案评估，不是对尚未实现能力的功能验收、渗透测试或法律意见。
- 没有真实教师参与本次整合，因此未证实的需求只能标为“待真实课例证明”，不能断言不存在。
