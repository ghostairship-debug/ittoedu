# 果铃 2.0 收敛方案

**状态：** 决策收敛稿 v2.2 · 2026-09-25 基线提交 `25b12b4b`，并整合轻编辑、文件会话细节与 Runtime/HTML/Flow 创作能力收口（见 §7B）。方案修订不代表产品实现或验收完成。

**执行入口：** 本文记录当前产品与架构决定；[GPTpro 执行包](GPTpro方案/guoling_2_0_execution_plan/00_README.md)保存任务、依赖、合同样例和验收定义。两者按本轮决定同步；发现矛盾时先修正文档，不恢复被取代路线。

**后续扩展边界补充范围：** 产品正在按既有授权开发；harness 补充只明确后续方案和交接说明，不修改产品代码、不暂停或重排当前开发、不新增实施任务或发布前置、不运行付费模型、不提交 Git。新增格式、独立代码成果和薄 runner 的实现待具体功能接入；不因该补充追溯要求重做已开发部分。

**工作台交互补充范围：** Owner 已确认第 2 节及[工作台与编辑器细则](GPTpro方案/guoling_2_0_execution_plan/mid_term/WORKBENCH_EDITOR_LAYOUT.md)的布局、日常编辑与就地 AI 入口。它们细化当前 M01/M03/M04/M07/M10，相关界面集成时落实；本次会话只改方案与交接，不改业务代码或正在维护的任务/验收状态。尺寸等细节在实现时复核，独立编辑器窗口仍待定。[本轮可转发交接词](GPTpro方案/guoling_2_0_execution_plan/delivery/UI_HANDOFF_2026_09_23.md)。

## 1. 已确定的产品决定

1. 当前没有用户，大规模重构正当其时；旧路线、既有 Store 与执行器不构成限制，不以范围过宽缩减完整目标。
2. 通用 AI 内容工作台，教育是首发场景；教学方法作为可选技能，不写死在产品外壳。一级对象只有空间、文件、会话。
3. 文件与会话正交；h5lesson 是可关闭文件，Flow、Slides、Spatial 是其内部表面。Markdown 默认正文，源文是另一视图。
4. **果铃自建统一完整执行器**，覆盖产品内直接编辑、内容/素材生成、多步骤任务、受控代码构建与导入。可复用成熟 SDK/Agent 核心，不要求从零复制通用编码产品。
5. **API 接入为主。** 用户可配置 DeepSeek、MiniMax 或其他供应商及多模态模型进行低成本生产；通过 API 提供的 Token Plan/其他允许套餐是正式使用场景。
6. **OAuth 是用户可选连接，GPT OAuth 首轮上线。** 开发使用 Owner 当前账号通过正式登录流程接入，前期图片生成使用该连接；独立图片 API 供应商与账号待定。其他供应商按实际支持接入；API 文本/编辑主链不强制用户拥有 GPT 订阅或启用 OAuth，需要生图时须有已验证的图像连接。
7. 对话/规划、视觉理解、图片生成/编辑可以分别选择连接。开发质量基准与用户模型选择分开；低成本 API 生产必须真实验收。
8. 外部 AI/CLI 通过一套 MCP 使用果铃能力，作为开放协作与兜底入口。取消三家 CLI 深度嵌入作为必做项，保留实际内容能力。

## 2. 交互与信息架构

默认左资源、中内容、右 AI：左侧资源管理器在上、会话列表在下，默认常驻，用户可主动隐藏；中间文档标签、简洁工具栏与主要画布/正文；右侧 AI 助手和输入区常驻。沿用左上角工作空间新建/切换入口，不增加同义项目层级。面板真正收展与记忆尺寸，不通过高频收展才能管理多文件。知识库、模板、工具管理和任务详情按需进入，不全部占据一级导航。

资源区使用“资源管理器”名称；文件管理以右键、拖拽、快捷键等 Windows 常见习惯为主，顶部保留少量高频入口。会话可新建、搜索、重命名、删除，与文件保持正交。

课件内容区底部固定横向场景/页面导航，每个场景卡片下方直接放自己的小状态按钮，当前场景和状态分别突出。场景多时横向滚动，控制底栏高度以保留大画布；场景树原有层级与 Flow/Spatial/Mix 的定位能力必须保全，非 Slides 页面不伪造呈现状态。导航属于当前文档，不与工作空间文件树混排。

工作台可插入文字、图片、视频、音频，支持资源管理器媒体拖入；选中后用快捷工具条和按需属性浮层完成常用手动编辑。保留既有快捷工具条内直接输入、提交的元素级 AI 修改，不强制跳转右侧助手。两处 AI 入口共用执行、目标、权限和文档变更；各 Surface 保留自身模型，Flow 正文不强行变成自由画布元素。

专业编辑器主体布局保留，只将原有常驻右侧工具面板改为 WPS 式侧边按钮按需展开，AI 也放在按钮组；资源/会话默认收起，如保留入口则浮出。两种模式编辑同一文档并共享 History、资源与保存。独立窗口/标签分离待定，不作为本轮前置。具体布局与待验证场景见[交互细则](GPTpro方案/guoling_2_0_execution_plan/mid_term/WORKBENCH_EDITOR_LAYOUT.md)。

发送前展示文件、页面、对象或正文范围。显式局部修改限制范围；普通聊天的选区/图片是上下文，不增加应用自己的自然语言意图分类器。运行时按实际权限使用能力，不追随焦点偷换目标。

普通编辑默认原位变化、完成自动提交、一次撤销；用户要求先审阅时改变同一 EditSession 的接受策略。长任务、页数或复杂度不能自动变成候选交付。

同一任务可交替直接编辑与构建工具，不要求用户预选 live/build；不因调用构建而自动切换模型或计费来源。超出已有授权时再处理具体权限。

## 3. 文档内核与工具边界

```text
API / Token Plan / 可选 OAuth → 果铃统一执行器 ─┐
人工编辑 ─────────────────────────────────────┼─ 统一 Tool Gateway
外部 AI / CLI → 果铃 MCP 桥 ──────────────────┘       ├─ 文档工具 → DocumentSession → 视图 / History / 保存 / 恢复
                                                     ├─ 文件 / 构建 / 媒体服务 → 各自操作或 job 回执
                                                     └─ 后续可选薄 runner → 外部 Agent → 同一 MCP
```

- 主进程文档服务持有 DocumentRegistry 和每文档独立 DocumentSession；纯 TypeScript 内核不依赖 Electron。每份文档一个正式 writer、操作序列和 History。
- renderer 中的 ProseMirror、画布与 Store 是视图、缓存或未 ACK 编辑投影；人工输入可即时显示，AI 读取前须完成已发出的人工修改。
- documentId/revision/epoch 是文档身份；路径是可变 binding；当前页、选区和缩放属于 ViewState。会话或模型连接不能替代文档身份。
- Markdown 以保真源文/资源引用为正式内容，h5lesson 沿用受支持 V9 与资源侧车，各有 Driver，不统一成万能 JSON。
- ToolCatalog 导出人工操作、函数工具与 MCP schema；内部调用不必绕 MCP。宿主注入身份、基准、权限、operationId 和历史分组，模型只拿短句柄与内容。
- MCP bridge 接同一 Gateway 和能力服务；文档操作只连接同一文档服务，多客户端不能启动各自可写内核。人工、内置 AI、外部 MCP 的同类操作共用权限、领域规则和回执。
- 领域 planner 按纯度迁到 core，DOM、Phaser、播放器测量保留窄视图端口。复用能力，不保留另一套正式 Store/History。

DocumentSession 只承担正式文档内容的权威与历史，不充当所有任务的容器。任务可以引用零份或多份文档；新文件、数据处理与作业使用各自的资源引用及授权，不能为适配文档工具伪造当前文档。模型循环不依赖具体格式或“元素”结构；各能力自行定义操作、提交与验证语义。图中后续分支表示扩展位置，不表示本次新增交付项。

## 4. 正文流、直接编辑与构建

### 正文草稿和正式提交

正文使用 `edit.begin → edit.delta/snapshot → edit.complete/abort`。每文档一个活动正文生成组；重叠区域受控，其他区域人工输入继续并可靠映射。完成后一次事务/一次撤销，人工操作穿插时按正式操作序列处理。

Provider 原始工具参数分片先进入执行器解析缓冲。调用、目标与授权固定后，可以增量解码指定 content 字符串，再发类型化文本事件投影草稿；无需完整 JSON 到齐或额外模型轮才显示已确认文字。**不完整参数不能执行正式操作。** 字段重复、转义截断、缺片、目标变化或异常结束须撤回或明确失败，不猜补。完整参数与最终文本通过校验后才提交。

明确目标绑定的独立文本流同样可用，但不得截取普通聊天/思考冒充正文。外部 MCP 仅完整调用时按操作级更新，不假称逐 token 生成。

生成中只保存已提交 revision；取消当前未完成草稿，保留此前正式操作。停止屏障、幂等、恢复和一次撤销由任务/文档内核保证；不能把本地取消等同供应商已退款或请求未收费。

### 受控构建和导入

统一执行器在独立 scratch 编码、调用受控构建、读取错误修复、隔离预览，再经正式入口导入。live/build 是工具权限与交付方式的区别，不是两套循环。

源工程和正式文件不向构建子进程开放旁路写入，scratch 允许必要操作；进程、文件、网络、凭据与资源限制须实际实现，不能只设置 cwd 或写提示词。编译通过不等于交互正确，真实宿主检查与资源闭包保留。

只删除普通编辑的 candidate/replacement 必经链。构建、导入、Runtime/Component 继续保留 staging、检查与 canonical commit。跨文档分别提交、如实显示部分完成，不假称全工作区原子事务。

## 5. 执行器、模型连接与计费

| 层 | 职责 |
|---|---|
| ExecutionEngine | 任务循环、工具往返、上下文、取消/恢复、预算与状态 |
| ModelProvider / 媒体 Provider | 一次请求、真实流、协议与用量、图片生成/编辑 |
| Connection | 供应商、endpoint、凭据引用、认证方式、账户与能力 |
| Billing | 按量、Token Plan/其他订阅、预付或未知；独立于认证 |
| Tool Gateway | 目标、权限、幂等、领域操作与真实回执 |

API Key 不等于按量付费，OAuth 不等于通用订阅额度。套餐的模型目录、额度窗口、并发和适用范围按实际路由处理；未知余额显示未知，不把 token 估算当成套餐实际扣费。

完整执行器须负责上下文预算/压缩、工具参数与结果、错误分类、有限重试、操作查询、任务恢复和成本记录；不能用“加一个 SDK”低估这些责任。供应商的原生续接/签名等必需字段由 Provider 保留，不一律压缩成最低公共消息格式。

运行事实、操作/job 状态和成果引用独立于可压缩的模型上下文；模型结束、进程退出、成果提交与验证通过分别记录。任务按其实际目标、必要子任务和验证证据结算，不把缺失或未知结果标为成功。此处细化既有 S05/S07/S13 语义，不增加固定审查轮或新验收门。

图片理解、生成和编辑分别发现与验收。低成本文本搭配独立多模态模型是正式组合；模型能力不支持时显示缺口或使用用户预先配置的角色连接。不后台更换供应商、模型、档位或费用来源；用户已允许的回退须记录，范围外再询问。

高能力 GPT 可建立开发质量上限对照，但产品不绑定 GPT；低成本 API/可用套餐需有真实生产证据。SDK 选择、内部测试模型与用户默认设置分离。本轮不建设平台充值、销售套餐或托管计费服务。

开发主路由是 TeamoRouter 的 DeepSeek（provider `teamorouter`，`https://api.teamorouter.com/v1`），第二路由为 DeepSeek 官方 API。开发测试运行时读取 `TEAMOROUTER_API_KEY` / `DEEPSEEK_API_KEY`，产品运行时凭据进入自己的安全存储。模型与能力在探针时按实时目录和实际调用钉定；Owner 提供的路由说明中 `deepseek-flash` 指 V4.1、`deepseek-v4-flash` 指 V4，不得混写，dsh 配置清单不是供应商目录权威。

首轮提供 GPT OAuth，Owner 已选择当前账号并授权正式登录及前期 OAuth 生图验证。独立图片 API 待定，不是 B05 的账号前置；B05 前必须验证所选 OAuth 图像连接与所需能力，不能把登录成功当作生图/编辑成功。该开发连接选择不强制所有用户启用 OAuth，不改变 API 主使用方式。

OAuth 是可选使用能力，GPT 及其他拟支持连接上线前分别验证。OpenClaw 文档记录了 ChatGPT/Codex OAuth 生图/编辑路径，但图像型号可用范围与 API 不完全相同，部分路由可能选择原生 Codex runtime。资料可作技术参考，不能代替果铃实际 endpoint、模型、执行者和计费证据；API 主链独立成立。

## 6. 外部 MCP 与兜底

外部 Codex、Claude、OpenCode 和其他客户端使用同一果铃 MCP Server。只维护注册、配置位置、传输/认证兼容；不深度适配原生聊天、思考、模型目录与会话生命周期。

本地使用经验证的 stdio bridge 或 loopback Streamable HTTP，连接绑定授权；HTTP 具备身份/来源校验与短期令牌。预览页面无桥和凭据。远程客户端无法访问本机时明确不支持，不默认建设公网隧道。

交接包含用户要求、目标、已提交事实、剩余工作、必要附件与重新观察入口。交接前停止并结算旧 run，外部取得新授权后推进。MCP 不负责果铃自动启动、接管或停止外部 CLI 的整个 Agent；无缝自动换执行器不是 2.0 承诺。2026-09-24 Owner 决定：中途交接不作为 2.0 验收，原 S12-T04 与 REL-T12 删除；外部直接读写同一文档仍按 S12 验收。

果铃只展示实际 MCP 调用和结果，不伪造外部完整聊天、思考、费用或逐字流。撤权后拒绝晚到写入，但不能保证外部模型/独立 shell 停止。MCP 不是外部进程沙箱；磁盘旁路修改由 FileService 处理重载/保存冲突，不能冒充宿主提交。

后续允许果铃通过薄 runner 主动启动外部 Agent，承担有限启动、事件、取消和结果收集；它是可选外部作业能力，不是 ModelProvider 或另一套文档业务内核。交接会结算旧 run，委派则由父任务等待并接收子任务结果，两者不能混用。具体授权收拢、进程状态和成果核验见 [L03](GPTpro方案/guoling_2_0_execution_plan/long_term/L03.md)。本次只明确方向，runner 未排期，不加入当前 S12 或 2.0 发布前置。

## 7. 重构与兼容范围

删除旧用户聊天/缓存迁移、旧聊天/缓存索引、双内核切换和不存在用户的升级回退门。保留样本副本、开发基线、崩溃恢复、保存保护与数据版本。

已有文件按受支持 V9 与 Markdown 合同读取，不承诺所有历史格式。**代码迁移地图保留**：它描述旧入口如何被新内核接管，与旧用户数据迁移不同。高级课件、媒体、交互、播放器与导出须逐项保全。

## 7A. 2026-09-25：2.0 追加创作能力收口

当前 Codex 既有验收先完成剩余项目并形成稳定 baseline；不在最后三项验收过程中插入 Runtime/HTML/Flow 内核改动。原有通过证据保持有效，不因追加需求回退为未完成。该 baseline 已于 2026-09-25 以 `25b12b4b` 提交；尚余的 REL-T11 按 §7B 挪到 M18 之后。

baseline 之后，2.0 继续完成五项必做能力：M15 Runtime/Component 轻编辑与自动图文发现；M16 Flow 混合画布与页面 Runtime；M17 HTML 高保真机械导入；M19 Slide 有限画布尺寸参数化；M18 后置 Representation Planning 与创作链收口。完成这些新增验收后才形成新的 G-2.0。2026-09-25 讨论后另增 M20、M21，并与上述五项整合重排，见 §7B。

Runtime 编辑只做文字、图片和少量已有参数。用户可以在试运行推进到某一动态状态后返回当前状态编辑；宿主自动发现 DOM 与 Phaser 常见图文（Phaser 只做 Text 与静态 Image），Three 不做适配。AI 不再承担任何登记（含文案表与编辑目标注册）；Scene Runtime 与 Component 复用轻编辑基础。不得扩成任意 DOM/CSS/JS/Three 深编辑器。

HTML 首版默认整体 Runtime 高保真导入，程序机械完成资源抽取、引用重写、包装和正式导入，不调用 LLM 重新实现。Native 拆分后置为有明确编辑收益时的优化；听力案例作为首个真实保真基准，但不建立像素评分服务或固定多 Agent 审核。

Flow 保留 Markdown/SharedDocumentEditor 连续正文，同时把 Native 文字、图片、形状与组件作为自由图层（默认随段落，可选固定纸面）；浮动能力沿用组件机制，不另建浮动 Runtime。正文新增文字继续用段落/标题命令；产品与 AI 的“插入文字”改为插入“文本框”（Native Text）。Flow 页面 Runtime 作为正文中的整块区域，可承载长 HTML 并随内容高度延展，不重构成 Slide 子类。

Slide 定义收敛为有限二维自由画布，1280×720 仍是兼容默认而非唯一尺寸；Flow 负责连续流式空间，Spatial 负责无限二维世界与镜头。三种 Surface 主要区分空间组织方式，而不是把 Runtime/Native/Component 能力人为分成三个等级。

创作工作流改为：教学设计 → 理想呈现/框架 HTML → Representation Planning → Native / Asset / Runtime → 机械装配。教学设计与理想呈现阶段不得加载编辑器能力白名单；只有表示规划阶段读取短 Capability Profile。该环节是一次薄规划，不增加评分 Agent、架构审查 Agent 或多轮门禁。

## 7B. 2026-09-25：Owner 使用反馈与整合开发结论

Owner 实际使用工作台后提出四处问题，并要求与 M15–M19 整合开发，而不是先修完再开发后续任务。以下为讨论后的决定，与 §7A 或《工作台与编辑器细则》冲突时以本节为准。

- **文件与会话（M20）：** 资源管理器新建菜单改为“新建 Markdown 文档 / 新建课件 / 新建文本文档 / 新建文件夹”，预填默认名、缺后缀自动补、新建后直接打开；`.txt` 做成正式纯文本文档类型（UTF-8 字节保真可编辑，非 UTF-8 提示并可用系统程序打开，第一版只收 `.txt`）；课件轻量顶栏加“另存为”；右侧会话头显示“工作空间 › 文件夹 › 文件”，只代表默认引用和新建位置的优先级，不限制修改范围，不加不一致提示。
- **轻编辑外壳（M21）：** 顶栏改为单行固定高度，选择不再引起画布跳变；选中任何对象都出现统一的浮动快捷条（样式同文字编辑工具条），拖动时隐藏；取消属性卡与 X/Y/宽/高 数值框，操作以拖动为主；颜色默认常用色板，连续调色只作“更多颜色”；“深度编辑”改名“编辑器”，进入编辑器只保留右上角按钮。
- **元素 AI 卡（并入 M15）：** 快捷条上的“AI 修改”打开绑定该元素的临时 AI 卡，内含对话记录、撤销/重做与输入框；不进会话列表，与右侧对话框无关，只在文件打开期间存在，元素删除即消失。同一元素排队、不同元素并行，不设并发上限；卡内 AI 只改该元素；单独撤销只回退 AI 改过的属性；选区失效改为按目标判定。选中文字（Markdown 与讲义同一编辑器）的卡片关掉即结束；文档中的图片、表格等有稳定 ID，按对象处理。顶栏保留“进行中/需回答”小指示。
- **软件登记，AI 不登记（M15）：** Runtime/组件中的文字和图片由软件在运行时自动识别，改动按“原文 + 所在区域”匹配、图片/音频按受管资源替换，并由软件只读扫描源码给出页面文字清单；删除 AI 能力说明中的文案表与编辑目标登记义务，不把编号写进 AI 源码。不做 Three.js 适配，Phaser 只做 Text 与静态 Image。
- **Flow（M16）：** 自由图层默认随段落，可选固定纸面并置于正文下方作背景或水印；浮动对象沿用现有组件机制，不另做浮动 Runtime；“插入文字”改名“文本框”。
- **HTML 导入（M17）：** 入口为资源管理器右键 `.html` 与插入菜单“HTML 页面…”；导入时把内嵌 base64 资源抽成受管资源。
- **有限画布（M19）：** 尺寸整份课件一个，新建时选择，修改尺寸时已有内容按比例缩放；与 M21 同批完成。
- **验收收敛：** M18-T01～T03 并入 REL-T11 的同一次真实运行核对；REL-T11 挪到 M18 之后只跑一次；M14-T05 Owner 核对放在最后。M18 对课件 Skill 阶段产物与教师确认环节的影响在 B18 开工前另行确认。
- **整合顺序：** B13 文件与会话（M20）→ B14 轻编辑外壳与有限画布（M21、M19）→ B15 统一编辑目标与元素 AI 卡（M15）→ B16 Flow 混合画布（M16）→ B17 HTML 导入（M17）→ B18 创作链收口与终局核对（M18、REL-T11、M14-T05）。后续批次复用前面的快捷条与 AI 卡，不各做一套编辑界面，坐标代码只改一遍。

## 8. 实施顺序

批次由 [任务注册表](GPTpro方案/guoling_2_0_execution_plan/task_registry.json)生成并同步到 [实施顺序](GPTpro方案/guoling_2_0_execution_plan/delivery/SEQUENCE.md)。每个任务只有一个完成批次；同批按依赖集成，稳定接口后的独立叶子可并行。早期演示不等于整个任务已完成。

<!-- BEGIN GENERATED BATCHES -->
| 批次 | 主题 | 完成任务 | 完成点 |
|---|---|---|---|
| B00 | 开发基线与能力保全 | S01 | 固定源码起点、样本和职责地图，不做旧用户迁移；旧路线卡已处置，无与本批冲突的协调任务。 |
| B01 | 文档内核与双 Driver | S02、S03 | 纯内核到主进程/投影同批接通；Markdown 与薄 V9 并存、非当前页修改、撤销和恢复。 |
| B02 | 同源工具与文件服务 | S04、S09 | 直接操作和真实保存重开成立；工具不依赖焦点或候选。 |
| B03 | 统一执行器、正文流与事件 | S05、S06、S07 | 真实 API 正文修改、流式投影、取消与幂等；内部实现按同批依赖顺序，S07 在 S05 后集成。 |
| B04 | 附件、独立会话与外部 MCP | S08、S10、S12 | 附件真实输入、模型角色配置、至少两种客户端调用同一 Server；S10 在 S08 后集成。 |
| B05 | 受控构建与图片生产 | S13、S14 | 同一循环完成构建修复/导入；图片生成编辑与资源闭环，低成本模型组合实测。 |
| B06 | 基础设施集成门 | S11 | 全部 S 任务真实证据齐备，API、MCP、图像和构建集成验证；达到 G-INFRA。 |
| B07 | 布局、统一输入与标签 | M01、M07、M02 | 共享布局与 Composer、API 模型选择、真实多文档生命周期；按 M01 先行集成。 |
| B08 | 内容区、附件、过程与 Explorer | M03、M08、M09、M10 | 同一内核驱动轻量/深度编辑，附件及图片成果、外部调用可见，文件操作真实生效。 |
| B09 | 四表面共编与 Markdown 保真 | M04、M05 | 选区/焦点不串写，普通 Markdown 结构保真；M05 在 M04 后集成。 |
| B10 | 原位生成与保存恢复 | M06、M11 | 正文、人工穿插、一次撤销、停止、保存冲突完整闭合；M11 在 M06 后集成。 |
| B11 | 首次使用与课件能力保全 | M12、M13 | API/套餐首次使用、可选 OAuth、MCP 配置可达；三表面、互动、媒体与导出保全。 |
| B12 | 完整产品开发收口 | M14 | 独立使用、低成本完整任务、外部接手与性能验收；G-PRODUCT 和当前开发 G-2.0，不触发安装包/便携版发行准备。 |
| B13 | 文件与会话细节 | M20 | 新建菜单区分 Markdown/课件/文本文档并补默认后缀；.txt 在工作台内编辑保存；轻量顶栏另存为与“编辑器”按钮；会话头显示所属位置。与后续轻编辑改造互不牵扯。 |
| B14 | 轻编辑外壳与有限画布 | M21、M19 | 顶栏单行固定，选择不再引起画布跳变；统一选中快捷条与常用色板；只保留右上角“编辑器”入口；Slide 画布尺寸按课件参数化，命中、选框、拖拽与快捷条定位共用同一套换算。 |
| B15 | 统一编辑目标与元素 AI 卡 | M15 | 原生对象、文档对象、选中文字与 Runtime/组件图文共用一套编辑目标；元素 AI 卡就地修改并可单元素撤销；Runtime 图文由软件自动识别，AI 不登记。 |
| B16 | Flow 混合画布恢复 | M16 | Flow 保留 Markdown/文档正文；自由图层默认随段落、可选固定纸面（含背景/水印）；“文本框”插入原生文字；正文可承载页面 Runtime；复用 B14/B15 的快捷条与 AI 卡。 |
| B17 | HTML 高保真导入 | M17 | 已有 HTML 不经 LLM 重写，机械导入为完整 Runtime（内嵌资源抽为受管资源），关键布局/交互/数据不因原生化而丢失，并复用 B15 的图文轻编辑。 |
| B18 | 创作链收口与终局核对 | M18 | 教学设计与理想呈现不读取编辑器能力；理想 HTML 后做一次薄 Representation Planning 并机械执行；在同一次 REL-T11 真实端到端运行中核对，最后由 Owner 做 M14-T05。 |
<!-- END GENERATED BATCHES -->

S02/S03 同时用 Markdown 与薄 V9 Driver 验证：两份课件与未保存 Markdown 并存、修改非当前页对象、撤销、保存重开。纯内核、主进程和视图接线是任务内部步骤，不设置重复完成归属。

真实窗口演示贯穿 S04–S07；MCP 在 Gateway 和基础事件/文件服务齐备后尽早接入。构建与图像在统一执行器上完成，不先嵌入三家 CLI。

G-INFRA 要求全部 S 任务的合同、真实 API 编辑/构建/图像及 MCP 边界成立；G-PRODUCT 要求 M01–M21 当前开发范围的用户流程闭合；G-2.0 开发候选同时满足两门和核心行为发布用例。安装包/便携版、干净 Windows 首次使用、重装卸载及分发资产/许可按 2026-09-24 Owner 决定延期至后续发行准备，不成为当前 DAG 前置；正式发行仍须另验这些 `not_run` 项并由 Owner 签收。支持矩阵按具体 API/套餐/OAuth/客户端验收，未测不能标稳定。

## 9. 探针与判断标准

| 探针 | 时点 | 问题与判定 |
|---|---|---|
| P1 API 与流解析 | S05/S06 接线前 | TeamoRouter DeepSeek 为主，探针时钉定模型 ID、首选 V4.1 Flash，官方 API 为对照/备用；工具往返、中文/转义/分片、额度错误；确定性乱序/截断测试零错写，主 API 真实正文流成立；能力不足明确最终提交模式 |
| P2 循环与恢复 | S05 完成前 | 取消、回执丢失、压缩后接续；未知请求不盲目重发收费请求/操作；按 Provider/Engine/Gateway/内核归因 |
| P3 MCP 互通 | S12 完成前 | 两种客户端同一 Server，单 writer、读改/回执/幂等/撤权；配置差异不复制业务适配 |
| P4 构建与图像 | S13/S14 完成前 | scratch 可写而正式文件不可旁路写；编译失败修复、真实图片生成/编辑、导入撤销重开与离线资源 |
| P5 OAuth | S05 连接验收；B05 图像实测及首轮上线前 | GPT OAuth 使用 Owner 当前账号正式登录；核对认证、实际执行者、模型、图片生成/编辑与费用；仅实测项标稳定，不支持/未知清楚显示，API 文本/编辑主链无依赖 |

每个探针先做一轮聚焦验证再归因，只有相关变化或可证伪的新假设才重试；不无限探针或付费同因重跑。固定实际版本/模型/路由，不把旧安装版本写成永久目标。见 [测试与证据](GPTpro方案/guoling_2_0_execution_plan/delivery/TESTING.md)。

参考 Windows 机器上，确定性增量到可见和普通局部工具处理/广播 p95 目标均 ≤200ms，不含模型/网络时间。取消屏障后新增写入为零，同操作重放仅一次正式修改。真实任务同时记录首次成功、修复后成功、人工介入、得到正确成果的总时间和费用，不承诺未经测量的倍数提速。

TeamoRouter（`teamorouter`）DeepSeek 与官方 DeepSeek API 已获开发真实模型测试授权，覆盖 S05/S06/S11/S13/S14 及 REL 中适用的文本/视觉、规划与工具调用；GPT OAuth 当前账号登录及前期图像验证按 Owner 2026-09-23 决定执行。外部 MCP 所需 CLI 继续使用既有 Luna/DeepSeek 授权并记录实际配置，不推断旧路由。产品支持名单不自动授权任意付费模型或图片 API；两把 API Key 也不证明已有 Token Plan，S05-T07 必须核实真实套餐与按量连接，条件不齐记缺口而不替代通过。本轮不运行这些探针或登录。

## 10. 验收、状态与证据

<!-- BEGIN GENERATED COUNTS -->
当前开发范围有 **35 个实施任务、185 个验收用例**；任务状态：`planned` 7、`verified` 28；当前验收状态：`not_run` 31、`passed` 154；Owner 待验收：M14-T05。后续发行准备另有 **4 个用例**（`not_run` 4），未列入当前开发完成门。统计来自同源 JSON；`verified` / `passed` 是工程状态，不等于 Owner `accepted`，延期用例也不算通过。
<!-- END GENERATED COUNTS -->

删除 S01-T03、S01-T04、S10-T02、REL-T02 旧迁移/切换用例；M12-T04 保留 2.0 自身重装/卸载与用户文件保留合同，但延期到后续发行准备。新增/改写执行器、API/套餐、OAuth、MCP、构建、图片与低成本组合用例，数量服从覆盖，不维持每任务固定五项。

验收定义以 JSON 为唯一维护源，任务页验收段、总表、追踪表与离线阅读器由工具同步。状态按实际证据登记；延期发行用例保持 `not_run`，文档校验和合同样例类型检查不代替产品测试。

状态设计覆盖正常、空态、加载、失败、停止、冲突、权限、额度/限流、附件超限、外部修改、恢复、部分完成、外部接手和不支持实时流。先定义行为，在对应界面任务前补视觉；概念图不代替异常状态验收。

缺陷按用户实际影响定级：数据丢失、串写、停止后新写入、虚报保存和核心流程无法完成阻断发布；假设性安全纵深、文档卫生和流程问题单独标注，不冒充当前可用性 P0/P1。真实安全/数据风险单列。独立用户与实际运行证据仍必需。

## 11. 被取代的讨论结论

- 自建只负责直接编辑、CLI 长期专管构建 → 自建循环默认覆盖产品任务；后续可通过薄 runner 委派有限外部任务，不恢复 CLI 各自拥有业务逻辑的长期分工。
- GPT/OAuth 作为主入口 → API/Token Plan 为主，OAuth 可选。
- 低成本模型只作备选 → 低成本生产为正式验收场景。
- 三家 CLI 深度适配必须保全 → 一套 MCP 与薄配置兼容，保全实际内容能力。
- 工具分片直接执行，或必须另开模型轮才显示 → 增量解码受控投影，完整校验后提交。
- 最小纵切等于缩减范围 → 小范围早期验证贯穿完整目标实施。

## 12. 文档权威与历史关系

本文与 GPTpro 执行包是当前目标方案。AGENTS.md 同步入口与已确定边界；旧开发总纲、架构合同和路线入口标注取代范围，保留源码历史事实。WORKING_PROTOCOL 继续负责执行、写锁、验证，不把方案修订当作实现授权。

1.x 方案、双形态 UI、旧优化报告和 r20 评审作为历史依据保留，不物理移动/删除。本轮不修改产品 Schema，不宣称旧源码已经退出。设计图保留为视觉参考，默认导航、直接编辑和连接方式以本稿文字为准。

## 13. 剩余工程选择

SDK/轻量核心、Provider 适配、MCP 传输/注册、构建隔离、能力探测与实测模型由工程验证决定，无需重新让 Owner 决定已收敛方向。需要新的费用授权或实质产品边界变化时才按事实提出。

后续扩展按 [L02](GPTpro方案/guoling_2_0_execution_plan/long_term/L02.md) 与 [L03](GPTpro方案/guoling_2_0_execution_plan/long_term/L03.md) 推进：现在明确非文档工具、分类型成果提交和外部委派边界；格式驱动、作业与授权保留可扩展余地；完整 Office、独立 HTML 调试、小型代码工作区和薄 runner 待真实功能接入再排期。既有 S13 的受控构建/导入与 S14 图片生产仍是当前交付，不能借“后续扩展”延期。当前会话交接见 [交接说明](GPTpro方案/guoling_2_0_execution_plan/delivery/AGENT_HANDOFF.md)。

- 已决定：首轮上线 GPT OAuth，使用当前账号正式登录，前期通过该连接生图；P5 不再等待 Owner 另选账号，实际连接与能力仍待验证。
- 待定：独立图片 API 的供应商与账号，在引入该连接前确定；不要求 B05 前提供第二套图片账号。
- 已决定：旧 r20 卡按 Owner 2026-09-23 授权吸收有效经验后退役；未完成旧验收保留历史状态，2.0 仍适用的能力保全要求纳入既有任务，见[退役记录](GPTpro方案/guoling_2_0_execution_plan/evidence/R20_RETIREMENT.md)。
- 工程待核实：现有 API 账号实际套餐/按量类型；若 S05-T07 缺少真实套餐连接，记录缺口，仅需新增采购或授权时再交 Owner 决定。

## 14. 变更记录

- 2026-09-22 v1.0：初始收敛稿，按模式区分自建与 CLI。
- 2026-09-22 v2.0：统一自建执行器；API/Token Plan 为主、OAuth 可选；外部 MCP 开放；同步依赖、验收、接口样例、阅读器及开发入口。本轮仅方案工作。
- 2026-09-23 第二轮：明确 DeepSeek 开发主备路由；首轮 GPT OAuth 使用当前账号、前期 OAuth 生图，独立图片 API 待定；修正入口与措辞，补 B01 检查点与可复现校验器自检，吸收旧 r20 经验后退役协调卡。任务/用例数量、依赖与产品未执行状态保持不变。
- 2026-09-23 后续扩展边界补充：保留自建执行器主架构，明确文档与非文档能力、分类型成果提交、完成证据及薄 runner 的委派/交接边界；同步相关任务叙述、长期路线与交接词。不修改产品源码、当前任务/验收状态、数量或依赖，不要求暂停开发或追溯重构。
- 2026-09-23 工作台交互收敛：确认左资源/会话、中内容、右 AI；课件底部场景及其状态固定导航；四类基础元素插入、选择后属性编辑，并保留原处快捷工具条 AI 修改。专业编辑器保留主体布局，右侧工具按需展开，独立窗口待定。同步相关任务叙述、布局参考、交接和阅读制品；补充检查点待验证，不改产品代码、正在维护的 JSON 状态或依赖。
- 2026-09-24 验收收敛（Owner 决定）：删除评测性质的 S11-T03、REL-T09 与中途交接 S12-T04、REL-T12；REL-T01/T03/T04/T07/T10 分别并入 M12-T01、M12-T05、S11-T05、M14-T01、M14-T05；简化 S07-T03、S05-T08、REL-T05/T06；M09-T04 保留内置 AI 提问并改为选项卡，删除子任务要求。其余已通过用例的定义与状态不变。
- 2026-09-25 v2.2：S01–M14 当前状态提交为 2.0 基线 `25b12b4b`；记录 Owner 使用反馈与讨论结论（§7B），新增 M20（文件与会话细节）、M21（轻编辑外壳），M15 扩为统一编辑目标与元素 AI 卡，M19 提前与 M21 同批，批次重排为 B13–B18；新增 11 条、改写 13 条验收用例，REL-T11 与 M18 合并核对并置后。
