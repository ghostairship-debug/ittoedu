# ITTOEDU 1.1→2.0 开发方案重构执行计划

> 状态：已完成并于 2026-09-02 追加“内部生产 + 1.1 主动模块化 + 持续轻治理”修订（本文仍不是执行状态源）
> 日期：2026-09-02
> 方案来源：原根目录 ZIP（已形成 98 行完整归档映射后退出；映射不再是当前路线硬门）
> 核验依据：根目录 开发方案综合核验评估报告-2026-09-02.md
> 当前仓库基线：执行时以届时 HEAD 与工作树为准，不沿用原 ZIP 固定的旧 SHA
> 正式权威：`COURSEWARE_DEVELOPMENT_PLAN.md`、`docs/development-plan/roadmap/`、`ARCHITECTURE_CONTRACT.md` 与 `WORKING_PROTOCOL.md`；本文只记录本次重构决定和落库结果，不表示任何产品开发节点已实例化、active 或完成

## 一、目标与已锁定决策

本轮已完成开发方案、合同和执行规格重构，没有修改产品功能。仓库内正式文档现在是唯一执行权威；原 ZIP 在完成交叉映射和校验后删除。最新修订明确：产品是受控团队使用的内部生产工具；1.1 不只清 V8，还必须主动拆解现有跨 Owner 巨石；后续每个版本把新能力晋升到不可降级矩阵并复用一个依赖棘轮，不建设常设架构治理平台。

已锁定的 Owner 决策如下：

1. 任何改造不得削弱当前受支持产品面，包括 UI、V9 工程、三种 Surface、保存恢复、Undo/Redo、Preview/Player、Runtime/Component、Builder、诊断及全部现有导出。
2. 1.1 完成可执行代码、测试、脚本、示例、fixture 和生成制品中的旧 V8 模型、Schema、Player/Export payload 与测试工具链清零；历史文档和 Git 历史允许提及旧名称。
3. 1.1 发布源码标签和固定课例离线 HTML，不做安装包。
4. 1.2–1.9 每个小版本发布内部源码标签，不生成发布 HTML；AI 能力在普通内部生产构建中于 1.6–1.9 默认隐藏，但受控 dogfood channel 可按纵切显式启用。
5. 2.0 在内部生产构建中正式开放 AI；默认交付内部源码标签和固定课例离线 HTML，仍不承诺安装包，不表示对外发行。
6. Table、Chart 都成为 V9 Native 新 discriminator；Published Course 继续使用 V2 窄扩展。
7. 生成 Component/Runtime 是核心能力；自动门禁通过后，无人工审核即可获得当前可信扩展已具备的全部宿主能力。Provider Secret 和未公开的 Electron Main 能力仍不因此暴露。
8. 首发支持用户自行安装、自行认证的 Codex、Claude、OpenCode。
9. CLI 本身作为 Agent 内核；编辑器逐步提供 session harness、暂存区、自动准入、宿主 canonical command 提交边界和 1.8 起的 MCP 课件工具，不重复实现模型规划循环。
10. AI 会话和工具轨迹保存在应用本地，按“工程 ID + 文件位置”隔离；Save As 不复制，会话可删除，不进入 .h5lesson 或导出物。
11. 产品不按材料类型阻止发送；首次使用显示外部 CLI 数据风险说明。删除应用记录不虚假承诺同时删除 CLI 自身历史。
12. 每个版本由 Owner 使用固定课例签署 accepted；签署用于内部候选治理，不表示公开发行。
13. 默认信任模型是内部受信团队/代码；不为公开插件市场、多租户、任意恶意第三方代码或零信任终端预建平台。Provider Secret、原始 Main、任意 OS 命令、未开放脚本与权威工程错写仍是硬边界。
14. `editorStore.ts`、App/Workspace/Properties/Flow UI、Slide Published adapter 与 Course package builder 是 1.1 已指定的主动模块化热点；组合根最终只接线，任何中间提交都不得双写或降级。

## 二、正式文档与单一真相

### 2.1 正式权威

本次已完成以下文档重构：

1. 已更新根开发总纲、AGENTS、架构合同和 V9 兼容政策，正式记录上述 Owner 决定并消除冲突文本。
2. 已建立 `docs/development-plan/roadmap/`，包含：
   - README.md：版本目标、真实依赖 DAG、发布制品和启动规则；
   - PRESERVATION_MATRIX.md：不可降级的产品行为基线；
   - OLD_PLAN_CROSSWALK.md：原 ZIP 98 项任务逐项归类为替代、合并、废止或可选；
   - manifest.json：只保存 task ID、版本、依赖、可选性、写锁和规格位置，不保存执行状态；
   - 1.1/：完整弱模型执行规格；
   - 1.2/ 至 2.0/：每版任务图、接口、退出条件和发布门。
3. 路线规格不进入 `docs/development-plan/tasks/`，因此不会污染当前任务板。只有依赖已满足、届时事实仍成立、写锁可取得且需要协调时，才按现行模板实例化任务卡。
4. 当前工作区已有的协议简化改动应保留并合并，不得回退或覆盖 repo-index、任务板生成器等无关修改。

### 2.2 路线一致性检查

增加一个轻量路线检查命令及目标测试，至少验证：

- ID、依赖和文档链接有效，图无环；
- 核心路径不得依赖 OpenMAIC 等可选项；
- 1.1 每个节点都有独立执行规格；
- 验证命令及测试文件真实存在；
- 不允许通配符、“最小等价检查”或不存在的 verify:installer；
- 候选身份由当前任务固定；归档文档允许保留历史 SHA，路线校验器不再用历史文本禁令代替候选验证；
- 任务声明的 candidate identity、输入闭包或证据失效条件与实际不匹配时不得返回成功；路线本身不得固定旧 SHA，也不要求首次 reconciliation 前的保守 inventory 冒充当前 HEAD 精确真相；
- 1.1 README、manifest 与独立 spec 的 ID、title、完整依赖、写锁和链接双向一致；inventory writer 必须持有专用锁且在 DAG 中全序；
- DAG 必须无环、可选节点不进入核心闭包；是否存在并行 frontier 由真实依赖和写锁决定，不作为产品正确性的硬门；
- 路线 manifest 不承担 queued、active、blocked 状态，当前协调状态仍只看任务板。

### 2.3 原 ZIP 的退出

正式路线已建立 98 行旧任务归档交叉映射。它用于追溯而非当前路线硬门；原方案 ZIP 已按精确路径删除，不保留第二份可被误认为权威的方案。

根目录最终评估报告继续保留，作为方案重构的证据来源。

## 三、不可降级门

### 3.1 受支持产品面

PRESERVATION_MATRIX.md 应固定以下不可降级行为：

- 新建、打开、保存、另存、恢复、脏状态、Undo/Redo；
- Slide、Flow、Spatial、Mixed 创建、编辑和导航；
- Text、Formula、Image、Video、Shape、教师控制器；
- 全局层、Surface 层、命名状态、交互、课程状态和导航守卫；
- Component API 4、Runtime API 2/3 及当前已经证明的宿主范围；
- 简洁/专业模式和开发工作台；
- 当前页试运行、整课预览；
- 离线便携单 HTML、在线轻量单 HTML、Web Package、PPTX、PDF、Flow DOCX；
- V9 archive、无界面验证、外部 Builder 和能力索引；
- 素材、组件导入/替换及资源历史。

### 3.2 迁移硬规则

- 必须先交付并验证等价 V9/Published consumer，再删除旧 consumer。
- 不得靠删除入口、测试、导出格式、动态能力或静态化功能实现旧代码清零。
- 每个 1.1 任务必须声明所影响的矩阵行，并运行对应最近层行为检查。
- 任一中间提交都必须保持产品可运行；不允许“等最终任务再恢复功能”。
- 当前正式能力缺失、状态降级或导出语义变弱时，任务立即停止并回滚。
- 删除测试前必须确认它只验证不受支持的旧格式，或已有 V9/V2 测试覆盖对应受支持行为。
- 不允许为了通过检查修改断言、隐藏错误、改成 no-op 或把真实内容替换成静态占位。

### 3.3 旧代码清零的精确定义

最终机器检查覆盖：

- src/**
- tests/**
- scripts/**
- examples/**
- artifacts/**
- fixture 和正式生成制品

最终必须满足：

- 不再导入或导出旧 projectTypes、projectSchema、旧 Player/Export payload 和旧 archive 模块；
- 可执行代码中不再使用独立 token ProjectDocument、SceneDocument、SceneNode、ExportPayload；
- 不再存在 schemaVersion 8 作者工程、archive、fixture、测试工厂或生成样本；
- Legacy 台账中的当前 confirmed consumer 为零；
- 旧模块文件在全部 consumer 为零后删除。

清零检查排除 Markdown 历史材料、最终评估报告、Git 历史、node_modules 和构建缓存。用户指南可以继续说明旧 V8 工程不受支持。

## 四、1.1 弱模型执行协议

每份 1.1 任务规格必须包含：

1. 一个可观察结果和当前证据；
2. 精确到文件和符号的 Read first；
3. 精确允许写入范围、禁止范围和写锁；
4. 关联的不可降级矩阵行；
5. 不需要执行者做架构选择的逐步实现顺序；
6. 越界、基线漂移、功能不等价和 consumer 未清零时的停止条件；
7. 确定性验收结果；
8. 最多三条已经核实存在的精确检查；
9. 回滚点和简短交接格式。

执行者一次只处理一个规格。不得自动执行下一任务，不得自行扩展 Schema、建立兼容双轨、创建第二 Store/Session/History，或用 as any、passthrough、silent fallback 掩盖问题。

## 五、1.1 真实任务 DAG

### 5.1 路线与基线

| Task | 结果 | 依赖 |
|---|---|---|
| r11-000-authority-contracts | 正式写入路线、清零、不降级、AI、Native、Published 与信任决定 | 无 |
| r11-001-preservation-baseline | 固化 28 行行为期望、fixture identity 与证据失效闭包，并建立候选复算门 | 000 |
| r11-002-legacy-inventory-zero-check | 刷新唯一 Legacy 台账，建立 ratchet/ready/zero 三态扫描与只减不增协议 | 001 |

### 5.2 合同去旧依赖

| Task | 结果 | 依赖 |
|---|---|---|
| r11-010-domain-contract-extraction | 把 Native、Design、Media 等仍有效原语移出旧类型大文件，wire 语义不变 | 002 |
| r11-011-v9-native-schema-independence | V9 Native strict Schema、materializer 和 presentation override 不再依赖旧 Scene Schema；生成 JSON Schema 能表达真实 Native data | 010 |
| r11-012-published-v2-schema-independence | Published V2 类型与校验不再经过旧工程模型 | 011 |
| r11-013-shared-native-consumers | 公式、文本、图形、素材引用等共享 consumer 改用正式合同 | 012 |
| r11-014-media-design-component-consumers | 媒体、设计 Token、组件元数据 consumer 改用正式合同 | 012 |

### 5.3 Editor lane

| Task | 结果 | 依赖 |
|---|---|---|
| r11-020-slide-effective-read-model | Slide 命中、选区、缩略图使用只读 V9 effective view | 013 |
| r11-021-slide-properties-editors | 属性、动画、互动、开发面板不再读取旧 Scene 投影 | 020 |
| r11-022-slide-actions | 复制、粘贴、重复、删除、重排完全走 V9 command/history | 020 |
| r11-023-flow-viewstate | Flow 编辑视图只读 V9 session，保留语义正文与导航边界 | 013 |
| r11-024-spatial-viewstate | Spatial 编辑视图只读 V9 session，保留 camera/path/relation 语义 | 013 |
| r11-026-slide-properties-modules | 把 Slide/Global/Runtime 属性从 Properties root 迁入真实模块 | 021 |
| r11-027-flow-authoring-modules | Flow Workspace、文本、Overlay、属性改为无 Store 的受控模块 | 023 |
| r11-028-spatial-authoring-modules | Spatial Workspace 与属性形成独立 Surface 模块 | 024 |
| r11-029-slide-workspace-modules | Workspace 收敛为路由，Slide/动态 overlay/画布 controller 解耦 | 022、027、028、033 |
| r11-025-editor-store-v9-only | 在 UI consumer 迁出后清除 Surface V8 镜像与旧 writer | 026、029 |

### 5.3.1 App 与 Store 最终收口

| Task | 结果 | 依赖 |
|---|---|---|
| r11-034-app-project-lifecycle-module | App 打开、保存、Save As、dirty 与 Recovery 形成单一生命周期模块 | 025、051 |
| r11-035-app-delivery-module | Preview、preflight 与全部导出发起形成 Delivery 模块 | 025、032 |
| r11-036-app-import-input-modules | 素材/组件导入和全局键盘路由从 App 解耦 | 014、025 |
| r11-037-editor-store-owner-modularization | Core/App/Slide/Flow/Spatial/Feature 按 Owner 拆分，root 只组合接线 | 025、032、034、035、036 |

### 5.4 Player lane

| Task | 结果 | 依赖 |
|---|---|---|
| r11-030-native-render-boundary | 建立只读 Native render input，并把 Slide Published Native painter 从 lifecycle adapter 中拆出 | 013、014 |
| r11-031-published-slide-player | Slide Published adapter、Player Scene 和 authoring patch 直接消费 Published/V9 模型 | 012、030 |
| r11-033-runtime-authoring-preview-v2 | 当前页试运行/作者预览切换到 Published V2，并统一文本、字体、素材和生命周期语义 | 014、031、050 |
| r11-032-player-v2-only-entry | 作者预览和三类导出 consumer 全部迁移后，Player 入口只接受 Published V2 | 033、041、042、043 |

### 5.5 Export 与 Diagnostics lane

| Task | 结果 | 依赖 |
|---|---|---|
| r11-040-v9-health-preflight | GUI、CLI 和导出预检统一使用 V9 diagnostics | 013、014 |
| r11-041-pptx-v2-only | PPTX 只走当前 Published V2 producer/capture，保持现有可编辑对象和警告语义 | 031、040、050 |
| r11-042-pdf-v2-only | PDF 只走 Published V2 print/capture，不恢复旧 raster fallback | 031、040、050 |
| r11-043-html-web-v2-only | 两种单 HTML/Web Package 只携带 V2，并拆分脚本分析、预检与 emitter | 031、040、050 |

### 5.6 Archive、测试与最终删除

| Task | 结果 | 依赖 |
|---|---|---|
| r11-050-v9-fixture-foundation | 为所有仍受支持行为建立 V9/Published 测试工厂 | 011、012 |
| r11-051-v9-archive-only | 删除 V8 archive、迁移器、保存/打开适配器和全部旧样本 | 050 |
| r11-052-supported-test-migration | 旧 Player/Export/Runtime 测试改为验证受支持 V9/V2 行为；纯旧格式测试删除 | 037 |
| r11-055-architecture-modularity-gate | 用 owner/import ratchet 证明组合根只接线、无第二 writer/Facade/依赖环 | 037、052 |
| r11-053-legacy-inventory-reconciliation | 在固定集成 product candidate 上原子复核唯一 Legacy 台账 | 055 |
| r11-054-delete-legacy-modules | 只按 053 的精确零 consumer 清单删除旧类型、Schema、Player、Export 和辅助模块 | 053 |
| r11-060-zero-gate | 代码、测试、脚本、示例、fixture、生成制品达到零旧符号、模块和格式样本 | 054 |
| r11-061-no-regression-candidate | 固定 product candidate identity 执行 legacy、preservation、源码与固定 HTML 门，并复用未失效证据 | 060 |
| r11-062-owner-release | Owner 完成固定产品矩阵检查，发布 v1.1.0 源码和离线 HTML | 061 |

Editor UI、Archive/Test 可和 Published lane 在写锁无冲突时推进；同一 `workspace-properties` 或 `published-producer` 锁仍由单 writer 顺序领取。UI owner 纵切完成后才清 Surface mirror，App 三支完成后才拆 Store composition root；055 证明真拆分，053 再单写者 reconciliation，054 才删除。启动时按届时 HEAD 核实，归档 ZIP/SHA 不恢复任务。

## 六、1.2→2.0 总体 DAG

    1.1 V9-only 稳定底座
    ├─ 1.2 Table/Chart/Line/Background
    ├─ 1.3 Recipe/设计资产
    └─ 1.6 本地 CLI Harness
    1.2 + 1.3 → 1.4 Authoring Tools/Builder v2
    1.4 → 1.5 材料/PPTX/内容 QA
    1.3 + 1.4 + 1.5 + 1.6 → 1.7 生成内核
    1.7 → 1.8 CLI Agent + MCP/Skills
    1.8 → 1.9 Chat/Dogfood
    1.9 → 2.0 内部生产 AI

版本发布门按 1.1→2.0 顺序串行，但不人为阻止无写入冲突的前置实现。

## 七、各版本结果与退出条件

### 7.1 版本 1.2：Native 编辑闭环

- Table、Chart 新增严格 V9 Native 分支和 Published V2 对等分支。
- Table 覆盖稳定行列单元格、行列增删重排、宽高、样式、键盘和原生 PPTX。
- Chart 首发 bar、line、area、pie、donut。
- Line 直接绘制、端点、折点、吸附和细线命中独立交付。
- Background 覆盖 Scene、命名状态、Slide Surface 及现有 Flow/Spatial 能力。
- 每项能力完成 Contract、Factory、UI、Command、History、Save/Reopen、Player、HTML、适用导出、Diagnostics、Capability、Accessibility 闭环。
- 发布源码标签，不发布 HTML。

### 7.2 版本 1.3：Recipe 与设计生产力

- 页面 Recipe：cover-v1、concept-v1、worked-example-v1。
- 互动 Recipe：step-reveal-v1、choice-feedback-v1、classify-sort-v1。
- Recipe 立即展开为普通 V9 内容，不形成第二 DSL。
- 完成参考页骨架复用、批量查找替换、Design Token 应用和快速诊断。
- 容量超限使用换档、拆页或 Flow，不默认无限缩字。
- 发布源码标签。

### 7.3 版本 1.4：Authoring Tools 与 Builder v2

- update target 对现有 CourseAuthoringTarget 逐字段无损序列化。
- create target 使用独立 create-scope，不伪造 itemId 或 authoringAddress。
- 补齐 Slide、Flow 正文、Spatial camera/path/relation、global/background/network 正式工具。
- 工具统一 receipt、revision、stale、history 和 resource transaction。
- 先闭合三项动态 carrier 硬门：
  1. Component Registry 区分工程、package、version、source/content identity；
  2. Runtime/Component direct project asset 引用进入 Published asset closure；
  3. 生命周期失败销毁或隔离旧实例并显示静态后备或可见错误。
- 三项闭合后才能开放代码工具和 Builder v2 的 Component/Runtime escape hatch。
- Builder v1 保留兼容窗口，v2 不重新 spread 内部模块。
- 发布源码标签。

### 7.4 版本 1.5：材料、PPTX 和内容质量

- 材料缓存保存在应用本地，可读、检索、引用和删除，不进入 CourseProject 主合同。
- 可见引用以普通课程内容写入，不能只存在于不可携带 trace。
- PPTX 使用受限解析、文件大小和解压比限制、document+sidecar 原子事务及不支持项报告。
- Style remix 复制可编辑骨架后替换槽位。
- 数学、答案一致性、图表正文一致性和来源定位进入内容 QA。
- OpenMAIC 是独立可选轨，不进入发布关键路径。
- 发布源码标签。

### 7.5 版本 1.6：本地 CLI Harness

- 实现 LocalAgentCliAdapterV1。
- 首发 Codex、Claude、OpenCode。
- 支持自动探测、自定义路径、版本、启动、流式事件、恢复、取消和错误分类。
- CLI 自行登录和保存凭据，应用不读取或保存 API Key。
- Windows 启动使用解析后的明确可执行文件和参数数组，不拼接 shell 命令字符串。
- 会话映射和聊天记录保存在应用 userData 的版本化目录。
- AI 默认隐藏；三套 CLI 均不可用时，人工编辑全部功能仍然正常。
- 发布源码标签。

### 7.6 版本 1.7：生成内核

- 1.7 是 batch candidate pipeline，不提前开放 MCP 或 live authoring channel。
- CLI 只接收应用生成的不可变最小 context snapshot 或已确认 Markdown；不得获得 `.h5lesson` 路径、Store、仓库源码或 live project API。
- Candidate 只允许 strict/versioned typed authoring intent envelope 或 dynamic package manifest；禁止 generic V9 patch/import。
- 宿主重新校验 canonical target、revision 与 admission，把 intent 映射到 1.4 commands，并以单一 document + resource transaction 提交；CLI candidate receipt 与 host commit receipt 分离。
- 独立节点 `r17-013-host-candidate-commit` 拥有 strict parser、1.4 intent mapper、revision recheck、单事务 commit/receipt，避免首个单页用例临时发明公共提交边界。
- 通过 CLI Agent 候选完成单页、整课和现有课件局部修改。
- 使用 Native → Recipe → Existing Component → Generated Component → Runtime 的载体阶梯。
- Component/Runtime 源码先进入应用暂存区。
- 自动准入至少覆盖编译、协议、依赖、素材闭包、精确 origin、生命周期、资源上限、静态后备和真实宿主 smoke。
- 通过后自动成为可信扩展并获得当前完整正式宿主能力；不需要人工代码审核。
- 三种真实 CLI 都必须证明 staging 绝对路径、`..`、symlink、跨 session、`.h5lesson` 与仓库源码越界失败；无法约束的文件工具保持禁用并改用 structured stdout。
- stale、cancel、坏 envelope 或检查失败时工程零写入，稳定版不提供跳过门禁的人工覆盖。
- 风险页最多一次局部自动修复，不无限 self-review。
- 发布源码标签，AI 仍默认隐藏。

### 7.7 版本 1.8：CLI Agent、MCP 与 Skills

- 1.8 首次向 CLI 提供版本化 live MCP Authoring Tools、上下文和回执；1.7 的 host candidate mapper 继续作为内部提交内核，不冒充 MCP。
- Codex、Claude、OpenCode 保留各自已有的规划、Skills、子任务和工具循环。
- 编辑器不另建重复的模型规划或 Agent Runner。
- 一个中立 Agent Profile 生成三种 CLI 的会话配置。
- 首发 course-design、course-build、pro-editing、visual-craft、interaction-craft、qa-repair、style-remix。
- CLI 文件工具只写应用暂存区；对 authoritative project 的修改只能经过产品 MCP 工具。
- 教师修改导致 revision 变化时，旧工具调用返回 stale 并零写入。
- 发布源码标签，AI 仍默认隐藏。

### 7.8 版本 1.9：Chat 与内部 Dogfood

- Chat shell、运行状态、工具时间线、Stop、Undo、预览和错误/stale 提示。
- 上下文引用：当前选择、当前页、整课、材料。
- Markdown 和公式安全渲染；消息 HTML/脚本不执行。
- 支持应用重启后的本地会话恢复、按会话/工程/全部删除。
- 明确应用会话删除与 CLI 自身历史删除的差异。
- 使用真实课例进行内部 Dogfood。
- 发布源码标签，AI 仍默认隐藏。

### 7.9 版本 2.0：内部生产 AI

- 在内部生产构建中正式开放 CLI 设置、生成、编辑、Agent 和内置 Profile；不把它描述为对外分发。
- 未安装 CLI 时提供明确安装/登录指导，不削弱手工编辑器。
- AI 写入可停止、可撤销，迟到结果与 stale 结果零写入。
- 首次使用显示外部 CLI 可能向其 Provider 发送上下文的一般风险说明；产品不按数据类型阻止材料。
- 完成可访问性、失败恢复、隐私说明、文档和固定课例 Owner 验收。
- 默认发布源码和固定课例离线 HTML，不做安装包。

## 八、未来接口与数据合同

### 8.1 V9 与 Published

- 1.1 只搬迁类型和 Schema 所有权，不改变 V9 或 Published V2 wire。
- 1.2 为 NativeElementContent 增加 table、chart 两个严格分支。
- 旧 V9 工程继续可读。
- 旧编辑器或旧 Player 遇到新分支明确失败，不静默丢弃。
- Published V2 与匹配 Player 成对交付，不承诺旧 V2 reader 前向兼容。

### 8.2 Authoring Tool target

AuthoringToolTargetWireV1 必须完整包含：

- projectId
- documentRevision
- revisionPolicy
- sessionGeneration
- surfaceType
- surfaceId
- locationId
- stateId
- owner
- ownerKey
- itemId
- authoringAddress

创建类工具使用独立 create-scope target；更新类工具不得丢失 canonical target 的任何字段。

### 8.3 CLI Harness

LocalAgentCliAdapterV1 至少提供：

- probe
- start
- resume
- cancel

标准事件至少包含：

- session
- text
- tool-call
- tool-result
- usage
- completed
- failed

1.7 的 CLI 不直接读取或修改 authoritative project：它只消费不可变 snapshot，并经 structured stdout/artifact channel，或在启用文件工具时经当前 session staging 返回严格 candidate；宿主用 1.4 canonical commands 提交。宿主只摄取 realpath 闭合的当前 candidate，不把该边界宣传成 CLI 进程的 OS sandbox。Native/Recipe/Existing Component 不等待动态门，Generated Component/Runtime 才走静态与真实宿主准入。1.8 起，live authoritative read/write 只能经过产品 MCP 工具。

### 8.4 会话数据

- 配置和会话使用应用本地版本化文件。
- 会话 owner 使用工程 ID 与规范化文件位置共同识别。
- Save As 创建新的本地 workspace identity，不复制旧会话。
- 支持清除一个会话、当前工程或全部应用记录。
- 会话、材料和 tool trace 不进入 CourseProject、Published、Component、Runtime 或导出物。
- 应用只能承诺删除自己的记录；CLI 自身历史按适配器能力另行处理和说明。

### 8.5 自动可信代码

“完整宿主自动可信”表示自动准入后的扩展可以使用当前正式提供给可信 Runtime/Component 的父页面、本地、桌面和网络接口。

它不自动授权：

- Provider 凭据；
- 原始 Electron Main 对象；
- 任意 OS 命令；
- 未正式开放的远程脚本；
- 未经合同批准的新宿主接口。

## 九、验证与发布

### 9.1 方案重构自身

- 新路线文档链接有效；
- manifest 无环、核心依赖闭包不含可选任务；并行 frontier 只作报告，不作硬门；
- 旧任务映射作为归档校验有效，不要求它永久控制当前路线；
- 可选任务不阻塞核心路径；
- 所有命令和测试路径真实存在；
- 不存在 NEXT_TASK 自授权；
- git diff --check 通过；
- 原 ZIP 已在上述条件全部成立后按精确路径删除；评估期间的四处临时解压副本同时清理，`tmp/worktrees` 未触碰。

### 9.2 1.1 最终门

固定候选只执行一次：

1. check:legacy-zero；
2. check:preservation；
3. `verify`；它已经通过 `pretest:e2e` 包含固定 example/离线 HTML 检查，不在输入未变时重复执行 `check:examples`。不得调用要求 installer artifacts 的 `verify:release`。

Owner 固定检查至少覆盖：

- Slide、Flow、Spatial、Mixed；
- 保存重开、Undo/Redo；
- 当前页试运行、整课预览；
- 两种单 HTML、Web Package、PPTX、PDF、DOCX；
- Component、Runtime；
- 外部 Builder；
- 无 Session、无资源、生命周期异常等可见失败路径。

### 9.3 后续版本

- 每个新增能力同时验证人工 UI、Authoring Tool、保存重开、Player、适用导出、诊断和能力索引。
- 1.6 以后必须证明未安装、未认证或异常退出的 CLI 不影响手工编辑。
- 1.7 以后固定恶意、超限、编译失败、生命周期失败和素材缺失 candidate，证明自动准入失败时工程不变。
- 2.0 对三种 CLI 分别完成同一固定生成与局部修改课例，并验证 Stop、stale、Undo、重启恢复和会话删除；Owner 同时冻结并观察 `examples/render-host-benchmark/render-host-benchmark-v2.html`，发布节点只能发布签署后的同一 bytes。
- 自动化只授予 engineering/content candidate；每个版本最终由 Owner 对固定候选签署内部 accepted。

## 十、落库结果与后续启动

本次重构已完成：

1. Owner 决策已同步到总纲、AGENTS、架构合同和 V9 兼容政策。
2. 已建立 PM-01–PM-28 不可降级矩阵与原方案 98 行完整归档映射；后续新能力在各版本 release 节点晋升为新 PM 行。
3. 已建立正式路线目录、严格 manifest、双向路线检查器和目标测试。
4. 1.1 已形成 39 个弱模型可直接执行的独立规格；主动模块化按 Properties、Flow、Spatial、Slide Workspace、App lifecycle、Delivery、import/input、Store 收口和 architecture gate 分卡，Legacy inventory 仍由单一 writer 全序维护。
5. 1.2–2.0 已形成版本任务图、接口、退出门和发布制品定义；1.7 新增 `r17-013-host-candidate-commit`，明确 batch candidate 与 1.8 live MCP 的版本边界。
6. 正式 manifest 共 135 个节点、48 份规格文档，核心路径不依赖 optional OpenMAIC；并行只由真实依赖和写锁决定，不再是 checker 硬条件。
7. 路线校验、目标测试、类型检查和 diff 检查均通过；原 ZIP、三个工作区临时解压目录和一个系统临时解压目录已按核实的精确路径删除，`tmp/worktrees` 与其他用户文件未触碰。

后续产品开发从 `r11-000-authority-contracts` 开始，但该路线节点当前不是协调状态。只有依赖、届时 HEAD 事实、目标测试与写锁条件都满足时，才按 `WORKING_PROTOCOL.md` 实例化；一次只执行一个规格，完成后停止，不自动领取后继节点。
