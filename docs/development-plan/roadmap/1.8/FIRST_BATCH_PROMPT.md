# 1.8 首批审查修复启动说明

本说明供下一次明确启动产品实施时使用；2026-09-08本轮仅更新计划，没有执行修复。后续原生能力与095–104顺序见[开发计划](../../AI_ASSISTANT_DELIVERY_PLAN.md#3-正式依赖与执行顺序)。

目标仓库：当前IttoEdu工作区。先核对HEAD和相关未提交改动，保留Gemini已有实现与用户文件，不reset/clean/stash，不从旧HEAD覆盖新增V2 transport。本批只修[首批review](../../reviews/1.8-first-batch-review.md)的四项问题，边界与检查见[执行包](FIRST_BATCH_EXECUTION.md)及089/090–094原规格。

执行顺序：

1. 090/093修Claude身份：仅原生确认的externalSessionId可进入恢复记录；经应用首次对话→结束进程→继续，验证同一上下文。不要把本地随机占位值当成外部会话。
2. 090/092/094修退出与取消：error/close/主动close清空并拒绝所有pending RPC，等待有界、关闭幂等；初始化失败不一直running，Stop/删除结束并释放槽位，随后可启动新任务。
3. 091联调三个adapter的实际配置：Harness在新建/恢复的合法边界应用唯一偏好；model/effort进入真实请求并由原生确认，失败不假报current或静默换模型。不要只修改返回对象或UI选中值。
4. 089修Flow超宽控制器：复用正式recovery bounds，使重复render/resize稳定，默认视口实际点击缩放/收起可达；长正文必须真实发生滚动，不能以面板部分相交/toBeVisible证明按钮可操作。

由一个协调者写harness、repository/service、共享合同、工厂、Chat与公共测试并持有共同写锁。协调者只委派实际文件非重叠的CLI叶子，Flow在独立写域并行；使用隔离工作区，同一实体文件始终单writer，不另建与粗锁重叠占锁的并行卡。当前细粒度CLI叶子锁已存在，无需重新设计并行框架。一般实现、分文件和测试安排由执行者负责。

沿用V9、Published V2、唯一工程/资源事务、唯一会话Owner和完整原生CLI加GUI与编辑器连接的长期架构，不自建模型循环。当前有限generation-snapshot只支持preview手动应用；不提前宣称095完整观察、100反馈或101完整UI已完成，不新增live工程API、独立MCP服务、通用工具RPC平台或第二History。CLI原生已有工具连接、Skills与文件/终端能力依真实配置和授权保留；候选摄取闭合不等于给整个CLI套只读沙箱。

本批通过不表示092–094原生能力完全对等；090/091/092–094后续在同一Owner接齐原生工作上下文、工具、授权及GUI回传。旧read-only、工具白名单或拒绝授权配置下的probe只按当时有效覆盖范围复用，不作为目标配置或完整对等证据。首批不得顺带承担全部接线。

r18-105已撤回；1.9按042未命名/首存、045材料结构与分片、044自动/手动流程、041聊天工作台推进。软件体验与创作速度/质量同等重要，2.0完成内置Skills及全部课件操作，无需外部AI、终端或手工Builder补生成、检查、修复与交付。2.x沿原生CLI接实际媒体/运行时服务。这些后续目标不进入当前四项修复。

每个修复选1–3条直接验证，优先复用review反例与现有专属测试；准备规则统一按[开发计划§6.1](../../AI_ASSISTANT_DELIVERY_PLAN.md#61-准备与局部验证)，依代码变化和命名用例准备必要产物一次，再用执行包中的npx --no-install vitest/playwright精确入口。纯逻辑/Schema测试不因此构建，局部E2E不能整文件触发三CLI付费矩阵；同一有效公共检查只跑一次。新增反例在实施diff补命名测试并更新选择，0匹配不算通过。模型配置核对真实wire/原生结果；Claude核对磁盘身份与应用继续；失败收口核对等待结束及运行槽；Flow核对真实窗口按钮点击/裁剪和滚动。模拟进程证明确定性失败，不充当真实CLI完整工作流验收。

四项修复后核对首批既有发送/读取/取消/候选手动应用无回归，交付实际结果、未通过项与可复用证据并停止；不自动推进后续完整原生接线或095以后，不自行commit/push/发布或标S3 accepted。103的三CLI与双入口、050、051/052 PPTX、083/087和060/S3原门保留。实施卡与任务板仅记录真实状态，不以测试数量或目录存在写全部完成。
