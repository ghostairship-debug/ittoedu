# r19-060-release：1.9工程候选收口与源码发布交接

- Release: 1.9
- Dependencies: `r19-050-internal-dogfood`, `r19-051-pptx-media-effects`
- Optional: 否
- Write locks: `none`

日期：2026-09-18。本文是目标规格，当前实施次序、事实和验收统一见[完整实施方案](../../R19_FRONTEND_SPECIAL_IMPLEMENTATION_PLAN.md)；本轮仅文档重建。

## 目标与现状
050完整教师链及051媒体线通过，040–049全部必选结果闭合，形成可审阅的1.9 engineering candidate。当前未完成；本次计划重建不创建候选标签、不发布、不签accepted。

## 执行入口与职责
以[主方案](../../R19_FRONTEND_SPECIAL_IMPLEMENTATION_PLAN.md)、[1.9索引](README.md)、[工作协议](../../WORKING_PROTOCOL.md)与050/051有效证据为准。只汇合候选证据，产品缺陷回其Owner取得相应写域修复；不新建任务/记录/验证平台。

## 收口步骤
1. 对照 V01–V15 和13个路线节点核对完成、已验、未验；复用未受影响的正文/Flow/Word、材料、PPTX及1.8证据，源码和实际测试候选必须一致。
2. 补本候选缺失的完整工程保全检查。先读 package.json 与脚本生命周期，列出将执行的具体用例/模型及准备项；npm run verify 隐含整套 E2E，不能未经核对裸跑触发历史付费矩阵。同一候选已有有效完整证据不重复跑。
3. 版本门仍需覆盖人工编辑、三Surface、保存恢复/重开、Player、适用导出、AI目录/目标/任务生命周期、文件共编和PPTX。使用等价拆分入口时记录完整范围和排除项；不能以聚焦局部绿代替尚缺的版本门，也不能把未执行写成通过。
4. 本版交付后把[保全矩阵 PM-01](../PRESERVATION_MATRIX.md)晋升为：工作台轻改 + 编辑器模式，取消简洁/专业分档；DeveloperTab 已有代码能力仍可达。现在不改矩阵正文（源码门控仍在）。
5. 权限/cwd：记录各 CLI 实际配置与授权差异并诚实显示缺口；不把应用层「工作空间内完整权限」策略生效当必过门，不建 OS 沙箱。
6. V14：先定位可证伪复现并修对应 Owner。原电路样本不可达则记录缺失，用当前等价反例（Flow ID 写入 `scene.go` 必须失败可见）。不得声称旧课例已修。
7. Word：无环境则复用 2026-09-15 证据并保持“未新实测”，不伪称本轮实测。
8. 汇总真实CLI范围、首次/修复后表现与耗时、视觉/互动复核、剩余限制和2.0范围；已知主流程/数据错误不移交为可忽略建议。
9. 准备源码差异、验证索引、候选说明与拟发布标签 v1.9.0-rc.N。只有已有明确发布授权才提交/推送/建标签；否则交付可审阅结果，明确发布待授权，不反复询问实现步骤。

## 完成标准与边界
050/051与全部必选结果真实通过，无未关闭核心流程阻断或用户数据错误；版本候选、构建与证据一致。源码候选准备完成、实际标签发布、Owner签署分别记录。无发布授权不谎称已发布，也不撤销已完成的产品证据。
本版不发布安装器或固定课例HTML制品；**软件离线HTML导出能力必须验证**。S4与完整软件内QA/修复在2.0，不能因此缺交1.9受影响能力。不造V10或补旧格式迁移，不把长期Office原格式编辑等纳入本版。应用层权限策略与跨轮固定目标都不是 060 必过门。

## 最终交接
提供候选commit/未提交diff身份、每项证据/适用范围、真实样本/操作入口、已修问题、明确未验项、发布状态与2.0承接清单。历史失败不覆盖，未解决的必选项继续回Owner修复；没有测试/发布动作时如实说明。
