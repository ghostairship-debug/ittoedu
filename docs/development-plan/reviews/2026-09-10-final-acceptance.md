# 1.8 同候选最终工程验收（2026-09-10—11）

当前结论：本次约定的工程验收与失败修复已完成，可交给 Owner 复核当前候选。原 124 项 E2E 范围最终 100 通过、24 条件跳过、0 失败、0 串行未执行；另新增两项真实应用会话／隔离验收通过。首轮 16 项 E2E 失败及 17 项单元／集成失败已定向收敛，首次失败记录保留。最终类型检查、桌面构建、能力清单和相关示例检查通过。没有 Owner S3 签署、源码标签或发布，不能宣称 1.8 accepted。

## 范围与候选

Owner 已授权执行最终验收、并行子智能体，以及遇到实际失败时提高思考强度修复。原工作树的既有未提交改动保留；基线 HEAD 为 `c839c205e594ab61bfa01c22b4a4f28303862d8d`。本报告只记这次验收新增修复，不把工作树全部差异归为本轮改动。

- root：`C:/Users/74755/Documents/HTML课件编辑器`。
- 实际运行候选：`C:/Users/74755/Documents/courseware-r18-worktrees/r18-final-acceptance-0910`。源码补丁顺序集成，真实应用窗口由唯一 Owner 串行管理；执行中的产品源码和构建不并行改写。
- 证据：`output/r18-final-acceptance-20260910/`。初始 `candidate-01-source.json` / ZIP 保留；它不冒充修复后候选。最终候选为 `r18-final-20260910-02`，源码清单与 ZIP 见 `candidate-02-source.json` / `candidate-02-source.zip`，归档回读验证；root 与实际运行目录的 src/tests/scripts/config 完全一致。清单分别记录实际测试构建和本地复核构建，旧证据按下文影响域复用。
- 无新增付费模型调用。三 CLI 历史实测只按相关实现未变的范围复用，另在当前应用真实恢复查看。当前下拉框默认模型与历史任务实际模型分列，不改写原配置记录。

## 执行结果与失败历史

| 范围 | 首轮 | 后续及当前范围 |
| --- | --- | --- |
| 单元／集成 | 355 文件：343 通过、12 失败；3566 项：3534 通过、17 失败、15 跳过 | 17 项原失败均已定向收敛；新增修复的命名回归通过。保留首次超时与失败，不称全量首次通过，不再重跑未受影响的 343 文件。 |
| 筛选后的真实应用 E2E | 124 项：70 通过、16 失败、24 条件跳过、14 因前项失败未执行；45.3 分钟 | 16 项原失败已按原行为或正式合同修复复验。最终 100 通过、24 条件跳过；原 14 项串行未执行均已补跑通过，无剩余失败或未执行。 |
| 新增当前应用组合验收 | 三 CLI 历史恢复、持久 profile 的 A/B/C 隔离 | 两项通过；隔离用例前两次陈旧诊断文案断言失败保留，未放宽工程／会话／材料／候选零回放条件。 |
| 最终工程检查 | 修复后 Renderer、Electron、E2E 三组类型检查 | `final-typecheck.log`、`final-build-desktop.log`、`final-capabilities-check-02.log` 通过；最终生成清单 70 文件，索引 16115 / 16384 字节。 |
| 示例 | sample 与 lesson 原检查通过；新 Player 使 benchmark HTML 过期 | 只刷新过期 benchmark；`final-benchmark-test.log` 的确定性生成／一致性命名用例通过（1 项，其他 6 项未选中）。 |

原始日志为 `logic-vitest.log`、`e2e-first.log` 和 `e2e-first-results/`。后续诊断、focused、isolation、chat、renderer、remaining、editor 批次各有独立日志与失败上下文；`summarize-e2e.mjs` 按文件及标题关联，不按易变行号关联，也不让后续 skip 覆盖实际执行。`e2e-final-index.json` 保留每项完整失败／修复历史。子智能体已有 focused 日志（含失败）和真实 adapter 失败探针汇集于 `logic-rechecks/`。

筛选前排除的 11 项付费模型测试和 4 个历史专项文件不在 124 项内。24 项条件跳过与原 14 项串行未执行不同，均不计通过；这些专项要求显式选择付费原生轮、保留 checkpoint、原工作文件恢复或已有失败／损坏课例路径，逐项名单见 `conditional-e2e-scope.md`，不借最终验收重放历史工作文件或重开付费矩阵。首个重验过滤器包含报告用的套件分隔符，实际只收集到 5 项；按实际收集结果计数，之后单独确认 13 项编辑器标题再执行，没有把计划数量当完成数量。测试标题里的历史 V8 名称不表示开放 V8 工程读取。

## 本次确认并修复的产品问题

| 用户可见问题与机制 | 修改边界 | 直接验证 |
| --- | --- | --- |
| Spatial 文本同值属性仍重新派生自动尺寸，产生不应出现的几何／历史写入 | `nativeTextLayout.ts` 判断文字、样式、尺寸的实际变化；保持唯一 History writer | 原 no-op 引用断言保留，补同值宽度和完整样式反例；相关布局、状态、历史及 Runtime 共 17 项命名检查通过，真实 Native 自动尺寸编辑／保存重开证据保留。 |
| 开发 StrictMode 清理后复用已 disposed 的聊天 bridge，首次发送直接 stale | `CourseChatPanel` 的 bridge/controller 创建与销毁归于同一 effect；保留卸载后迟到回调屏障 | 15 项生命周期检查通过；真实 30 次上下文／30 事件性能用例和三个聊天失败／格式／讨论组合用例通过。 |
| Claude 初始化握手前认证失败被通用退出码覆盖 | `claudeProcessTransport` 保留现有认证识别得到的安全原因，未知退出仍沿用原分类 | 初始化前后、协议、崩溃与启动失败共 15 个真实本地 adapter 探针，并经 Main/preload 综合现场用例通过。 |
| 组件标题双击时，第一次鼠标按下产生的无变化预览仍重挂组件，第二次点击丢失目标 | `SlidePublishedAdapter` 复用相同包／版本／fallback 的有效实例；实际属性或尺寸变化按更新 hook 处理，不支持更新才重挂 | 原物理双击、保存重开与预览交互通过；新增实例复用／实际更新／必要重挂反例通过，未用强制点击或 Store 直接编辑替代手势。 |
| Flow 格式工具栏盖住下一段；编辑与试运行的实际 viewport 相差 18px，正文横移 9px | 工具栏移到独立固定头区，窄容器采用第二行；编辑与试运行共同使用已有 18px 播放边条预留常量并测量同一实际正文 viewport | 原段落双击、活动草稿跨三表面保存、Flow 空段／换行／选择／重开均通过；Wave A/C、三表面几何、1280/1440 控制器和 1280 助手停靠／新 HTML 均通过。保留原几何精度、原生纸张滚动和播放边条，无偏移补偿或强制点击。 |

相关 Renderer 修复的 26 项命名逻辑检查通过；其后最终三组类型检查通过。上述修复保持 V9、Published V2、现有 Runtime／Component 协议及唯一工程资源事务，没有新增权限、第二 Store 或模型循环。

## 工具、夹具与断言修正

这些不冒充当前产品不可用的 P1：

- repo-index 合法 Markdown 链接被括号截断：修复尖括号、嵌套／转义括号与标题解析；保留锚点和越界检查，Generator / Semantic / Query 共 26 项通过。
- 正向 PNG 夹具 CRC 已损坏：更换 6 处正常输入常量，真实动态准入／坏源码零资源写入／PPTX／共享组件／字体用例复验通过；坏图和不支持素材反例保留，不削弱正式解码器。
- 图片替换测试混淆下一场景与下一步；Flow 富文本错误要求同样式字符必须合并为一个 DOM span；跨表面几何错误要求 Flow 固定 1280×720；Wave A 仍期待助手默认隐藏。分别按当前导航合同、逐字符完整样式、Flow 响应式 1:1 与 Slide/Spatial 实际舞台缩放、1.8 默认可见修正；原保存／导出和错误样式／几何反例保留。
- Codex 恢复参数、workspace 缓存、disposed 原因、worker frame/button 和全局 Runtime wrapper 的陈旧断言按当前实现修正；未改正式指针行为。聊天脚本补齐真实原生握手和当前 staging request 来源，使格式失败真正到达宿主解析／同任务续修，未用协议超时冒充候选错误。
- 两个 Harness、一个 TypeScript adapter、一个 legacy scanner 用例在全量负载下超时／超预算，原阈值不变，逐个实际命名复验通过；首次超时原因未证实，不称确定性根因已修复。
- 隔离树首次遗漏文档引用的已有外部证据、同级组件目录，补齐真实依赖后生成四组件制品；没有把缺目录时的 skip 当矩阵通过。

## 当前应用会话、隔离和现场证据

`retained-session-review/result.json` 记录三 CLI 的历史会话在当前应用中的真实恢复：Claude / Codex / OpenCode 分别投影 385 / 5 / 250 个事件。使用原 profile 副本与工程的准确身份，原 profile 和工程不变；无新任务、事件或宿主结果。真实历史配置仍为 Claude 用户临时 DeepSeek/max、Codex Luna/max、OpenCode Luna/high。本次证明应用历史恢复，不证明新模型调用或外部原生任务重新连接；当前下拉框探测默认值不改写历史参数。

`workspace-isolation-review/evidence.json` 记录同一持久 profile 七个阶段：A/B/A，另存 C，重启后 A/B/C；A/B 材料和会话归属正确，C 保留课件内容但获得新身份且不继承原资料／会话。异工程材料访问拒绝，打开不回放候选、工程不被迟到写入。记录由正式 V2 仓库种入，打开、另存、保存、重启及官方 IPC 读取均走真实 Electron；它不冒充模型生成记录。

当前 E2E 覆盖图像、动态准入与死循环、共享／单实例组件、人工历史、保存恢复、PPTX、三表面导航／缩放／控制器、外部 Builder、字体与离线导出。具体范围看每个命名用例，不能把代表性用例扩大为所有内容组合。

实际画面复核见 `visual-review.md`。已查看当前绿图与旧共享红图对照、当前 SmartArt／公式编辑结果、完整 SmartArt 三种受支持代表页、完整公式源第 4 页、Builder Flow/Spatial、三 CLI 恢复、A/B/C 重启，以及修复后 Flow 助手停靠／新 HTML。原始失败截图保留。新 HTML 可见本次人工改稿和实际计数 1。

## 有效证据复用与剩余边界

旧付费实测、PPTX 全量导入及 Builder 增量制品仅按各自未变 producer/consumer 范围复用。文本同值 guard 不改变有变化的导入／编辑语义；Claude 新修改只保留初始化失败原因；组件修改针对 authoring 实例生命周期；Flow 常量复用保留原 Player gutter 行为。相关 authoring、聊天、Main/preload、Flow 与组件真实行为已定向补验，不因重新构建而重跑无关全矩阵。旧 HTML 仍标注原导出身份，新 Flow HTML 单独记录。

剩余边界：Owner 教学内容和实际体验判断尚未签署；未检查整份 29 页逐页视觉一致；SmartArt 第 4 页不支持，独立于工厂初始空白页；本机 Word 实际呈现、物理触控／触控板没有新证明；Spatial local world/API3 保持已声明静态边界。单张白底 PNG 不证明 alpha，展开的控制器可能占据底部阅读视野。这些边界不冒充已验，也不凭假设抬高为当前核心流程 P0/P1。

速度与准确性分列：本轮确定性性能用例 30 次当前上下文读取 P95 28.4ms（预算 500ms），30 事件投影 P95 242ms（预算 300ms）；中位数分别 8.45 / 126.5ms。它证明本机这两个 UI 路径，不证明模型生成整体提速。首轮失败、原生命令纠错、格式续修、等待教师时间和历史模型耗时仍按原记录保留。

Owner 可从 [S3 复核入口](2026-09-10-s3-review-entry.md)查看当前制品。最终签署与发布仍需 Owner 对当前结果作出明确决定；本次不晋升保全矩阵，不提交、打标签或发布。
