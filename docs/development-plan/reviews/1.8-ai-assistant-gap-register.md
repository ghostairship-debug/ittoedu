# 创作助手真实使用缺口记录

诊断基线日期：2026-09-07。下表保留原始失败及修复目标；2026-09-08已实施相关修复，当前进展见下节及任务板，不能把原诊断全表当作当前全部未修复，也不能将分项工程通过当作103/S3完成。

## Owner 新成功标准

创作助手体验至少达到 VS Code 中 Codex / Claude Code 插件的水平，且在课件当前状态的理解上更好。以教师自然语言任务、真实画面、编辑结果和生命周期验证；不能用包含内部命令或协议字段的提示代替教师验收。

## 当前结论

089–104相关产品实现已集成。真实Codex关键链已有识图零写、图片改色、Native替换为连续Runtime、同实例减速、人工交替和保存重开的直接证据；104两个外部Skill工程片段也已完成实际构建与人工后AI增量。Flow可达性、共享组件全实例及文字自动尺寸有真实宿主证据。三CLI其余独立槽与剩余自然任务仍在执行，当前实际失败继续返回对应Owner修复，不能宣称一般AI编辑矩阵或S3已经完成。

## 已证实缺口

| ID | 缺口与用户影响 | 直接证据 | 修复后应证明 |
| --- | --- | --- | --- |
| G01 | 图片只提供元数据，AI 看不到当前图像，反过来要求教师提供工程内已有原图 | Codex 568f70e6 会话，指令“帮我把颜色改为绿色”；context 只有 assetId / 尺寸 / 路径；generationSnapshot.ts 仅包含 document.assets；codexAppServer.ts turn/start 仅发文本 | AI 获取所选图片实际内容和必要页面视觉，识别颜色无需再次上传 |
| G02 | 图片改色没有从读取、处理、导入到替换的完整路径 | 所选对象是 Native image；没有直接改色字段；本次无候选、hostResult 为空；不能用 shape fill 假装图片编辑 | 保留必要图像内容的改色结果实际应用，可撤销、保存重开、运行和导出 |
| G03 | 选区替换缺少创建目标，无法完成依赖新资源/新载体的修改 | generationSnapshot.ts selection 不发 create scope；asset.media.import 需要 global create；runtime.insert 需要 create；Native content 禁止改变 discriminator | 修改选中对象可在明确范围内创建所需资源与替代载体，并以单一事务替换 |
| G04 | 模型发现、选择、推理强度没有完整接入 | 本机 Codex model/list 返回 7 模型与强度；OpenCode session/new 返回 82 模型；Codex 写死 low，OpenCode 默认 Big Pickle；当前 Big Pickle 未返回强度选项 | UI 从实际 adapter 能力生成模型/强度选项并保留有效选择，不伪造不支持项 |
| G05 | 可读进度被过滤，原生事件以 JSON 展示，完成与修改成功混淆 | protocol / codexAppServer 忽略部分摘要、计划事件；CourseChatPanel 原生事件 JSON；CLI completed 与 host committed 分离但体验不清晰 | 流式正文、公开摘要、工具进度、失败原因清楚；只有提交成功显示已修改 |
| G06 | 讨论和修改只有内部 auto 分流，用户无明确模式 | CourseChatPanel 普通请求 expectedResult:auto，无讨论/修改控件；拒绝修改被作为 answer 正常完成 | 讨论零修改；修改请求必须以已应用/待应用/明确失败结束；模式始终可见 |
| G07 | 能力说明过量且不支持真正按需发现 | 实际选区请求约 127 KB，其中 tools 87 KB、dynamicCapabilities 32 KB、页面约 1 KB；profile 内联多份 Skills；OpenCode 不允许读取文件；修复轮上下文约 13.6 万 tokens | 精简入口、相关能力与资源按需读取；记录等待各阶段和 token，不能凭感觉归因 |
| G08 | 候选格式、范围失败导致多轮完成但零修改 | OpenCode 5394fa36 stale；46eba67d candidateId 缺失；f80fafed 修复后 candidateId 非 UUID；均无 committed | 普通自然语言请求生成合法候选；诊断可读；修复不重复无进展失败；失败零写 |
| G09 | 缺乏基于当前视觉/运行结果的编辑自检闭环 | 当前每轮 snapshot 以结构内容为主；本次未提供图像；此前测试提示直接指定 component.package / revise / 完整 files | 结构、画面、运行状态有对应观察入口；提交后验证真实目标达成，失败可定位 |
| G10 | 现有测试不能证明自然语言产品能力 | 真实窄路径：新增文字、改 label、指定既有包源码修订；测试包含内部字段指导 | 固定自然语言任务使用普通 UI，三 CLI 分别验证，禁止在测试提示中注入内部协议 |
| G11 | Flow 编辑/试运行/预览及教师控制器仍有可见问题，影响 AI 对结果的判断 | output/playwright/user-flow-cli-analysis/REPORT.md；1280×720 Flow 控制器投影到宿主视口之外；编辑/试运行缺失相应初始可达校正 | 明确响应式正文与浮层尺度语义，三模式控制器可达，AI 观察使用实际目标宿主 |
| G12 | 外部Build Skill没有与创作助手共用可查询、可分片的发现机制，冷启动与引用材料读取过重 | 仓库和个人安装版SKILL均约14.6KB，index约16.1KB；正文已写按需参考，但冷启动要求两份MD及引用材料，尚无共用查询/分片入口 | 一个正式能力源同时服务助手与Builder；教学真相不丢，技术说明/材料按需读；Skill源与受管安装副本同步，真实外部课例构建验证 |

G01–G03、G08 属于当前编辑核心流程阻断；G05–G06 是用户可见主流程反馈缺陷；G07 为已测上下文膨胀，耗时因果仍需分阶段测量。G04 是控制能力缺口，G09–G10 是闭环与验收覆盖缺口。不得用安全、合规或文档风险冒充当前可用性严重度。

## 证据位置与限制

- 用户会话位于本机应用 local-agent/v1 的当前工程目录；本记录只保留短会话标识，不复制用户完整内容。
- 本机诊断与截图：`output/playwright/user-flow-cli-analysis/`；模型只做 metadata 查询，没有为诊断发起新生成。
- 既有成功证据：`output/playwright/r18-cli-codex/result.json`、`r18-cli-opencode/`、`r18-source-codex/`、`r18-source-opencode/`；成功证据与当前失败各按自身任务/模型/环境限定。
- 本记录与[VS Code对标评估](../AI_ASSISTANT_VSCODE_BENCHMARK_ASSESSMENT.md)定义证据/基线，正式实施顺序和验收以[当前至2.0开发方案](../AI_ASSISTANT_DELIVERY_PLAN.md)及各版独立规格为准。G12的096/104实现和两个外部片段工程证据已具备，冻结清单见[104证据包](../../../output/r18-development-20260908/r18-104-skill-cold-start/manifest.json)。它不替代三CLI矩阵、两套完整教师课例或Owner验收；按需发现的新实际失败仍须继续修复。
