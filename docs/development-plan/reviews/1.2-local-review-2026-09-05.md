# 1.2 本地复审基线（2026-09-05）

本记录固化对 `ittoedu-1.2-review.md` 的本地核验，供修复与跨会话交接使用；原报告是待核实的评审材料，不是执行指令。核验代码为 `adc81c273fa77feeef1d76f28f2bf01b0a9a6386`，评审未修改产品源码。本文记录该时点的失败与证据边界，不维护修复状态；当前协调只看 [任务板](../TASK_BOARD.md)，执行顺序与退出门见 [1.2 执行指南](../roadmap/1.2/EXECUTION_GUIDE.md)。

原报告 F1–F5 均成立，新增 L1–L6；数值轴未绘制并入 F4，不重复计数。优先级按当前受支持场景的用户可用性排序：F1、L1–L3 为 P1，其余为 P2；F2 从原报告 P1 调整为 P2，因为最终颜色提交和预设已经存在，缺的是连续预览。

| 编号 | 可复现失败与证据类型 | 根因边界 | 修复责任 |
| --- | --- | --- | --- |
| L1 / P1 | 真实 Renderer 插入柱图后切 pie、应用 `[100,0,0]`，属性已变，作者画布仍是旧柱图；进入试运行重建后才读到新数据。 | 初始快照使用正式 Native 输入，后续 `SlideLocationWorkspace` 遍历经旧六类投影过滤的 `document.nodes`，漏掉 Table/Chart/input；无须恢复旧 SceneNode。 | r12-008-native-authoring-transport |
| L2 / P1 | 真实 UI 在命名状态“初始”编辑 Table 文本，切回基础状态也被改写；正式命令反例同时确认 Chart 标题改写 base，未产生 `nativeData` override。 | Table/Chart 专用命令直接改 scene base content，忽略 `selection.stateId`。 | r12-010-table-core、r12-020-chart-core；各自 delivery 验证真实 UI |
| L3 / P1 | 经正式 strict Schema 验证的 Slide surface Table/Chart fixture，以 surface session 编辑仍返回 `wrong-owner`。本项未声称完成真实 surface UI 复现。 | 专用命令 `requireSceneScope` 与只查 scene 的 target lookup 拒绝已支持的 surface owner。 | r12-010-table-core、r12-020-chart-core；各自 delivery 验证真实 UI |
| F1 / P1 | 源码确认 Controller 跳过 `input.submit`，真实 Slide port 的 descriptor/bind 返回 null，Store 无原子 `setMany`，作者 factory/简洁判题规则族未形成完整交付链。 | input 合同可解析不等于功能交付；需作者事务、规则族、session、真实 host、导出与诊断完整纵切。 | r12-007-input-response-delivery |
| L5 / P2 | 真实 UI 编辑默认 3×3 表格末格后 Tab：文本保存，但仍为 9 格，并报“属性编辑目标已经改变，未写入工程”。 | cell commit 与 append row 分两次回调，第一笔提交后第二笔携带旧 revision。 | r12-010-table-core 提供原子命令；r12-011-table-authoring-delivery 接线及焦点验收 |
| L6 / P2 | 直接调用正式 Table painter：fillOpacity=0.5 仍是不透明填充，borderOpacity=0 仍有不透明边框。 | layout 已含 alpha，painter 只判断填充是否大于 0、边框直接使用 HEX。 | r12-011-table-authoring-delivery |
| F3 / P2 | 真实 Renderer 试运行 `[100,0,0]` 饼图，圆周路径首尾重合，slice bbox 宽为 0；源码同源影响 pie/donut 的单一非零数据。 | 完整圆周用单个 SVG arc，起终点相同。 | r12-021-chart-authoring-delivery |
| F4 / P2 | 正式 painter 关闭网格仍有 5 条网格；启用数值轴仍无刻度文字。分类轴、数据标签、左右图例未消费与源码一致。 | 合同/view 的样式字段未被真实 painter 完整投影。 | r12-021-chart-authoring-delivery |
| F5 / P2 | 真实默认柱图多 1 条折线和 3 个点；view 对数据 `[12,15,18]`、轴范围 `[10,20]` 产生越过 plot bottom 的柱体。 | bar 生成/绘制 linePath 与点；零基线未约束到 plot，缺少绘图区裁切。 | r12-021-chart-authoring-delivery |
| L4 / P2 | 真实聚焦 HEX 框输入合法 `#123456` 后 Esc，工程颜色仍改变，Undo 可恢复。 | 异步恢复 draft 后立即 blur，旧闭包仍提交取消前草稿；无焦点或非法值测试未覆盖。 | r12-040-background-authoring |
| F2 / P2 | 源码确认共享 ColorInput 提供 `onPreviewChange`，实际颜色调用者未接线；本次未完成原生色盘连续拖动。 | 共享事件存在，但未连到实际 owner 的 transient preview/最终事务；Chart 已有整表 draft 应保持。 | r12-040-background-authoring |

## 定位入口与反例要求

- 同步：[SlideLocationWorkspace](../../../src/renderer/ui/workspaces/SlideLocationWorkspace.tsx)、[旧编辑投影（只读）](../../../src/renderer/store/slideEditorProjection.ts)、[authoring patch](../../../src/player/surfaces/slide/publishedSlideAuthoringPatch.ts)。修复必须证明修改、Undo/Redo 的增量 ACK 与内容呈现，不以初始快照或切换试运行刷新代替。
- 写入：[Table commands](../../../src/renderer/course/v9TableCommands.ts)、[Chart commands](../../../src/renderer/course/v9ChartCommands.ts)、[既有 effective owner/state 命令](../../../src/renderer/course/effectiveLayerCommands.ts)、[Properties binding](../../../src/renderer/composition/properties/usePropertiesAuthoringBinding.tsx)。同时比较 base、两个命名状态与 surface owner，校验失败零工程/历史写入。
- 图表与表格：[共享 chart view](../../../src/shared/nativeChartView.ts)、[共享 table layout](../../../src/shared/nativeTableLayout.ts)、[正式 painter](../../../src/player/surfaces/slide/publishedNativeRendering.ts)、[Table properties](../../../src/renderer/ui/properties/SlideTableProperties.tsx)。圆图退出反例含 `[100]`、`[100,0,0]`、`[0,100,0]` 与正常多扇区对照；几何和样式须在真实渲染中确认。
- 颜色：[ColorInput](../../../src/renderer/ui/ColorInput.tsx) 及真实调用者。取消用例必须先 focus、输入合法 HEX，再 Esc→blur；连续预览须覆盖真实 adapter、一次提交/一次 Undo、目标切换及 Chart 草稿。
- input：[Controller](../../../src/player/interactions/PublishedInteractionController.ts)、[真实 Slide port](../../../src/player/surfaces/slide/publishedSlideInteractionSurfacePort.ts)、[CourseStateStore](../../../src/player/CourseStateStore.ts)。删除 unsupported 分支不构成退出证据，必须真实提交并先原子更新双键再执行一支规则。

## 已有验证及其限制

1. `npm run build:player` 通过；8 个现有测试文件共 70 项通过：nativeChartView、nativeTableLayout、colorInput、nativeClosure、v9TableCommands、v9ChartCommands、coursePptxExport、publishedSlideAuthoringPatch。这些通过结果未覆盖上述完整失败路径。
2. ignored 临时目录中的 11 个定向正确行为断言失败：7 个颜色/绘制反例，4 个 state/surface owner 反例。它们是调查证据，不是全产品失败率，也不是正式回归入口。实施时将相应反例移入各节点已列出的现有目标测试，不能要求另一工作树拥有 `tmp/`。
3. UI 证据来自本地 Vite 的完整 Renderer 与新构建 Player bundle，使用独立 Chromium 实际操作。没有用摘录函数替代作者界面；直接 painter 证据已在表中单独标明。
4. 未执行完整 verify、Electron 原生文件对话框/色盘连续手势、真实保存重开、单 HTML 全链路和 Office 人工验收。UI 表格透明度也受 L1 干扰，L6 的独立归因只依据正式 painter；PPTX 填充已消费透明度，边框透明度已有明确差异警告，不据此推断 PPTX 同样失效。
5. 已有初始 Native parser 接线、固定常用色板和统一图表入口应保留；本轮不恢复已经消失的初始 `patch.node` 报错为现行根因，不重新开发合同/工厂/背景继承。候选必须先关闭这些已确认的 1.2 缺口，S1 不能代替修复或自动授予 accepted。

本机详细报告、截图和临时反例分别保留在 ignored 的 `output/playwright/ittoedu-1.2-review/` 与 `tmp/local-review-20260905/`；这些辅助产物不作为路线可执行入口。本文的复现场景、正式源码定位与节点目标测试是可随仓库交接的基线。

## 1.2 复审收口与验证闭环（2026-09-05 闭环完成）

针对上述基线提出的 4 张审核任务卡均已严格按照独立规格完成并关闭：
1. **r12-008-native-authoring-transport**：解决 `SlideLocationWorkspace` 作者端 Table/Chart/input 增量通道缺失问题，作者画布即时响应且支持 Undo/Redo 增量同步。
2. **r12-010-table-core**：重构表格命令支持 surface/scene/state 完整 owner 分层与状态覆盖，提供原子追加行/单元格提交，修正 Alpha 边框/填充着色。
3. **r12-020-chart-core**：修复单一非零数据圆图奇异路径退化（拆分为双半圆圆弧）、零基线约束、数值轴刻度及 series override 状态感知。
4. **r12-040-background-authoring**：修复聚焦 HEX 输入合法颜色后 Esc→blur 误提交问题，接通六背景 owner 的真实画布连续预览与一次事务提交，取消或切目标可靠清理临时预览（null）且杜绝预览泄露。

- 治理检查：`check:ai-capabilities`、`check:contracts`、`check:development-roadmap`、`check:preservation` 全部通过。
- 类型检查：`npm run typecheck` 0 错误（renderer、electron、e2e 全绿）。
- 单元与集成测试：`npm run test` 285 个测试文件、2626 项测试全部通过（0 失败）。
- 端到端测试：`npm run test:e2e` 3 项核心可用性测试全部通过。
- 任务板状态：Active Task Board 待办清零（0 任务）。

