# 1.5 材料与内容生产力复核

材料、受限 PPTX、样板改写和内容检查均通过现有 V9 内容与资源事务接入，已由 Owner 于2026-09-06签署 S2，接受当前范围及已知遗漏，对应 `v1.5.0` accepted 源码标签；可直接复核的文件与步骤见 [S2 验收单](../acceptance/S2-tools-and-materials.md)。

## 用户入口与支持范围

- “创作工具 → 教学材料库”：粘贴文本、导入 UTF-8 TXT / Markdown / CSV、来源搜索、引用和删除。按工程 ID 与规范化文件路径隔离；另存为不复制旧材料。可见引用是普通文字，删除缓存不删除已插入内容。
- “创作工具 → 批量编辑与参考页 → 导入 PPTX”：先检查并预览页数、对象与转换说明，确认后追加新的演示表面，整包只产生一次文档与素材历史。当前支持母版/版式共享装饰、占位符继承、水平文字、基础预设形状与线条、普通分组坐标、裁剪 PNG / JPEG 和未合并 Native Table。对象与文本框保持可编辑，组合形状的文字拆为独立文本框。字体按当前系统解析，画布按原比例居中适配，文本框内边距归零。这是明确的受限转换，不宣称 PowerPoint 全保真。
- PPTX 按 Owner 2026-09-06 的补充决定改为部分导入：合并表格、图表、SmartArt、复杂分组变换和无法转换的媒体按页与对象提示并跳过；动画、链接和部分视觉效果省略后保留静态内容，复杂背景回退白色。预览展示遗漏清单并允许确认可用内容；主文档不可读、资源超限或完全没有可用对象才停止。ZIP 限制为输入 32 MiB、展开 128 MiB、单项 32 MiB、4096 项、200 倍压缩比、100 页、2000 对象。图片须由真实浏览器解码。所有解析与资源准备留在暂存内存，不创建第二工程或磁盘写入路径。
- 同一面板的“样板改写”：选择现有演示页，每个原生文字对象构成明确槽位；预览列出来源、原文、新文和容量结果。缺槽位、超容量、过期预览或带动态载体/交互/状态覆盖的来源明确拒绝。确认后复用正式参考页身份与资源重映射，生成普通 V9 副本，整体撤销不改来源。新文字不会通过无限缩小字号塞入。
- “工程检查”：只读 finding 显示 rule ID、严重度、依据、建议与 canonical target。四类内容检查均为确定性检查，没有另设模型推理循环。

## 内容检查可判定范围

1. 数学：Native / Flow 公式 AST 的未填槽位与普通正文中 `公式：<线性公式>` 的语法错误使用 `content-math-parse`；真实共享公式布局器的警告使用 `content-math-render`。可访问文字不是线性公式，不把它误送入解析器。
2. 答案：单个明确的产品单选规则族中，正文 `答案：<选项原文>` 与实际有限选择 evaluator 比较。多个歧义规则族、带条件的任意手写交互或不透明扩展代码不推断正确答案。
3. 图表：正文 `图表：标题 / 系列 / 类别 = 数值` 与同一 owner 中可唯一定位的正式图表数据比较。支持 Native Chart 与 Flow Chart。普通自然语言不被当作可判定数值声明。
4. 来源：显式 `来源：` 的空值、待补标记、空定位，以及缺少 citation 的 Flow 引文给出定位提醒。不会把所有普通文字都判为必须引用。

这些检查不能证明任意自然语言的学科正确性，也不能证明来源真实可靠。教师仍负责最终内容复核；没有 finding 不等于学科审核通过。规则只使用可携带正文与既有结构，不添加隐藏 QA 元数据。

## 早期工程证据（新增范围以下方收口复核为准）

- 材料的聚焦与真实 Electron 证据见 1.5 路线记录；包括文件导入、检索、引用、删除与 Save As 隔离。
- `tests/unit/courseProjectHealth.test.ts` 25 项通过；四类预置错误及数学渲染警告进入总 collector，可解析 canonical target，含依据与建议；修正内容后消失，检查不写工程。
- `tests/unit/productivity.test.tsx` 与上述 QA 合计 35 项通过；样板槽位、容量、身份独立、archive 重开、过期预览和一次撤销已有定向证据。
- `tests/unit/courseProjectArchive.test.ts` 增加正常 PptxGenJS 两页文件与主题背景后共 16 项；覆盖受限 PPTX 文字/形状/素材、Unicode 范围、Schema、单 revision、资源随文档提交与撤销、archive 重开、文档和 sidecar 失败注入，以及大小/解压比/损坏关系/不支持对象的原有拒绝行为（后者已由下述部分导入复验替代）。翻转、上下标和半透明文字不会被静默转换。
- 真实 Electron“PPTX 与样板”与“内容 QA”两项通过（2.1 分钟）。前者经 UI 导入正常 / 拒绝异常 PPTX、保存、整体 Undo/Redo、重开、空槽拒绝、确认改写、来源独立和离线 HTML 图片/文字播放；后者逐个定位四类 finding 到正确对象的属性，并保存证明检查零写入。日志 `output/r15-content-e2e.log`；截图 `output/playwright/r13-review/r15-imported-editor.png`、`r15-remix-preview.png`、`r15-remix-player.png`、`r15-content-qa.png`，已查看实际呈现。
- 首轮播放脚本遗漏控制器展开，随后又使用了与父容器坐标命中不符的 locator 点击；已按现有控制器实际鼠标命中与跨表面折叠状态修正，用例通过。没有改动控制器实现或弱化播放断言。
- 诊断 ledger、预检和 GUI consumer 的两处旧完整清单断言已补充新增内容规则。能力索引生成/check 通过，索引为 15537 / 16384 字节；类型检查和 Renderer 构建通过。1.4 未受影响的完整桌面与结构证据继续有效，没有重跑无关套件。
- `output/S2-acceptance/` 保留同一分数工程、离线 HTML、26 条正式 Builder receipt 对应构建输入、材料 TXT 与正常/异常 PPTX。工程有 4 个 Surface、6 个位置，四个故意内容错误可定位。两个输入 PPTX 已通过真实 Chromium 解析，正常文件为 7 个可编辑对象与 1 个素材，纯表格反例因无可用对象不写入；新增同页混合表格的部分导入样例见下述补充。`output/r15-s2-case-build.log` 与 `output/r15-demo-pptx-check.log` 保存结果。

PPTX 部件与关系读取参考 [Microsoft PresentationML 结构](https://learn.microsoft.com/en-us/office/open-xml/presentation/structure-of-a-presentationml-document)。OpenMAIC 未采用，不阻塞核心路径。

## Owner 补充：部分导入（2026-09-06）

当前工作区在 `v1.5.0-rc.1` 基础上改为保留可用内容并提示遗漏，旧标签未移动。对象转换失败撤回该对象的暂存结果，不留下已创建的形状半成品；跳过图片或页面后仅提交保留对象实际引用的素材。没有可导入对象时显示原因并保持工程不变。未来兼容范围见 [1.5 路线](../roadmap/1.5/README.md#pptx-增强收口顺序)。

- 归档聚焦测试 19/19 通过，覆盖部分导入的可编辑对象、单次历史、素材撤销、保存重开、失败零写入、效果省略、背景回退、损坏页面/媒体隔离，以及无半对象与无可用内容拒绝；日志 `output/r15-partial-pptx-unit.log`。
- 类型检查与 renderer 构建通过；日志 `output/r15-partial-pptx-typecheck.log`、`output/r15-partial-pptx-build.log`。
- 真实 Chromium 检查正常 PPTX、纯表格和混合表格文件：混合文件保留 7 个可编辑对象与 1 个素材，同时报告被跳过的表格；日志 `output/r15-partial-pptx-inputs.log`。
- 真实 Electron 经预览提示确认部分导入、整体 Undo/Redo、保存重开、Remix 与离线 HTML 播放通过；日志 `output/r15-partial-pptx-e2e.log`。提示页截图 `output/playwright/r13-review/r15-partial-pptx-preview.png` 已查看，页码、遗漏对象和确认按钮清晰可见。


## S2 删除回归与线条导入整改（2026-09-06）

教师报告初始场景/页面无法删除。真实 Store 复现发现：`deleteSurfaceFromDraft` 在当前编辑位置仍有效时提前返回，遗留已删除的 `startLocationId`，工程校验拒绝提交；初始页只含一个场景时也经过此分支。修复为先更新持久化起点，再选择编辑位置。另移除场景删除前独立提交可见性修补的路径，引用清理由正式删除命令在同一事务完成；在 Flow/Spatial 工作区通过大纲删除 Slide 场景时也使用正式课程位置命令。

PPTX 原先拒绝 `cxnSp`、翻转、零宽/零高外框与箭头，即使 Native 已有表达能力。当前接入明确样式的直线、常见实心箭头和可表达的折线，保存独立端点/折点；同时修复 `createShapeNode` 接受但未保留 `lineGeometry` 的遗漏。播放截图进一步暴露零厚度外框裁掉线宽和箭头，现按描边和箭头所需空间扩展外框，保持原始端点与旋转中心，避免通过改变实际位置来消除裁剪。

验证范围：Store 删除聚焦 6 项通过（含初始场景、初始整页、初始页唯一场景、Flow 活动时删除初始场景及最后位置保护）；课程位置测试 13 项通过；最终归档/导入 21 项通过，含真实 PptxGenJS 水平/竖直/翻转线和精确折点测试。日志分别为 `output/r15-initial-delete-repro.log`、`output/r15-delete-lines-unit.log`、`output/r15-lines-final-unit.log`。Native 线几何与最终归档/导入合计 66 项通过。类型检查、Player 与 renderer 构建通过。

播放像素检查还发现原有三角箭头路径重复经过尖端，填充面积为零；Canvas 与 Phaser 的三角箭头路径已修正，stealth 的凹点仍保留。实际验收针对箭头像素高度、线宽和边缘裁剪，不以存在 shape DOM 代替绘制正确。

Owner 后续取消自动图片后备及其外部渲染依赖，当前以原生映射和明确提示后的部分导入验收，不把线条补齐等同于 PPTX 全面兼容或 S2 教师验收通过。当前改动基于 `945ed40`，未创建新的 accepted 标签。

最终真实 Electron 复验 1/1 通过：通过大纲删除初始场景与整页、单次 Undo/Redo、保存重开、导入真实 PPTX 线条，并在离线 HTML 中检查三条线的绘制像素。蓝色三角箭头高度、绿色竖线线宽和三条线无边缘裁剪均通过，实际截图已查看。日志 `output/r15-delete-lines-e2e.log`，截图 `output/playwright/r13-review/r15-imported-lines-player.png`。未重跑与本轮变更无关的全量桌面回归。

## 调整后的 1.5 原生收口（2026-09-06）

Owner 明确取消自动图片后备、LibreOffice/Office 和商业渲染后端。普通未合并表格提前到 1.5；合并表格在 1.6 按合同、编辑、跨消费者和 PPTX 映射顺序交付，路线与 manifest 已同步。没有改动 V9 判别器或创建第二工程模型。

- 多母版与版式公共装饰导入独立演示 Surface 的共享层，使用精确位置可见性；占位符正文保持各页独立。支持的连续共享底层导出为可复用 PPTX 母版，前景内容按页保留次序。
- 普通未合并表格导入既有 Native Table；保留单元格正文、行列尺寸及基本样式。合并表格明确提示，不用普通矩形格冒充合并区域。旋转/翻转分组、复杂图示和表格高级样式仍有限制，见转换说明与增强计划。
- `courseProjectArchive.test.ts` 与 `coursePptxExport.test.ts` 合计 **54/54** 通过，覆盖共享可见性、实例文字、单事务/归档恢复、普通表格、合并拒绝、分组坐标、裁剪、导出母版复用与共享前景次序；日志 `output/r15-native-closure-unit-final.log`。
- 真实 Electron 新增闭环通过：A/B/A/隐藏装饰四页导入、整体 Undo/Redo、共享装饰修改、单页标题与单元格修改、保存重开和独立离线 HTML 播放。截图已查看，文字、共享装饰与表格内容可见；对应 `output/playwright/r13-review/r15-native-import-{preview,editor,player}.png`。旧 PPTX/Remix 桌面用例本轮亦通过。
- 类型检查、renderer 构建、能力索引生成和路线校验通过；能力索引 **15972 / 16384** 字节，路线 **127** 节点。日志 `output/r15-native-closure-typecheck.log`、`output/r15-native-closure-build.log`、`output/r15-native-capabilities.log`。
- 五份真实 PPTX 验收输入已生成并通过 Chromium 检查：普通内容 7 对象/1 素材，纯普通表格 1 对象，混合普通表格 8 对象/1 素材，多母版 4 页/4 共享对象，纯合并表格明确拒绝且零写入。结果 `output/S2-acceptance/pptx-input-check.json` 与 `output/r15-native-acceptance-inputs.log`。
- 实际截图发现旧表格 painter 的绝对行列尺寸叠加折叠边框，导致右/下边框超出容器被裁。改为按布局比例分配行列，边框纳入固定表框；Player 与 renderer 重新构建后，真实桌面闭环 **1/1** 再次通过，截图确认四边完整。日志 `output/r15-native-closure-e2e-final.log`、`output/r15-table-border-player-build.log`、`output/r15-table-border-renderer-build.log`。最终路线、合同快照和能力索引 check 均通过。

工程候选已供 S2 复核；教师对真实课件的视觉与连续互动签署仍未完成，不创建 `v1.5.0` accepted 标签。早期纯表格拒绝和占位符全部跳过的证据仅描述当时阶段，不代表当前支持范围。

## 真实29页课件收尾：r15-037 / r15-038 / r15-036

本次在既有未提交改动之上完成真实课件整改，不替用户提交、移动候选标签或签署 S2。源样本仅在本机读取，未加入仓库。当前可编辑对象643个，目标修复集126个源对象全部恢复；剩余67次跳过均属于已明确安排到后续版本的复杂内容。

- **统一画布**：Native materialized frame 的同步 Schema 从至少16px改为有限正数，与既有 V9 frame 接受域一致。没有改 V9 字段或放宽零、负数、NaN、Infinity。原始514个对象（包括28个失败对象）与修复后643个对象的正式 patch 解析均零失败。重新导入后有27个小尺寸对象；原第1页的另一个小文字框仅含误继承的项目符号，随继承修复正确消失。
- **普通映射**：按对象角色选择 master 文本样式，非占位文字不套 bodyStyle；递归分组先解析有效字体与样式，再展开坐标。分组 chExt 是坐标单位，不直接乘到以点/EMU定义的字号与描边；它们只进行一次页面到画布换算。普通边框与直线共用样式映射，支持 sysDash/sysDot、miter/bevel 的可表达结果，并按页提示转角/虚线节奏简化；roundRect 的 adj 映射为实际圆角半径，flowChartProcess映射为矩形；单字符 just 恢复为等价左对齐，长段落仍明确提示。
- **可见正文**：逐页截图暴露第13页题干被固定文本框裁剪。现用既有共享文字布局器落实源 spAutoFit 和 wrap=none 的初始几何；只在源允许自动扩框时按当前字体测量扩框，并提示复核邻近布局。真实课件共7处扩框，固定小框不被放大。第13页三处题干通过真实 Published painter 的文字矩形边界检查，句尾、显式换行和 P(A) 完整显示。
- **可编辑导出**：修正 rounded-rectangle 的 PptxGenJS rectRadius 单位，应传英寸长度而非宽高比例；圆角导入—导出—再导入有语义回归。真实课件的643个对象导出后再解析仍为643个、零新增跳过；资产闭包与普通对象编辑能力保留。
- **提示与验收材料**：OLE对象不再误报为图表；有明确 Equation/MathType progId 时显示旧版公式类别，缺失标识时如实显示OLE嵌入对象。本真实文件27个旧公式的progId为空，不猜测二进制语义。固定输入新增 `小尺寸与分组文字.pptx`，6个普通可编辑对象及两条边框简化说明，经真实Chromium检查；已有五份输入继续通过。

逐对象核对使用源页、源对象名称、正文与输出对象清单，并处理同页重名和显式段落换行，不能以输出数相减代替恢复数。`output/pptx-tree-closure/object-reconciliation.json`记录193→67、126/126恢复、零目标残留阻断；原始诊断文件保留在原目录。

| 证据 | 结果与位置 |
| --- | --- |
| 归档/导入与PPTX导出 | 最终59/59通过；`output/r15-autofit-unit.log`。涵盖自动扩框、固定小框、分组字体/线宽、圆角往返、单字对齐及长段落反例、OLE提示、既有母版/表格和资源事务。 |
| 协议与真实宿主patch | 协议10项、Published patch 8项通过；原始514与当前643对象的materializer→patch解析零失败，见 `output/r15-original-frames.log` 与 `output/pptx-tree-closure/transport.json`。 |
| Electron与离线HTML | 真实29页逐页启动、整体Undo/Redo、小尺寸增量修改和ACK、分组文字改写/Undo/Redo、保存重开、29页离线播放，以及既有母版/表格/Remix，共3个用例通过；`output/r15-closure-e2e-final.log`。新增自动扩框后的真实29页最终复验1/1通过，实际DOM文字边界不越框；`output/r15-autofit-e2e-final.log`。早期一次离线控制按钮的测试点击等待已改用仓库既有的真实坐标点击方式，最终用例没有增加retry或弱化断言。 |
| 实际呈现 | 已查看首页标题、第24页树状图、第25页字母与29页联系表；第13页后续扩框截图及文字边界检查见 `output/playwright/r15-common-closure/autofit-page-13.png`、`output/pptx-tree-closure/autofit-check.json`。 |
| 可编辑PPTX | `output/r15-tree-export-final.log` 与 `output/pptx-tree-closure/export-roundtrip.json`，643→643且零新增跳过。 |
| 构建与合同 | Player/renderer/Electron构建、最终typecheck、合同快照、能力生成、路线校验与git diff --check通过；能力索引16027/16384字节，路线133节点。没有改持久化合同生成物；未重复运行未受影响的1.4全量回归，未运行完整verify。 |

供教师直接打开的本机副本位于 `output/S2-acceptance/树状图导入复核.h5lesson`、同名HTML及`树状图可编辑导出.pptx`。副本通过正式位置删除命令去掉工厂初始空白页，直接从原第1页开始；该副本的29页离线导航另经验证（`output/r15-acceptance-player.log`），不复制修改教师原件。

剩余67次跳过：旧OLE公式27、上下标等文字效果14、图片颜色替换透明9、翻转文字框6、自由路径4、括号/标注调整4、渐变3。它们继续按1.6–1.8既定节点交付；复杂内容可在明确提示后部分导入，不能把本样本直接宣称为完整可教学成品。S2仍须教师复核视觉、内容与连续操作，签署前不创建 `v1.5.0` accepted 标签，也不进入1.6。

## S2 Owner 签署（2026-09-06）

Owner 在查看真实课件后明确表示：“还是会丢挺多东西啊，不过算了，先这样吧，S2先过，git commit并push”。据此接受当前1.4–1.5范围并发布 `v1.5.0` 内部源码标签。上述待签署表述记录的是各轮当时状态，本节为最终结论；不声称所有人工步骤分别实测通过。67次复杂内容遗漏及1.6–1.8整改安排继续保留，本样本不因此成为完整教学成品。未新增代码变动的验证沿用以上有效证据，完整verify仍为未运行。
