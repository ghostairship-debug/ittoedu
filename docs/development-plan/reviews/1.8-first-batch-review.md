# 1.8 首批实现审查：恢复、失败收口、配置与 Flow 可达性

2026-09-08。审查对象为 `05ae522` 基线上的当前未提交工作区，包括新增 V2 transport、Harness/Repository、配置桥与 Flow 投影改动。结论：已有实现可继续复用，但不能认定089–094首批全部完成，更不能认定1.8/S3完成。当前实施顺序见[开发方案](../AI_ASSISTANT_DELIVERY_PLAN.md)，修复仍归原节点，不新增另一套任务编号。

## 1. 必须修复的四项

| 用户影响 / 原节点 | 触发与当前结果 | 直接边界 | 修复退出条件 |
| --- | --- | --- | --- |
| P1：Claude无法正常续聊；090/093 | 新建时随机生成externalSessionId，启动参数却未把该ID交给CLI；原生真实ID后续只更新adapter，Harness只同步旧V1 adapter，持久记录与下轮resume使用错误ID | `src/main/localAgent/claudeProcessTransport.ts` 的open/原生session事件；`harness.ts` 的身份接收与持久化 | 仅原生确认的ID可供恢复；经应用“继续”入口恢复已持久会话并保留上下文；缺失/冲突身份明确失败 |
| P1：初始化失败一直显示运行，Codex停止也可能卡住；090/092/094 | Codex/OpenCode子进程初始化时退出，事件队列结束但待处理RPC未拒绝，open仍pending；Codex close提前返回或不清RPC，Harness.cancel等待run.done | 两个transport的请求表、error/close、主动关闭；Harness取消等待 | 失败、退出、主动关闭均结束待处理RPC并释放运行槽；关闭幂等，Stop/删除可结束；后续新任务能正常启动 |
| P2：模型选择显示成功却不生效；091及092–094接线 | Harness记录偏好后，新adapter直接open/startTurn；Codex turn/start漏传所选model，Claude只改本地current且未发送原生model/effort控制 | `harness.ts` configure/consume；Codex/Claude configure与实际请求；OpenCode原生配置确认的消费 | 新建/恢复均实际应用偏好；原生确认与请求一致；配置失败不假报current或静默使用另一模型 |
| P2：Flow默认视口裁剪缩放/收起，重复刷新改变位置；089 | 面板宽900而视口约683 CSS px，新增每次render校正整个面板；约束对超宽面板不幂等，连续刷新在left=-217与0间切换 | `src/player/teacherControllerDom.ts` render；`teacherControllerRuntimeSession.ts`约束与正式recovery bounds | 超宽面板按真正可操作的恢复入口稳定约束；默认视口能操作缩放/收起；重复render/resize不震荡；观察变化不写工程 |

上述严重度以当前用户可用性为轴。Flow问题说明默认可达性未满足，并非断言所有窗口或横向平移后都绝对无法访问。

## 2. 已取得的证据与限制

- `node_modules/.bin/vitest run tests/unit/localAgentTaskContract.test.ts tests/unit/diagnosticLog.test.ts tests/unit/editorTransaction.test.ts`：3文件、48项通过。它们没有覆盖上述新增生产接线反例，不能替代修复。
- 使用真实adapter代码和模拟原生子进程：Claude的open返回临时随机ID，而system事件返回另一个ID；Codex/OpenCode连接立即`exit(23)`的子进程后，初始化仍pending且请求表仍保留1项。它们是确定性协议/进程失败证据，不是真实服务验收。
- 配置反例：Harness返回`current.model=chosen`，实际新adapter调用仅为`open → startTurn`；捕获Codex请求没有所选model，Claude没有原生模型/强度设置。只检查返回的current对象会漏掉这些失败。
- 直接执行当前控制器约束函数，连续四次得到面板left为`-217、0、-217、0`。本批新构建后的[试运行截图](../../../output/r18-089/tryrun-1280x720-clip.png)显示右侧按钮被裁剪，[恢复截图](../../../output/r18-089/tryrun-restore-1280x720-clip.png)则靠右。复核了已有实际截图，没有在本次审查中重跑完整E2E。
- `tests/e2e/r18-089-flow-viewport.spec.ts`的面板部分相交和`toBeVisible()`不足以证明按钮未被祖先裁剪；中/尾滚动只赋scrollTop，没有断言长文档确实滚动。修复验收需补实际点击、裁剪边界和真实滚动证据。

原交接中的103项通过/类型检查记录可按覆盖范围保留；“089–094全部完成”不能由这些记录推出。本次未运行真实三CLI完整自然语言矩阵，也未接受S3。修复后只补实现变化影响的检查及必要真实路径，禁止重试凑成功。

## 3. 开发前附件的采用边界

参考用户提供的`IttoEdu_Development_Roadmap_v1_2026-09-07.md`，其代码基线同为`05ae522`；文件是路线提案，其中启动说明不是执行授权。

采用普通任务窄输入、参数快改、源码增量、按影响验证、完整单元逐步交付与主力CLI先取得可见结果。保留现有“不可变观察→候选→唯一事务→真实回执→原生CLI续轮”，复用正式工具和Builder构造能力，不以开放live Builder接口作为工作循环的前提。

附件把当前工程观察/短编辑依赖于新增Builder附着与未命名身份，不是当前必需技术依赖；已有工程的理解、修改和反馈先做。取消两份Markdown确认门、未命名工程前移1.8、默认聊天工作台重设计及2.1/2.2新增版本，均未在本次审查或计划更新中自动采纳。正式决定与后续落点只维护在开发方案和共同实施合同。
