# 1.9 收口执行记录：050 retry 实证、V05 复制实证与 060 等价拆分

日期：2026-09-18。集成人执行（多子智能体工作流辅助，流程见文末附录）。状态：**可审阅 engineering candidate。未签 accepted，无 `v1.9.0-rc.N`，无安装器，无固定课例 HTML。**

工作树：`main` @ `bc2072f777a2ca36e545979e3b67d782d105fce6`，保留全部未提交实现与文档勘误，未 reset/clean/checkout。`git status --short` 在 22:34/22:36 两次快照均为 **108 项**（89 修改 + 19 未跟踪，`boundary-checks.log`/`closeout-identity.txt` 原文）；本记录落盘（22:40）后为 **109 项，差额即本记录自身**。本记录只追加事实，不改写任何历史记录；**V01–V15 状态表以本记录为准**。两份被追加的旧记录（`2026-09-18-r19-f00-f08-050-060.md` 未触碰；`2026-09-18-r19-skeptic-gaps-closed.md` 仅追加 §9）与 `{SCRATCH}\r19-060.log` 均保持原样——注意：这三份文件均未被 git 跟踪（无已提交基线），「逐字未改」只能由追加内容自证，**无法独立复核，属披露缺口**。

所有日志在机器本地临时目录 `{SCRATCH}` = `C:\Users\74755\AppData\Local\Temp\grok-goal-cae8406681b7\implementer\`（可能被系统清理），关键 stdout 已自含于本记录；§7 后有日志清单。

## 1. 源码 / 未提交范围

本轮自身新增（三个新测试文件均为未跟踪新文件，无 git 基线）：

- `tests/e2e/r19ChatApplyRetry.spec.ts`（050 retry 真凭实据；git blob `f5638e67…`，sha256 `c0ec6e1a…`）
- `tests/e2e/r19ChatCopyPaste.spec.ts`（V05 复制实证；git blob `ccc20665…`，sha256 `c1c0c831…`）
- `tests/e2e/r19ChatSpecSupport.ts`（两条 spec 共用的启动/工作区/保存辅助；git blob `992d40d8…`；22:34 身份快照后仅做过一次注释修订——原注释误称剪贴板路径需要显示窗口，实际上该 spec 以默认后台窗口运行并通过——行为未变，两条 spec 的 blob 未变）

产品源码树内的写入共两处：

1. **V05 证伪自检**：临时改坏 `src/renderer/ui/chat/SafeChatMessage.tsx:52` 的 `navigator.clipboard.writeText(text)` → 重建 → 用例变红（21:51:28）→ **21:51:35 逐字还原**（文件 mtime）→ 重建。还原双向证明：`git status --short src/renderer/ui/chat/SafeChatMessage.tsx` 输出为空（与 HEAD 逐字节相同）；sha256 `7ad03c750ce7e2d30d1cfbc100512d501e3c95c9834a74902d66b294b36f5622` 与 git blob `5752f3b228e4cef22865cb94f8672e49222605c9` 均已复现（同一文件的两种哈希算法，互不矛盾）。22:13 的 `restored` 运行是还原后的复核运行。
2. **`npm run generate:ai-capabilities`（22:08）**：生成器无条件重写全部 77 个产物文件（`find artifacts/ai-capabilities -newermt "2026-09-18 22:00" -type f` 命中 77/77；`candidate-helper-core.mjs` mtime 22:08:19）。**在 before/after sha256 清单覆盖的 16 个文件（7 个顶层 JSON + 9 个 `skills/*/SKILL.md`）中，仅 `generation-evidence.json` 内容变化**（`bcecb1d4…` → `d48db7e9…`），其余 15 个逐字节一致；`candidate-helper-core.mjs` 不在该清单内，无内容级 before/after 证据（以生成后 `git status` 仍为同样 6 个 M 与该文件被重写的 mtime 为旁证）。同一步骤还重写了生成物 `src/shared/generated/courseAgentCapabilities.json`（mtime 22:08:19，相对 HEAD 1 行差异）；其**内容未变**的依据：22:08 的 check 首跑未报「打包能力资源过期」，即其内容在生成前已与生成器输出一致。

`docs/development-plan/reviews/2026-09-17-frontend-special-evidence/` 下被跟踪 PNG 在本轮 e2e 重跑中被用例自身刷新：**A1–A6**（22:14–22:15）、**C1–C5**（22:21）、**V31-nav-hierarchy / V31-nav-relaunch / V31-project-pick-confirm**（22:15）；B1–B4 仍为 09-17 23:41 原图。**这些刷新图属 09-18 重跑图，不得与 09-17 旧证据混用。**

补充口径：`R19_FRONTEND_SPECIAL_IMPLEMENTATION_PLAN.md` 相对 HEAD 的 `git diff` 是**上一会话的整篇改写（非本轮）**，本轮在该文件上的增量仅为第 3 行句尾追加——按「git diff 只显示追加」验收本文件会误判。

未写入：PM-01 保全矩阵正文、发布标签、安装器、accepted。

## 2. 当前包与下一动作

当前包：**050 retry 分支首次真实执行通过 + V05 系统剪贴板复制真实写入并完成证伪自检 + 060 等价拆分**（engineering candidate）。
下一动作（需发布授权才做）：`v1.9.0-rc.N`、Owner 审阅、PM-01 晋升（060 通过后由 Owner 决定）。S4/2.0 不提前。

## 3. verify 等价拆分（`verify = check:ai-capabilities && typecheck && test && test:e2e`，package.json 原文）

| verify 段 | 本轮实际执行 | 结果原文 | 覆盖判定 |
|---|---|---|---|
| check:ai-capabilities | `npm run check:ai-capabilities`（首跑）→ `npm run generate:ai-capabilities` → 复跑 | 首跑红：`AI 能力清单生成检查失败：` / `- 来源溯源证据过期 generation-evidence.json`；生成：`已生成 77 个 AI 能力文件；索引 16275 / 16384 字节，组件目录 available。`；复跑绿：`AI 能力清单已是最新状态；索引 16275 / 16384 字节，组件目录 available。` | **完整覆盖**（含生成器指定的修复路径；变更范围见 §1） |
| typecheck | 三个 project 分跑（各自退出码）+ 组合命令原样跑 | `TSC_RENDERER:0` / `TSC_ELECTRON:0` / `TSC_E2E:0`（`typecheck-closeout.log`）；另跑 `npm run typecheck` 本体 → `TYPECHECK_EXIT:0`（`typecheck-verify-command.log`） | **完整覆盖** |
| test | `npm test`（= pretest `build:player` + `vitest run` 全量，441 文件 = 400 unit + 41 integration；`Start at 22:09:11`） | `Test Files 11 failed | 430 passed (441)`；`Tests 28 failed | 4231 passed | 3 skipped (4262)`；`Duration 156.11s`（`npm-test-full.log`） | **已执行，但不是全绿**：11 文件与归因见 §5；其中 `localAgentHarnessV2` 簇单独复跑绿（`94 passed | 2 skipped`）；另本轮补跑 11 文件命名集（`--reporter=verbose`，23:01）：`Test Files 11 passed (11)` / `Tests 130 passed (130)` / `VITEST_EXIT:0` |
| test:e2e | 未跑全量（会拉起 Luna/DeepSeek 付费门控矩阵）；按命名规格逐条跑，两种启动路线并存：**dist 路线**（`VITE_DEV_SERVER_URL:''`）：r19FrontendSpecialPathA / PathC / r19WorkspaceNavHierarchy / r19DirectoryFirstSave / r19CourseChatScopeContinuity / r19LessonWorkspace + 两条新聊天 spec；**dev-server 路线**（现场 vite）：r19DocumentCoauthoring（红）、r19CommonEditingLayout、stabilizationCoreUsability（红）。每条日志头行均为 `Running 1 test using 1 worker`（零匹配反证） | 通过项见 §4 逐行；两条红见 §5 | **部分覆盖**：未跑 `pretest:e2e` 六步（player/check:examples/lesson-demo fixture/catalog-matrix fixture/renderer/electron）→ dist 路线跑在 22:12 构建 + 既有 fixture 之上（fixture 未刷新，已声明） |

**未跑 `npm run verify` 本体**——不做「等价即等同」的声明，只做如上逐段披露。

## 4. V01–V15 状态表（状态只取三态：本轮实跑 / 复用旧证据（本轮未重跑）/ 缺口）

**consumer 变更声明**（本轮未提交工作树）：V06 —— `src/renderer/App.tsx` 的 `onProjectSaved` 目录绑定分支（applyDirectoryBinding）；V07 —— 打开文件分流（目录会话下 `.h5lesson` 走 onOpenProject、MD 走真实 file ref）；V10 —— 编辑器焦点改为同一 `.lesson-workspace-chat` CSS 投影。下表按此口径要求这三行必须「真跑一次」或明标缺口。

| ID | 要求（行内缩写） | 状态 | 证据路径（本轮） | 本轮是否重跑 |
|---|---|---|---|---|
| V01 | 根目录与同名项目独立会话；未生成课件即可重开；准确选择旧历史 | 本轮实跑（一项缺口） | `{SCRATCH}\v-named-vitest-verbose.log`（23:01，11 文件 / 130 用例全绿）：`directoryConversationIdentity.test.ts`（4，含 `isolates same-name directory conversations and rejects cross-owner ids`、`binds first save and Save As on a directory conversation without a lesson`）+ `LessonWorkspaceShell.test.tsx`（11，含 `picks a specified older directory conversation instead of the latest`）+ `courseChatPanel.test.tsx`（58）。缺口：「未生成课件即可重开」无命名 e2e（间接实现：`useLessonWorkspaceController` 的 rememberConversation/findRememberedConversation） | 是 |
| V02 | 无预绑定首存连续；Save As 新目标不复用旧候选／句柄；失败和切根不丢稿 | **部分本轮实跑；后半＝缺口** | 首存：`r19 V02 directory conversation first-save uses the Save button without a pre-bound archive` **1 passed (7.9s)**（`r19-v-rerun-dirfirstsave.log`）；Save As 隔离片段：`r19 lesson: real PDF DOCX PPTX reading, document conflict and reopen, first save and Save As isolation` **1 passed (56.9s)**（`r19-v-rerun-lessonworkspace-r2.log`，cross-ref V08）。**「失败/切根不丢稿」本轮无证据亦无命名反例 → 缺口** | 是（部分） |
| V03 | 四向停靠／收展／全屏单实例；草稿/目标/任务连续；指针与键盘拖拽 | 部分本轮实跑 | `F08 path A: workspace root markdown edit, save, layout adjust, relaunch continuity` **1 passed (34.2s)**（`r19-v-rerun-pathA.log`；A1–A6 图 22:14 刷新）；`U06-real-layout commits rebased grouped layout and renders three readable image-description columns` **1 passed (3.4s)**（`r19-v-rerun-commonlayout.log`；产物 `output/u06-real-layout/2026-09-18T14-28-24-576Z/{facts.json,player-three-columns.png}`）；单测 `contentDockResizeSign.test.tsx`（2）本轮 verbose 绿。缺口：四向停靠/全屏单实例逐项断言未单列 | 是（部分） |
| V04 | 当前页→选择→清除回页；@ 参考与明确目标；发送后切页不改投 | 本轮实跑 | `r19 GUI chat keeps one panel configuration while selection and page requests freeze independently without launching a model` **1 passed (33.2s)**（`r19-v-rerun-courschatscope.log`）；单测 `courseChatPanel.test.tsx`（58）+ `chatComposerMenus.test.tsx`（2，含 `inserts an @ mention path without making it the edit target`）本轮 verbose 绿 | 是 |
| V05 | 消息／代码块可选择、复制、**实际粘贴**；/、@、输入法 | **复制＝本轮实跑（含证伪）；选择/实际粘贴/代码块/输入法＝缺口** | 复制：旧版规格 1 次绿 `r19-chat-copy-paste.log` **1 passed (20.9s)**（21:50）+ 强化后 2 次绿 `r19-chat-copy-paste-restored.log` **1 passed (21.0s)**（22:13）、`r19-v-rerun-chatcopypaste.log` **1 passed (21.1s)**（22:23），读回＝消息正文（未归一 152 字节＝CRLF，断言 `\r\n→\n` 归一后全等）；证伪自检：`r19-chat-copy-paste-breakcheck.log` **1 failed**，断言行 `Expected: not "SENTINEL-NOT-COPIED-1789739476814"`（同次 `toHaveText('已复制')` 是通过的——状态文案不可当证据）。权限/选择边界：`chatClipboardPermission.test.ts`（2）本轮 verbose 绿 | 是（复制）；否（选择/粘贴/输入法） |
| V06 | 简单新建／管理项目会话，最近列表即时更新，＋新建 MD／课件、标签关闭 | 部分本轮实跑 | consumer 变更点见上（App.tsx 目录绑定）。直接证据：`V31 nav hierarchy: project groups with own conversations + workspace sessions at bottom` **1 passed (22.7s)**（`r19-v-rerun-navhierarchy.log`；V31 三图 22:15 刷新）+ `r19-v-rerun-dirfirstsave.log`（首存绑定）；`LessonWorkspaceShell.test.tsx`（11）本轮 verbose 绿。缺口：「最近列表即时更新／＋新建 MD 课件／标签关闭」未逐项对应断言 | 是（部分） |
| V07 | 普通 MD AI 编辑、冲突、恢复、保留后续手改的撤回与真实保存状态 | **部分实跑 + 一条确定性红 + 门控缺口** | consumer 变更点见上（打开文件分流）。单测：`lessonDocumentAiTask.test.ts`（7）+ `documentAiTaskController.test.ts`（6，含 `starts a directory file edit without converting the ref into a lesson`）本轮 verbose 绿；e2e `r19DocumentCoauthoring.spec.ts` **确定性红**：单独复跑仍 `TimeoutError: locator.click: Timeout 10000ms exceeded`（等待 `getByRole('tab', {name:/^课件/})` 未出现，spec 第 95 行，`attribution-doccoauthoring.log` 22:38）。三条真实 AI 用例被 env 门控（`R19_FILE_AI_UNDO_RUN` / `R19_RECOVERY_LUNA_RUN` / `R19_MANUAL_APPLY`）且硬编码本机不存在的证据根 `output/r19-manual-luna/manual-parallel-20260915-170551` → 未跑，属缺口 | 是（含红） |
| V08 | 路径／附件／粘贴材料；PDF/DOCX/PPTX 各自真实读取消费 | 本轮实跑（首跑红→复跑绿） | 同 V02 的 r19LessonWorkspace 用例：首跑 **1 failed**（`getByRole('article')` 含 `circuit.pdf` 未渲染，原文在 `r19-v-rerun-lessonworkspace.log`）→ 复跑 **1 passed (56.9s)**（`r19-v-rerun-lessonworkspace-r2.log`）；另 `pptxMediaImport.test.ts`（9）本轮 verbose 绿 | 是 |
| V09 | 默认直接创作与先审稿两条路径，结果可编辑/保存/重开/预览/导出 | 复用旧证据（本轮未重跑） | `{SCRATCH}\r19-050.log`：`2 passed (4.3m)`（task-driven 2.2m / review-first 2.1m；marker `闭合电路探究` / `审阅闭合电路`；`{"id":"gpt-5.6-luna","effort":"medium","serviceTier":"priority","fast":true}`）。按 handoff §1 明令不重跑 | 否 |
| V10 | 两种编辑位置实际轻改/精修；场景状态；选区历史连续 | 部分本轮实跑 | 第二编辑位置（h5lesson 工作台）：`F08 path C: h5lesson workbench continuity, scene states, undo/redo, save/reopen, real preview` **1 passed (1.1m)**（`r19-v-rerun-pathC.log`；C1–C5 图 22:21 刷新）。**缺口**：编辑器模式焦点投影的唯一断言 `.lesson-workspace-shell[data-editor-focus=true]` 在 `r19LessonDelivery.spec.ts`（受 R19_DELIVERY_* 门控、本机缺证据根，未跑）；Path C 不点「在编辑器中打开」；「选区历史连续」未逐项对应 | 是（部分） |
| V11 | 长任务／压缩或恢复边界后用当前材料和人工稿继续；终态真实、停止后不新增提交；目录会话+冻结文件后须补一条当前目标接续 | 复用旧证据（本轮未重跑）；接续子项＝缺口 | 复用来源：`docs/development-plan/roadmap/1.9/r19-043-long-task-context.md`（节点定义）与主方案 §F07 动作五「043 旧 compact/usage 证据可复用」+ `reviews/2026-09-18-r19-f00-f08-050-060.md`（复用声明）；更细跑分日志本轮未定位（缺口） | 否 |
| V12 | Flow 新正文、源文、数学、Word 可编辑、两种预览/HTML、三 Surface 保全 | 复用旧证据（本轮未重跑） | 复用来源：`docs/development-plan/roadmap/1.9/` 下 r19-046～r19-049 四文件 + `reviews/2026-09-18-r19-f00-f08-050-060.md` 的复用声明；Word 复用 2026-09-15 COM 证据并**保持未新实测**；更细跑分证据本轮未重新定位（缺口） | 否 |
| V13 | PPTX 媒体实际播放／编辑、简单效果、保存重开／离线资源；失败不半写 | 本轮实跑（媒体片段） | `pptxMediaImport.spec.ts` 2 passed (5.8s)（`r19-v-rerun-pptxmedia.log`：video/audio 离线 HTML 播放）+ `pptxMediaImport.test.ts`（9）本轮 verbose 绿；parser 未改（复用 `r19-051.txt`）。051 全线的其余支持边界见 §5 未做 | 是（片段） |
| V14 | 原电路 revision27 或其等价当前复现被关闭 | 等价反例本轮实跑；原样本缺口 | `courseProjectHealth.test.ts`（27，含 `rejects a Flow location id written into scene.go`）本轮 verbose 绿；原样本缺失实测：`ls -d output/r19-current-teacher-luna` → `No such file or directory`（`gap-commands.log`）。**不声称旧课例已修** | 是（等价反例） |
| V15 | 同一候选的全体必选结果、源码与证据一致 | 本记录 | 本记录 + §7 诚实性审计表 + §7 后日志清单 | 是 |

## 5. 已知红与未做清单（如实保留，不修不掩）

**全量 vitest 失败种子（`npm test`，11 文件 28 例）**——本轮为 060 首次执行全量（此前各轮只跑命名子集）。失败集中在上一会话未提交实现所涉领域；按归因分四类，**未做 HEAD 对照（工作树不可动），逐条「基线 vs 本轮」归因未完成**：

- **负载/超时抖动类**：`localAgentHarnessV2.test.ts`（11 failed + 2 skipped；5s 超时 + ENOTEMPTY 清理竞态）→ 单独复跑绿（`94 passed | 2 skipped`），**判为**并发负载下抖动（仅一次单独复跑，未做重复/负载对照）；`lessonAuthoring.test.ts`（3×5s 超时）、`lessonAuthoringDesktop.test.ts`（3×5s 超时 + ENOTEMPTY）、`diagnosticLog.test.ts`（5s 超时）；
- **fixture/环境类**：`renderHostBenchmark.test.ts`（fixture 过期，需 `npm run refresh:examples`）、`coursewareCaseAssetReader.test.ts`（整例 30s 挂死）；
- **确定性断言失配类**：`courseProjectValidationDiagnostics.test.ts`（诊断码表多出 `interaction-location-reference-missing`，96 vs 95 → 直接导致 `check:preservation` EXIT:1）；`editor10ForbiddenTokens.test.ts`（`LegacyInventoryError` 4 处未登记 `createProject`：`useLessonWorkspaceController.ts:157/326`、`LessonWorkspaceView.tsx:96/465` → 与 `check:legacy-inventory` EXIT:1 同源）；`generationCapabilityWorkspace.test.ts`（prompt 原文与示例数组两处失配）；
- **UI 入口缺失类**：`lessonWorkspaceCopyOpen.test.tsx`（3 例：`Unable to find role="button" and name /原课例/`——现工作区 popover 只渲染「选择其他工作空间文件夹…」「新建工作空间」；与 `r19LessonCopyMove.spec.ts` 仍 `test.skip` 同源）。

**e2e 已知红**：

- `r19DocumentCoauthoring.spec.ts` 确定性红（V07，原文见 §4）；
- `stabilizationCoreUsability.spec.ts` 3 例红（dev-server 路线，22:23，`r19-v-rerun-stabilizationcore.log`）：同一根因——共享 `launchEditor` 第 163 行 `locator('.lesson-workspace-more > summary')` 解析到 3 个元素（strict mode violation；`LessonWorkspaceView.tsx` 渲染 3 处）；`coursewareAuthoringRunner.test.ts` 与之同源；
- `r19LessonWorkspace` 首跑红→复跑绿（判为 flake，双份原文保留）。

**未跑命令清单（原文）**：

```
npm run verify
npm run test:e2e （全量）
npm run verify:release
npx playwright test tests/e2e/r19CurrentTeacherAutomaticLuna.spec.ts   （从材料起的整课 AI）
Claude DeepSeek generate、OpenCode generate（本轮未跑任何真实模型 turn）
Word 新实测（无环境，复用 2026-09-15 COM 证据并保持未新测）
```

**其它未做**：`r19LessonCopyMove.spec.ts` 仍 `test.skip`，CopyMove UI 未恢复；V05 的「可选择/实际粘贴/代码块复制/中文输入法//、@ 菜单真实生效」未本轮实测（**缺口**）；V01/V02/V06/V10 的子项缺口见 §4；PM-01 正文未改；未签 accepted、无标签、无安装器、无固定课例 HTML；`npm-test-full.log` 中「3 skipped」的第三个 skip 未具名（默认 reporter 不列）。

**本轮剩余门原文**：`check:task-board` EXIT:0（`任务板已是最新状态。`）；`check:preservation` EXIT:1；`check:legacy-inventory` EXIT:1（后两条为上一会话遗留，超出本轮三件事范围，未修；原文 `{SCRATCH}\remaining-gates.log`）。

## 6. 模型路由（实际，不凭别名）

- 本轮两条新用例全部走**夹具 CLI**（`fixture-model`，`installChatFailureFixture` 注入），**未调用 `configureLuna()`**、未触任何真实模型额度。注意口径：夹具链验证的是**宿主候选门与提交流程真实**（真实 Electron 窗口 → 真实 `CourseChatPanel` → 真实 `GenerationTaskController` → 夹具假 CLI），不声称真实模型回合。
- V 补跑所选规格为夹具/本地类；`r19CourseChatScopeContinuity` 用例名自带 `without launching a model`。
- 050 链（复用，本轮未重跑）：Codex `gpt-5.6-luna` · medium · `serviceTier=priority`（Fast）。
- **警示**：`{SCRATCH}\r19-050-claude.log` 与 `r19-050.log` 逐字节相同（`diff` 无输出、均 692 字节、均 Luna 路由）——不得作为 Claude/DeepSeek 通道证据；`model-route.txt` 中「050 teacher chain … 2 passed (38.1s)」一行无留存日志，不作为证据引用。Claude/OpenCode 通道本轮未跑 generate。
- 未为追绿更换任何模型。

## 7. 构建准备与身份

- 本轮为 e2e 执行 `npm run build:renderer` + `npm run build:electron`：`dist-renderer/assets/main-D4Ud78Hf.js` @22:12:43（sha256 `37ec061c7cc3cfa91184dfc385cd547852d95610ceb720dfd457eaaab30391d0`）、`dist-electron/main/index.js` @22:12:46（`{SCRATCH}\e3-build.log`）；`find src -newermt "2026-09-18 22:09"` 为空 → 22:12 构建对应当前 src（生成物 `src/shared/generated/…json` 的 mtime 22:08:19 早于该时刻）。
- `dist-player/player.iife.js` mtime 22:17:41：**发生过一次 build:player 重建，未留存对应日志、归属不可考**（该时刻不在 `npm test`（22:09:11–22:11:47）与任何已记录命令的运行窗口内）；与 e2e 的 dist-renderer/dist-electron 绑定无关，单列。
- 未把 `npm run dev` 当证据。
- 文件 / 目标 / 会话身份：目录会话按规范化 `workspaceRoot`/`projectPath` + `conversationId`；打开文件不切会话；发送冻结本轮目标；应用成功**只认** `[aria-label="实际应用结果"]` 回执（本轮两条链均以此为准，050 retry 另断言「新对话=新 nativeThreadId」）。已知局限：`r19ChatApplyRetry.spec.ts` 的 `readApplyState` 沿用链式规格的历史宽松判定（`strong.includes(...)` 或撤销按钮存在性），严格性由同文件第 95 行的精确全等断言 `toHaveText('已应用课件修改')` 兜底——若未来删除该断言会静默变松。

**诚实性审计表（每条可被下一个 reviewer 用一条命令独立证伪）**：

| 主张 | 日志 | 计数校验 |
|---|---|---|
| 050 retry 完整路径 | `r19-chat-apply-retry-strengthened.log`（22:13）、`r19-v-rerun-chatapplyretry.log`（22:22） | 各含 6 条 `050 retry step` + `1 passed`，头行 `Running 1 test using 1 worker` |
| V05 复制读回=正文 | `r19-chat-copy-paste-restored.log`（22:13）、`r19-v-rerun-chatcopypaste.log`（22:23） | 各含 `V05 copy readback equals message body` + `1 passed` |
| V05 可证伪 | `r19-chat-copy-paste-breakcheck.log`（21:51:28） | 含 `1 failed` 与 `Expected: not "SENTINEL-NOT-COPIED-…"`。**21:51 的 trace.zip/error-context.md 机器产物已不存在（被后续 Playwright 运行按默认 outputDir 清空，已发生的事实）；被改坏的源码版本本身也未留存——该自检的现存证据＝runner stdout 日志原文＋还原后的双向 hash 复核**。当前 `test-results/` 内容：`.last-run.json` 与 `r19DocumentCoauthoring-cop-6e950-…/`（22:37–22:38 本轮归因运行生成的 doccoauthoring 失败制品，非 breakcheck） |
| ai-capabilities 红→绿 | `check-ai-capabilities-before.log` / `generate-ai-capabilities.log` / `check-ai-capabilities-after.log` | 原文见 §3；16 文件哈希清单 diff 仅 `generation-evidence.json` 一行 |
| typecheck | `typecheck-closeout.log` + `typecheck-verify-command.log` | `TSC_RENDERER:0`/`TSC_ELECTRON:0`/`TSC_E2E:0`；`TYPECHECK_EXIT:0` |
| 全量 vitest | `npm-test-full.log` | `11 failed | 430 passed (441)`、`28 failed | 4231 passed | 3 skipped (4262)` |
| 命名单测（本轮） | `v-named-vitest-verbose.log`（23:01） | `Test Files 11 passed (11)`、`Tests 130 passed (130)`、`VITEST_EXIT:0`，逐文件计数见本日志 |
| 边界未越 | `boundary-checks.log` | `git tag -l "v1.9.0*"` 空；PM-01 工作区干净；`git status --short | wc -l`=108（22:36 快照；含本记录为 109）；HEAD=`bc2072f77…` |

**`{SCRATCH}` 日志清单**（一行一个；未列者均为中间产物）：`closeout-identity.txt`(22:34 身份快照)、`boundary-checks.log`、`gap-commands.log`、`remaining-gates.log`、`check-ai-capabilities-before/after.log`、`generate-ai-capabilities.log`、`ai-capabilities-before/after.txt`、`ai-capabilities-status.log`、`typecheck-closeout.log`、`typecheck-verify-command.log`、`typecheck-full.log`(22:08，组合命令无退出码版)、`typecheck.log`(19:05)、`npm-test-full.log`、`v-named-vitest-verbose.log`(23:01)、`v-named-vitest-final.log`(22:36，默认 reporter 只列失败文件)、`named-tests.log`(19:05，上一轮)、`attribution-harness.log`、`attribution-doccoauthoring.log`、`r19-chat-apply-retry.log`(21:48 旧版规格)、`r19-chat-apply-retry-strengthened.log`、`r19-v-rerun-chatapplyretry.log`、`r19-chat-copy-paste.log`、`r19-chat-copy-paste-restored.log`、`r19-chat-copy-paste-breakcheck.log`、`r19-v-rerun-{dirfirstsave,navhierarchy,pathA,pathC,courschatscope,lessonworkspace,lessonworkspace-r2,doccoauthoring,pptxmedia,commonlayout,stabilizationcore}.log`、`safechatmessage-hash-after.log`、`e3-build.log`、`r19-050.log`(上一轮 2 passed 4.3m)、`r19-050-claude.log`(与前者逐字节相同)、`test-results-snapshot/`、`050-artifacts/`、`model-route.txt`、`r19-051.txt`、`r19-060.log`(19:00，上一轮)。

## 8. 结论与下一步

任务 A（050 retry 实证）：**完成**——终态失败（连续两轮没有进展，3 个 no-progress 原生回合）→ 停止不可用 → 会话可选 → 新对话 → 重发 → `[实际应用结果] 已应用课件修改`（严格全等）→ archive 正文含标记；共 3 次绿（旧版规格 1 次 21:48、强化版 2 次 22:13/22:22），且失败先行。
任务 B（V05 复制实证）：**完成**——21:50 原生绿 → 21:51 改坏必红（哨兵断言）→ 21:51:35 逐字还原 → 22:13 复核绿，源码 hash 双向复现。
任务 C（诚实 060）：**本记录**。`verify` 四段逐段披露；全量 vitest 首次执行且如实记红；剩余门、未跑命令、缺口全部在册。

建议下一步（按优先级）：① 归因并处置 §5 遗留红（CopyMove UI 入口、`.lesson-workspace-more` 选择器、诊断码表与 legacy 登记、fixture 刷新）；② V05 补零模型拖选+粘贴 spec、V10 补编辑器焦点断言（或解门控）；③ 两条付费通道 generate 与整课链（需额度与发布授权）；④ Owner 决定 PM-01 晋升与 `v1.9.0-rc.N`。

## 附录：本轮多子智能体流程（事实记录）

集成人为唯一写者，5 个并发工作流：① 执行核心（侦察 2 并发 → 执行 4 串行 → 验证 2 并发 → 起草 → 审计，判 NEEDS_FIX）；② 规划面板（3 独立视角方案 → 3 镜头交叉验证 → 整合 → 完备性批评）；③ 收口执行（1 串行执行道 + 2 只读道）；④ 执行后交叉审查（3 镜头 + 完备性批评 + 裁决，判 NEEDS_FIX）；⑤ 修订复核（本轮 verbose 命名单测 + `npm run typecheck` 本体，补齐可核出处）。涉及系统剪贴板、`dist-*` 构建与仓库写入的步骤严格串行；只读侦察/验证/审查全程并发。审查纠正的事实包括：`7ad03c75`（sha256）与 `5752f3b2`（git blob）为同一文件的两个哈希；`r19-050-claude.log` 不可作通道证据；`test-results` 机器产物丢失需如实记录；逐文件单测计数一度被错挂在本轮日志上（已改引 23:01 verbose 日志）；`candidate-helper-core.mjs` 不在哈希清单内不得称「逐字节未变」；`localAgentHarnessV2` 为 11 failed+2 skipped 而非「13 例」。本文档为修订后版本，修订依据见附录①④两个工作流的审计与裁决记录。
