# 1.9 执行记录：F00–F08 接线、050/051 与 060 工程候选

日期：2026-09-18。集成人唯一执行。状态：**可审阅 engineering candidate。未签 accepted，无 `v1.9.0-rc.N`，无安装器，无固定课例 HTML。**

工作树：`main` @ `bc2072f7`，保留全部未提交文档勘误与本批实现。未 reset/clean。任务板 Tasks: 0，未建卡。

## 1. 源码 / 未提交范围

本批在批次 A（F01）与已开始的 B 之上继续：停靠符号、指定历史、目录文件 AI、单一自动目标、/ 与 @、四阶段不再作为普通任务门、工作台/编辑器不卸载对话、系统宿主失败回执、紧凑工作台创作助手 overlay、Windows 盘符规范化。

未写入：保全矩阵 PM-01 正文、V10 Schema、安装器、发布标签。

## 2. 当前包与下一动作

当前包：F08 汇合后的 050/051/060 工程候选。下一动作（需发布授权才做）：`v1.9.0-rc.N`、Owner 审阅、PM-01 晋升。S4/2.0 不提前。

## 3. 通过 / 失败原文

通过（命名）：

- 单元 11 文件 127 项，含 `rejects a Flow location id written into scene.go`、`sends an ordinary edit without confirmed four-stage documents`、`picks a specified older directory conversation`、`keeps course and chat mounted when collapsing chat and entering editor focus`、`reports a failed system-host open`、PPTX media 9 项。
- F08 path A（33.3s）、path C（1.0m）、chat scope continuity（32.0s）、PPTX embedded video/audio 离线 HTML、r19LessonWorkspace 三格式+首存/另存（57.9s）、native directories Luna 路由（无 generate）、directory conversation Luna Fast 一轮（47.1s，修复后）。
- `npm run check:ai-capabilities`；`tsc --noEmit` 与 `tsc -p tsconfig.electron.json --noEmit`。

失败保留：

- directory CLI 首轮：`INVALID_ARGUMENT`「收到的操作参数无效。」助手未出现「核桃」。根因是 renderer 在 `process.platform !== 'win32'` 时未把 Windows 盘符小写，Zod 拒收 `frozenTarget`/`workspace`。已修 `normalizeWorkspacePath`，同规格重跑通过。
- 原电路 revision 27 样本：`output/r19-current-teacher-luna` 本机缺失。V14 用当前等价反例，不声称旧课例已修。

## 4. 文件 / 目标 / 会话身份

目录会话按规范化 `workspaceRoot`/`projectPath` + `conversationId`。打开 `.h5lesson` 不切目录会话。发送冻结本轮目标；resume 不把 `lastFrozenTarget` 当不可见锁。CLI cwd 为会话目录 realpath。

## 5. 构建准备

本批为 e2e 执行了 `npm run build:renderer` 与必要的 `npm run build:electron`。`dist-player/player.iife.js` 已存在。未把 `npm run dev` 当证据。未盲跑 `npm run verify`（会进入全量 Playwright，含大量付费门控规格）。

## 6. 实际模型路由

`r19 native directories: Luna routing without model turns`：

- Codex：`gpt-5.6-luna`，Fast = `priority`
- OpenCode：`openai/gpt-5.6-luna`（effort 支持）；`openai/gpt-5.6-luna-fast` effort unknown，未拿别名当路由
- Claude：目录无 Luna；本通道授权 DeepSeek，本批未跑 generate

directory CLI 实际使用 Codex `gpt-5.6-luna` · medium · Fast，一轮讨论「核桃」。未为追绿换模型。

## 7. 剩余门与未跑命令

未跑：`npm run verify`、`tsc -p tsconfig.e2e.json` 单独捕获、Claude DeepSeek generate、OpenCode generate、从材料起的整课 AI 代表链（旧四阶段 `r19CurrentTeacherAutomaticLuna` 不用作 050）、Word 新实测。

Word 复用 2026-09-15 COM 证据，保持未新实测。CopyMove UI 仍 skip，后端 asCopy 单测保留。PM-01 正文未改。

## 8. 命令未跑清单

```
npm run verify
npm run test:e2e
npx playwright test tests/e2e/r19CurrentTeacherAutomaticLuna.spec.ts
npx playwright test tests/e2e/r19TeacherRetainedClaudeDeepSeek.spec.ts
```
