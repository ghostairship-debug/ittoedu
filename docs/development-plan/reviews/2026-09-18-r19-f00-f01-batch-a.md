# 1.9 批次 A 执行记录：F00 写域 + F01 身份边界

日期：2026-09-18。集成人唯一执行。状态：**F00 完成；F01 已落地。** 未跑 `npm run verify`、未调用真实模型、未签 accepted。

工作树：`main` @ `bc2072f7`，保留全部未提交文档勘误（方案/合同/路线/设计说明）与三份 2026-09-18 审查文档。未 reset/clean。任务板 Tasks: 0，未建卡。

## 1. F00 包状态

| 包 | 状态 | 源码事实 |
|---|---|---|
| F00 | 已完成 | 本记录；对照 git status 与直接源码，未全仓审计 |
| F01 | 本批已修 | 按 conversationId 定位后核真实 owner；项目会话用索引而非每次 `listAll`；目录会话首存/另存不换 conversationId；打开课件不切目录会话；侧栏/切换器不再声称策略已生效。审查阻断已修 |
| F02 | 部分已修 | 上下停靠不再重复挂载 chatPane（`keeps a single chat instance when stacked`）。左右拖拽增量、全屏卸载仍待做 |
| F03 | 待修复 | 入口是 `useLessonWorkspaceController.ts`。无课件时已能按记住的 conversationId 重开；指定历史 UI/查找仍不足。有目录会话时打开 `.h5lesson` 不再切会话 |
| F04 | 待接线 | 目录聊天不传完整 `documentTarget`；`lessonDocumentAiTask` 仍限 lesson ref。047 正文编辑可复用 |
| F05 | 待修复 / 待验证 | 工作台仍接近全量编辑器；编辑器模式未按双层场景/状态打磨。046–048 工程能力可复用 |
| F06 | 待接线 / 待验证 | `open-external` 已走 `shell.openPath`；未保存↔外部改稿组合未按新合同验。无 Word 环境则复用 2026-09-15，保持未新实测 |
| F07 | 复制已接线，其余待做 | 聊天 `user-select: text` 覆盖全局 none；main 仅放行 `clipboard-sanitized-write`。未用 mock clipboard 报系统剪贴板通过。双下拉、四阶段、`/` `@` 仍待做 |
| F08 | 待验证 | 专项集成未开始 |
| 050 | 待验证 | 完整教师链未闭合；原电路 revision 27 返回入口历史失败仍在 |
| 051 | 已证实可复用 / 待验证 | 媒体核心按变化复用，本批不重做；汇合前核有效证据 |
| 060 | 待验证 | 候选门未跑。PM-01 现不改保全矩阵正文 |

## 2. 本批（A / F01）精确写域

集成人唯一写入。不派子代理改代码。

共享 / 合同：

- `src/shared/workspaceIdentity.ts`
- `src/shared/lessonWorkspace.ts`
- `src/shared/lessonDesktopContract.ts`
- `src/shared/localAgentContract.ts`
- `docs/development-plan/R19_LESSON_DOCUMENT_WORKSPACE_CONTRACT.md`（状态句）
- `docs/development-plan/R19_FRONTEND_SPECIAL_IMPLEMENTATION_PLAN.md`（真实状态）

main：

- `src/main/workspaceIdentity.ts`
- `src/main/lessonProjects.ts`
- `src/main/localAgent/lessonConversationRepository.ts`
- `src/main/localAgent/service.ts`
- `src/main/lessonDesktopService.ts`

renderer（身份/保存 consumer；Chat 只接冻结目标，不改双下拉/复制）：

- `src/renderer/App.tsx`
- `src/renderer/app/LessonWorkspaceHost.tsx`
- `src/renderer/lessonWorkspace/LessonWorkspaceShell.tsx`
- `src/renderer/lessonWorkspace/controller/useLessonWorkspaceController.ts`
- `src/renderer/lessonWorkspace/view/LessonWorkspaceView.tsx`（权限文案；打开文件不切会话的投影）
- `src/renderer/ui/chat/LessonConversationChat.tsx`（发送冻结目录目标）

测试：

- `tests/unit/directoryConversationIdentity.test.ts`（新建）
- `tests/unit/workspacePathNormalize.test.ts`（新建）
- `tests/unit/LessonWorkspaceShell.test.tsx`（打开课件不切目录会话）
- `tests/unit/lessonDesktopBinding.test.ts`（课例另存原行为保全）

不写入：Chat 双下拉、`globals.css` 复制、IPC 通道名、V9 Schema、Skill、PPTX parser、生成物、`package-lock`。

## 3. 复制故障（F00 核过，F07 才改）

1. `src/renderer/styles/globals.css` `body { user-select: none }`；`.chat-message` / `.course-chat` 未覆盖为可选择。
2. `src/main/security.ts` `setPermissionCheckHandler(() => false)` 且权限请求一律拒绝，含 `clipboard-sanitized-write`。
3. `SafeChatMessage` 使用 `navigator.clipboard.writeText`；main 无剪贴板 IPC。窗口 `sandbox: true`。
4. 不预设只改按钮。批次 B 槽 Copy 处理选择 + 真实剪贴板；禁止 mock clipboard 报通过。

## 4. 每包 1–3 条最近层命名验收

| 包 | 命名检查 |
|---|---|
| F01 | `isolates same-name directory conversations and rejects cross-owner ids`；`binds first save and Save As on a directory conversation without a lesson`；`keeps the directory conversation when opening a course file` |
| F02 | 四向拖拽/键盘（现 Path A/C 需按真实入口改）；收展换向不重挂任务 |
| F03 | 根+两项目+多会话查找/切换（NavHierarchy 按实际入口）；无课件重开指定历史 |
| F04 | DocumentCoauthoring / FileAiReopenUndo 受影响路径 |
| F05 | Path C / Delivery 去掉简洁专业后的两种位置 |
| F06 | 内部打开 MD/.h5lesson；系统宿主失败回执 |
| F07 | 新建复制场景（真粘贴）；CourseChatScopeContinuity 当前页/选择；禁止 mock clipboard |
| F08/050 | CurrentTeacher 代表链；V14 等价反例（Flow ID 写入 `scene.go` 必须失败可见） |
| 051 | 既有 pptx 媒体分项，未变则复用 |
| 060 | `npm run verify` 仅汇合时，先读生命周期 |

假阳性必须先改再跑：DirectoryConversationCli 整区「核桃」；Path B 只启动；LessonWorkspace 预 bind；Delivery 旧简洁/专业；CopyMove skip。

## 5. 三个交互样例（批次 A 起看真实窗口，本批先用身份/保存证明）

1. **空目录对话**：打开真实空文件夹 → 直接新会话 → 无 `.h5lesson` 也能发消息；关闭重开后回到**同一**会话（记住 conversationId，不是只接最近一条的替代品）。默认目标=当前目录。
2. **当前页 / 选择**：打开课件不切会话；默认当前页，点选后当前选择，清除回当前页。发送冻结身份/版本/范围。本批只接目录级冻结目标并落记录；页/选择提示与去掉双下拉在 F07。
3. **项目会话与两种编辑位置**：工作空间内指定文件夹为项目，项目会话 cwd=项目目录；工作台打开内容与「在编辑器中打开」共用工程/历史/任务。本批证明会话归属与首存/另存；布局/工具密度在 B/D。

跨轮固定目标：非必选。本批只记录**当次发送**的 `lastFrozenTarget`，不是不可见锁。

## 6. 权限（F01 诚实显示，不是 060 必过门）

Harness 对 directory 作用域 `realpath(normalizedDirectory)` 作为原生 cwd；工程会话用工程文件父目录。应用层「范围内完整 / 范围外只读」**未接线**。本批去掉侧栏已生效措辞，改为显示当前会话目录 +「权限以各 CLI 原生配置/授权为准」。不建 OS 沙箱，不把策略生效当本包必过。

## 7. F01 完成证据

源码范围：§2 清单。未提交文档勘误全部保留。

命名 vitest（`npx --no-install vitest run <file> -t <name>`）：

- `isolates same-name directory conversations and rejects cross-owner ids` 通过
- `locates a project conversation after the index is rebuilt` 通过
- `binds first save and Save As on a directory conversation without a lesson` 通过
- `unifies separators, trailing slashes and Windows case` 通过
- `keeps the directory conversation when opening a course file` 通过

连带保全：`lessonWorkspace`、`lessonDesktopBinding`（课例另存仍新建对话）、`lessonNewConversationTarget`、`lessonConversationNavigationRecords`、`lessonConversationChat` 通过。`tsc --noEmit` 与 `tsc -p tsconfig.electron.json --noEmit` 通过。

`LessonWorkspaceShell` 其余 4 条仍用已改名的「新建课例」选择器，本批未整文件改写；零匹配不算 F01 通过。

身份：目录会话 owner = workspaceRoot 或 projectPath + conversationId；工程目标 = projectId/path/revision/epoch；发送写入 `lastFrozenTarget`（当次快照，不是不可见锁）。首存绑定同一 conversationId 并升 epoch；目录另存仍同一 conversationId、升 epoch、保留 sessionIds 作历史，旧 epoch 不能再 attach。

构建准备：未跑 pretest/`build:player`。模型路由：无调用。

## 8. 未跑命令 / 剩余门 / 下一动作

未跑：`npm test`、`npm run test:e2e`、`npm run verify`、`npm start`、`npm run dev`。无模型、无真实三 CLI cwd 实测（本包不把应用层权限策略当必过门）。

剩余：页/选择冻结与双下拉删除、四阶段、`/` `@` 在 F07；指定历史管理在 F03；普通 MD AI 在 F04；050/051/060 未开始。

只读审查（explore）原判定两处阻断，已当场修：

1. `lesson-resume` 不再回退 `lastFrozenTarget`；`resolveSendFrozenTarget` 只接受本条请求或默认当前目录。新增 `does not reuse a previous frozen target as an invisible lock`。
2. `detachLesson` 不再清空目录会话；工具栏替换课件不会切走目录对话。命名用例补 `detachLesson` 后会话仍在。

下一动作：批次 B 剩余——左右拖拽方向、F03 指定历史、F04 普通 MD AI；然后 C/D/E 到 050/051/060。

## 9. 批次 B 已做（未完成 B）

- 复制：`globals.css` / `course-chat.css` 覆盖选择；`security.ts` 仅允许 `clipboard-sanitized-write`。检查：`allows only sanitized clipboard write for the main renderer`、`overrides global user-select none on chat transcript text`。未跑真实粘贴 E2E。
- 单实例：上下/堆叠停靠只保留一份 `chatPane`。检查：`keeps a single chat instance when stacked`。
