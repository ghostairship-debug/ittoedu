# 1.8 S3 复核入口

2026-09-08当前状态：本页下方原三CLI记录是其当时传输和用例的历史证据。今天新增089–104实现及普通自然语言矩阵后，103/050仍有未完成项与正在修复的实际失败；不能沿用下文历史“当前原生CLI已通过”的措辞宣称新矩阵通过。最新协调见[任务板](../TASK_BOARD.md)与[G01–G12状态](1.8-ai-assistant-gap-register.md)。S3与Owner签署仍未完成。

新增已实施工程项：Owner确认U12“场景与步骤分层”，上/下一步在场景内推进并在边界自动跨场景，场景按钮直接跳过内部步骤；同画布镜头归步骤。r18-087导航工程结束门已完成，S3新增实际复核，检查跨场景首末步、目录层级/计数、翻页笔、重播与观察恢复、动态状态保全。详见[085–087计划](../roadmap/1.8/r18-085-navigation-levels.md)。该新增语义已取得[独立工程证据](1.8-navigation-level-exit.md)，教师仍需实际复核。

当前是待验收实现，尚未达到 `v1.8.0 accepted`。用户随后报告的实时聊天、对象选择和 Flow 排版已进行修复，当前结果见 [可用性修复记录](chat-flow-usability-2026-09-07.md)：当前原生传输下Claude既有完整证据有效；Codex、OpenCode基础与组件源码连续两轮均已通过，Codex纯讨论零工程修改也通过。最新结果见[CLI调用与格式修复](1.8-cli-call-format-repair.md)。先前三 CLI 成功记录属于旧传输，不能替代当前验证。历史结果见 [开发记录](1.7-1.8-development-2026-09-07.md)。

最新视口/共享绘制反馈、试运行与整课预览缩放、组件内部源码修改及历史基线失败已按[可用性整改方案](../roadmap/1.8/USABILITY_REPAIR_PLAN.md)实现并取得真实carrier证据；工程结束门与S3签署分别记录，不用工程验证代替教师结论。

后续新增 Flow 组件浮层转正文和专业开发 Runtime.js/Manifest.json 直接编辑，定位及验收分别见该方案 R08/R09。源码工程包已去除editableCopy写门、改为共享新版本事务；Flow转换已接真实后备捕获、嵌套落点选择和同一资源历史事务。

2026-09-07路线调整：先按[三表面架构整合方案](../THREE_SURFACE_ARCHITECTURE_INTEGRATION_PLAN.md)闭合有限工程结束门r18-083，再汇合当前原生三CLI的r18-050及本版PPTX两条必选线，回到r18-060/S3教师复核。15个整合节点及后续版本依赖已同步正式DAG；1.9–2.0新增扩张暂缓，独立阻断可先修。Owner已批准D1方案A并授权本轮实施；此前Codex服务超时已由本轮真实应用复核解除。

重新复核增加：Flow顶部与长文滚动/选框、两窗口编辑/试运行/预览一致性、已支持段落对齐/行距的DOCX、Spatial正式Native与global API2作者consumer、播放zoom保答案/Component/Runtime状态、Flow正文转换、人工/AI同包修改及共享实例保全。Spatial local world/API3静态呈现不能宣称完整动态支持；高级排版不进入本次结束门。

播放zoom按Owner最新补充复核：整课缩放优先从非Component/Runtime区域的手势/键鼠发起；动态区域优先内部逻辑，仅明确无冲突时转交，未知不接管。教师控制器有独立可见的“缩放”按钮，展开缩小/倍率/放大/恢复面板；播放区域底部横向、右侧纵向边条用于平移整个观察视图，Runtime铺满画面或占用内部拖拽时仍可操作。按钮、边条与手势共用同一临时view状态，控制器/边条固定且可达，缩小/resize后校正偏移；边条不重复修改Flow正文scroll，也不能只移动Spatial world而漏掉global Runtime。从外部或按钮发起整课缩放时，控制器以外全部内容和字体同比放大，控制器不变，答案/焦点/实例进度不重置。 必须实际覆盖动态实例内部缩放、未知实例不接管、混合触点/动态焦点、全屏global Runtime下控制器按钮与横纵边条兜底、放大后四边/四角可达、缩小/resize后偏移恢复，以及100%到200%文字约×2。Flow正文scroll不重复修改；Spatial边条移动普通HUD/global Runtime，world漫游仍不带动HUD。未具备的触控/触控板硬件如实列未验。

## 本轮已取得的直接证据

- 全局教师控制器在删除其最后include位置后保留同一item，重定向到剩余位置；真实Mixed删除、一次Undo/Redo、保存重开通过。
- Flow已采用响应式CSS尺度，正文/浮层共同正逆映射。真实两窗口、长文scroll、paper拖动、旧右下全局浮层选择显现/拖动/回到文档原位、保存与离线HTML通过；仅视图定位不产生文档历史。
- 组件源码人工编辑已通过真实Electron两文件草稿、Slide/嵌套Flow/Spatial全部实例准入及后备更新、一次资源Undo/Redo、重开与Flow离线HTML。嵌套Flow正文再次发起修改、三实例同步、旧自动后备清理、当前实例独立fork及Undo也已在同一真实流程通过（54.5秒）。旧同版本writer与editableCopy写门已退出；显式单实例fork使用相同事务资源Owner。
- Claude真实原生CLI读取既有完整组件包并连续两轮修订、一次Undo/Redo、保存重开及离线HTML通过（约1.9分钟）；`../../../output/playwright/r18-source-claude/generated.h5lesson`（历史链接目标未保留）、`../../../output/playwright/r18-source-claude/generated.html`（历史链接目标未保留）与events/receipts保留。修复前OpenCode组件源码用例：Big Pickle与付费DeepSeek V4 Flash首轮格式错误、补正等待超时；付费Muse Spark 1.3能返回补正但仍生成非法JSON。OpenCode本机保存的两段原始assistant文本与宿主收到文本完全一致，已排除传输丢字；不放松解析或增加repair次数，当时未通过；随后通过正式ACP staging结果通道完成两轮源码修订，当前结果以[CLI修复记录](1.8-cli-call-format-repair.md)为准。
- 按Owner本轮授权，原生OpenCode基础固定材料基准临时使用`opencode/muse-spark-1.3`通过（总计约2.2分钟，生成到保存107.6秒）：材料引用、首轮Native生成、同会话第二轮改名、AI Undo、教师编辑/Undo、保存重开及离线HTML均通过。实际模型由CLI日志确认，`../../../output/playwright/r18-cli-opencode/generated.h5lesson`（历史链接目标未保留）、`../../../output/playwright/r18-cli-opencode/generated.html`（历史链接目标未保留）、result.json宿主回执及provider-output.json原始候选文本保留；两者requestId已对应，后者不是重建的宿主事件。模型通过仅本次进程的`COURSEWARE_OPENCODE_MODEL`覆盖，没有改用户全局CLI配置。后续Codex与OpenCode基础/源码四项真实流程均通过，当前已解除调用待验，见CLI修复记录。
- Spatial global Canvas Runtime API2作者面已接正式Owner：真实Electron双击修改第一carrier、第二carrier同一DOM保全、Undo/Redo、保存重开及UI导出离线HTML通过（29.4秒），实际截图已查看；观察/layers变化与锁定解锁的目标稳定由聚焦测试覆盖。已修复旧blob URL失效，使用Published稳定资源地址；不扩展local world/API3。产物在`../../../output/playwright/r18-076/spatial-global-runtime-authoring.h5lesson`（历史链接目标未保留）与`../../../output/playwright/r18-076/spatial-global-runtime-offline.html`（历史链接目标未保留）。
- Flow组件转正文已通过19项聚焦用例与真实Electron：缺失后备经真实Component捕获、单资源事务Undo/Redo、转回浮层、归档重开、Published点击、DOCX正文一次投影通过；另实际属性面板选择嵌套目的位置→点击转换→选中新block→一次顶栏Undo通过（12.6秒）。取消/迟到零写由单测覆盖，未冒充真实用户取消验证。
- 候选格式错误/required缺标记/第二次失败已通过真实Electron fixture，仍严格只有一次repair预算；它证明宿主处理，不代替真实CLI。
- 段落对齐/行距使用共同语义并进入可编辑DOCX。产品导出与LibreOffice实际呈现无裁剪/重叠；本机Word已安装，但独立隐藏COM只读导出在30.4秒内未完成、无PDF，已只清理本次自建进程；Word呈现仍未证实，不能将LibreOffice结果记为Word通过。

- 播放观察控制已统一到单一临时Playback View：三Published host、当前位置试运行、预览及HTML共用；旧播放fit消费点已退出，Spatial试运行容器移出作者缩放层。真实Mixed HTML在`file:`、浏览器offline且拦截HTTP的条件下通过五段操作：Native/Runtime文字与Component尺寸约×2、控制器×1、同一DOM/iframe和内部计数保全、四角边条可达、跨iframe拖条、Flow阅读位置及Spatial镜头保全、缩小/resize恢复。证据见`../../../output/r18-077-playback-evidence/mixed.html`（历史链接目标未保留）和`../../../output/r18-077-playback-evidence/offline-result.json`（历史链接目标未保留）。实际截图已查看；物理触控/触控板未验。
- 最终根工作区真实Electron整合复测通过：共享源码51.7秒、控制器/两窗口Flow几何约1.2分钟；Spatial作者/保存重开/作者缩放后试运行/观察200%实例保全/离线HTML54.1秒。15组204项定向回归和三份TypeScript配置检查通过。

工程结束门的有限范围、旧路径退出与验证限制见[整合交付记录](1.8-surface-integration-exit.md)。当前原生CLI已复核通过，S3 Owner结论继续缺失，不能宣布1.8 accepted。

## 打开聊天窗口

按 Owner 2026-09-07 决定，1.8 默认显示创作助手。在仓库 PowerShell 中正常启动编辑器：

```powershell
npm run dev
```

点击编辑器右下角的“创作助手”打开右侧聊天面板；首次对话前保存工程。已启动的旧 Electron 进程需要重新启动以加载本次修改；无需设置环境开关。

## 可直接检查的产物

这些文件是本地工程验证样例，不是对外发布物，也没有代替教师确认教学文档。

| 内容 | 可编辑工程 | 离线运行 | 复核重点 |
| --- | --- | --- | --- |
| 三页分数课 | `../../../output/playwright/r18-whole-course/generated.h5lesson`（历史链接目标未保留） | `../../../output/playwright/r18-whole-course/generated.html`（历史链接目标未保留） | 先平均分、再二分之一、最后判断；内容完整和顺序正确 |
| 分数探索组件 | `../../../output/playwright/r18-generated-component/generated.h5lesson`（历史链接目标未保留） | `../../../output/playwright/r18-generated-component/generated.html`（历史链接目标未保留） | 改等份数、增加分子、重置；图形与分数同值，缩放不丢状态 |
| 连续振动Runtime | `../../../output/playwright/r18-generated-runtime/generated.h5lesson`（历史链接目标未保留） | `../../../output/playwright/r18-generated-runtime/generated.html`（历史链接目标未保留） | 动画连续、暂停/继续、振幅和重置；教师能双击画布标题编辑 |
| 配方页 | `../../../output/playwright/r18-recipe/generated.h5lesson`（历史链接目标未保留） | `../../../output/playwright/r18-recipe/generated.html`（历史链接目标未保留） | 展开教师控制器后进入第二页；修改后的标题和概念说明保留 |
| 目录朗读组件 | `../../../output/playwright/r18-existing-component/generated.h5lesson`（历史链接目标未保留） | `../../../output/playwright/r18-existing-component/generated.html`（历史链接目标未保留） | 标题可改；重音、停顿、连读标记真实显示 |

## 教师实际复核

先看当前三页课的内容是否适合课堂，再打开两个动态样例连续操作。工程中修改一段文字或一个参数，撤销/重做后保存重开，确认内容与实际播放一致。聊天的材料引用、候选预览、应用、Stop、继续修改和人工并发提示需要实际操作；按 Owner 决定，普通内部构建已默认显示创作助手。

PPTX 新增图表、自由路径/括号/渐变、简单图示和旧公式的工程侧结果已记录在开发记录中；教师须按自己的真实源文件复核可编辑内容及排版，不能将“对象零跳过”理解为全部视觉效果无损。

Owner 结论：**尚未提供**。本文件不预填通过，不晋升保全矩阵，不创建 accepted 标签。硬件手势、Word实际呈现、Spatial local API3及已声明兼容边界仍须与教师结论一并评估；CLI本轮成功不代表未来模型候选永不失败。
