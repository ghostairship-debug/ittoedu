# 1.9 方案重建与下一位 AI 交接

日期：2026-09-18。授权范围：只重建方案，不实现。起始 HEAD 为 bc2072f777a2ca36e545979e3b67d782d105fce6；开始时已有两份未跟踪的9月18日审查/评估文档。本轮保留它们，没有产品代码、Skill、测试、构建、真实CLI/模型、提交或发布动作。

## 当前入口与决定

唯一执行入口：[1.9工作台体验与完整交付方案](../R19_FRONTEND_SPECIAL_IMPLEMENTATION_PLAN.md)。收到后续实现指令后，从该文第9节接手，沿F00–F08继续到050/051/060；无需重新讨论已确认的目录归属、当前页/选择默认或固定四稿取消。

- 会话归目录，编辑目标归文件；单一自动提示，发送冻结；@参考不是编辑目标。
- 材料路径/附件/粘贴直达，默认按任务推进，明确要求审稿才停在真实当前制品。
- 消息与代码可选择/复制，/与@调用实际能力，项目和会话管理简化。
- 工作台轻改与编辑器模式精修，共用工程/选择/历史/任务；布局不重置状态。
- 首存、另存、移动/复制、停止/恢复、文件冲突及AI部分撤回有明确合同。

## 已同步的正式规划

总纲、AGENTS开发入口、架构合同、目录/文件合同、设计说明、AI开发/长期方案和1.9索引/受影响规格已同步。共用编辑方案保留历史证据，当前派工入口移到主方案。2.0仅补继承说明，不提前开工。

正式manifest只同步9个1.9标题；13节点ID、依赖、Optional、写锁和spec定位保持。046–048正文/Flow/Word、051 PPTX核心规格不重写。已有有效证据按变化复用，U01–U10不重开整批。

## 文档检查与真实未完成项

已做规划元数据一致性、相对文件链接存在性与git diff空白检查；未运行产品测试、真实应用、构建或模型。文档检查通过不代表产品通过。

050完整教师任务链、060候选门、原revision27返回入口缺陷及新交互/修复仍未闭合。主方案V01–V15与旧规格处置表分别列明所需真实证据；静态适配、启动后取消、skip及命中用户输入不能报通过。

## 下一步

接手时保留整个当前工作树及未跟踪报告，先读主方案/任务板和实际将改Owner，确认届时源码增量。F00只校准直接差距并确定首批精确写域，不重做全仓审查；优先F01身份与F02生命周期，同时可独立处理复制等明确用户故障。按合同、实现、直接consumer和命名验证形成可运行批次。

每批只更新真实结果与一份执行记录；中断时记录基线、改动、有效验证/未验、实际模型路由、样本与下一动作。完整候选可审阅后，发布动作另按实际授权，自动化不代签Owner accepted。

## 独立审查后的文档勘误（同日后续）

独立只读审查结论：主方案骨架可执行，但权威入口仍有旧规则（路线表 1.9 行、开发计划四阶段、开发文档入口“前端完成后”、架构合同指向四稿历史锚点）。无新 Owner 决策；下列口径已写入主方案与 050/060，不问用户：

- 权限：1.9 只证明各 CLI 实际 cwd/配置/授权差异并诚实显示；不把应用层策略生效当 060 必过门，不建 OS 沙箱。
- 跨轮固定目标非必选；可见可解除或删除，禁止不可见锁定。
- V14：原样本不可达则记录缺失 + 当前等价反例（Flow ID 写入 `scene.go` 失败可见）。
- 043：复用 compact/usage；身份变更后补目录会话+冻结文件接续。
- Word：无环境则复用 2026-09-15 证据，保持未新实测。
- PM-01 等到 060 再晋升，现在不改保全矩阵正文。

勘误范围：路线 README、AI 开发计划、开发文档入口、架构合同、共用编辑 4.3/4.4、046–048/051 状态横幅、043/045/050/060、主方案、总纲 5.1、AGENTS 1.1、U01–U10 任务包、1.8 共同实施合同文首、正文合同状态句。未改产品代码、Skill、测试、任务板、manifest 节点 ID。文档层达到可交接执行（从 F00 开工）；仍不实施、不跑产品测试。
