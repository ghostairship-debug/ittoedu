# 1.9 集成与候选验收记录（版本验收未完成，本轮已停止）

> 最新状态：Owner 已将本轮收窄为有限收尾，现已完成并停止，见[有限收尾记录](2026-09-17-r19-limited-closeout.md)。下文保留历史执行过程；其中“进行中”“继续修复”不构成重新启动授权。050/060 与原课例 revision 27 返回入口仍未通过，不再为本轮追加真实模型调用或全量验证。

基线 `74a1776bd0592a9e981ef354db3d2616c90c7315`，工作目录 `D:/果铃工作台`。本记录接续 U01–U10 已集成成果，当前仍在执行 050 完整自动代表链及 060 候选验证；不是已完成版本、Owner accepted 或发布授权。没有创建提交、标签或安装器。

## 有效依赖

040–049、051 的现有行为证据按[共用编辑方案](../R19_SHARED_DOCUMENT_EDITOR_IMPLEMENTATION_PLAN.md)和[U01–U10 集成记录](2026-09-17-r19-usability-execution.md)复用。真实手动四阶段、PDF/DOCX/PPTX 各自读取与消费、真实文件共编、Flow 与可编辑 Word 数学、三 CLI 生命周期均已有对应正式记录。部分历史 raw output 在当前目录不可达，不等于从未验证；对应源码或 consumer 变化时补充相关检查。当前逐项索引在 `output/r19-final-20260917/dependency-evidence-audit.md`。

本次新增代表课例为“电路探究三种学习空间”：真实 DOCX 材料、四份阶段文件、同一工程的 Slide/Flow/Spatial 连续教学、保存重开及适用导出。工程身份始终保持 `project_j8qDiiXd47pGgFGHGOknF`。未人工修改冻结候选或用替代工程冒充原任务结果。

## 真实模型表现（尚未收口）

Codex 使用实际目录确认的 `gpt-5.6-luna`、priority；自动阶段 medium，接续 high。失败、模型准备及返工计入机器时间；测试入口在模型调用前失败的轮次单独记录。

| 阶段 | 真实结果与限制 |
|---|---|
| 自动读取与四稿 | 真实材料被采用，课例目录中形成四份真实文件。最初构建被外部 Skill 的确认停点错误阻断，零工程写入。修正内部自动模式与外部独立 Skill 的适用边界后，仅接续构建，未伪造教师确认。 |
| 自动构建接续 | 18 步正式提交，revision 19；随后候选使用非法 Slide interaction target 而停止。部分成果通过真实保存入口落盘，不能称整课完成。 |
| 首次 high 接续 | 原工程与四稿保留；20 分钟超时，三份整课候选被拒，零新提交。前两次是候选结构错误；第三次暴露整课替换后宿主仍使用旧 location 的选择连续性缺陷，已修复并有真实 Store/Undo/Redo 检查。 |
| 修复后 high 接续 | 复用同一个原生外部会话，40 分钟后到期，零新提交、保存仍为 revision 19。早期候选有不受支持的 Flow input 与悬空 Spatial 引用；后续动态准入错误扫描隐藏 Flow 的正常 deferred fallback，误归因于当前 Slide Runtime。原始失败保留。 |
| 动态准入修复后接续 | 同一外部会话约 9 分钟完成整课正式提交，revision 19→20，三表面成果已保存并重开。提交后观察因上下文预算失败，任务为 partial，不能将真实已提交成果称为未保存，也不能称教学审核通过。普通教师提示没有内部 API 或人工候选修改。 |
| 真实教师反馈修正 | 根据六页实际播放反馈接续 11.6 分钟，正式提交 revision 20→21，保存重开与四稿保持均通过；提交后观察仍超预算，任务 partial。正在按新制品复查教学行为，并按实际完整请求修正预算投影。 |
| rev21 接续启动检查 | 新回合在原生 turn 开始前失败，只有 3 个事件、0 次提交，未调用模型回合。恢复会话时追加未送达正式回执发生在请求预算投影之后，导致最终文本超限；正在以真实冻结 request 与 pending receipts 重放修复该边界。 |

截至 03:05 的只读计时快照显示：140 次 shell 调用，123 成功、17 失败；失败主要为 quoting、错误路径和探针脚本语法。五次上下文压缩约 253 秒。没有单个命令长时间卡死的证据，主要成本在历史/Schema 重复读取、压缩及实现返工。完整记录在 `output/r19-final-20260917/automatic-live/`，计时快照在 `native-timing-audit.json`；不据此宣称提速百分比。

## 本轮实际修复

- 内部自动流程采用宿主已接受的阶段文件，外部 Skill 独立使用仍遵守教师逐阶段确认。
- 整课替换提交时归一化当前选择；旧位置失效时按仍存在的物理表面/场景及新起点恢复，不再因旧 location 拒绝有效替换。
- Flow 有效草稿用于作者观察，非法/组合输入与过期草稿继续保留原恢复语义；DOM 观察身份同步绑定当前 document draft。
- 独立课件工作台占满可用容器，避免错误进入三栏工作台的第一列；保留真实课例三栏布局。
- Native 图片解码按 URL 协议设置 CORS：宿主/网络资源在赋 src 前设 anonymous，本地 file/data/blob 清除旧模式；四项针对性检查及全新进程中的完整图片替换、离线网页包、Flow/Mixed 副本检查均通过。一轮复验错误使用修复前已启动进程内的旧 embedded Player，已由实际导出 bundle 证实，单列环境失败。
- 动态准入只检查本次 location 的当前 Surface 后备，保留当前目标真实失败的拒绝。精确恢复旧冻结候选并匹配源码身份后免费重放：Switch 完整生命周期通过；Flow 随后准确报告候选自身 `SurfaceRuntime is not defined`；后续目标未执行，不计通过。证据 `replay-candidate/post-fix-admission-result.json`。
- 动态生命周期夹具补齐真实可见内容后，发现 Flow/Spatial 捕获漏过组件父级上的 capture barrier；已改为从正式稳定 owner 捕获，六项针对性检查及实际宿主拒绝/零写入命名用例通过。
- 对话菜单改为正常纵向布局，真实鼠标创建分支与范围删除通过。续轮失败按执行时间、无进展和格式修复失败分别显示，避免把无进展误报超时；对应六项单元检查及真实格式修复路径通过。
- 教师控制器作者拖动按真实可见 footprint 约束，Slide/Spatial/Flow 共用位置约束，保留作者完整尺寸及普通内容越界语义。三项针对性命令检查及完整 Wave A（2.2 分钟）通过，涵盖拖动取消、历史、保存重开和 Player 可达性。
- 根据本次真实误用补充正式能力说明：Spatial world 支持 Generated Component；静态后备必需条件只属于 Flow 正文；Native line 缺省水平，竖线由端点表达。示例经正式 Schema 解析通过，未修改渲染语义或人工修补课例。
- 公式模态编辑隔离底层 Phaser 选择回送，正式开始和取消均管理同一草稿；只有 canonical receipt 成功才关闭对话框，失败保留输入。真实 8B 取消后换层、复杂 AST、保存重开、Player、HTML/网页包/PDF/PPTX 全段通过（2.9 分钟），临时诊断已删除。
- 提交后上下文超限的实际原因是 Main 追加 58,024B 的当前课例后超出 renderer 的早期预算判断。完整回执现保留在资源中，Main 对包含课例的请求及最终 guidance/profile 包装字符串逐级摘要页面。rev21 完整请求 624,855B，实际 Main wire 141,246B、native prompt 145,677B；52 项针对性检查通过，真实下一轮仍须确认提交后接续。
- 后续冻结失败记录进一步证明最终恢复前缀也必须计入预算。修复改为显式 initial/host-feedback 阶段及同阶段 prompt factory，在真正发送入口扣除待送达回执；小回执保留具体诊断，大回执保留身份/状态/影响并提供完整文件。满 1000 项输入资源时独立回执文件仍在本轮 candidate root 内，不提高上限或删输入。首次恢复真实 payload 为 147,479B，提交后续轮为 153,933B，完整回执 156,521B 及 16 个原资源共 487,688B 均逐项保全。临时仓库/冻结 clock 重放的夹具调整有明确记录，不冒充真实模型成功；随后同一原生会话接续已完成：rev22 正式提交，完整回执 delivered，task completed，并保存重开；预算闭环获得真实模型证据。
- 同一 harness 的完整检查先为 91/94 通过，暴露的三项资源上限/诊断回归经根因修复关闭；最后六项命名检查包含两条真实冻结请求重放，均通过。真实 Flow 摘要的 undefined 可选字段在 JSON 序列化边界统一省略后再校验。投影返回类型与正式回执分开，最终 Main/Renderer 类型检查及 Main 构建通过。
- 旧 E2E 入口、组件包闭包及已移除候选字段按当前正式合同迁移。固定定位预览的裁切检查改由 Chromium 原生交集测量，保留可见性与小于 1px 的阈值，没有放宽产品边界。

## 验证事实与剩余门

完整免费单元/集成首轮为 431 文件：415 通过、16 失败；4191 用例中 4151 通过、25 失败、15 跳过。失败经具体根因逐项修复与命名重验，不能把首轮写成全绿。长时外部 runner 最终通过真实编辑、保存重开及四种导出；日志 `runner-panel-final.log`。

当前已通过的实际行为包括字体嵌入、PPTX 音视频、Published Runtime/Component 的离线/在线/网页包、组件目录与四种导出、Flow 转正文及 Undo、三表面活动草稿观察、正式宿主图片/文字/动画连续帧观察、导航层级与外部磁盘冲突。分批结果保存在 `output/r19-final-20260917/*.log`；失败、跳过与 fail-fast 未运行项保留，全部候选检查尚未闭合。

静态检查已通过主类型、合同、路线、字体与更新后的 AI 能力清单。Preservation 本轮只修正过时 Owner 路径并通过结构检查，未据此重签历史行为或 Owner 观察。Legacy inventory 在最终代码稳定后统一刷新。中途 package manager 误移动本地依赖导致的环境失败单列，已恢复原依赖；不将这些失败判为产品退化或通过。

revision 20 的实际六页试用已完成：跨 Surface 导航与 Flow 四项输入跨页保留正常，但首屏线路不闭合，开关/A/B 操作主要更改文字而缺少真实电路图形变化；预测、重试、正文推进与两次观察并列仍缺失，Spatial 尚未承担脚本要求的 A/B 操作。教师控制器与输入框存在可见布局重叠，而非透明命中层问题。原归档未被审查修改。普通教师反馈及截图见 `teacher-review/teacher-feedback.txt`、`teacher-review/ui-diagnostic/`，需由正常创作链完成修正后再验收。

免费回归已全部关闭。Wave A、Wave B（6.1 分钟）与 Flow Wave C（1.3 分钟）均已完整通过；聊天失败边界、格式修复、PPTX/内容 QA/材料库、共享组件、Flow 所见即所得、预览网络及公式 8B 等遗漏批已补齐。Wave B 将 Flow 的渲染几何与真实子像素 DOMRect 比较，仍保持 1/64px 误差界限，Slide/Spatial 继续按正式设计坐标 fit 验证。最终三类类型检查、Renderer/Main 构建、能力生成及 12 项能力消费检查、Legacy zero 均通过。

revision 21 再次实际试用确认先预测后操作、错误预测后的恢复重试、状态切换、观察历史与 Flow 四框跨页保持已通过。ROOT 复看截图及实际坐标发现实验电池未接入、闭合刀闸与导线仍有间隙，已纠正原审阅中过度肯定的图形结论。尚缺首屏及实验真实闭合、Spatial A/B 操作与两次证据保留、Spatial 正文完成入口、控制器与记录框避让及总结完整线路图。原工程未被审查改写，更新反馈和截图在 `teacher-review/rev21-teacher-feedback.txt`、`teacher-review/rev21-ui/`。

revision 22 原生修复耗时 11.5 分钟，一次非法 Spatial 载体候选被拒后模型自行修复，最终 1 次提交。真实预览确认首屏闭合及 Flow 控制器避让通过；A/恢复/B 操作与正文跳转可执行。但开关回线仍只连接电池同一端，刀闸与导线不齐；Spatial 旧大图与新小图并存，闭合状态仍有缺口，缺少全景/定位与正确因果解释门，总结第三图与文字重叠且缺少真实断点。检查未改变归档，证据见 `teacher-review/rev22-final-qa/`。因此保留已有通过项，通过自然教师反馈继续修复，不运行已知必失败的完整交付。

revision 23 原生修复与保存重开通过（8.5 分钟、1 次提交、completed/delivered）。新增 Spatial 视角按钮与共同路径输入、总结三解释框/安全确认/正文回看入口；但实际截图仍证实开关左触点前断口、Spatial 旧斜线残留与底部裁切、总结图文重叠和安全提示遮挡。本次 QA 的两个定位超时及恢复弹窗入口失败单列，不把它们直接判成数据丢失；归档未经审阅改动。附实际截图的普通教师反馈继续原生同会话修复，见 `teacher-review/rev23-teacher-feedback.txt`。交付驱动补齐当前位置试运行全路径、正文到达验证和守卫断言，E2E 类型检查通过，尚未运行最终 delivery。

复看 revision 23 输入失败截图后，已证实不是单纯定位问题：独立归档副本的 Runtime textarea 执行正常 Ctrl+A/Backspace 时，window 事件 target 被 Shadow DOM 重定向为 DIV，编辑器错误全选/删除，预览重挂并丢失内容，原磁盘归档未写入但可产生恢复草稿。已在编辑快捷键统一路由增加预览/试运行只读边界与 composedPath/嵌套焦点输入判定，尊重已处理事件与 IME；13 项聚焦单元及 Renderer 类型检查通过。更新构建后的冻结副本真实同路径复验通过：textarea 保持存在、正常输入正确，退出预览保存后工程 JSON 与原副本完全一致，revision 23 不变，控制台无错误。证据 `preview-keyboard-repro/` 与 `preview-input-diagnosis.md`；不再把两个旧 QA timeout 的原因直接写为裁切。

revision 24 同会话局部修改 1 次正式提交并保存重开（17.4 分钟，completed/delivered），先拒绝一次预测按钮不可见的候选后自行修正。三分钟实际 QA 完成 14 张截图与往返：A/恢复/B 实际间隙、全景/聚焦/返回、错误共同路径解释拒绝/正确放行、Flow 四框以及总结回看/安全入口成立；但开关横线仍停在左触点前，Spatial 电池圆角仍有接点细缝；开关 3 轮原始预测/观察离页后变为 2 条摘要；总结错误句“闭合线路不完整，灯泡不会发光”被放行。四项精确普通教师反馈附实际截图继续修正，详见 `teacher-review/rev24-final-qa/result.json`、`rev24-teacher-feedback.txt`。完整交付尚未运行。

revision 25 在同一原生会话中完成四项教师反馈修正，1 次正式提交、completed/delivered、保存重开通过。任务活跃时间 553.5 秒，含保存重开的驱动为 10.0 分钟。实际教师 QA 的 14 张截图及行为记录确认接点连续、A/恢复/B 真实断口、错误解释拒绝/正确解释放行，以及三轮原始预测/观察离页返回逐字保持；归档未被 QA 改写，控制台无错误。证据 `teacher-review/rev25-final-qa/result.json`。原材料、四阶段文件及工程身份保持。

最终交付驱动首次失败是已移除的“目录与课例”旧测试入口，已按当前固定三栏与分区折叠更新，没有删减保存/重开、材料/文档及窄窗口检查。下一轮暴露真实产品问题：内嵌试运行的教师控制器目录按整个窗口计算弹层位置，关闭按钮被编辑器工具栏遮挡。共享宿主现按实际播放区约束弹层尺寸及位置，保留滚动与控制器主体 footprint，不改写课件源码。首次修复后的 Slide 路径通过，但 Flow 仍失败：updateGeometry 展开 options 时物化了动态 canvas getter，后续视口变化导致缩放比例失真；现保持同一 options 与动态 getter。真实 FlowSurfaceHost（正式工厂→Published）调整两种视口的反例修改前失败、修改后通过；连同三种普通尺寸共 2 项真实 Chromium 测试及两类 TypeScript 检查通过。旧失败分别保留于 `final-delivery.log`、`final-delivery-current-ui.log`、`final-delivery-popup-fixed.log` 和 `controller-flow-popup-before/`，不计通过。当前 Player/Renderer 已重建，完整交付复验进行中。

截至 revision 25，按可读任务的 startedAt/lastActivityAt 计得自动材料/四稿/构建与接续修复的已知机器活跃时间 7,993.539 秒（约 133.23 分钟），包含已记录失败与返工；材料提取独立耗时及部分早期缺失记录未知。可读时间包络 17,907.568 秒，其中未分类间隔 9,914.029 秒，不能全部称用户等待。完整 time-to-correct 尚未知，不把最后 10.0 分钟当全程。详情 `final-timing-summary.json`，旧 `native-timing-audit.json` 快照保持。

目录修复后的原课例完整检查已走过当前位置试运行的前 12 次访问，包括正文进入 Flow、四框输入/保持、返回实验的三轮原始历史、Spatial A/恢复/B 和总结错误解释拒绝。第 13 次访问在总结页“回看探究记录”失败：课例 Runtime 将 Flow locationId 传给仅接受 Slide sceneId 的 goToScene，真实点击没有离开总结页。这是模型内容缺陷，不是驱动定位错误；记录 `final-delivery-flow-fixed.log`（7.7 分钟）及 `output/r19-lesson-delivery/2026-09-16T23-04-06-957Z/`。已用只含可观察结果的普通教师反馈接续同一 Luna 原生会话，medium/priority，仅修此按钮，不人工修改课例。

剩余：修复并直接验证总结回看记录入口，完成两种预览及离线交付和最终源码/制品一致性检查。免费回归、原课例此前聚焦教学 QA、回执预算与目录弹层反例已关闭，但局部通过没有覆盖这次新发现的入口。全部门通过后方可将本记录更新为 1.9 engineering candidate。源码 rc 标签等待发布授权，Owner S4 属于 2.0，均未提前晋升。

本次回看入口后续修复保留全部失败：medium 轮无提交；high 轮 revision 26 将返回改为先到实验再延时前进，独立归档副本真实点击停在实验，未到记录页；另一次教师原文包含“先跳实验…延时方案”被自动意图正则误判为计划，0 提交。产品现收窄为明确计划谓词，10 项聚焦检查及真实下一任务 intent=edit 证明修复。revision 27 随后正式提交普通可编辑按钮，但仍把 Flow ID 写入 scene.go，工程保存重开不等于行为正确。精确机器活跃时间截至该轮为 9,441.825 秒（157.364 分钟），完整 time-to-correct 未闭合。

当前根修补齐学生正文的正式精确位置导航 location.go / 可选 goToLocation：保持 V9/Published V2 及原导航 Owner，跨三表面保留守卫、历史、课程状态、取消和载体生命周期；作者 UI/工具、引用删除复制、健康诊断及同源能力一并接线。真实 Chromium 已验证三表面直达、命名 Slide 状态、无效/同位置/守卫拒绝及已卸载载体零写入。发现 revision 27 错误提交源于 project.document 仅结构校验且 Native 点击检查遗漏 globalInteractions；前者已加入正式互动健康 error 硬门，缺失场景/位置零提交反例通过，后者补全全局点击真实检查。尚待集成后原课例普通教师修正与最终三种播放器交付，不能据局部测试宣称 050/060 完成。
