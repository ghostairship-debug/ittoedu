# 1.8 三表面整合工程交付

后续新增边界：本记录只证明r18-070–084原范围。Owner随后确认场景/步骤分层，已另立[085–087计划](../roadmap/1.8/r18-085-navigation-levels.md)并追加到S3前置；连续步进在场景边界自动跳转，场景按钮是跳过内部步骤的快捷入口。新增导航已在[085–087结束记录](1.8-navigation-level-exit.md)单独验证，不扩大本记录原通过范围。

2026-09-07。r18-083有限工程结束门已闭合，1.8仍待S3验收。范围为正式DAG的r18-070–084，按Owner批准的D1方案A实施；不包含1.9扩张、安装器或accepted发布。当前工作区包含原有未提交修改，本轮没有整体提交或推送。

## 可观察结果与直接证据

| 范围 | 实现与退出旧路径 | 验证 |
| --- | --- | --- |
| U01/U02/U03，Flow布局 | 响应式正文、paper/viewport浮层用共同CSS正逆映射；退出固定letterbox及私有滚动补偿 | 根工作区真实Electron两窗口、顶部浮层、长文滚动、paper拖动、旧屏外浮层选择/拖动、保存重开、试运行与HTML；最终复测通过 |
| U04，Native/Spatial Runtime | Native内容迁入Player中立owner，作者面只留几何和编辑适配；删除Flow/Spatial简化私有绘制。Spatial global Canvas API2使用正式动态host | Native样式/路径及依赖棘轮通过；Spatial真实双击编辑、资源地址、另一carrier身份保全、Undo/Redo、归档重开、作者zoom后试运行与离线HTML通过 |
| U05，观察缩放和平移 | 单一Playback View临时状态；三host与试运行/预览/HTML共用；删除各入口播放fit重复consumer，作者fit保留；控制器与横纵边条固定 | Mixed离线file-origin五段检查通过：文字和动态内容×2、控制器×1、四角可达、跨iframe拖条、Flow scroll和Spatial camera保全、resize恢复；未知动态内部输入不接管 |
| 共享history与U09 | 三表面基础历史复用Core owner；全局控制器删除最后include位置时重定向到剩余位置，保留原item/config | 资源历史、事务、混合表面及控制器删除测试；真实删除/Undo/Redo/保存重开保全 |
| U06/U11，包源码 | 人工与AI共用新版本包修订、全部实例准入与资源事务；删除editableCopy写门、同版本异内容writer和遗漏Flow正文的target路径；显式fork同样走资源owner | 真实两文件修改→Slide/嵌套Flow/Spatial三实例后备→一次Undo/Redo→重开→Flow再次发起共享修订→fork/Undo→离线HTML；最终复测51.7秒通过。Claude实际两轮源码修订约1.9分钟通过 |
| U07，格式repair | required缺通道、非法JSON和候选错误共用唯一一次repair预算；失败和迟到结果零写入，严格解析保留 | 真实Electron失败fixture与聚焦测试；付费Muse Spark基础真实多轮通过；复杂源码两次非法JSON被拒绝，本机provider原文证实不是宿主传输截断 |
| U10，Flow组件转正文 | 属性面板选择明确嵌套落点；缺失静态后备走真实实例捕获；移除浮层/插入block/资源一次提交 | 19项聚焦用例，真实捕获/转换/Undo/Redo/反向转换/重开/Published互动/DOCX一次正文投影；实际面板点击与顶栏Undo通过 |
| E09，段落交付 | 已有对齐/行距由共同正文语义投影到作者DOM、Published、print plan和DOCX，删除私有默认值/遗漏 | 可编辑DOCX结构及LibreOffice真实呈现通过；本机Word隐藏COM未完成，Word视觉未验 |

没有新增V9 discriminator、第二Store/History、live CLI project API或Player→Renderer反向依赖。组件包与资源事务的失败/迟到检查在canonical commit前完成；CLI外部失败不冻结人工编辑。

## 候选验证

- 三份TypeScript配置检查、desktop构建、能力索引检查与DAG检查已通过。
- 最终受影响的15组204项定向回归通过；真实共享源码与两窗口几何纵切2/2通过，Spatial作者/试运行/HTML纵切通过。
- [独立Mixed HTML](../../../output/r18-077-playback-evidence/mixed.html)在Chromium `file:`、offline并拦截HTTP时五段通过；[offline-result.json](../../../output/r18-077-playback-evidence/offline-result.json)保存实际几何、实例和相机值。对应截图已查看。
- 完整单元/集成套件运行一次：299文件、2911项先通过，9文件出现13项失败及14项被前置失败跳过。逐项核对后修正旧DOM测试目标、Spatial global API2能力fixture、搬迁链接/既有PPTX锚点，并刷新实际消费当前Player的渲染示例HTML；对应9文件复测均通过（播放交互54、Runtime文字20、Flow媒体7、索引25、渲染示例7项）。未修改业务代码来迎合旧DOM断言，原颜色/相机/重播/资源与无写入断言保留。完整首跑日志在`output/r18-final-unit-suite.log`，不将首跑称为全绿，也不重复已通过的299文件。
- 复用各包有效E2E，只补受影响整合纵切；没有运行完整`npm run verify`，没有重试相同外部Codex故障或重复收费CLI请求。

## 仍需区分的限制

原整合结束时，U08原生Codex仍是已记录的外部服务超时，未按相同条件反复请求。OpenCode按本轮授权用进程级`COURSEWARE_OPENCODE_MODEL=opencode/muse-spark-1.3`完成基础两轮，未更改用户全局CLI设置；复杂源码JSON质量不足仍保留失败证据。Claude当前原生与包源码闭环通过。

物理触控/触控板未验，合成事件不代替硬件体验。Spatial local world/API3仍按既有静态能力边界；没有宣称全面动态支持。Word视觉未证实，LibreOffice通过不替代Word。既有r18-051/052 PPTX证据按原适用边界复用，不宣称全部视觉无损。

后续Codex/OpenCode基础及组件源码连续两轮均已通过，当前调用阻断解除，见[CLI修复记录](1.8-cli-call-format-repair.md)。r18-060/S3仍等待教师实际签署；[S3复核入口](1.8-S3-review.md)保留材料和未验边界。不晋升保全矩阵、不建立accepted标签。
