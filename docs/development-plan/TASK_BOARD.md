# Active Task Board

> Generated by `npm run generate:task-board`. 当前协调任务摘要（queued/active/blocked）；完成后删除卡，完成事实由实质 diff / commit 与检查结果承载。本文件不可手改。

Tasks: 6 · queued: 6

| Task | Status | Owner | Write locks | Outcome |
|---|---|---|---|---|
| [r11-029-red-gates-return](tasks/1.1/r11-029-red-gates-return.md) | queued | — | workspace-properties | HEAD `bb1f848` 上 `npm run check:preservation` 失败于 PM-08：`tests/unit/globalLayerUi.test.tsx` 的 "keeps the global Runtime inspector above a retained Spatial graph selection" 在 `src/renderer/composition/crossSurfaceCommands.ts:582` 抛出 "找不到 Spatial 路径：retained-spatial-path"，因为该夹具从未创建这条路径，而 029 之后 `setSpatialGraphSelection` 对不存在的路径 fail-loud。2026-09-03 Integrator 修订：单纯把该调用改成 `useEditorStore.setState` 绕过命令**不成立**，失败只会前移到 "Unable to find an element by: [data-testid=\"global-runtime-inspector\"]"——`src/renderer/ui/properties/SpatialPropertiesContextBuilder.ts#buildSpatialPropertiesOwner` 的 `graphTargetExists` 判定会把指向不存在路径的选择整体判为 `status: 'stale'`（"所选空间关系已失效，请重新选择。"），连全局 Runtime 检查器一起不渲染。该 fail-loud 是 026/029 的刻意产品行为，不在本卡范围内改动；本卡改为让夹具持有一条**真实**路径，使测试名声称的不变量（保留的 Spatial 图选择不得压掉全局 Runtime 检查器）真正被证明。第二道红门：`npm run check:legacy-inventory` 报 7 项未登记观察，其中两项是 029 新增文件对 LEG-011 目标 `src/shared/projectTypes.ts` 的 import：`src/renderer/authoring/spatialAuthoringIntents.ts:1` 与 `src/renderer/ui/workspaces/SlideWorkspaceConnector.tsx:5`；这四个类型（`ShapeType`、`TextRunStyle`、`TextRun`、`FormulaAstNode`）的正式 owner 是 `src/shared/contracts/native-v1/types.ts`，`projectTypes.ts` 只是转发，改 import 来源是类型等价的。规则与交接模板见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。 |
| [r11-032-player-capture-seam](tasks/1.1/r11-032-player-capture-seam.md) | queued | — | published-producer | `src/renderer/export/playerCapture.ts:1` 仍 `import type { PlayerApp }`，并保留以 `PlayerApp` 为参数的旧捕获函数（`waitForPlayerScene`:72、`waitForPlayerLoaderIdle`:84、`waitForPlayerCaptureReady`:118、`phaserSnapshot`:728、`capturePlayerStage`:746），它们在 src 中零调用，只被 `tests/unit/playerCapture.test.ts` 的旧成功用例消费。这是台账 LEG-002 中最后一条产品 consumer，也是 `npm run check:legacy-inventory` 报出的两项观察（`playerCapture.ts:1`、`playerCapture.test.ts:2`）。完整步骤与表格见 [规格执行版](../../roadmap/1.1/r11-032-player-v2-only-entry.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。本卡是纯删除，没有红测试；以结构事实代替红→绿。规格中的 Flow-only 项不在本卡范围。 |
| [r11-034-app-project-lifecycle-module](tasks/1.1/r11-034-app-project-lifecycle-module.md) | queued | — | app-save-recovery, workspace-properties | `src/renderer/app/useCourseProjectLifecycle.ts:157–162` 的 `sameProjectIdentity` 只比较 `projectId`。重开同一个工程文件后，进行中的保存会用旧路径 `acknowledgeSaved` 新会话（:384、:393），进行中的恢复写入会把旧会话内容写成恢复副本（:191）。`sessionGeneration` 不能区分会话（新会话始终代次 0，见 `courseAuthoringSession.ts:229`）；正确信号是 hook 自己的 `loadEpochRef`（:221）。证据：先按规格执行版第 1 步写 `tests/unit/useCourseProjectLifecycle.test.tsx` 的两条测试并运行，两条必须红。完整 arrange/act/assert 与改动表见 [规格执行版](../../roadmap/1.1/r11-034-app-project-lifecycle-module.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。 |
| [r11-035-app-delivery-module](tasks/1.1/r11-035-app-delivery-module.md) | queued | — | published-producer, workspace-properties | `src/renderer/app/useCourseDelivery.ts` 三处缺陷：`continuePreflightExport`（:451–483）核对 identity 后调用的四个 emit 各自用 `requireSnapshot` 重读当前快照（:253、:271、:288、:311），`emitHtml` 还先 `await prepareBundledFontEmbedding()`（:252），期间的文档修改会被导出却未经预检；`locatePreflightItem`（:485–489）不核对 identity；`exportLargeHtmlAsWebPackage`（:516–519）直接 `emitWebPackage()`，跳过 `web-package` 目标的预检。证据：先按规格执行版第 1 步在 `tests/integration/courseExportPreflightApp.test.tsx` 新增三条测试并运行，三条必须红。完整 arrange/act/assert、按钮文案与改动表见 [规格执行版](../../roadmap/1.1/r11-035-app-delivery-module.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。 |
| [r11-036-app-import-input-modules](tasks/1.1/r11-036-app-import-input-modules.md) | queued | — | editor-store-history, workspace-properties | `src/renderer/app/useMediaImport.ts` 在文件对话框之后核对了 identity（:344、:430、:482），但 `await prepareSelection(...)` 解码（:345、:431、:483）之后直接提交：`commitCandidateMedia`（:374、:441、:512）、`placeImageNodes`/`placeVideoNodes`（:391、:529）、`importSounds`（:455）都不带目标修订，解码期间的文档变化会被写进新文档；`replace` 路径在 `readImageDimensions`（:316）后也没有再核对。`MediaImportIdentity`（:32–35）与 `App.tsx:68–72` 的 `captureCourseIdentity` 只有 `projectId`、`revision`，切换页面不改变修订号，`add` 模式会放到新页面。证据：先按规格执行版第 1 步写 `tests/unit/useMediaImport.test.tsx` 的两条测试并运行，两条必须红。完整 arrange/act/assert 与改动表见 [规格执行版](../../roadmap/1.1/r11-036-app-import-input-modules.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。 |
| [r11-037-w0-spatial-session-generation](tasks/1.1/r11-037-w0-spatial-session-generation.md) | queued | — | editor-store-history | `npm run check:preservation` 在 PM-08 修复后失败于 PM-13：`tests/integration/courseMediaLibraryImportVerticalSlice.test.ts` 的 `it.each` 中只有 `mixed-spatial` 变体失败（`expected 1 to be greater than 1`，断言 `undoneSession.token.generation` 应大于 `committedSession.token.generation`），`slide-heavy` 与 `flow-heavy` 通过。该失败在 HEAD `bb1f848` 上已用 `git stash` 复现，早于并独立于 `r11-029` 返工卡。根因：`src/renderer/store/slices/spatialAuthoringSlice.ts:225–243` 的 `spatialCourseAuthoringSessionAfterResult` 一律经 `switchCourseAuthoringLocation` 计算下一个会话，而该函数（`src/renderer/authoring/courseAuthoringSession.ts:314–321`）在 `locationId` 与 `surfaceType` 未变时**保持**代次；资源型撤销/重做恰好不改变位置，因此代次不递增。Slide 在 `slideAuthoringSlice.ts:178–195` 显式用 `generation + 1` 构造 token，Flow 在 `flowAuthoringSlice.ts:374` 调用 `courseSessionAfterSurfaceHistory`（`src/renderer/store/editorStoreKernel.ts:195–222`，专为此设计：收到 `transactionStep` / `resourceTransition` / `sidecarDirection` 时递增代次），Spatial 两条都没走。本卡是 `r11-037` 的 W0 前置缺陷修复，不做任何模块化搬迁；W3 迁移 Spatial persist 时必须保留本卡建立的行为。规则与交接模板见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。 |
