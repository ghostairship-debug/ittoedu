# Active Task Board

> Generated by `npm run generate:task-board`. 当前活跃任务摘要（queued/active/blocked）；完成的任务随实质提交删除卡文件，完成事实由 product commit 承载。本文件不可手改。

Tasks: 1 · queued: 1

| Task | Status | Owner | Risk | Hotspot | Outcome |
|---|---|---|---|---|---|
| [host-unify-authoring-preview](tasks/host-unification/host-unify-authoring-preview.md) | queued | — | S2 | workspace-properties, published-producer, editor-store-history | 教师在 **Slide** 编辑画布上看到的文本排版、字体与远程素材结果，与"当前位置试运行 / 整课预览 / 网页导出"一致——今天不一致，因为两侧是两套渲染实现。**本卡只统一 Slide 一个 Surface**：Flow 编辑仍是 `src/renderer/ui/FlowWorkspace.tsx` 的 DOM 作者面（`Workspace.tsx:1422` 的 `canvasMode === 'edit'` 分支，本来就没有 blob 宿主），Spatial 编辑仍是 `SpatialLocationWorkspace` 的专用 DOM/SVG 作者面；两者继续保留各自作者面，本卡不改、不冒充"三表面统一"，它们的作者面是否也该统一须另做技术论证与拆卡。Slide 编辑画布现状：`blob:` iframe + V8 `ExportPayload` + Phaser `PlayerApp` + 自研 canvas 断行（`Workspace.tsx` 的 `SlideLocationWorkspace` runtime-preview effect 内 `createRuntimePreviewPayloadResources` → `buildStandaloneHtml` → `createRuntimePreviewBlobResources`；blob 实际创建在 `src/renderer/preview/runtimePreviewDocument.ts:43-52`；payload 由 `src/renderer/store/editorStore.ts` 的 `projectCandidatePreviewDocument` 构造，`schemaVersion: 8` 在其返回对象内；`src/renderer/preview/runtimePreviewPayload.ts:95` 组装 `ExportPayload`；`src/player/index.ts:23-39` 起 `PlayerApp`）。试运行：主 renderer + Published V2 DOM 宿主 + 浏览器断行（`src/renderer/ui/coursePlayerTryRun.ts:66-139` 的 `mountPublishedCourseTryRun`，`:115` `createPublishedCourseSession`，`:119` `session.mount`；`src/player/surfaces/slide/SlidePublishedAdapter.ts:173` 委派 `paintPublishedNativeText`，换行实为 `src/player/surfaces/publishedNativeText.ts:11` 的 `white-space: pre-wrap`）。**最硬的两条当前失败证据**：(1) **自动缩小字体在 Published 侧根本没实现，教师直接可见**——`src/shared/textLayout.ts` 有 shrink（`fitFontSize` `:287-309`，`:293` 是 guard，`:294-308` 是逐 1px 降到下限 8px 的收缩循环）、auto-height（`:380-393`）与裁切（`:426` `context.clip()`）三套实现，而 `publishedNativeText.ts` 全文只设样式与 span——`overflow: hidden`（`:10`）、`white-space: pre-wrap`（`:11`）、`fontSize`（`:13`）——没有任何测量或收缩；`src/player/` 下只有 `src/player/renderNode.ts:53` 一处 import `textLayout`，`src/player/surfaces/**` 零处。同一个设为自动缩小的文本框，Slide 编辑画布会缩小、试运行/整课预览/网页导出直接溢出裁掉。(2) **教师用界面上的"显示范围"就能让 Slide 编辑画布起不来，命中不变量 11**——完整操作序列：打开一份同时含 Slide 与 Flow location 的工程（`tests/fixtures/course-project-v9/mixed.h5lesson` 即是，三种 location 各一个）→ 选中一个全局层元素 → 属性面板"显示范围"选"仅所选" → 只勾选 Flow 页（`src/renderer/ui/PropertiesTab.tsx:2040` 的复选框列的是 `document.locations` 全表，含 flow-block / spatial-camera；`src/renderer/course/globalLayerCommands.ts:567-583` 的 `validateLocationVisibilitySpec` 只校验 location 存在且非空，不要求其中有 slide-scene，所以该状态是合法 V9）→ 切回任意 Slide 页。此时 `editorStore.ts` 的 `locationIdsToSceneIds`（`:824-832`，`:830` 只映射 `slide-scene`、`flatMap` 静默丢弃 Flow/Spatial）把 `locationIds` 映成 `[]` 再喂给 `projectCandidatePreviewDocument` 的全局图层 `sceneIds`，V8 `globalLayerVisibilitySchema`（`src/shared/projectSchema.ts:575-585`）在 `mode !== 'all'` 且 `sceneIds` 为空时拒绝，iframe 内 `src/player/payload.ts:23` 抛"课件 Payload 缺少必要数据或版本不受支持"；`Workspace.tsx` 全文无 `safeParse`，当年设计的降级提示（PRJ-01）未实现，所以没有可见降级只有失败。**2026-08-26 已按上述三步在 `mixed` 夹具上实测**：V9 `safeParse` 接受该文档、`locationIdsToSceneIds` 产出 `[]`、V8 以"按场景控制全局元素时至少需要一个场景 ID"拒绝——三步全部成立。注意这不是"只含 Flow location 的工程"，那种工程会被 `Workspace.tsx:1461-1485` 的 session 路由分到 `FlowLocationWorkspace` / `SpatialLocationWorkspace`，根本走不到 `projectCandidatePreviewDocument`，写成那样的复现是假红。另两条同源差异：内置字体只注入顶层文档（`src/renderer/main.tsx:27` → `src/shared/fonts/installBundledFontFaces.ts:19-28`，把 `@font-face` 塞进 `<style id="bundled-font-faces">`，生产仅此一个调用点、从不传预览文档），`224be20` 把 `STIX Two Math` 提到公式字体链首位后编辑画布显示回退字体，且一旦本会话点过导出、字节被 `src/renderer/export/bundledFontEmbedSourceFetch.ts` 的闭包缓存后表现翻转（同机同课件导出前后不同）——**该症状已由 `9c39f69` 单独修复（给预览 blob 按需补 `@font-face`），本卡不重复实现**；但那是按需子集而非等价宿主（`tests/unit/workspacePreviewBundledFonts.test.tsx:256` 反向钉死"无声明即零 face"，而顶层文档始终装齐全部内置族），只消除症状、不消除宿主分裂，也不触及自动缩小与远程素材两条。远程素材：编辑画布用传进 iframe 的本地字节（`runtimePreviewPayload.ts:86-93`，placeholder + transferable ArrayBuffer）、试运行走远端 URL（`coursePlayerTryRun.ts:29`）。台账 LEG-002（`active-debt`）已补记这三条教师可见后果。Owner 于 2026-08-26 依修订后的不变量 15（已完成论证的架构性偏差 + Owner 决定）重新立项本根治方案；2026-08-25 曾批过同方向方案 A 与 10 人日估工，那是"三表面一次跑完"的量级，**不适用于本卡**：本卡限定 Slide 单一纵切、拆两个可分别 revert 的提交，量级重估为 2–4 个工作日，其后是否继续扩面由届时的真实结果决定。 |
