# r11-036-app-import-input-modules 媒体导入：解码之后再核对 identity，identity 加入当前位置

- Status / Owner: queued /
- Outcome / Evidence: `src/renderer/app/useMediaImport.ts` 在文件对话框之后核对了 identity（:344、:430、:482），但 `await prepareSelection(...)` 解码（:345、:431、:483）之后直接提交：`commitCandidateMedia`（:374、:441、:512）、`placeImageNodes`/`placeVideoNodes`（:391、:529）、`importSounds`（:455）都不带目标修订，解码期间的文档变化会被写进新文档；`replace` 路径在 `readImageDimensions`（:316）后也没有再核对。`MediaImportIdentity`（:32–35）与 `App.tsx:68–72` 的 `captureCourseIdentity` 只有 `projectId`、`revision`，切换页面不改变修订号，`add` 模式会放到新页面。证据：先按规格执行版第 1 步写 `tests/unit/useMediaImport.test.tsx` 的两条测试并运行，两条必须红。完整 arrange/act/assert 与改动表见 [规格执行版](../../roadmap/1.1/r11-036-app-import-input-modules.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。
- Write scope: `src/renderer/app/useMediaImport.ts`；`src/renderer/App.tsx` 仅 `captureCourseIdentity` 与其 import；新建 `tests/unit/useMediaImport.test.tsx`。禁止 Store、`assetManager.ts`、`mediaBatch.ts`、`v9AssetAdapter.ts`、`useComponentLibrary.ts`、`useEditorKeyboardRouter.ts`、inventory；除测试文件外不允许新建任何名字。两条测试有任一在修改前就通过即停止。
- Write locks: editor-store-history, workspace-properties
- Acceptance: 两条新测试红→绿证据完整；`grep -c "assertFreshIdentity(" src/renderer/app/useMediaImport.ts` 为 8；`grep -cE "left\.locationId === right\.locationId" src/renderer/app/useMediaImport.ts` 为 1；`grep -c "locationId: selectActiveCourseLocationId" src/renderer/App.tsx` 为 1；`grep -c "assertFreshIdentity(" src/renderer/app/useComponentLibrary.ts` 仍为 4；`grep -cE "useEditorStore|from '\.\./store/" src/renderer/app/useMediaImport.ts` 为 0；提交信息 `fix(app): recheck media import identity after decoding and include the active location (r11-036)`。
- Validation: `npx vitest run tests/unit/useMediaImport.test.tsx tests/unit/assetTransactions.test.ts tests/unit/editorActionRouting.test.ts tests/integration/mixedCrossSurfaceHistory.test.tsx tests/unit/readModelBoundary.test.ts tests/integration/courseMediaLibraryImportVerticalSlice.test.ts tests/integration/imageReplacementVerticalSlice.test.ts`；`npm run typecheck`；`git diff --check -- src/renderer/app/useMediaImport.ts src/renderer/App.tsx tests/unit/useMediaImport.test.tsx`
