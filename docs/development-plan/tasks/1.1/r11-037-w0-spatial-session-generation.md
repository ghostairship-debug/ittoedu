# r11-037-w0-spatial-session-generation W0：Spatial 资源型撤销/重做必须递增作者会话代次

- Status / Owner: queued /
- Outcome / Evidence: `npm run check:preservation` 在 PM-08 修复后失败于 PM-13：`tests/integration/courseMediaLibraryImportVerticalSlice.test.ts` 的 `it.each` 中只有 `mixed-spatial` 变体失败（`expected 1 to be greater than 1`，断言 `undoneSession.token.generation` 应大于 `committedSession.token.generation`），`slide-heavy` 与 `flow-heavy` 通过。该失败在 HEAD `bb1f848` 上已用 `git stash` 复现，早于并独立于 `r11-029` 返工卡。根因：`src/renderer/store/slices/spatialAuthoringSlice.ts:225–243` 的 `spatialCourseAuthoringSessionAfterResult` 一律经 `switchCourseAuthoringLocation` 计算下一个会话，而该函数（`src/renderer/authoring/courseAuthoringSession.ts:314–321`）在 `locationId` 与 `surfaceType` 未变时**保持**代次；资源型撤销/重做恰好不改变位置，因此代次不递增。Slide 在 `slideAuthoringSlice.ts:178–195` 显式用 `generation + 1` 构造 token，Flow 在 `flowAuthoringSlice.ts:374` 调用 `courseSessionAfterSurfaceHistory`（`src/renderer/store/editorStoreKernel.ts:195–222`，专为此设计：收到 `transactionStep` / `resourceTransition` / `sidecarDirection` 时递增代次），Spatial 两条都没走。本卡是 `r11-037` 的 W0 前置缺陷修复，不做任何模块化搬迁；W3 迁移 Spatial persist 时必须保留本卡建立的行为。规则与交接模板见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。
- Write scope: 只允许修改 `src/renderer/store/slices/spatialAuthoringSlice.ts`：在 `persistSpatialResult` 中，对已由 `spatialCourseAuthoringSessionAfterResult` 得到的 `nextCourseAuthoringSession`，当 `extra.transactionStep`、`extra.resourceTransition` 或 `extra.sidecarDirection` 任一存在时，改用现有 `courseSessionAfterSurfaceHistory(...)` 得到递增代次的会话（import 自 `../editorStoreKernel`，Flow slice 已是同一用法）。禁止修改 `courseAuthoringSession.ts`、`editorStoreKernel.ts`、Slide/Flow slice、`crossSurfaceCommands.ts`、任何测试的断言、inventory；不允许新建任何文件、类型或函数（只允许新增一条 import 与一处局部赋值）。若修复需要改 `switchCourseAuthoringLocation` 语义或 `courseSessionAfterSurfaceHistory` 签名，停止交接。
- Write locks: editor-store-history
- Acceptance: `mixed-spatial` 变体由红转绿且 `slide-heavy`、`flow-heavy` 仍绿（红→绿两次输出都粘贴，红态用 `git stash` 或改动前的运行记录）；`npm run check:preservation` 通过（PM-08 与 PM-13 同时为绿）；`grep -c "courseSessionAfterSurfaceHistory" src/renderer/store/slices/spatialAuthoringSlice.ts` 为 2（1 处 import + 1 处调用）；`grep -c "generation + 1" src/renderer/store/slices/spatialAuthoringSlice.ts` 为 0（不得自行拼 token）；提交信息 `fix(spatial): bump the authoring session generation on resource-bearing undo and redo (r11-037 W0)`。
- Validation: `npx vitest run tests/integration/courseMediaLibraryImportVerticalSlice.test.ts tests/integration/imageReplacementVerticalSlice.test.ts tests/unit/assetReferences.test.ts tests/unit/crossSurfaceResourceHistory.test.ts tests/integration/mixedCrossSurfaceHistory.test.tsx tests/unit/spatialProductIntegration.test.tsx tests/unit/spatialCameraSession.test.tsx`；`npm run typecheck`；`npm run check:preservation`；`git diff --check -- src/renderer/store/slices/spatialAuthoringSlice.ts`
