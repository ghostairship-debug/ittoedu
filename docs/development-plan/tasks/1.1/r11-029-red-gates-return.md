# r11-029-red-gates-return 029 返工：修复检查点上的两道红门

- Status / Owner: queued /
- Outcome / Evidence: HEAD `bb1f848` 上 `npm run check:preservation` 失败于 PM-08：`tests/unit/globalLayerUi.test.tsx` 的 "keeps the global Runtime inspector above a retained Spatial graph selection" 在 `src/renderer/composition/crossSurfaceCommands.ts:582` 抛出 "找不到 Spatial 路径：retained-spatial-path"，因为该夹具从未创建这条路径，而 029 之后 `setSpatialGraphSelection` 对不存在的路径 fail-loud。2026-09-03 Integrator 修订：单纯把该调用改成 `useEditorStore.setState` 绕过命令**不成立**，失败只会前移到 "Unable to find an element by: [data-testid=\"global-runtime-inspector\"]"——`src/renderer/ui/properties/SpatialPropertiesContextBuilder.ts#buildSpatialPropertiesOwner` 的 `graphTargetExists` 判定会把指向不存在路径的选择整体判为 `status: 'stale'`（"所选空间关系已失效，请重新选择。"），连全局 Runtime 检查器一起不渲染。该 fail-loud 是 026/029 的刻意产品行为，不在本卡范围内改动；本卡改为让夹具持有一条**真实**路径，使测试名声称的不变量（保留的 Spatial 图选择不得压掉全局 Runtime 检查器）真正被证明。第二道红门：`npm run check:legacy-inventory` 报 7 项未登记观察，其中两项是 029 新增文件对 LEG-011 目标 `src/shared/projectTypes.ts` 的 import：`src/renderer/authoring/spatialAuthoringIntents.ts:1` 与 `src/renderer/ui/workspaces/SlideWorkspaceConnector.tsx:5`；这四个类型（`ShapeType`、`TextRunStyle`、`TextRun`、`FormulaAstNode`）的正式 owner 是 `src/shared/contracts/native-v1/types.ts`，`projectTypes.ts` 只是转发，改 import 来源是类型等价的。规则与交接模板见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。
- Write scope: `tests/unit/globalLayerUi.test.tsx` 只改该 `it` 的 arrange 段：在 `createNewSpatialProject()` 之后调用两次公开动作 `useEditorStore.getState().addTextNode()` 造出两个真实 world 图层项，再读 canonical 文档并 `structuredClone`，在克隆的 `spatial-2d` surface 的 `world.paths` push 一条 `{ id: 'retained-spatial-path', name: '保留路线', layerItemIds: [两个真实 world 项的 layerItemId] }`，其余 arrange（global runtime 项、`loadCourseProject`、`activateCourseLocation`、`setEditingScope('global')`、原 `setSpatialGraphSelection` 调用、`setState` 与 `toMatchObject`）与三条 `expect` 断言一字不动；`src/renderer/authoring/spatialAuthoringIntents.ts:1` 与 `src/renderer/ui/workspaces/SlideWorkspaceConnector.tsx:5` 的 import 来源改为 `shared/contracts/native-v1/types`（相对路径按文件位置换算，导入的类型名不变）。禁止改产品逻辑、`SpatialPropertiesContextBuilder.ts`、`crossSurfaceCommands.ts`、inventory、其余 5 项观察所在文件；不允许新建任何名字。若 `addTextNode` 造不出 world 项，或路径 push 后 `setSpatialGraphSelection` 仍抛错，停止交接。
- Write locks: workspace-properties
- Acceptance: `npm run check:preservation` 的 PM-08 证据命令通过（`npx vitest run tests/unit/courseLayerComposition.test.ts tests/unit/globalLayerUi.test.tsx tests/unit/sceneStateUi.test.tsx` 全绿）。2026-09-03 Integrator 修订：该门整体通过**不属于**本卡验收——PM-08 变绿后暴露出 HEAD `bb1f848` 上就已存在的 PM-13 失败（`tests/integration/courseMediaLibraryImportVerticalSlice.test.ts` 的 `mixed-spatial` 变体，`expected 1 to be greater than 1`），已用 `git stash` 在同一 HEAD 上复现确认与本卡无关，交由 `r11-037-w0-spatial-session-generation` 修复；本卡只承担把该门从 PM-08 推进到 PM-13。`npm run check:legacy-inventory` 输出的未登记观察恰好剩 5 项（`src/renderer/export/playerCapture.ts:1`、`tests/unit/architectureDependencyRatchet.test.ts:596`、`tests/unit/coursePptxExport.test.ts:71` 两项、`tests/unit/playerCapture.test.ts:2`），原样粘贴；`grep -c "shared/projectTypes" src/renderer/authoring/spatialAuthoringIntents.ts src/renderer/ui/workspaces/SlideWorkspaceConnector.tsx` 均为 0；该 `it` 仍调用 `setSpatialGraphSelection` 且三条 `expect` 未改；提交信息 `test(spatial): give the retained graph selection a real path and use native-v1 type owners (r11-029 return)`。
- Validation: `npx vitest run tests/unit/globalLayerUi.test.tsx tests/unit/courseLayerComposition.test.ts tests/unit/sceneStateUi.test.tsx tests/unit/spatialProductIntegration.test.tsx tests/unit/v9SlideProductIntegration.test.tsx`；`npm run typecheck`；`npm run check:preservation`；`npm run check:legacy-inventory`；`git diff --check -- tests/unit/globalLayerUi.test.tsx src/renderer/authoring/spatialAuthoringIntents.ts src/renderer/ui/workspaces/SlideWorkspaceConnector.tsx`
