# r11-032-player-capture-seam Player 捕获缝：删除 playerCapture 中最后的 PlayerApp 引用

- Status / Owner: queued /
- Outcome / Evidence: `src/renderer/export/playerCapture.ts:1` 仍 `import type { PlayerApp }`，并保留以 `PlayerApp` 为参数的旧捕获函数（`waitForPlayerScene`:72、`waitForPlayerLoaderIdle`:84、`waitForPlayerCaptureReady`:118、`phaserSnapshot`:728、`capturePlayerStage`:746），它们在 src 中零调用，只被 `tests/unit/playerCapture.test.ts` 的旧成功用例消费。这是台账 LEG-002 中最后一条产品 consumer，也是 `npm run check:legacy-inventory` 报出的两项观察（`playerCapture.ts:1`、`playerCapture.test.ts:2`）。完整步骤与表格见 [规格执行版](../../roadmap/1.1/r11-032-player-v2-only-entry.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。本卡是纯删除，没有红测试；以结构事实代替红→绿。规格中的 Flow-only 项不在本卡范围。
- Write scope: `src/renderer/export/playerCapture.ts`、`tests/unit/playerCapture.test.ts`。禁止 `src/player/**`、其他 export 文件、inventory、Published wire；不允许新建任何名字。任一待删函数在 src 中仍有调用即停止。2026-09-03 执行时范围发现：V2 函数（`firstPublishedCapturableSurfaceId` 起，当前 :918 之后）不引用本文件前 916 行的任何符号，因此删掉 PlayerApp 那一簇会让 `waitUntil`(:19)、`withTimeout`(:52) 与整个 DOM 绘制簇（`numericCss` :143 至 `stageHasCompositedDom` :726，唯一调用方是 `capturePlayerStage`）一并变成不可达代码。本卡授权删除该传递闭包，判据固定为：删除后在本文件与全仓 `grep -rlE "\bNAME\b" src tests` 均为 0；`settleCaptureFrames`(:98–111，被三处 V2 函数调用) 与 `firstPublishedCapturableSurfaceId` 必须保留；同时移除仅供已删代码使用的 import。禁止在本卡顺手改任何 V2 函数体。
- Write locks: published-producer
- Acceptance: `grep -c "PlayerApp" src/renderer/export/playerCapture.ts` 为 0；`grep -c "PlayerApp" tests/unit/playerCapture.test.ts` 为 0 且 :356、:420 两个拒绝用例仍存在并通过；`npm run check:legacy-inventory` 输出中不再出现 `playerCapture.ts:1#target-reference:LEG-002-player-app` 与 `playerCapture.test.ts:2#target-reference:LEG-002-player-app`，其余观察项原样粘贴；提交信息 `refactor(export): drop legacy PlayerApp capture functions and type import (r11-032)`。
- Validation: `npx vitest run tests/unit/playerCapture.test.ts tests/unit/coursePrintArtifacts.test.ts tests/unit/renderPptxComponentSnapshots.test.ts tests/unit/renderPptxRuntimeSnapshots.test.ts tests/unit/publishedCourseProtocol.test.ts`；`npm run typecheck`；`npm run check:legacy-inventory`；`git diff --check -- src/renderer/export/playerCapture.ts tests/unit/playerCapture.test.ts`
