# r11-035-app-delivery-module Delivery：预检与发出用同一份快照，过期预检不导航，大文件改网页包必重做预检

- Status / Owner: queued /
- Outcome / Evidence: `src/renderer/app/useCourseDelivery.ts` 三处缺陷：`continuePreflightExport`（:451–483）核对 identity 后调用的四个 emit 各自用 `requireSnapshot` 重读当前快照（:253、:271、:288、:311），`emitHtml` 还先 `await prepareBundledFontEmbedding()`（:252），期间的文档修改会被导出却未经预检；`locatePreflightItem`（:485–489）不核对 identity；`exportLargeHtmlAsWebPackage`（:516–519）直接 `emitWebPackage()`，跳过 `web-package` 目标的预检。证据：先按规格执行版第 1 步在 `tests/integration/courseExportPreflightApp.test.tsx` 新增三条测试并运行，三条必须红。完整 arrange/act/assert、按钮文案与改动表见 [规格执行版](../../roadmap/1.1/r11-035-app-delivery-module.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。
- Write scope: `src/renderer/app/useCourseDelivery.ts`、`tests/integration/courseExportPreflightApp.test.tsx`；`src/renderer/App.tsx` 仅限签名变化导致的最小编译修正。禁止任何格式 producer、Published wire、Player host、Main/Preload、Store、两个对话框组件；不允许新建任何名字。三条测试有任一在修改前就通过即停止。
- Write locks: published-producer, workspace-properties
- Acceptance: 三条新测试红→绿证据完整；`grep -c "requireSnapshot(" src/renderer/app/useCourseDelivery.ts` 不大于 2；`grep -c "exportCourse('web-package')" src/renderer/app/useCourseDelivery.ts` 为 1；`grep -nE "emit(Html|WebPackage|Pptx|Pdf)\(" src/renderer/app/useCourseDelivery.ts` 输出中每处调用带 `pending.snapshot`（粘贴）；`grep -cE "useEditorStore|from '\.\./store/" src/renderer/app/useCourseDelivery.ts` 为 0；提交信息 `fix(app): emit the preflighted snapshot, reject stale findings, re-preflight web package (r11-035)`。
- Validation: `npx vitest run tests/integration/courseExportPreflightApp.test.tsx tests/integration/coursePdfExportApp.test.tsx tests/unit/coursePlayerTryRunFit.test.ts tests/unit/readModelBoundary.test.ts`；`npm run typecheck`；`git diff --check -- src/renderer/app/useCourseDelivery.ts tests/integration/courseExportPreflightApp.test.tsx`
