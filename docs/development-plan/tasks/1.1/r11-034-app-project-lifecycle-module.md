# r11-034-app-project-lifecycle-module App 生命周期：迟到的保存与恢复结果不得写入被替换的会话

- Status / Owner: queued /
- Outcome / Evidence: `src/renderer/app/useCourseProjectLifecycle.ts:157–162` 的 `sameProjectIdentity` 只比较 `projectId`。重开同一个工程文件后，进行中的保存会用旧路径 `acknowledgeSaved` 新会话（:384、:393），进行中的恢复写入会把旧会话内容写成恢复副本（:191）。`sessionGeneration` 不能区分会话（新会话始终代次 0，见 `courseAuthoringSession.ts:229`）；正确信号是 hook 自己的 `loadEpochRef`（:221）。证据：先按规格执行版第 1 步写 `tests/unit/useCourseProjectLifecycle.test.tsx` 的两条测试并运行，两条必须红。完整 arrange/act/assert 与改动表见 [规格执行版](../../roadmap/1.1/r11-034-app-project-lifecycle-module.md)；规则见 [执行者指南](../../roadmap/1.1/EXECUTION_GUIDE.md)。
- Write scope: `src/renderer/app/useCourseProjectLifecycle.ts`；新建 `tests/unit/useCourseProjectLifecycle.test.tsx`；`src/renderer/App.tsx` 仅限类型收窄导致的最小编译修正。禁止 Store、Main/Preload、archive、`recoveryWriteCoordinator.ts`、inventory；除测试文件外不允许新建任何名字。两条测试有任一在修改前就通过即停止。
- Write locks: app-save-recovery, workspace-properties
- Acceptance: 两条新测试红→绿证据完整；`grep -cE "expected\.projectId === current\.projectId && expected\.epoch === current\.epoch" src/renderer/app/useCourseProjectLifecycle.ts` 为 1；`grep -c "portsRef.current.captureIdentity()" src/renderer/app/useCourseProjectLifecycle.ts` 为 1；`grep -cE "useEditorStore|from '\.\./store/" src/renderer/app/useCourseProjectLifecycle.ts` 为 0；提交信息 `fix(app): drop late save and recovery results after session replacement (r11-034)`。
- Validation: `npx vitest run tests/unit/useCourseProjectLifecycle.test.tsx tests/integration/draftSaveTransaction.test.tsx tests/unit/courseDraftPersistence.test.ts tests/unit/recoveryWriteCoordinator.test.ts tests/unit/courseProjectIo.test.ts tests/unit/readModelBoundary.test.ts`；`npm run typecheck`；`git diff --check -- src/renderer/app/useCourseProjectLifecycle.ts tests/unit/useCourseProjectLifecycle.test.tsx`
