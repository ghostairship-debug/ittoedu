# S1 Task Card — Adopt V4-generated Example Bytes

## State and assignment

- Policy version: 2
- Risk tier: S1
- Task class: integration
- Necessity / skip condition: final candidate 12 passed V4 but its pretest regenerated three tracked example outputs. The HTML was pre-registered; two archives additionally carried the approved teacher-controller safe default and invalidated the candidate boundary. Skip only if committed outputs already equal the exact bytes generated and validated by that V4; they do not.
- Complexity delta: neutral
- Validation ceiling: V2
- Validation budget: 12 minutes
- Reviewer budget: 1
- Reviewer risk surface: generator ownership, semantic diff, exact-byte provenance and evidence reuse
- Evidence reuse: reuse candidate 12's one green V4. `build:examples`, `build:lesson-demo:fixture` and `build:render-benchmark:fixture` generated these bytes before E2E; their own reopen checks passed, the photosynthesis output fed E2E test 26, and the render-host HTML passed E2E test 30. Do not regenerate or rerun a broad suite.
- Invalidating paths: the three allowed outputs; `scripts/build-examples.ts`; `scripts/build-interactive-lesson.ts`; `scripts/build-render-host-benchmark.ts`; project/archive safe defaults; Player bundle/export payload generation
- Task ID: `arch-5-final-13-adopt-v4-generated-example-bytes`
- Phase / wave: `ARCH-5 / final generated-output provenance`
- Status: `claimed`
- Owner / Reviewer / Integrator: `Coordinator / independent generated-byte provenance reviewer / Coordinator`
- Claimed at / released at: `2026-08-25T12:19:44+08:00 / not released`
- Worktree / branch: `shared root / codex/architecture-stabilization`
- Baseline HEAD: `64abba2`; exactly three V4-generated tracked outputs are dirty and all product/test/fixture inputs are otherwise clean.
- Context: candidate 12 is immutable and rolled back; this card adopts only bytes already generated and exercised, not a repair or second build.
- Freshness / relevant dirty inputs: `examples/sample-project.h5lesson`, `examples/photosynthesis-interactive-lesson.h5lesson`, `examples/render-host-benchmark/render-host-benchmark.html`, this card and generated task board.
- Depends on: `arch-5-final-12-post-audit-fixed-v4-performance-outcome` rolled back after its single green V4 plus boundary failure.
- Blocks: owner-waived final engineering closure and final repo-index refresh.
- Risk statement: silently discarding the generated bytes would restore stale safe-default examples, while regenerating would break exact tested-byte provenance. Adoption requires semantic comparison and one independent read-only approval.
- Retry count / last failure class: `0 / predecessor candidate boundary admitted only one of three tracked generator outputs`

## Product outcome

Tracked examples match the current deterministic generators and the exact bytes validated during the one green V4: teacher controllers start safely collapsed, and the offline render benchmark embeds the current Player without changing its lesson payload.

## Exact output boundary

- `examples/sample-project.h5lesson`: `18,612` bytes, SHA256 `F8EBAA940C1CBA9611DFB8C42A1C349DF7392E0C068668FB392B02822DC04FA3`.
- `examples/photosynthesis-interactive-lesson.h5lesson`: `50,923` bytes, SHA256 `E57785860386B8FB37AB1C284AE592E13A07C04E1D3F77AF3533640663C25AAE`.
- `examples/render-host-benchmark/render-host-benchmark.html`: SHA256 `7ABF5B8167B586A31A9C05D226A08955665A6F066D1A87BB91FF09B5EDAA4837`.

## Scope

### Allowed write

- the three exact outputs above
- this card and generated task board

### Forbidden write

- generators, product source, tests, source fixtures, contracts, configuration and any other generated output

## Acceptance

- [ ] Both archives differ from HEAD only at `globalLayer.0.node.defaultCollapsed: false → true`, and their generator reopen checks passed before V4 E2E.
- [ ] The render-host HTML lesson payload is semantically byte-equivalent before/after; only the embedded current Player/build shell changes.
- [ ] Exact sizes and SHA256 values match the registered boundary.
- [ ] Candidate 12's build order proves generation preceded the consuming E2E tests.
- [ ] One independent reviewer approves without regeneration or broad-suite rerun.
- [ ] `git diff --check` passes and only the three registered outputs plus governance files differ.

## Validation

- Read-only in-memory archive entry and embedded-payload comparisons against `64abba2`.
- SHA256/size inspection and `git diff --check`.
- No generator, test, build or package rerun.

## Rollback

- Revert the three generated-output bytes; no schema, migration or user data is involved. This would intentionally restore stale example defaults and must also invalidate the final closure.

## Result evidence

- Pending exact-byte adoption and independent provenance review.
