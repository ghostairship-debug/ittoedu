# r20-contextual-authoring 轻量上下文共编集成

- Status / Owner: blocked / Codex
- Outcome / Evidence: 工程候选 e39052fc 上的上下文共编工程部分已收口：`npx vitest run tests/unit/courseChatPanel.test.tsx tests/unit/flowContextSelection.test.ts --maxWorkers=1` → `Test Files 2 passed (2)`、`Tests 69 passed (69)`、exit 0；`npx vitest run tests/unit/markdownSourceMap.test.ts tests/unit/courseChatObservation.test.ts tests/unit/externalAiReferences.test.ts tests/unit/externalReferencesOperation.test.ts tests/unit/chatTranscriptAccessibility.test.tsx --maxWorkers=1` → `Test Files 5 passed (5)`、`Tests 83 passed (83)`、exit 0（两条链均 2026-09-21 16:21 +08:00 复测；用例数随批次变化，**不得当常量引用**）。**本批修复与申报**（此前未申报的改动，现补入本卡）：(1) `src/renderer/ui/chat/CourseChatTranscript.tsx` 与 `tests/unit/chatTranscriptAccessibility.test.tsx`——修复 `aria-busy` 在流式追加中途回落：原实现只看最后一个事件，正文分片与工具/用量事件交错时被误判为已结束，`aria-busy` 提前回落、流式内容被反复播报；现改为倒序扫描全部事件并按会话跟踪 settled（计算 `CourseChatTranscript.tsx:22-40`，渲染 `:90-93`，播报策略见 `:45-55`）。(2) `scripts/releaseArtifactBoundary.ts` 与 `scripts/verify-release.ts` 是本批发布边界修复的实际接入点，已补入 Write scope。(3) 另修三处真实缺陷，逐条见验收记录第九节：打包缺 `.agents` 方法文件（`electron-builder.yml` 只列运行时读取的 4 个文件；**不可写整目录 `.agents/**`**，会连带打进被 gitignore 的 `editor-root.local.json` 本机绝对路径，守卫 `tests/unit/packagedRuntimeBoundary.test.ts`）、列表项续行映射损坏（`src/shared/document/markdownSourceMap.ts` 的 barrier fail-closed，守卫 `tests/unit/markdownSourceMap.test.ts`）、e2e 真实付费回合未固定授权路由（`tests/e2e/stabilizationCoreUsability.spec.ts:1045-1046` 等）。`npm run check:contracts`、`npm run check:development-roadmap`、`npm run check:task-board` 均 exit 0；`src/renderer/ui/chat/contextualCourseCommand.ts` 已由 CRLF 转为 LF（内容与 HEAD 逐字节相同，`git diff --ignore-cr-at-eol --stat HEAD -- src/renderer/ui/chat/contextualCourseCommand.ts` 空输出）。仍未跑（不得读作通过）：受 `R20_MARKDOWN_SELECTION_LUNA_RUN`／`R20_FLOW_SELECTION_LUNA_RUN` 门控的真实选区用例、041 PPTX 7 项与原生矩阵 3 项（本候选上无可追溯记录，`docs/development-plan/reviews/2026-09-21-r20-contextual-acceptance.md` 第三节"041 PPTX 7 项未在本候选上运行"条目与第六节第 4 条记录仅有 45 字节状态文件）、`npm run verify` 本体——本批硬禁令禁止运行任何真实付费模型用例，故该门在本候选上没有任何新证据。**（2026-09-21 复核订正）** 全量套件已在当前工作树复跑转绿：`npx vitest run --maxWorkers=2` → `Test Files 461 passed (461)`、`Tests 4445 passed | 3 skipped (4448)`、exit 0（**第四轮第二遍**，449.19s；第一遍在同样负载下为 `1 failed | 4444 passed`，唯一失败 `repoIndexQuery.test.ts > distinguishes fresh, partially-stale, stale, and relevant dirty inputs` 单独运行 14/14 全过，第二遍即转绿——**这两遍的差异本身就是负载抖动的直接证据**，两遍耗时 ~449s 远高于早前的 238–295s。见验收记录第二节 2.1 的"2026-09-21 复核更新"与 10.4）；原先的红（安静机器 9 文件/14 项、并发 11 文件/15 项）是**并发负载抖动**，失败集随负载浮动，其中 4 条确由验收记录文件缺失导致、该文件现已存在。**仍不得**把这一次绿读作"自动化门已全绿"：`check:legacy-ready`／`check:legacy-zero` 仍硬失败（台账摘要漂移），`npm run verify` 本体未跑。环境阻塞／未执行：**025 真实 VS Code 插件对照不是环境阻塞，是未执行**（2026-09-21 复核订正，原判据"本机 `code` 指向 Cursor 3.20.21，无 VS Code"**不成立**：`where.exe code` 返回 4 条，第 3/4 条即真实 VS Code `D:\Users\74755\AppData\Local\Programs\Microsoft VS Code\bin\code(.cmd)`；以绝对路径调用其 CLI，`code --version` → 1.133.0、`code --list-extensions` → exit 0 且含 `anthropic.claude-code` 与 `openai.chatgpt`，即对照所需的**两家插件都已安装**。原判据误把默认 PATH 首项当成了本机唯一的 `code`，并把 `~\.cursor\extensions` 当成了 VS Code 的扩展目录。本轮未执行的原因是它需要交互式 GUI 会话与跨多个课例任务的付费模型运行；两个扩展的登录状态未知）、030 真实读屏复核（**环境阻塞**：只有 Narrator 10.0.26100.8972，无 NVDA／JAWS）、050 S4 只能由 Owner 本人签署。机制缺口（不是可修代码缺陷）：`scripts/check-legacy-consumers.ts` 只有 `check:*` 入口，无 `--write`／`--reconcile`，台账 `docs/development-plan/inventories/legacy-consumers.json:66-68` 只能人工维护；`npm run check:legacy-ready` 与 `npm run check:legacy-zero` 因产品树摘要漂移硬失败（`scripts/check-legacy-consumers.ts:854-856` 抛 `stale-inventory`）。**摘要值不得写进文档**：它不是提交的函数——同一候选、同一会话内 15 分钟就实测到两个不同值（2026-09-21 16:06 +08:00 → `97d8cf6e…`；16:21 +08:00 连续三次均 → `f7cca29c…`），因为其输入集包含被 gitignore 的 `artifacts/*`；只保留命令与结论：`npm run check:legacy-ready` → exit 1、`legacy:stale-inventory: product digest 已偏离台账`，`npm run check:legacy-inventory`（ratchet）仍 exit 0 且七项计数全零。**（2026-09-21 16:21 +08:00 复核：`check:ai-capabilities` 的红已消失）** `npm run check:ai-capabilities` → exit 0，原文「AI 能力清单已是最新状态；索引 16275 / 16384 字节，组件目录 available。」。此前记录的 EXIT=1（「来源溯源证据过期 generation-evidence.json」，`artifacts/ai-capabilities/generation-evidence.json` 的 `inputs.sourceFiles` 中 `package.json` 与 `src/shared/localAgentContract.ts` 已偏离）已由持有 `generated-index` 锁的一方重生成消除，**不再需要 Owner 运行 `npm run generate:ai-capabilities`**；索引字节数同样随批次变化，不得当常量引用。**（2026-09-21 第四轮订正：本轮修复三处真实缺陷并就地改正一处过度断言，全部真跑 + 变异验证，逐条见验收记录第九节 9.7 与第十节）** (1) **[P1，本批自己引入的回归]** `src/shared/document/markdownSourceMap.ts`：`blockBody` 原先只剥一个尾换行，而 marked 的 raw 对标题后跟空行会保留该空行（`"# a\n\n"`），导致 `expected !== units.join('')` 在 `:152` 静默丢弃槽位、`:253` 把标题判成 `unmapped`——用户可见后果是**失败关闭**（「当前选区无法精确定位」），不是错写。已改为剥**全部**尾换行（块自身的首尾**空格**是可见文本，必须保留；先用 `trim` 的第一次尝试是**错的**，会打红既有用例 `maps a block whose text starts or ends with a space`）。守卫 `tests/unit/markdownSourceMap.test.ts` 37 项（35→37）。(2) **[P2]** `src/renderer/ui/FlowWorkspace.tsx` 源文守卫代次死锁：effect 依赖缺 `sessionToken.generation`，而代次递增会重挂载编辑器（`:146` 的 key）且新实例永不回调 `onContextualTargetChange`，守卫永久卡住。真实触发路径是**源文模式下的 Ctrl+Z**（`flowAuthoringSlice.ts:931-941` 的 undo 必带 `sidecarDirection` ⇒ `editorStoreKernel.ts:199` 递增代次）。守卫 `tests/unit/flowWorkspace.test.tsx` 18 项（17→18）。(3) **[P0，只做诚实化、未做结构修复]** 外部处理说明的引用清单**不穷举**：Main 的清单在 `service.ts:242` 定格后，harness 仍在 `:313`／`:374`／`:378`／`:381-382` 追加 `pending-host-results.json`、`host-result.json`、`observation/host-feedback.json`、`repair/component-changes-<requestId>/…` 四类资源。`service.ts:231` 原注释"can never describe a payload other than this one"是**假不变式**，已改为如实描述；对话框区块标题与 `aria-label` 由「本轮实际引用」改为「**本轮显式引用**」并加一句不穷举声明（`useExternalAiNotice.tsx:278-289`），错误文案 `:107` 同步。守卫 `tests/unit/useExternalAiNotice.test.tsx` 11 项（10→11）。**结构性修复本轮明确不做**，理由与推荐方案见验收记录第十节 10.3——在禁 `build`／禁 `verify`、无真实窗口与真实 CLI 交互的条件下改这段 Main 会话生命周期代码，风险高于收益。**因此在本卡上不得声称引用清单已完整**；第六节禁令继续有效。解除条件：Owner 授权并对同一候选签署 S4、具备 025／030 真实环境、由持有 `legacy-inventory` 锁的 Owner 按「先代码提交、后台账提交」顺序重对齐台账摘要；下一决策者：Owner。
- Write scope: 共享锁与主工作树由 Codex 持有；本批写域为上下文共编产品路径（`src/renderer/ui/chat/**`——含本批修复的 `CourseChatTranscript.tsx`、`useExternalAiNotice.tsx`、`src/renderer/ui/FlowWorkspace.tsx`、`src/renderer/course/flowContextSelection.ts`、`src/shared/document/**`、`src/main/localAgent/**`——含本批注释订正的 `service.ts`、`artifacts/ai-capabilities/**`）及 `package.json`、**发布边界脚本 `scripts/releaseArtifactBoundary.ts` 与 `scripts/verify-release.ts`**（本批发布边界修复的实际接入点，此前未申报）、本任务卡与验收记录。随本批一并申报的测试与共享模块：`tests/unit/chatTranscriptAccessibility.test.tsx`、`tests/unit/useExternalAiNotice.test.tsx`、`tests/unit/flowWorkspace.test.tsx`、`tests/unit/packagedRuntimeBoundary.test.ts`、`tests/unit/releaseArtifactBoundary.test.ts`、`tests/unit/markdownSourceMap.test.ts`、`tests/unit/externalAiReferences.test.ts`、`tests/unit/externalReferencesOperation.test.ts`、`src/shared/externalAiReferences.ts`（`src/renderer/ui/chat/externalAiReferences.ts` 已删除）。禁止路径：`docs/development-plan/inventories/legacy-consumers.json` 的 `reconciledProductTreeDigest`／`reconciledProductCommit` 在无 Owner 授权与配套代码提交时不得手改；`examples/render-host-benchmark/render-host-benchmark-v2.html` 为冻结制品，不得运行 `refresh:examples`／`refresh:render-benchmark:fixture`。越界停止条件：需要写上述范围之外的路径时停止并交回 Owner，不改域外文件。
- Write locks: contracts-schema, chat-ui, workspace-shell, authoring-flow, ai-session, generated-index, cli-adapters, main-preload
- Acceptance: Markdown 与 Flow 选择成为精确目标，卡片与聊天同一冻结提交路径，stale 零写入，人工保存／重开／撤销不回退；`npm run verify` 现在包含 `check:contracts`、`check:development-roadmap` 与 `check:task-board`；保全矩阵是独立门 `npm run check:preservation`（输出为「27 个 automated pass；Owner 观察未签署：PM-01」，**已在当前候选实跑 exit 0**；故意不进 verify——它跑的用例已在 `vitest.config.ts:15` 的 `include` 内、被 `npm test` 覆盖，补入只是重复执行）；2.0 其余生产门只有在真实证据齐备且经 Owner 签署后才晋升 accepted，自动化最多证明 engineering candidate。
- Validation: `npx vitest run tests/unit/courseChatPanel.test.tsx tests/unit/flowContextSelection.test.ts --maxWorkers=1`；`npm run check:contracts && npm run check:development-roadmap && npm run check:task-board`；`npm run check:preservation`（**本候选已实跑 exit 0**，见 Acceptance）与 `npm run check:legacy-ready`（**硬失败，须由持有 `legacy-inventory` 锁的 Owner 重对齐台账后再跑**）为独立门，不在 `verify` 内。
