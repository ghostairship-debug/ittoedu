# host-unify-authoring-preview Slide 编辑画布与试运行统一到同一套 Published 宿主

- Status / Owner: queued /
- Risk / Hotspot: S2 / workspace-properties, published-producer, editor-store-history
- Outcome / Why now: 教师在 **Slide** 编辑画布上看到的文本排版、字体与远程素材结果，与"当前位置试运行 / 整课预览 / 网页导出"一致——今天不一致，因为两侧是两套渲染实现。**本卡只统一 Slide 一个 Surface**：Flow 编辑仍是 `src/renderer/ui/FlowWorkspace.tsx` 的 DOM 作者面（`Workspace.tsx:1422` 的 `canvasMode === 'edit'` 分支，本来就没有 blob 宿主），Spatial 编辑仍是 `SpatialLocationWorkspace` 的专用 DOM/SVG 作者面；两者继续保留各自作者面，本卡不改、不冒充"三表面统一"，它们的作者面是否也该统一须另做技术论证与拆卡。Slide 编辑画布现状：`blob:` iframe + V8 `ExportPayload` + Phaser `PlayerApp` + 自研 canvas 断行（`Workspace.tsx` 的 `SlideLocationWorkspace` runtime-preview effect 内 `createRuntimePreviewPayloadResources` → `buildStandaloneHtml` → `createRuntimePreviewBlobResources`；blob 实际创建在 `src/renderer/preview/runtimePreviewDocument.ts:43-52`；payload 由 `src/renderer/store/editorStore.ts` 的 `projectCandidatePreviewDocument` 构造，`schemaVersion: 8` 在其返回对象内；`src/renderer/preview/runtimePreviewPayload.ts:95` 组装 `ExportPayload`；`src/player/index.ts:23-39` 起 `PlayerApp`）。试运行：主 renderer + Published V2 DOM 宿主 + 浏览器断行（`src/renderer/ui/coursePlayerTryRun.ts:66-139` 的 `mountPublishedCourseTryRun`，`:115` `createPublishedCourseSession`，`:119` `session.mount`；`src/player/surfaces/slide/SlidePublishedAdapter.ts:173` 委派 `paintPublishedNativeText`，换行实为 `src/player/surfaces/publishedNativeText.ts:11` 的 `white-space: pre-wrap`）。**最硬的两条当前失败证据**：(1) **自动缩小字体在 Published 侧根本没实现，教师直接可见**——`src/shared/textLayout.ts` 有 shrink（`fitFontSize` `:287-309`，`:293` 是 guard，`:294-308` 是逐 1px 降到下限 8px 的收缩循环）、auto-height（`:380-393`）与裁切（`:426` `context.clip()`）三套实现，而 `publishedNativeText.ts` 全文只设样式与 span——`overflow: hidden`（`:10`）、`white-space: pre-wrap`（`:11`）、`fontSize`（`:13`）——没有任何测量或收缩；`src/player/` 下只有 `src/player/renderNode.ts:53` 一处 import `textLayout`，`src/player/surfaces/**` 零处。同一个设为自动缩小的文本框，Slide 编辑画布会缩小、试运行/整课预览/网页导出直接溢出裁掉。(2) **教师用界面上的"显示范围"就能让 Slide 编辑画布起不来，命中不变量 11**——完整操作序列：打开一份同时含 Slide 与 Flow location 的工程（`tests/fixtures/course-project-v9/mixed.h5lesson` 即是，三种 location 各一个）→ 选中一个全局层元素 → 属性面板"显示范围"选"仅所选" → 只勾选 Flow 页（`src/renderer/ui/PropertiesTab.tsx:2040` 的复选框列的是 `document.locations` 全表，含 flow-block / spatial-camera；`src/renderer/course/globalLayerCommands.ts:567-583` 的 `validateLocationVisibilitySpec` 只校验 location 存在且非空，不要求其中有 slide-scene，所以该状态是合法 V9）→ 切回任意 Slide 页。此时 `editorStore.ts` 的 `locationIdsToSceneIds`（`:824-832`，`:830` 只映射 `slide-scene`、`flatMap` 静默丢弃 Flow/Spatial）把 `locationIds` 映成 `[]` 再喂给 `projectCandidatePreviewDocument` 的全局图层 `sceneIds`，V8 `globalLayerVisibilitySchema`（`src/shared/projectSchema.ts:575-585`）在 `mode !== 'all'` 且 `sceneIds` 为空时拒绝，iframe 内 `src/player/payload.ts:23` 抛"课件 Payload 缺少必要数据或版本不受支持"；`Workspace.tsx` 全文无 `safeParse`，当年设计的降级提示（PRJ-01）未实现，所以没有可见降级只有失败。**2026-08-26 已按上述三步在 `mixed` 夹具上实测**：V9 `safeParse` 接受该文档、`locationIdsToSceneIds` 产出 `[]`、V8 以"按场景控制全局元素时至少需要一个场景 ID"拒绝——三步全部成立。注意这不是"只含 Flow location 的工程"，那种工程会被 `Workspace.tsx:1461-1485` 的 session 路由分到 `FlowLocationWorkspace` / `SpatialLocationWorkspace`，根本走不到 `projectCandidatePreviewDocument`，写成那样的复现是假红。另两条同源差异：内置字体只注入顶层文档（`src/renderer/main.tsx:27` → `src/shared/fonts/installBundledFontFaces.ts:19-28`，把 `@font-face` 塞进 `<style id="bundled-font-faces">`，生产仅此一个调用点、从不传预览文档），`224be20` 把 `STIX Two Math` 提到公式字体链首位后编辑画布显示回退字体，且一旦本会话点过导出、字节被 `src/renderer/export/bundledFontEmbedSourceFetch.ts` 的闭包缓存后表现翻转（同机同课件导出前后不同）——**该症状已由 `9c39f69` 单独修复（给预览 blob 按需补 `@font-face`），本卡不重复实现**；但那是按需子集而非等价宿主（`tests/unit/workspacePreviewBundledFonts.test.tsx:256` 反向钉死"无声明即零 face"，而顶层文档始终装齐全部内置族），只消除症状、不消除宿主分裂，也不触及自动缩小与远程素材两条。远程素材：编辑画布用传进 iframe 的本地字节（`runtimePreviewPayload.ts:86-93`，placeholder + transferable ArrayBuffer）、试运行走远端 URL（`coursePlayerTryRun.ts:29`）。台账 LEG-002（`active-debt`）已补记这三条教师可见后果。Owner 于 2026-08-26 依修订后的不变量 15（已完成论证的架构性偏差 + Owner 决定）重新立项本根治方案；2026-08-25 曾批过同方向方案 A 与 10 人日估工，那是"三表面一次跑完"的量级，**不适用于本卡**：本卡限定 Slide 单一纵切、拆两个可分别 revert 的提交，量级重估为 2–4 个工作日，其后是否继续扩面由届时的真实结果决定。
- Write scope / Baseline: Baseline 以领取当日 HEAD 为准，最近一次逐行核对为 `a4dd298` 之后的字体许可修正提交。**允许写入**：`src/renderer/ui/Workspace.tsx`（仅 `SlideLocationWorkspace` 的预览宿主创建/销毁与增量更新接线，不动 `FlowLocationWorkspace` / `SpatialLocationWorkspace` 分支）、`src/renderer/ui/coursePlayerTryRun.ts`、`src/renderer/preview/**`、`src/renderer/store/editorStore.ts`（仅 `locationIdsToSceneIds` 与 `projectCandidatePreviewDocument`，不动历史/撤销/保存路径）、`src/player/surfaces/**`、`src/shared/textLayout.ts`（仅让 Published 侧复用其既有算法，不改算法语义）、以及上述文件的直接目标测试。**禁止写入**：`src/renderer/ui/FlowWorkspace.tsx` 与任何 Spatial 作者面模块（`SpatialPathEditor.tsx`、`SpatialCameraPanel.tsx`、`src/renderer/course/spatialEditorCommands.ts`、`src/renderer/phaser/v9SpatialHitAdapter.ts`）——它们不在本卡范围，需要改动即说明范围判断错了；任何 Schema 与合同（`src/shared/contracts/**`、`src/shared/projectSchema.ts`、`src/shared/courseProjectSchema.ts`、`src/shared/publishedCourseSchema.ts`）、`artifacts/ai-capabilities/**`、`.github/**`、`examples/**` 与任何已提交归档字节。**`README.md` 与 `docs/USER_GUIDE.md` 属于收口同步范围**：实现期间不得先行改写它们来提前宣告统一，但宿主统一合入后必须在同一批交付内做最小事实同步——两份文档现在描述的"编辑画布走 `blob:` iframe + Phaser、试运行走主 renderer 内 Published V2 DOM、两侧断行与自动缩小不同"在统一后即刻过期，不得实现合入就关卡而留下过期说明。**越界停止条件**（命中即停并升级 Owner，不得绕行）：统一需要任何 V9 或 Published V2 的字段、判别器或语义变更；需要缩水任何已有教师能力（含 Runtime 原位编辑的 `ctx.authoring.register()` / `data-courseware-edit-key` 命中区与 `PlayerAuthoringPatch` 增量更新语义）；需要改动已提交 fixture 字节或导出语义；需要为通过检查而弱化断言；需要动 Flow 或 Spatial 作者面。开始前必须先取得 workspace-properties / published-producer / editor-store-history 三个热点的单写入者身份——**`Workspace.tsx` 近期被高频改写（`9c39f69` 一次就改了 175 行），本卡所有行号须在动手当天以当时 HEAD 重新核对，文中符号名才是稳定锚点**。
- Acceptance: ① Slide 编辑画布与试运行由同一套 Published V2 宿主渲染同一份 V9 事实：`SlideLocationWorkspace` 不再创建 `blob:` iframe、不再构造 V8 `ExportPayload`、不再启动 `PlayerApp`；Flow / Spatial 编辑画布保持现状不变，其 `canvasMode === 'edit'` 分支与专用作者面不得被改写；② 同一个设为"自动缩小"的文本框在 Slide 编辑画布与试运行/整课预览中得到相同的最终计算字号与相同真实行数——是两侧都按 `textLayout` 既有算法收缩，不是"都不缩小"；③ 未点过导出的全新会话中，Slide 编辑预览文档声明的 font-face 集合 ⊇ 顶层文档，公式按 `STIX Two Math` 显示，且同一课件在"本会话已导出"与"未导出"两种状态下表现相同；④ **作者态必须 inert**：统一后的宿主在编辑画布下必须保留现有 `PlayerHostMode = 'authoring'` 的全部禁用语义——播放交互不响应指针（今天是 `src/player/PlayerApp.ts:159` 的 `pointerEvents: 'none'`）、宿主动作与教师控制器动作停用（`:250`）、按 `capture` 模式且 `freezeCourseState`（`:257-258`）、演示者输入停用（`:299`）、场景选择器停用（`:362`）——今天 `coursePlayerTryRun.ts:115` 建的是普通 playback session，Published 侧**没有**等价 authoring 模式，必须先补出来；作者手势（框选、拖拽、文本编辑）不得被运行交互抢占，全局层作者入口保持唯一，不得新增与全局控制器重合的 escape control；⑤ 一份含 Slide 与 Flow location、且某个全局层元素"仅显示于 Flow 页"的合法 V9 工程，切到 Slide 页时编辑画布必须**正常可编辑**——该工程已被证明是合法 V9 且 Published 侧具备渲染事实，因此"给出可见降级提示"不构成本条的完成方式，不得以降级顶替可用；可见降级只保留给另有证据证明确实不可渲染的未知输入（不变量 11、12）；⑥ 远程素材两侧取同一来源，且来源优先级明确为**有本地已保存字节时一律优先本地字节**，只有在没有本地字节、或产品语义明确要求实时远端内容时才走 `remote.url`——不得为满足"两侧一致"而把编辑画布改成依赖网络，从而使原本离线可见的素材断网不可用；若产品确需不同语义，先升级 Owner，不得由执行者自行取舍；⑦ Runtime 原位编辑与 `PlayerAuthoringPatch` 增量更新在唯一宿主上继续工作：复用 `src/shared/playerAuthoringProtocol.ts` 既有语义（含 `ack` 通道与 `PlayerHostMode`），**不新建第二套作者协议**；在 `PublishedCourseSession` / Slide Published 宿主上增加直接的 authoring 更新入口（今天 `publishedDynamicHosts.ts:67-72` 的 `PublishedCourseSessionOptions` 没有任何 authoring mode 或 patch 入口，`PlayerAuthoringPatchCommand` 只由 `src/player/index.ts:186` 解析、`PlayerApp.ts:560`/`:721` 消费）；保留现有 revision、ack、Runtime/Component target 与 stale-session 语义；增量更新不得退化成每次销毁并重挂整个宿主；若必须修改冻结合同或另立协议，立即停止并升级 Owner。
- Focused validation: 真实排版结论一律在真实浏览器取，Vitest 只做纯算法与数据事实——`vitest.config.ts:13` 是 `jsdom`，总纲第 2 节明确禁止用 jsdom 或样式字符串代替真实布局，因此**不得**把最终字号/断行 oracle 放进 Vitest（`tests/unit/formulaCrossSurface.test.tsx` 把 `measureText` 打桩成 `length * 14`，四个"表面"同属 canvas 家族，是假 parity 的反面教材，不可复用其手法）。① `npx vitest run tests/integration/courseLayerCompositionParity.test.ts`——只扩到"两侧调用同一个 `textLayout` 入口、同一份 V9 事实映射出同一份 Published 数据"这一层纯事实（现 `facts()` `:103-125` 与 `expectCompositionParity()` `:127-148` 无字体、无计算样式、无实测几何，保持不引入这三者），外加"全局层 `locationIds` 含非 slide-scene 时不再产出空 `sceneIds`"的红→绿断言（改前必红，可用 `mixed` 夹具直接构造）；② `npx playwright test tests/e2e/editor.spec.ts`——真实排版与宿主身份都在这里取：`expectCoursePlayerTryRunReady`（`:235-250`）的宿主身份断言改为"只有一套宿主"，并新增三条：(a) 自动缩小文本框在编辑画布与试运行下 `getComputedStyle` 的 `font-size` 与真实行数一致（改前必红），(b) 全新会话（未点过导出）中编辑画布公式使用内置 STIX face，(c) 编辑画布下教师控制器与播放交互 inert——点击控制器不改课程状态、作者框选/拖拽不被抢占，并直接断言 `getByTestId('teacher-escape-controls')` 计数为 0（仓库已有该稳定选择器与零计数先例：`tests/e2e/editor.spec.ts:2267`/`:2298`、`tests/e2e/render-host-benchmark.spec.ts:133`/`:395`）——当前源码没有逃生控件，这条是防止宿主迁移过程中重新引入与全局控制器重合的控件，不新增测试命令；③ `npx playwright test tests/e2e/editor.spec.ts` 内新增或复用一条真实 UI 序列覆盖 Acceptance ⑤：加载 `mixed` 夹具 → 选中全局层元素 → "显示范围"设为"仅所选"并只勾 Flow 页 → 切到 Slide 页 → 编辑画布可用或出现可见降级提示（改前必红，机制已于 2026-08-26 三步实测确认）。不新增人工检查项。
- S2 safety / rollback: 失败路径——若 `PlayerAuthoringPatch` 增量更新、Runtime 原位编辑命中区或作者态 inert 无法在 Published 宿主上成立，停在"两套宿主并存但共用同一套文本布局/字体/素材来源"的中间态并升级 Owner，不得为赶统一而硬切、砍掉原位编辑，也不得放弃 inert 换取统一。回滚起点为领取当日 baseline；实现拆成"先共用文本/字体/素材来源"与"再撤掉 blob 宿主"两个可分别 revert 的提交，任一步失败只 revert 该步。范围安全——Flow 与 Spatial 作者面不在本卡内，一旦发现必须改它们才能收口，即停并升级，不得就地扩容。数据安全——全程只用 `examples/photosynthesis-interactive-lesson.h5lesson`、`tests/fixtures/course-project-v9/mixed.h5lesson` 与新建的最小 V9 fixture，不用真实教师工程；不改保存/恢复/历史路径，不写已提交归档字节。合并前按协议第 4、6 节须过 Published/Player 专项门与独立 Reviewer。
