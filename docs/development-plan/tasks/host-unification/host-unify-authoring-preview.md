# host-unify-authoring-preview 编辑画布与试运行统一到同一套 Published 宿主

- Status / Owner: queued /
- Risk / Hotspot: S2 / workspace-properties, published-producer, editor-store-history
- Outcome / Why now: 教师在编辑画布上看到的文本排版、字体与远程素材结果，与"当前位置试运行 / 整课预览 / 网页导出"一致——今天不一致，因为两侧是两套渲染实现。编辑画布：`blob:` iframe + V8 `ExportPayload` + Phaser `PlayerApp` + 自研 canvas 断行（`src/renderer/ui/Workspace.tsx` 的 runtime-preview effect 内 `createRuntimePreviewPayloadResources` → `buildStandaloneHtml` → `createRuntimePreviewBlobResources`；blob 实际创建在 `src/renderer/preview/runtimePreviewDocument.ts:43-52`；payload 由 `src/renderer/store/editorStore.ts:869-910` 的 `projectCandidatePreviewDocument` 构造，`schemaVersion: 8` 在 `:892`；`src/renderer/preview/runtimePreviewPayload.ts:95` 组装 `ExportPayload`；`src/player/index.ts:23-39` 起 `PlayerApp`）。试运行：主 renderer + Published V2 DOM 宿主 + 浏览器断行（`src/renderer/ui/coursePlayerTryRun.ts:66-139` 的 `mountPublishedCourseTryRun`，`:119` `session.mount`；`src/player/surfaces/slide/SlidePublishedAdapter.ts:173` 委派 `paintPublishedNativeText`，换行实为 `src/player/surfaces/publishedNativeText.ts:11` 的 `white-space: pre-wrap`）。**最硬的两条当前失败证据**：(1) **自动缩小字体在 Published 侧根本没实现，教师直接可见**——`src/shared/textLayout.ts` 有 shrink（`fitFontSize` `:287-309`，`:293` 是 guard，`:294-308` 是逐 1px 降到下限 8px 的收缩循环）、auto-height（`:380-393`）与裁切（`:426` `context.clip()`）三套实现，而 `src/player/surfaces/publishedNativeText.ts` 全文只设样式与 span——`overflow: hidden`（`:10`）、`white-space: pre-wrap`（`:11`）、`fontSize`（`:13`）——没有任何测量或收缩；`src/player/` 下只有 `src/player/renderNode.ts:53` 一处 import `textLayout`，`src/player/surfaces/**` 零处。同一个设为自动缩小的文本框，编辑画布会缩小、试运行/整课预览/网页导出直接溢出裁掉。(2) **合法 V9 编辑可能让编辑画布起不来，命中不变量 11**——`editorStore.ts:824-832` 的 `locationIdsToSceneIds` 在 `:830` 只映射 `slide-scene`、`flatMap` 静默丢弃 Flow/Spatial，结果在 `:885` 喂给全局图层 `sceneIds`；iframe 内 `src/player/payload.ts:23` 用 V8 `projectDocumentSchema` 重新 `parse` 并抛"课件 Payload 缺少必要数据或版本不受支持"，两条拒绝路径都成立（`src/shared/projectSchema.ts:831` 的 `scenes: ... .min(1)`，以及 `:577-585` `globalLayerVisibilitySchema` 在 `mode !== 'all'` 时 `sceneIds` 不得为空）；`Workspace.tsx` 全文无 `safeParse`，当年设计的降级提示（PRJ-01）未实现，所以没有可见降级只有失败。另两条同源差异：内置字体只注入顶层文档（`src/renderer/main.tsx:27` → `src/shared/fonts/installBundledFontFaces.ts:19-28`，把 `@font-face` 塞进 `<style id="bundled-font-faces">`，生产仅此一个调用点、从不传预览文档），`224be20` 把 `STIX Two Math` 提到公式字体链首位后编辑画布显示回退字体，且一旦本会话点过导出、字节被 `src/renderer/export/bundledFontEmbedSourceFetch.ts:93-94` 的闭包缓存后表现翻转（同机同课件导出前后不同）——**该症状已由 `9c39f69` 单独修复（给预览 blob 按需补 `@font-face`），本卡不重复实现**；但那是按需子集而非等价宿主（`tests/unit/workspacePreviewBundledFonts.test.tsx:256` 反向钉死"无声明即零 face"，而顶层文档始终装齐全部内置族），只消除症状、不消除宿主分裂，也不触及自动缩小与远程素材两条。远程素材：编辑画布用传进 iframe 的本地字节（`runtimePreviewPayload.ts:86-93`，placeholder + transferable ArrayBuffer）、试运行走远端 URL（`coursePlayerTryRun.ts:29`）。台账 LEG-002（`active-debt`）已补记这三条教师可见后果。Owner 于 2026-08-26 依修订后的不变量 15（已完成论证的架构性偏差 + Owner 决定）重新立项本根治方案；2026-08-25 曾批过同方向方案 A 与 10 人日估工，仅作量级参考，范围以本卡当前源码事实为准。
- Write scope / Baseline: Baseline `9c39f69`。**允许写入**：`src/renderer/ui/Workspace.tsx`（仅预览宿主创建/销毁与增量更新接线）、`src/renderer/ui/coursePlayerTryRun.ts`、`src/renderer/preview/**`、`src/renderer/store/editorStore.ts`（仅 `locationIdsToSceneIds` 与 `projectCandidatePreviewDocument`，不动历史/撤销/保存路径）、`src/player/surfaces/**`、`src/shared/textLayout.ts`（仅让 Published 侧复用其既有算法，不改算法语义）、以及上述文件的直接目标测试。**禁止写入**：任何 Schema 与合同（`src/shared/contracts/**`、`src/shared/projectSchema.ts`、`src/shared/courseProjectSchema.ts`、`src/shared/publishedCourseSchema.ts`）、`artifacts/ai-capabilities/**`、`.github/**`、`docs/USER_GUIDE.md`、`README.md`、`examples/**` 与任何已提交归档字节。**越界停止条件**（命中即停并升级 Owner，不得绕行）：统一需要任何 V9 或 Published V2 的字段、判别器或语义变更；需要缩水任何已有教师能力（含 Runtime 原位编辑的 `ctx.authoring.register()` / `data-courseware-edit-key` 命中区与 `PlayerAuthoringPatch` 增量更新语义）；需要改动已提交 fixture 字节或导出语义；需要为通过检查而弱化断言。开始前必须先取得 workspace-properties / published-producer / editor-store-history 三个热点的单写入者身份——**`src/renderer/ui/Workspace.tsx` 近期被高频改写（`9c39f69` 一次就改了 175 行），本卡所有行号须在动手当天以当时 HEAD 重新核对，文中符号名才是稳定锚点**。
- Acceptance: ① 编辑画布与试运行由同一套 Published V2 宿主渲染同一份 V9 事实：`Workspace.tsx` 不再创建 `blob:` iframe、不再构造 V8 `ExportPayload`、不再启动 `PlayerApp`；② 同一个设为"自动缩小"的文本框在编辑画布与试运行/整课预览中得到相同的最终字号与断行结果——是两侧都按 `textLayout` 既有算法收缩，不是"都不缩小"；③ 未点过导出的全新会话中，编辑预览文档声明的 font-face 集合 ⊇ 顶层文档，公式按 `STIX Two Math` 显示，且同一课件在"本会话已导出"与"未导出"两种状态下表现相同；④ 一份 `locations` 中不含 `slide-scene` 的合法 V9 工程，编辑画布可正常起来；若确有不可渲染情形，必须给出可见降级提示而非空白或静默失败（不变量 11、12）；⑤ 远程素材两侧取同一来源，不再一侧本地字节一侧远端 URL；⑥ Runtime 原位编辑与 `PlayerAuthoringPatch` 增量更新在唯一宿主上继续工作。
- Focused validation: ① `npx vitest run tests/integration/courseLayerCompositionParity.test.ts`——把它从"只比图层数据事实"（现 `facts()` `:103-125` 与 `expectCompositionParity()` `:127-148` 无字体、无计算样式、无实测几何）扩出一个真正的双宿主 oracle：同一节点在两侧各渲一遍再比较自动缩小后的字号与断行；**该断言在改动前必须为红**，否则说明 oracle 没接到真实渲染（注意 `tests/unit/formulaCrossSurface.test.tsx` 是假 parity，四个"表面"同属 canvas/`textLayout` 家族且把 `measureText` 打桩成 `length * 14`，不可复用其手法）；② `npx playwright test tests/e2e/editor.spec.ts`——`expectCoursePlayerTryRunReady`（`:235-250`）的宿主身份断言改为"只有一套宿主"，并新增"全新会话（未点过导出）中编辑画布公式使用内置 STIX face"一条；③ 人工最小检查：新建一份只含 Flow location 的 V9 工程并打开编辑画布，确认可用或出现可见降级提示。
- S2 safety / rollback: 失败路径——若 `PlayerAuthoringPatch` 增量更新或 Runtime 原位编辑命中区无法在 Published 宿主上成立，停在"两套宿主并存但共用同一套文本布局/字体/素材来源"的中间态并升级 Owner，不得为赶统一而硬切或砍掉原位编辑。回滚起点 `9c39f69`（或届时的新 baseline）；实现拆成"先共用文本/字体/素材来源"与"再撤掉 blob 宿主"两个可分别 revert 的提交，任一步失败只 revert 该步。数据安全——全程只用 `examples/photosynthesis-interactive-lesson.h5lesson` 与新建的最小 V9 fixture，不用真实教师工程；不改保存/恢复/历史路径，不写已提交归档字节。合并前按协议第 4、6 节须过 Published/Player 专项门与独立 Reviewer。
