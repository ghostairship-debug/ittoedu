# repair-arch0-fixture-component-provenance 修复 ARCH-0 固定夹具组件来源

- Status / Owner: queued /
- Risk / Hotspot: S1 / none
- Outcome / Why now: 基线 `442d4e1` 上执行 `npx vitest run tests/integration/architectureBaselineFlows.test.tsx --reporter=verbose` 为 1/5 失败；三个 `tests/fixtures/architecture-baseline/*.h5lesson` 报告均 `valid=true`、`canExport=true`、error 0，但合成 Component metadata 缺少完整 provenance，产生 `component-package-hash-missing` warning。只修 fixture 生成事实，不改产品诊断。
- Write scope / Baseline: baseline `442d4e1`；只允许修改 `scripts/build-architecture-baseline-fixtures.ts`、`tests/unit/architectureBaselineFixtures.test.ts`、`tests/integration/architectureBaselineFlows.test.tsx` 以及生成的 `tests/fixtures/architecture-baseline/slide-heavy.h5lesson`、`flow-heavy.h5lesson`、`mixed-spatial.h5lesson`、`manifest.json`。目标只在 `evidencePanelComponent()`：组件身份固定为现有 `COMPONENT_ID` = `com.ittoedu.baseline.evidence-panel`、`COMPONENT_VERSION` = `4.0.0`；对该函数现有 `files` 用 `fflate.zipSync` 和 `ARCHITECTURE_BASELINE_FIXTURE_MTIME` 形成可复现的原始 `.h5component` bytes，`sha256` 必须调用本文件第 126 行现有 `sha256(bytes)` 计算这些 archive bytes，`importedAt` 固定为 `ARCHITECTURE_BASELINE_FIXTURE_MTIME`，`sourceLabel` 固定为 `ARCH-0 synthetic fixture: com.ittoedu.baseline.evidence-panel@4.0.0`。同时保留现有 `contentSha256: componentContentSha256(files)`；两种 hash 不得混用。禁止修改 health collector、finding code/severity、Schema、CLI report、Player/Export、性能逻辑或普通产品工程；若必须越过这些范围才能消除 warning，停止并改为 blocked，说明缺失合同，不得屏蔽 warning。
- Acceptance: 三个 synthetic Component 都有完整、确定且语义正确的 provenance；三个 archive 均 `valid=true`、error 0、warning 0、`canExport=true`，保存重开断言保持通过；连续构建 archive 与 manifest 字节一致。
- Focused validation: 先执行一次 `npx tsx scripts/build-architecture-baseline-fixtures.ts` 写入当前 fixture；再执行 `npx vitest run tests/unit/architectureBaselineFixtures.test.ts tests/integration/architectureBaselineFlows.test.tsx`。unit 已包含双构建和磁盘字节比较，不再紧接一次同义 generate `--check`，不跑全量测试/E2E/verify。
