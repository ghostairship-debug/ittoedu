# repair-v8-02-photosynthesis-v9-oracle 重建 photosynthesis 最小 V9/V2 行为 oracle

- Status / Owner: active / codex/repair-v8-02
- Risk / Hotspot: S2 / none
- Outcome / Why now: `pretest:e2e` 仍生成并检查 photosynthesis 的 V8 归档、V8 Player HTML 与旧组件包；真实 E2E 只依赖三页导航、前两页指针后的像素变化、第三页指针操作与截图、零 HTTP(S) 请求和零 `pageerror`，当前 V9 archive + Published V2 已足以无降级重建这组最小行为门。
- Write scope / Baseline: baseline `ddab68b2173af79ba6d72922786303bc6f8c3bf0`；仅允许写 `scripts/build-interactive-lesson.ts`、`tests/e2e/editor.spec.ts` 中精确的“课例验收”用例、`tests/unit/exampleGenerationBoundary.test.ts`、`.gitattributes` 对应 endpoint、`docs/development-plan/inventories/legacy-consumers.json` 中 photosynthesis 当前 endpoint/fact/evidence，替换 `examples/photosynthesis-interactive-lesson.h5lesson`，并删除/替换只属于该链的 `examples/photosynthesis-lab-component/**` 与 `examples/photosynthesis-lab.h5component`；禁止修改产品 archive/producer/Player、package 脚本语义、sample/benchmark/portability/release verifier、其他两卡文件及 Integrator 独占的计划/能力/generated 输出。
- Acceptance: `--refresh` 由当前 course factory 生成三 Slide 的 Course Project V9 归档和离线 Published Course V2 standalone HTML，`--check` 对 tracked 输出零写入，`--prepare` 只准备 E2E HTML；归档可由 `openCourseProjectArchive` 以 `schemaVersion: 9` 打开，HTML payload 是 Published V2 且不包含 V8 `ExportPayload`/Player；浏览器保留现有三页键盘导航、前两页像素差门槛、第三页指针操作与截图、零 HTTP(S) 请求及零 `pageerror`；该链对 `create/openProjectArchive`、`buildExportPayload`、旧 photosynthesis component 源/包的活引用归零，inventory 仅移除该 endpoint，不虚称 sample/benchmark 等其余 V8 consumer 已清退。
- Focused validation: `npm run build:player && npm run refresh:lesson-demo:fixture && npm run check:lesson-demo:fixture`; `npx vitest run tests/unit/exampleGenerationBoundary.test.ts && npm run --silent validate:project -- examples/photosynthesis-interactive-lesson.h5lesson`; `npm run prepare:lesson-demo:fixture && npx playwright test tests/e2e/editor.spec.ts -g "课例验收：三页光合作用课例可离线互动"`。
- S2 safety / rollback: 只改 committed fixture/生成器并在临时浏览器中验收，不迁移真实用户课件、不打开 V8 产品入口；删除旧 component 前必须证明生成、测试、package 与发布 consumer 已转到 V9/V2；回滚起点为 baseline，任一现有行为门无法由最小 V9/V2 fixture 等价证明时保留旧 endpoint 并报告，不用内容保真迁移或降低断言兜底。
