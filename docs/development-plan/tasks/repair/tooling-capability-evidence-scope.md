# tooling-capability-evidence-scope 收窄能力索引生成证据范围

- Status / Owner: queued /
- Risk / Hotspot: S1 / none
- Outcome / Why now: `artifacts/ai-capabilities` 约 102 KiB 且被课件 Builder 真实消费，应保留；但 `generation-evidence.json` 当前把约 50 个 broad main/preload/Player/preview/archive/export producer 文件列为 source evidence，导致无关实现改动刷新证据。只收窄 provenance 输入，不改变任何能力声明。
- Write scope / Baseline: baseline `442d4e1`；只允许修改 `scripts/generate-ai-capabilities.ts`、`tests/unit/aiCapabilities.test.ts` 与生成的 `artifacts/ai-capabilities/**`。把 `sourceEvidence()` 的 `sources` 精确替换为且仅为以下 25 个路径：`package.json`；`scripts/generate-ai-capabilities.ts`；`scripts/validate-project.ts`；`docs/contracts/COURSE_PROJECT_VALIDATION_REPORT_V1.md`；`src/renderer/components/importComponentPackage.ts`；`src/renderer/export/exportSize.ts`；`src/player/HostEvidenceRecorder.ts`；`src/shared/assessmentEvaluators.ts`；`src/shared/builtInComponentCatalog.ts`；`src/shared/componentCatalog.ts`；`src/shared/componentContentIntegrity.ts`；`src/shared/componentSchema.ts`；`src/shared/componentTypes.ts`；`src/shared/constants.ts`；`src/shared/courseProjectSchema.ts`；`src/shared/courseProjectTypes.ts`；`src/shared/courseProjectValidationDiagnostics.ts`；`src/shared/diagnosticCodes.ts`；`src/shared/interactionSchema.ts`；`src/shared/interactionTypes.ts`；`src/shared/publishedCourseSchema.ts`；`src/shared/publishedCourseTypes.ts`；`src/shared/runtimeSchema.ts`；`src/shared/runtimeTypes.ts`；`src/shared/surfaceRuntimeTypes.ts`。保留 catalog expected/actual/package hashes 与所有输出 hashes。禁止自行增减清单、修改产品能力文案/状态、Schema/合同、产品源码、catalog 内容、CI、repo-index，禁止实现 import graph、自动 provenance registry 或新证据平台；若测试证明某个清单项并非直接证据，不自行裁决，改为 blocked 并报告精确反例。
- Acceptance: source evidence 是小而精确的直接输入集合；明确不含 `src/main/createWindow.ts`、`src/preload/index.ts`、`src/player/**` 及 preview/archive/export producer 实现；component catalog 与 output hash 证据保持完整；除 `generation-evidence.json` 外，`artifacts/ai-capabilities/**` 与基线逐字节相同；Builder 读取入口和能力声明不变。
- Focused validation: 执行一次 `npm run generate:ai-capabilities`，再执行 `npx vitest run tests/unit/aiCapabilities.test.ts`；测试应同时断言精确小集合、上述 broad 路径不存在、其他制品字节不变。生成结果未再变化时不补跑 `check:ai-capabilities`，不跑全量测试/E2E/verify。
