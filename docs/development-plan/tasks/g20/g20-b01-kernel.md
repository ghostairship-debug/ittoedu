# g20-b01-kernel 果铃 2.0 滚动集成

- Status / Owner: active / root
- Outcome / Evidence: 2026-09-25：当前登记 178 条验收，154 passed、24 not_run；其中 4 条发行准备延期、18 条为新增 M15–M19，旧范围尚余 REL-T11 与后置的 M14-T05 Owner 审计。M12-T05 按软件连接能力证据 passed，M12 工程 verified；OAuth 附件已真实传到，模型语义误判单列质量观察，429 为当前额度事实。M14-T01～T04 工程 passed，M14 工程 verified；M14-T05 置于 M15–M19 后，未作 Owner 接受。通用 Images API 的 TeamoRouter 真实生成/插入/保存重开实窗通过（1 次图片请求，费用 unknown）。REL-T11 TeamoRouter 旧 run 在第 11 次文本请求后 partial，可续现场保留；官方样本前 41/42 次文本请求完成，第 42 次收到 HTTP 200 后 transport unknown，未完成最终 build.check/import/互动。未提交、推送或发布。

- Write scope: root 持共享计划、集成与重叠源码写域；本轮 6 Sol 子代理只在分配的非重叠测试文件写域新增 M03 首开实窗用例，root 与 Astra 复核结果。共享文件由唯一 writer 顺序集成。本批改动：`src/renderer/documents/courseEditorChrome.css`、`src/renderer/workbench/ExecutionAssistant.tsx`、`src/renderer/ui/FlowWorkspace.tsx`（提示文案）、`src/main/security.ts`、`src/main/createWindow.ts`、新增 `src/main/mediaCapturePrompt.ts`（课件摄像头/麦克风逐次授权）、`src/main/workbench/images/ChatGPTImageProvider.ts`（402 归为额度），新增 e2e `g20M12ConnectionRecoveryUI`、`g20S10OrthogonalUI`、`g20M05ModeUI`、`g20M09RealTaskCards`、`g20M13MediaPermission`、`g20M14NarrowScale`，单测 `g20MediaCapturePermission`、集成 `g20S05PlanQuotaNoFallback`、探针 `output/g20/s05/plan-vs-metered/probe.cjs`，单测 `courseLightToolbarStableHeight` 与 `g20ExecutionAssistant` 追加用例，S14 探针与 `reopen-saved.cjs`；M09-T04：新增 `src/shared/workbench/userQuestion.ts`、`src/renderer/workbench/ExecutionQuestionCard.tsx`，改 `ExecutionEngine.ts`、`executionOutcome.ts`、`ExecutionDesktopService.ts`、`executionDesktop.ts`、`executionEvents.ts`、`preload/index.ts`、`ExecutionAssistant.tsx`、`ExecutionTimeline.tsx`、`executionTimelineModel.ts` 与两处 CSS，新增测试 `g20M09UserQuestion`、`g20QuestionCard`、e2e `g20M09QuestionCard`、`g20M09RealQuestion`，更新 `g20ScopedToolCatalog` 工具清单预期。Electron 一次仅一组，用户原有 PID 8688 不动。 第一批：新增 `src/shared/workbench/executionPermission.ts`、`executionInputMessages.ts`、`ExecutionApprovalCard.tsx`；改 `ExecutionEngine.ts`（权限与批准、选区快照）、`ExecutionDesktopService.ts`、`executionInputErrors.ts`、`DocumentToolGateway.ts`（`previewTarget`、`documentsOfHandles`）、`executionOutcome.ts`、`execution.ts`/`executionEvents.ts`/`executionDesktop.ts`、`preload/index.ts`、`ExecutionAssistant.tsx`、`AttachmentComposer.tsx`、`executionTimelineModel.ts`、`ExecutionTimeline.tsx`、`SharedDocumentEditor.tsx`（编辑卡限制在编辑器内）、相关 CSS 与拒收文案；删除 `ExternalServiceDisclosure.tsx` 及其两个测试；新增 `g20S10PermissionModes`、`g20PermissionComposer`、`g20ExecutionInputRefusal`、e2e `g20S10PermissionComposer`，并迁移 30 余个实窗用例与三个真实探针脚本。 第二批还涉及 ConversationStore/ConversationHome、AgentFileService、WorkspaceFilesDesktopService、工作台输入区与文件树，以及 `conversationStore`、文件工具、会话归属与 S07 reconnect E2E 测试。
- Write locks: app-save-recovery, ai-session, chat-ui, generated-index
- Acceptance: M12 软件连接、输入递送、同任务续作和结果状态按真实证据完成；不以模型每次理解正确作为连接门。旧范围只余 REL-T11 的完整真实混合任务；M14-T05 作为 Owner 终局核对排在新增 M15–M19 后，保持 not_run。M12-T01/T04、REL-T08/T10 为后续发行准备，不进入当前开发完成门。REL-T11 续作保留原冻结连接与计费，实测目的为暴露软件和模型实际行为，不为通过而缩短任务或拼接不同 run；Engine 无全任务 20 分钟和 24 请求硬上限，仍保留单请求保护。REL-T11 保持 `not_run`。
- Evidence update: `output/g20/rel-t11/real-3MoHnJ/evidence.json` 记录纠正后的 TeamoRouter `deepseek-flash` 路由，实际型号 `deepseek-v4-1-flash-260910`；24/24 请求完成、32 次工具调用、288,714 ms，因预算耗尽为 `partial`。两个位置保存重开 clean，图片 ready（1 个资源），两次 batch applied，离线 HTML 打开后 0 个 pageerror。图片插入、编译修复、check/import、Runtime 与互动仍未验证；实际费用未知。此前第二次 `deepseek-v4-flash`（0731）在 nullable tool-call delta 修复后仍报 `unsupported-tool-type`，因此停止同路由重复付费；V4.1 运行未记录协议诊断，不代表 Provider 问题已彻底解决。REL-T11 仍为 `not_run`，既有 passed 状态不变。
- Evidence update: 新样本 `output/g20/rel-t11/real-Q68IPQ/evidence.json` + 显式续跑 `continuation-evidence-1790263899243.json`：V4.1 实际型号 `deepseek-v4-1-flash-260910`；首 run 24 requests/36 tools、budget partial，图片 ready 且 `media.insert applied`，两页保存重开 clean；仅手动 UI HTML 导出后离线打开 0 pageerror。唯一续跑 24 requests/38 tools，累计 48/74，budget failed；仅一次故意错误 `build.compile`（`Unexpected token ';'`），未修复，未完成 check/import/Runtime/互动。protocolDiagnostics 为空、费用 unknown；不从中推断工具错误根因。私有 profile 已清理；早期 0731 遗留不动。REL-T11 仍 `not_run`，不改变任何 passed 状态。
- Evidence update: 三个 Provider 共用有界分类，将 `insufficient_quota`/`billing_hard_limit_reached` 识别为 quota，按分钟窗口的限速仍为 rate-limit；`g20ProviderHttpFailure.test.ts` 32/32、三套 typecheck、desktop build 与 `g20M12ConnectionRecoveryUI` 本地 HTTP Electron 1/1（401/402/quota429/rate429/断连五轮）通过，详情见 B08 及 `output/g20/m04/selection-I8XXih/m12-t02-ui-evidence.json`。零付费请求；不据此登记 M12-T05 通过。
- Evidence update: 后续本地无费聚焦用例沿同会话祖先 `file.create/open` → 当前权限重开 → 新 run 图像短句柄 → `media.insert applied`，2/2；`image.generate` 总计仅 1 次。`ImageGenerationService`/Gateway 2/2；产品 `hostContinuationImages` 结构化重签 provenance 无费 2/2。构建续接已按 Astra review 撤回 caller source-copy helper：旧 build job 不跨 run 续用；新 run 从当前基线新建 scratch 并重新 compile/check/import。本轮未完成旧源码自动带入，验收不得把旧 job 失败与新 job 成功直接拼成修复。Harness 最新聚焦验证 REL evidence + recovery 11/11；同一 run/job/path 完整 failed→fixed→ready/import 链及双 job 反例通过，E2E typecheck passed。Harness 私有受限 profile、显式单次付费 gate、全分页 usage 改动无费 3/3；当前稳定源码下 `npm run typecheck`、`npm run build:desktop` 通过，`g20BudgetContinuationUI` 实窗 1/1。原 Engine 摘要仍记录 image ready 与 build compile error；本次 V4.1 实测的隔离 profile 已删除，不能续跑 real-3MoHnJ 原 run；早先 0731 遗留 profile/进程仍保留，本轮无新付费。以上不构成 REL-T11 通过，状态仍 `not_run`。
- Validation: 已通过且相关实现/依赖/定义未变的 B07/B08 证据复用。10:05 统一构建之后只改了 Flow 提示文案，已重建 `output/g20/b08/build-flow-wording-20260924.log`（exit 0），当前桌面构建与源码一致；M09-T04 之后重建 `output/g20/b08/build-m09-question-2-20260924.log`（exit 0）。`npm run typecheck` 三套 exit 0；主进程改动后统一构建 `output/g20/b08/build-m13-media-20260924.log` exit 0。既有失败待单独修复：`tests/unit/flowContextSelection.test.ts` 3 条（store 初始化过时）、`tests/unit/windowCloseRecovery.test.ts` 6 条（M02 关闭保存流程变化后未更新）。真实模型复测须有新代码或可证伪新假设，记录路由、实际型号、人工介入与未知成本。 第一批：`output/g20/b08/typecheck-batch1-20260924.log` 与 `build-batch1-permission-20260924.log` 均 exit 0；实窗迁移用例首轮 42/46，4 条失败（3 条过时测试、1 条文件被占用）修正或重跑后通过；真实测量见 B08“第一批”一节。 第二批：`npm run typecheck` 三套通过，`npm run build:desktop` 通过；聚焦 unit/integration 与三条相关 Windows Electron 实窗结果及首次旧夹具失败均记于 B08 第二批小节。 第三批聚焦命令 `npx vitest run tests/integration/g20ScopedToolCatalog.test.ts tests/integration/g20AgentFileTools.test.ts tests/integration/g20ToolGateway.test.ts tests/integration/g20ExecutionEngine.test.ts tests/integration/g20HostToolServices.test.ts` 66/66，AgentFileTools 单项 13/13；`g20S10PermissionComposer` 1/1、修正夹具后的 `g20MixedBuildPartial` 1/1；`npm run typecheck` 三套与 `npm run build:desktop` exit 0。S04-T07 paired text 与 object evidence 均已记录；object demand 路径首轮较慢且 budget partial，该次记录为早期 partial；其后 demand-only object retest 完成且 Astra 裁定 S04-T07 passed、S04 verified，详见 B08 后续裁定。ToolCatalog/Gateway/Engine discoverability fix 聚焦 7/7、三套 typecheck 和 desktop build exit 0，相关 `g20MixedBuildPartial` 与 `g20MixedHistory` 在当前构建均 1/1 通过，详见 `output/g20/b08/e2e-after-tool-discovery-final.log`。`g20M06InlineGeneration.spec.ts` 实窗 4/4（2.9m），覆盖停止迟到 tail、Flow 提交、撤销/保存重开与非流式参数呈现。`g20S05CapabilityVisible.spec.ts` 本地 fixture 1/1（20.5s）；结合 M06 final-only 证据后 S05-T05 passed；S05-T08 按 B07 真实 API/OAuth 文本工具与 B08 本地 refresh/logout crosswalk passed，S05 已 verified。REL-T05 崩溃恢复 crosswalk 与 REL-T06 安全边界 31/31 已按各自合同范围 passed；REL-T08 资源 12/12 但仍有未分诊扫描命中和许可证缺口，保持 not_run。Owner 决定当前未完成软件阶段停止 package/installer 探索并延期到 release 阶段；M12-T01/T04 与 REL-T08 保持 not_run，列为后续发行准备，不开展 clean-VM 请求或 package 操作，验收合同不变。M03-T01 首开轻量实窗 1/1（14 秒），截图 `output/g20/m03/first-open-light/run-QAyPAr/first-open.png`；工程验收 passed，M03 verified，不代替 Owner/M14 验收。M14-T02 键盘与焦点当前构建实窗 2/2（受控文件框 composition；真实微软拼音证据复用 B08:49、B07:241），三套 typecheck 与 desktop build 通过，Astra 裁定工程 passed；本地模型切换 probe 1 次 HTTP、0 次付费请求，M14 仍 in_progress。S04-T07 与 S11-T05 均 passed，S04/S11 verified；计划 `refresh_plan.py`、`validate_plan.py` 已成功，任务板已生成。当前开发 156 项中 4 not_run；延期的 3 项发行用例不计入当前完成门。 本轮 `executionOutcome` 同文档 A/B 反例与 Engine 聚焦 18/18、预算续跑聚焦 46/46、三套 typecheck 与 desktop build 通过；`g20M12ConnectionRecoveryUI` 1/1、`g20BudgetContinuationUI` 1/1（24+1 次本地 SSE，请求续跑证据 `output/g20/m04/selection-uoOT1u/budget-continuation-ui-evidence.json`），首轮旧夹具失败修正后通过；REL-T11 本地夹具 1/1、付费请求 0，见 `output/g20/rel-t11/fixture-ZQUqTr/evidence.json`，该受控夹具不代替真实模型验收。 授权真实 REL-T11 首轮证据 `output/g20/rel-t11/real-4ul0ka/recovered-evidence.json`：TeamoRouter `deepseek-v4-flash` 按量 + GPT OAuth `gpt-image-2` 订阅，4 次文本请求，前三完成、第四 HTTP 200 后 `protocol unsupported-tool-type` 且 outcome unknown；`file.create` 成功，图片/编辑/构建均 0，62,583 ms、费用 unknown、终态 partial，未自动重试。原始 Provider fragment 未持久化，具体 type/模型回显差异原因不能裁定；nullable delta 兼容修复已有局部测试，但修复后的第二次 deepseek-v4-flash 实测仍触发同类错误，因此停止同路由重复付费。旧 harness partial 后等待不存在按钮并被 Ctrl+C 中断，复合 Stop-Process/Remove-Item 清理命令在 CreateProcess 阶段被策略拒绝且未执行；临时 profile/进程仍保留，无绕过。REL-T11 保持 not_run。
- Evidence update: `output/g20/rel-t11/real-3MoHnJ/evidence.json` 记录纠正后的 TeamoRouter `deepseek-flash` 路由，实际型号 `deepseek-v4-1-flash-260910`；24/24 请求完成、32 次工具调用、288,714 ms，因预算耗尽为 `partial`。两个位置保存重开 clean，图片 ready（1 个资源），两次 batch applied，离线 HTML 打开后 0 个 pageerror。图片插入、编译修复、check/import、Runtime 与互动仍未验证；实际费用未知。此前第二次 `deepseek-v4-flash`（0731）在 nullable tool-call delta 修复后仍报 `unsupported-tool-type`，因此停止同路由重复付费；V4.1 运行未记录协议诊断，不代表 Provider 问题已彻底解决。REL-T11 仍为 `not_run`，既有 passed 状态不变。
- Validation: 已通过且相关实现/依赖/定义未变的 B07/B08 证据复用。10:05 统一构建之后只改了 Flow 提示文案，已重建 `output/g20/b08/build-flow-wording-20260924.log`（exit 0），当前桌面构建与源码一致；M09-T04 之后重建 `output/g20/b08/build-m09-question-2-20260924.log`（exit 0）。`npm run typecheck` 三套 exit 0；主进程改动后统一构建 `output/g20/b08/build-m13-media-20260924.log` exit 0。既有失败待单独修复：`tests/unit/flowContextSelection.test.ts` 3 条（store 初始化过时）、`tests/unit/windowCloseRecovery.test.ts` 6 条（M02 关闭保存流程变化后未更新）。真实模型复测须有新代码或可证伪新假设，记录路由、实际型号、人工介入与未知成本。 第一批：`output/g20/b08/typecheck-batch1-20260924.log` 与 `build-batch1-permission-20260924.log` 均 exit 0；实窗迁移用例首轮 42/46，4 条失败（3 条过时测试、1 条文件被占用）修正或重跑后通过；真实测量见 B08“第一批”一节。 第二批：`npm run typecheck` 三套通过，`npm run build:desktop` 通过；聚焦 unit/integration 与三条相关 Windows Electron 实窗结果及首次旧夹具失败均记于 B08 第二批小节。 第三批聚焦命令 `npx vitest run tests/integration/g20ScopedToolCatalog.test.ts tests/integration/g20AgentFileTools.test.ts tests/integration/g20ToolGateway.test.ts tests/integration/g20ExecutionEngine.test.ts tests/integration/g20HostToolServices.test.ts` 66/66，AgentFileTools 单项 13/13；`g20S10PermissionComposer` 1/1、修正夹具后的 `g20MixedBuildPartial` 1/1；`npm run typecheck` 三套与 `npm run build:desktop` exit 0。S04-T07 paired text 与 object evidence 均已记录；object demand 路径首轮较慢且 budget partial，该次记录为早期 partial；其后 demand-only object retest 完成且 Astra 裁定 S04-T07 passed、S04 verified，详见 B08 后续裁定。ToolCatalog/Gateway/Engine discoverability fix 聚焦 7/7、三套 typecheck 和 desktop build exit 0，相关 `g20MixedBuildPartial` 与 `g20MixedHistory` 在当前构建均 1/1 通过，详见 `output/g20/b08/e2e-after-tool-discovery-final.log`。`g20M06InlineGeneration.spec.ts` 实窗 4/4（2.9m），覆盖停止迟到 tail、Flow 提交、撤销/保存重开与非流式参数呈现。`g20S05CapabilityVisible.spec.ts` 本地 fixture 1/1（20.5s）；结合 M06 final-only 证据后 S05-T05 passed；S05-T08 按 B07 真实 API/OAuth 文本工具与 B08 本地 refresh/logout crosswalk passed，S05 已 verified。REL-T05 崩溃恢复 crosswalk 与 REL-T06 安全边界 31/31 已按各自合同范围 passed；REL-T08 资源 12/12 但仍有未分诊扫描命中和许可证缺口，保持 not_run。Owner 决定当前未完成软件阶段停止 package/installer 探索并延期到 release 阶段；M12-T01/T04 与 REL-T08 保持 not_run，列为后续发行准备，不开展 clean-VM 请求或 package 操作，验收合同不变。M03-T01 首开轻量实窗 1/1（14 秒），截图 `output/g20/m03/first-open-light/run-QAyPAr/first-open.png`；工程验收 passed，M03 verified，不代替 Owner/M14 验收。M14-T02 键盘与焦点当前构建实窗 2/2（受控文件框 composition；真实微软拼音证据复用 B08:49、B07:241），三套 typecheck 与 desktop build 通过，Astra 裁定工程 passed；本地模型切换 probe 1 次 HTTP、0 次付费请求，M14 仍 in_progress。S04-T07 与 S11-T05 均 passed，S04/S11 verified；计划 `refresh_plan.py`、`validate_plan.py` 已成功，任务板已生成。当前开发 156 项中 4 not_run；延期的 3 项发行用例不计入当前完成门。 本轮 `executionOutcome` 同文档 A/B 反例与 Engine 聚焦 18/18、预算续跑聚焦 46/46、三套 typecheck 与 desktop build 通过；`g20M12ConnectionRecoveryUI` 1/1、`g20BudgetContinuationUI` 1/1（24+1 次本地 SSE，请求续跑证据 `output/g20/m04/selection-uoOT1u/budget-continuation-ui-evidence.json`），首轮旧夹具失败修正后通过；REL-T11 本地夹具 1/1、付费请求 0，见 `output/g20/rel-t11/fixture-ZQUqTr/evidence.json`，该受控夹具不代替真实模型验收。授权真实 REL-T11 首轮证据 `output/g20/rel-t11/real-4ul0ka/recovered-evidence.json`：TeamoRouter `deepseek-v4-flash` 按量 + GPT OAuth `gpt-image-2` 订阅，4 次文本请求，前三完成、第四 HTTP 200 后 `protocol unsupported-tool-type` 且 outcome unknown；`file.create` 成功，图片/编辑/构建均 0，62,583 ms、费用 unknown、终态 partial，未自动重试。原始 Provider fragment 未持久化，具体 type/模型回显差异原因不能裁定；nullable delta 兼容修复已有局部测试，但修复后的第二次 deepseek-v4-flash 实测仍触发同类错误，因此停止同路由重复付费。旧 harness partial 后等待不存在按钮并被 Ctrl+C 中断，复合 Stop-Process/Remove-Item 清理命令在 CreateProcess 阶段被策略拒绝且未执行；临时 profile 与进程仍保留，未绕过清理。REL-T11 保持 not_run。

## 历史证据

2026-09-24 当前登记见 `GPTpro方案/guoling_2_0_execution_plan/acceptance_cases.json` 与 `task_registry.json`（S04/S11 已核实并完成 refresh_plan/validate_plan/generate:task-board）；新增实证见 `evidence/B08_2026_09_24_INTEGRATION.md`。本批登记 S06-T03/T05、M11-T01/T02、M12-T02、M06-T01、S05-T06、S10-T01、S14-T03、M05-T01/T04、M09-T01、S11-T01，及按 Owner 决定后的 M09-T02、M13-T03、M14-T04、S05-T07（GPT OAuth 订阅 + DeepSeek 按量真实各一次，额度不足以本地 429/402 验证），以及新开发的 M09-T04 内置 AI 选项卡提问（本地实窗 + TeamoRouter DeepSeek 真实一次）。S14-T03 为模型首轮成果加声明插图与零模型重开的工程候选，Owner/教师视觉接受单列；S13 早期初始化失败不算 DeepSeek 请求失败，旧 S14 人工修复副本与 partial 续验不算首轮通过。按 Owner 2026-09-24 通用 Agent 决定完成第一批（四档权限、输入框重做、选区内容随任务）：登记 S10-T05/T07、M07-T07、S04-T06，S10、M07 恢复 verified；真实 TeamoRouter 冷/热轮各由 4 次模型请求降为 2 次。未提交、推送或发布。 第二批：会话归属作为集成行为记入 B08（不新增 acceptance ID）；S08-T05 保留原定义/status，并追加文件工具四档权限证据。会话归属实窗 g20ConversationHomesUI 1/1、Windows 文件树 1/1、M07 会话栏夹具更新后 1/1；Vitest conversation store/desktop 7/7、ExecutionDesktop 4/4、AgentFileTools 13/13、文件树 unit 7/7，WorkspaceFilesDesktop 竞态修复复验 3/3。首次 M07 失败因旧筛选前提，修正后通过，失败截图保留。S07-T03 断连补拉由本地 SSE Windows Electron 实窗 1/1 通过；初次失败来自夹具设置后的多余 reload，移除后通过。S04-T07 paired text 与 object 均有局部成功证据，但 completion/首轮时延结果不等价；后续 demand-only 对象任务完成并经 Astra 裁定，S04-T07 已 passed、S04 verified；先前 text/object partial 仍按原始证据保留。M13-T01 经三份 UI/IPC/undo-reopen evidence crosswalk passed，M13 五项均 passed，task_registry 已 verified。S05-T05/T08 按各自能力缺失与 API/OAuth 分离合同 crosswalk 后 passed，S05 九项齐全，task_registry 已 verified。REL-T05 崩溃恢复 crosswalk 与 REL-T06 安全边界 31/31 均按各自范围 passed；REL-T08 broad scan 23 hits 初步分诊为第三方 test/dev、可能的构建机路径及 diagnostic filename false positive；未证明凭据泄露，但路径命中未能安全核实。运行时 notice 未在 package 验证，四个 built-in 许可未知且 maintainer 未指派，应用 UNLICENSED、分发条款未审；保持 not_run，这是未完成的 package/release 核对，不是当前产品 P0/P1。S11-T05 的共享 DocumentSession/History 有界证据 1/1 与当前 Windows Electron mixed-history 1/1 已记入 B08，S11-T05 passed、S11 verified。M03-T01 当前构建 Windows Electron 首开轻量 1/1，root/Astra 目视确认后工程 passed，M03 verified；Owner 最终体验与 M14 独立用户试用另列。 本轮补充：只读失败与已应用修改并列说明，`invalid-target` 保持 partial；预算耗尽稳定 code/自动暂停/手动同任务续跑；REL-T11 本地同 run 全链实窗 1/1 仅为受控编排子证据，真实模型仍 `not_run`。当前登记继续为 156 项中 152 passed、4 not_run。 此后真实 REL-T11 首轮为 partial：仅 `file.create` 成功，图片/编辑/构建未发生；详见 B08，未登记通过。
