# S1 Task Card — ARCH-0B IDX-01B Semantic Coverage

## State and assignment

- Task ID: `arch-0b-idx-01b-semantic-coverage`
- Phase / wave: `ARCH-0B / wave 2 semantic`
- Status: `target-green`
- Owner / Reviewer / Integrator: `Semantic Worker / Coordinator / Coordinator`
- Claimed at / released at: `2026-08-24 Asia/Shanghai / —`
- Worktree / branch: `shared workspace, semantic-only write scope / codex/architecture-stabilization`
- Baseline HEAD: `02490c6`
- Claim commit: `e4285411122161e48a382f9616d2a077abc7e10c`
- Context: `bootstrap-manual + FACT/MAP + golden corpus design`
- Freshness / relevant dirty inputs: current generator pure seam integrated; generated bytes are intentionally awaiting semantic/performance stabilization
- Depends on: `arch-0a-map-00 (done); arch-0b-idx-01 pure generator target-green`
- Blocks: IDX-01 generated integration; IDX-02 exact feature queries
- Retry count: `0`

## Product outcome

Exact feature queries cover every stable product/development module needed by the 25-task gate without copying the import graph or presenting target architecture as current fact.

## Current fact and evidence

The initial semantic seed has only four Features and three Modules. It proves provenance/schema validation but cannot answer exact feature queries for Flow, Spatial, Components, Runtime, delivery formats, diagnostics, or desktop boundaries.

## Non-goals

- No generated facts, query implementation, product source, contract, package, or lockfile change.
- No complete function graph or duplicate FACT/MAP/Legacy counts.
- No invented external component-source paths.

## Scope and locks

### Allowed write

- `repo-index/semantic/features.json`
- `repo-index/semantic/modules.json`
- `repo-index/semantic/invariants.json` and `exclusions.json` only for missing stable cross-cutting rules
- `tests/unit/repoIndexSemantic.test.ts`
- This task card.

### Required read

- FACT/MAP and Legacy ledgers
- Module owner map, current must-preserve, target/transitional rules
- Proposed 25 golden tasks
- Current source/contract/test paths named by each semantic record

### Forbidden write

- `repo-index/generated/**`, config, generator/query tooling
- product source/contracts/package/lockfile, other task cards

### Hotspot locks

- Semantic single owner for this task; generated refresh remains Coordinator-only.

## Change budget

- Task timebox: `1 Worker day`
- Main files: `2 required semantic JSON + at most 2 narrow supporting semantic files + 1 test`
- Record budget: `18–22 Features; 12–18 Modules; small invariant/exclusion delta only`
- Deletion/dependency/UI/Schema/generated changes: `no`
- Target validation / expected time: `semantic focused test + generator to temporary output; under 1 minute`
- Max implementation retries: `2`

## Characterization

- Stable semantic should identify canonical files, aliases, entrypoints, runtime consumers, high-signal tests, owner and dependency boundary.
- `current-must-preserve`, `current-debt`, `target-acceptance`, and `transitional-allowance` must remain distinct.
- External Catalog source remains unavailable and low-confidence.

## Acceptance

- [x] Exact Feature IDs cover Slide, Flow, Spatial, Media, Components, Runtime, Interactions, Layers/Controller, Save/Recovery, Preview/Player, HTML/Web, PPTX, Print/PDF/DOCX, Diagnostics, DeveloperTab, main/preload/IPC, Legacy release, contracts, Core, and Repo Knowledge
- [x] Module records cover stable owner/dependency boundaries without copying complete imports
- [x] Every path/test/evidence entry exists and uses repository-relative POSIX form
- [x] Transitional records have removal/review gates; external source is not invented
- [x] Semantic IDs/aliases unique and deterministic
- [x] No generated/product/contract/package change

## Minimal validation

- `npx vitest run tests/unit/repoIndexSemantic.test.ts`
- Generate to an OS temporary directory and validate semantic paths/provenance
- `git diff --check`

## Rollback

- Start point: `dd0ee77430891608961e083e1635042e84fe4bad`
- Implementation commit: not created; Worker leaves a target-green uncommitted diff for Coordinator review
- Old path remains: four-feature seed and manual Bootstrap remain usable.

## Consumers and index

- Consumer delta: exact query vocabulary only; semantic coverage is `21 Features / 17 Modules`
- Legacy record IDs: semantic references may cite `LEG-001`–`LEG-010` but must not copy relation counts
- indexImpact: `semantic-update + Coordinator regenerate`

## Result evidence

- Expanded `repo-index/semantic/features.json` from 4 to 21 stable Feature records covering the complete exact-feature vocabulary required by the 25-task gate.
- Expanded `repo-index/semantic/modules.json` from 3 to 17 owner/boundary records. Every Module carries `dependencyPolicyPhase` and `dependencyPolicyMeaning: declared-policy-not-current-import-graph-compliance`, so policy is not presented as a current compliance claim.
- Preserved existing `invariants.json` and `exclusions.json`; the existing external-component-source exclusion already prevents invented external Catalog source navigation.
- Added `tests/unit/repoIndexSemantic.test.ts`: checks deterministic IDs, normalized alias uniqueness, Module references, path existence/POSIX form, transitional gates, dependency policy meaning, external Catalog exclusion, and real generator execution to an OS temporary directory.
- Validation on the claimed generator baseline: `npx vitest run tests/unit/repoIndexSemantic.test.ts` → 1 file / 3 tests passed; generator completed in the test's OS temporary directory without writing committed generated facts.
- Coordinator-review rerun after the DeveloperTab canonical-path correction: both semantic-only assertions passed; the temporary-generator assertion was blocked by concurrent out-of-scope dirty generator work (`shouldIndexSymbol` received the `Array.filter` index as `exportedLocalNames`, causing `exportedLocalNames.has is not a function`). No semantic path/alias/module assertion failed. Rerun the focused file after the IDX-01 generator worker settles; this Worker did not touch generator/tooling files.
- Scope: this task changes only Features, Modules, the focused semantic test, and this task card. Generated facts, config, generator/query tooling, package files, product source/contracts, invariants, exclusions, and other task cards remain untouched by this Worker.

## Findings / next allowed task

- Coordinator may regenerate `repo-index/generated/**` after semantic and performance work settle, then IDX-02 can bind exact `--feature` queries to these IDs/aliases.
- Exact alias lookup should use the same NFKC + lowercase + whitespace/underscore/hyphen normalization enforced by the focused test.
- `feature:legacy-release` and `module:tooling-release` remain transitional and may not outrank normal V9 Save/Preview/Export features without an explicit Legacy query.

## Ready checklist (Coordinator)

- [x] semantic design proposal reviewed
- [x] exact Feature IDs aligned with golden tasks
- [x] write scope/lock/budget valid
- [x] no related user dirty changes
- [x] no product escalation
