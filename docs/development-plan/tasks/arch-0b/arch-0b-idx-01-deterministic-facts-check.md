# S1 Task Card — ARCH-0B IDX-01 Deterministic Facts and Check

## State and assignment

- Task ID: `arch-0b-idx-01-deterministic-facts-check`
- Phase / wave: `ARCH-0B / wave 2`
- Status: `retrying`
- Owner / Reviewer / Integrator: `Tooling Worker / Coordinator / Coordinator`
- Claimed at / released at: `2026-08-24 Asia/Shanghai / retry 1 opened 2026-08-24 Asia/Shanghai`
- Worktree / branch: `shared workspace, repo-index-only write scope / codex/architecture-stabilization`
- Baseline HEAD: `0b34638b19a1787d94c895797fac8eab475d6765`
- Claim commit: `97bfee276f1944deb4c783bb7fad10715ecbd7e3`
- Context: `bootstrap-manual`
- Freshness / relevant dirty inputs: IDX-00 integrated; concurrent representative and inventory paths are disjoint and must not be staged or modified
- Depends on: `arch-0b-idx-00-ts7-parser-spike (done)`
- Blocks: IDX-02 query/Context Pack and IDX-03 golden tasks
- Retry count: `1` (independent review found four P1 correctness gaps and four P2 robustness/test gaps before generated integration)

## Product outcome

The repository can generate and check a byte-deterministic development index that covers all three TypeScript projects and honestly reports stale or invalid input domains.

## Current fact and evidence

No `repo-index/`, generator, check command, four-domain inventory/hash, or committed facts exist. IDX-00 supplies only the adapter verdict.

## Non-goals

- No query ranking or Context Pack in this card.
- No product runtime or external-repository source graph.
- No function call graph, database, watcher, embedding, or daemon.

## Scope and locks

### Allowed write

- `scripts/repo-index/**` except query/context modules reserved for IDX-02
- `scripts/generate-repo-index.ts`
- `repo-index/README.md`, `repo-index/config.json`, `repo-index/semantic/**`, `repo-index/generated/**`, `repo-index/contexts/.gitignore`
- `tests/unit/repoIndexGenerator.test.ts`
- `package.json` scripts only (no dependency changes)
- This task card; Coordinator exclusively integrates committed generated output.

### Required read

- IDX-00 adapter/model and result evidence
- All four knowledge-system documents
- Three tsconfigs, package/lockfile, contract manifest, docs/contracts, scripts, tests

### Forbidden write

- Product source, contracts/Schema, lockfile, golden tasks/expected, other task cards
- Query/context implementation

### Hotspot locks

- `generated repo-index` — Coordinator integration owner only.

## Change budget

- Task timebox: `2 Worker days`
- Main source files: `up to 8 tooling/test files plus semantic/generated data`
- Public exports: `generator/check internal API only`
- Deletion/dependency/UI/Schema changes: `no`
- Generated diff: `yes, only via deterministic generator`
- Target tests / expected validation time: `focused generator tests + double generation + check + typecheck; under 2 minutes target, generation under 10 seconds`
- Max implementation retries: `2`; max design attempts: `3`

## Characterization

- Successful input: current repository and three tsconfigs are valid.
- Known gap: no strict freshness or path-domain ownership exists.
- Tooling only; no save/preview/history implications.

## Acceptance

- [x] files/symbols/edges/tests/contracts/scripts/docs manifests generated
- [x] four mutually exclusive domains and input inventory validated
- [x] no generated self-reference, unowned indexed input, absolute path, HEAD, time, or machine identity
- [x] same inputs produce byte-identical outputs
- [x] `--check` uses temporary output and leaves worktree unchanged
- [x] shared files de-duplicated with project membership
- [x] package scripts accurately expose generate/check

## Minimal validation

- Focused generator unit test
- Generate twice to separate temporary directories and byte-compare
- `npm run repo:index:check`
- `npm run typecheck`
- `git diff --check`

## Rollback

- Start point: IDX-00 integration commit
- Pure implementation commit: `not created; Worker was instructed not to commit`
- Generated integration commit: `pending Coordinator review/integration`
- Old path remains: manual Bootstrap remains authoritative until IDX-03 gates pass.

## Consumers and index

- Consumer delta: adds development tooling only
- Legacy record IDs: none
- indexImpact: `self-generated by Coordinator`

## Result evidence

- Generated facts: `files.jsonl`, `symbols.jsonl`, `edges.jsonl`, `tests.jsonl`, `contracts.json`, `scripts.json`, `docs.json`, strict `manifest.json`, and sorted `input-inventory.jsonl`. File/Symbol/Edge/Test facts and contracts/scripts/docs top-level records all carry `schemaVersion`, `origin`, `statusClass`, and `evidence`.
- Strict domains: `647` inputs total — `source=624`, `semantic=4`, `config=4`, `tool=15`. `repo-index/config.json` owns roots/exclusions; every indexed path has exactly one domain. The three tsconfigs plus repo-index config are config inputs; generator/adapter/package/lockfile are tool inputs. Generated output, contexts, archived tasks/reviews, live task cards and derived `TASK_BOARD.md` are excluded. Excluding live task state prevents the same integration's `target-green → done` transition from self-staling strict output; task cards remain the task-status truth.
- Three-project coverage: `tsconfig.json=533`, `tsconfig.electron.json=90`, `tsconfig.e2e.json=115`; shared files are de-duplicated while retaining all project memberships.
- Fact counts: `647` files, `5,340` high-signal symbols, `13,355` edges, `1,460` test/describe nodes, `26` contract entries, `76` script entries and `77` indexed Markdown documents. Automatic edges cover contains/imports/type/dynamic/re-export/exports/entrypoint/tested-by/contract-reference without adding a call graph.
- Determinism: the focused test generated into two independent OS temporary directories and byte-compared every file; outputs were identical. Text hashing normalizes UTF-8 BOM and CRLF/CR to LF. Arrays, keys, paths and JSONL records have fixed ordering.
- Check behavior: `npm run repo:index:check` regenerated into an OS temporary directory, compared committed generated bytes, returned clean, and the focused test proved generated content hashes and mtimes were unchanged.
- Freshness characterization: a concurrent ARCH-0A performance-script edit made the first final check report the affected inventory/files/manifest/symbol outputs as stale; regeneration on the new source snapshot restored a clean check. Live task-card/board transitions did not affect strict hashes because they are explicitly excluded rather than masked with HEAD or time.
- Purity: strict output contains no generated self-input, HEAD, time, absolute repository path, username/machine field or CR/BOM. Semantic validation requires real repository-relative evidence paths and keeps current debt separate from target acceptance.
- Performance: latest default generation completed in `1.38 s`; latest check in approximately `1.34 s`; both remain below the `10 s` gate.
- Size: generated output is `9,742,492` bytes. It contains no copied source bodies. The size is dominated by `13,355` readable relationship records (`edges.jsonl` about `6.14 MB`), followed by symbol and test records; repetition is stable IDs plus required provenance/evidence/status fields rather than duplicated source text.
- Validation: focused generator suite passed `3/3`; adapter regression passed `3/3`; `npm run typecheck` passed all three TypeScript configs; generate/check and diff hygiene passed. `package-lock.json`, dependencies, product source, contracts/Schema, query/context and other task cards were not modified.
- Remaining review: Coordinator must review and integrate tooling/semantic/generated changes; manual Bootstrap remains authoritative until IDX-03 gates pass.
- Independent review finding: the pure generator commit `dd0ee77430891608961e083e1635042e84fe4bad` is retained, but generated integration is blocked until CRLF/LF byte identity, contract artifact tamper detection, inline type-import classification, local export edges, query-suffix resolution, runnable test command metadata, Markdown reference/fence/anchor validation, and Windows existing-target atomic swap coverage are repaired and rerun.

## Ready checklist (Coordinator)

- [x] IDX-00 integrated
- [x] adapter verdict and context fresh
- [x] paths/locks/budget valid
- [x] generated owner available
- [x] no relevant user dirty changes
- [x] no product escalation
