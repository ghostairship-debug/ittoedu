# S2 工具与材料验收

状态：以 `v1.5.0-rc.1` 为基础，已按 Owner 补充决定将 PPTX 调整为部分导入（当前工作区补丁，未重打标签），等待 Owner 教师复核。此文件没有代替教师签署。S1 已签署；按当前路线，下一步须在此完成 1.4–1.5 的人工门，才能进入 1.6。

## 可直接使用的文件

固定目录：`output/S2-acceptance/`。

| 文件 | 用途 |
| --- | --- |
| `S2-分数验收.h5lesson` | 在编辑器打开；26 次正式 Builder 工具事务生成 4 个 Surface、6 个位置，覆盖三种表面、单选互动、动态 Component、Flow 表格与 Spatial 路径/关系/镜头。 |
| `S2-分数验收.html` | 独立浏览器离线查看相同课例。教师控制器默认折叠，先点“展”；跨表面后按实际状态展开。 |
| `可编辑分数.pptx` | 真实 PPTX，含两段文字、四个形状和一个内嵌图片。浏览器实际解析确认得到 7 个可编辑对象和 1 个素材。 |
| `部分导入的分数与表格.pptx` | 同页包含正常分数内容与表格，预览提示跳过表格，确认后保留 7 个可编辑对象与 1 个素材。 |
| `应拒绝的表格.pptx` | 仅含表格的 PPTX；应提示表格无法转换及“无可导入内容”，工程不写入。 |
| `材料示例.txt` | UTF-8 材料导入、搜索、引用与删除演练。 |
| `build.mjs`、两份输入 Markdown | 固定工程 fixture 的公开 Builder V2 输入。它们是工程验收材料，不声称是新的教学策划签署。 |

建议打开工程后另存为练习副本，下面操作都在同一副本完成。验收课例故意保留四处内容错误：选择题页的错误答案声明，以及最后演示表面的公式、图表正文与来源错误。它们用于检查定位与修复，不是给学生的正式教学结论。

## 教师复核

| 步骤 | 操作与预期结果 | 签署 |
| --- | --- | --- |
| 1 | 查看第一演示页；连续点击分数 Component，二分之一/四分之一切换。到选择题点两个选项，反馈正确；进入 Flow 与 Spatial，正文表格、路径、箭头和镜头正确可用。 | 待 Owner |
| 2 | 在三种表面分别修改已生成的内容，执行 Undo/Redo，保存并重开；普通内容仍可编辑，资源和稳定身份不丢失。global 内容和导航归属正确。 | 待 Owner |
| 3 | 创作工具 → 教学材料库：导入 `材料示例.txt`，按“分母”搜索并插入引用；删除材料后课件中已插入引用仍在。另存为后新身份材料库为空，回原工程仍可独立操作。 | 待 Owner |
| 4 | 创作工具 → 批量编辑与参考页 → 导入 PPTX：选正常文件，阅读转换说明后确认；文字、图形和图片可编辑，一次撤销删除整次导入，重做恢复；重开仍有素材。再选 `部分导入的分数与表格.pptx`，应列出表格被跳过但仍允许确认，保留 7 个可编辑对象 / 1 个素材，并可整体撤销。仅含表格的反例因无可用内容而不写入。 | 待 Owner |
| 5 | 在同一面板选“样板改写”，以刚导入页为来源，填写全部文字槽位。先试空槽或很长文字，应阻止提交；缩短后预览并确认。副本保持布局，修改副本不改来源，一次 Undo/Redo 与保存重开一致。 | 待 Owner |
| 6 | 打开工程检查，依次定位四个 `content-*` 提醒。修正公式、答案、图表数值与来源后，对应提醒消失；检查本身没有写入或自动改答案。数学解析与渲染警告按不同规则呈现。 | 待 Owner |
| 7 | 从练习副本进入 Player、导出单 HTML 与适用格式；文字与图片正确、互动可连续操作。Slide PPTX 保持受支持原生对象可编辑，Flow DOCX 保留正文/表格，Spatial 静态相机结果按现有合同复核。 | 待 Owner |

工具 revision/stale、动态源码三个准入硬门、坏源码与迟到结果零写入、Builder 低信息输入和资源事务失败注入的工程证据见 [1.4 复核](../reviews/1.4-tools-builder-2026-09-06.md)与 [1.5 复核](../reviews/1.5-materials-content-2026-09-06.md)。教师不需要手工重演故障注入。

## 明示边界

- PPTX 是受限转换：复杂表格/图表、占位符、分组等暂时跳过并提示；动画、链接和部分视觉效果省略后保留静态内容，无法读取的背景用白色；需要保留这些内容时可先在 PowerPoint 转换；字体由当前系统解析，文本框内边距归零。不是 PowerPoint 全保真导入。
- 样板改写当前提供普通 Native 页的文字槽位；带动态载体、交互或状态覆盖的参考页明确拒绝。
- 内容 QA 检查明确语法和可唯一比较的数据，不证明任意自然语言的学科正确性或来源真实性。固定演练中的 `答案：` 要填写选项原文，图表主张格式见复核文档。
- 固定 Component 通过文件树构建，因此还可能提示没有原 ZIP hash、缩略图或来源记录；这些提醒不表示组件不能运行。Spatial world Runtime 仍无正式动态宿主，工具明确拒绝；Spatial 动态内容使用 Component。
- OpenMAIC 未采用，不阻塞 S2。未发布安装包或对外发行，`v1.5.0` accepted 标签须在 Owner 确认 S2 后创建。

Owner 结论：待填写。

## 工程复现

当前文件已生成，无需为教师复核重新构建。若需在同一候选源码恢复验收目录，从仓库根运行：

```powershell
node tests/fixtures/s2-acceptance-case/prepare-inputs.mjs
npm run build:courseware-case -- --case-dir output/S2-acceptance --builder build.mjs --project S2-分数验收.h5lesson --html S2-分数验收.html
```

第一步准备并实际检查正常、部分导入和无可用内容的 PPTX，第二步经产品 Builder V2 Facade 重建工程与离线 HTML。完整 receipt 由构建命令输出。
