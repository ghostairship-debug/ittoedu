# S4 软件内 AI 产品验收

状态：**未签署**。签署点为 [`r20-050-owner-acceptance`](../roadmap/2.0/r20-050-owner-acceptance.md)，随后由 [`r20-060-release`](../roadmap/2.0/r20-060-release.md) 发布同一候选的 `v2.0.0` 源码与固定课例离线 HTML（[2.0 路线](../roadmap/2.0/README.md) `:19`、`:35-36`）。按[工作协议](../WORKING_PROTOCOL.md) `:75`，无后缀 `vX.Y.Z` 只在四个既定 Owner 签署点写入，保全矩阵随签署点一并晋升。截至 2026-09-21 本清单未执行：[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第四节记录"050 Owner S4 签署只能由 Owner 本人复核并签署，非自动化可代""**未创建任何 `v2.0.0` 标签**；最新标签仍是 `v1.9.0-rc.1`（`git tag --list` 共 15 个标签）"。本清单只作签署点待执行清单，**不预填通过**。

真实自动化最多证明 `engineering candidate`；`accepted` 只能来自 Owner 对内部全链条、速度/质量和 PPTX 当前候选的实际复核（[工作协议](../WORKING_PROTOCOL.md) `:75`、[开发总纲](../../../COURSEWARE_DEVELOPMENT_PLAN.md)）。

## 签署点复核

| 步骤 | 操作与预期结果 | 通过／不通过判据 | 签署 |
| --- | --- | --- | --- |
| 1 | 在软件内完整走一次整课生产链：材料处理 → 教学设计 → 呈现 → 生成 → 修改 → QA/修复 → 保存重开 → 导出，全程不另开外部 AI、终端或 Builder。 | [`r20-020-public-authoring`](../roadmap/2.0/r20-020-public-authoring.md) 的 Acceptance：所有课件步骤在软件内部完成，三表面 Native/Component/Runtime 实际生成、修改、QA 修复、保存重开与导出有证据。标准整课 ≤30 分钟按冻结任务/材料/模型实测（[2.0 路线](../roadmap/2.0/README.md) `:13`）。不通过：任一必需步骤只能靠外部工具完成，或用首屏时间冒充整课完成。 | 未执行 |
| 2 | 分别用 Codex／Claude／OpenCode 各跑一次同一有限矩阵，记录分阶段与总耗时、失败原文。 | [`r20-040-three-cli-acceptance`](../roadmap/2.0/r20-040-three-cli-acceptance.md)：失败完整保留，少量模型样本不冒充可靠 p95；每条命名用例必须固定授权模型路由——[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第二节"040 最终门 9 条命名真实用例中 6 条未固定授权模型路由"条目指出 6 条命名用例只切 CLI 不配模型/强度，即便通过也不能证明授权路由。不通过：把未固定路由的通过读作路由证据。 | 未执行 |
| 3 | 核对 025 的原生能力与课件质量对等结论：逐项给出 G01–G12 的关闭证据或未关闭状态。 | [`r20-025-plugin-workflow-parity`](../roadmap/2.0/r20-025-plugin-workflow-parity.md) 的 G01–G12 定义与状态；缺少插件时按该规格只记录未完成对照，不得把无证据写成通过。**未执行（非环境阻塞）**：**（2026-09-21 复核订正）** 原写"本机 `code` 指向 Cursor 3.20.21 而非 VS Code"**不成立**——`where.exe code` 有 4 条，第 3/4 条即真实 VS Code `D:\Users\74755\AppData\Local\Programs\Microsoft VS Code\bin\code(.cmd)`，以绝对路径调用其 CLI 得 `code --version` → 1.133.0、`code --list-extensions` → 列出 `anthropic.claude-code` 与 `openai.chatgpt`，即**对照所需的两家插件都已安装**（[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第四节）。对照本轮未执行的原因是它需要交互式 GUI 会话与跨多个课例任务的付费模型运行；两个扩展的**登录状态未知**。不通过：把本机无法执行的插件对照写成已通过，或反过来把本机具备条件的对照写成"无法执行"。 | 未执行 |
| 4 | 只用键盘与读屏完成设置、引用、编辑位置与候选应用方式选择、提问/纠正、变更审阅、恢复和删除，并在实际窗口/读屏工具下复核后记录具体限制。 | [`r20-030-docs-accessibility`](../roadmap/2.0/r20-030-docs-accessibility.md) `:10`（帮助文档与实际三 CLI/Skill 行为一致）与 `:32`（实际窗口/读屏工具复核）。**环境阻塞**：本机只有 Narrator 10.0.26100.8972 与 SDK Inspect/AccEvent，无 NVDA、无 JAWS；聊天转写区 `src/renderer/ui/chat/CourseChatTranscript.tsx:92` 是 `role="log"` 的 live region，播报策略见同文件 `:45-55`，但**只经单元测试验证、未经任何真实读屏工具验证**（[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第四节"030 真实读屏流程"条目）。不通过：以静态 aria 属性或单元测试代替读屏实测。 | 未执行 |
| 5 | 检查材料引用、删除与 Save As 数据控制，以及首次外部处理说明的实际载荷描述。 | [`r20-022-materials-privacy-controls`](../roadmap/2.0/r20-022-materials-privacy-controls.md) 与 [`r20-011-first-use-risk-notice`](../roadmap/2.0/r20-011-first-use-risk-notice.md)：AI 记录不进工程或导出。**已知未闭合**：首次外部处理说明的引用清单不完整（Main 注入的课例文档全文、材料片段与材料原件不在清单中，[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第二节"首次外部处理说明的引用清单不完整"条目与第九节 9.7(f)）；说明确认记录不含 CLI，一个 CLI 的确认会静默覆盖另外两个（同记录第二节"说明确认记录不含 CLI"条目；**该条已订正为"当前工作树已修复、尚未提交"**，见第九节 9.3 第 4 条）。发布物数据边界本批新增自动化检查（`scripts/releaseArtifactBoundary.ts` + `tests/unit/releaseArtifactBoundary.test.ts`，接入 `scripts/verify-release.ts`，见同记录第九节 9.2 第 2 条），但**未在真实打包产物上端到端验证**（本机无 `release/` 与 `app.asar`；同记录第九节 9.1(a)"未验证"段记"未构建打包产物"，第三节"022/060 的发布物数据边界无自动化检查"条目仍按未跑登记）。不通过：在以上未闭合时宣称数据边界已验收。 | 未执行 |
| 6 | 用真实 PPTX 走一次导入增强的生产创作：支持范围可编辑且实际显示/播放正确，每类未支持项准确标出页码、类型与原因。 | [`r20-041-pptx-production-acceptance`](../roadmap/2.0/r20-041-pptx-production-acceptance.md)。**未跑**：041 的 7 项未在本候选上运行，`output/playwright/r20-pptx/` 只有 45 字节 `.last-run.json`，命令、过滤条件、用例数与耗时均未留存（[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第三节"041 PPTX 7 项未在本候选上运行"条目）。不通过：把不可追溯的“7 项 / 6.2m”当作证据。 | 未执行 |
| 7 | 核对架构与保全边界：唯一 Store／唯一工程真相／唯一 History，raw Store consumer 为 0，保全矩阵随签署晋升。 | `npm run check:legacy-inventory` 与 `npm run check:preservation` 在候选上的实际输出（[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第一节"架构 §8 棘轮"与"保全矩阵"两行：`confirmedEndpointCount:0`、`unknownCount:0`、`tokenHits:0`；`1.1 保全检查通过：candidate e39052fc…，27 个 automated pass；Owner 观察未签署：PM-01`）。该自动化结果**不是** Owner accepted。不通过：以自动化全绿代替签署，或新增第二个工程真相/历史。 | 未执行 |
| 8 | 冻结固定课例离线 HTML 与源码身份，真实断网打开，确认无 AI 记录/凭据。 | [2.0 路线](../roadmap/2.0/README.md) `:19`、`:46`：HTML 签署前生成并冻结，签署后不重生成；060 复用同一候选只核对身份、数据边界并实际断网打开，不重复执行会重新生成 HTML 的命令。冻结基准的字节同一性见[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第一节"冻结基准 HTML 未重生成"一行与同节"冻结 HTML 的字节同一性"段（`render-host-benchmark-v2.html 3798509 bytes`，SHA256 `3A4AD02C…E2777`）。不通过：为验收重新生成 HTML，或声称证据已随候选保存（`output/` 被 `.gitignore:10` 忽略，原始制品不在提交内）。 | 未执行 |
| 9 | Owner 在真实应用内签署 S4，再创建无后缀 `v2.0.0` accepted 源码标签与固定课例离线 HTML。 | 无后缀标签只在签署点写入（[工作协议](../WORKING_PROTOCOL.md) `:75`）；当前 `git tag --list` 无 `v2.0.0`。不通过：在 S4 未签署时创建 `v2.0.0`，或把工程候选读作 `art candidate`／`accepted`。 | 未执行 |

## 明示边界

- 本文件在 2026-09-21 作为签署点待执行清单创建。**创建本身不是 S4 通过**，也不表示上表任何一步已在本候选上运行。
- 签署点前的工程门状态必须如实带入本清单：**（2026-09-21 复核订正）** [当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第二节 2.1 原先记录 `npm test` 在候选上为红（安静机器 9 文件/14 测试失败，并发重跑 11 文件/15 测试失败，失败集随负载浮动），并据此写"`npm run verify` 不可能通过"。**该结论已被实测推翻**：在当前工作树跑全量 `npx vitest run --maxWorkers=2` → `Test Files 461 passed (461)`、`Tests 4438 passed | 3 skipped (4441)`、**exit 0**（461 与 `tests/unit` + `tests/integration` 的文件数一致，无文件被跳过）。原红是**并发负载下的抖动**，不是工程质量缺陷；其中 4 条 Markdown 链接失败确由本记录此前缺失导致，该文件现已存在（`tests/unit/repoIndexGenerator.test.ts`、`repoIndexSemantic.test.ts`、`repoIndexQuery.test.ts` 重跑 26 项通过、exit 0）。**仍不得**把这一次绿读作"自动化门已全绿"：`check:legacy-ready`／`check:legacy-zero` 仍硬失败（台账摘要漂移），`npm run verify` 本体未跑。
- **未跑（不得读作通过）**：`r20MarkdownSelectionAiLuna.spec.ts`、`r20FlowSelectionAiLuna.spec.ts` 需 `R20_*_LUNA_RUN` 门控，本候选未运行；020 的标准整课 ≤30 分钟目标未验证（唯一整课实测 57.4 分钟，超过目标）；上下文卡片只覆盖 Markdown 文件与 Flow 两个表面；021 的完整方法未迁入（选择只按 purpose 二分，`skillRoots` 声明零消费者）。逐条见[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第三节"未跑（不得读作通过）"。
- 本清单不重开 1.9 签署：`v1.9.0-rc.1`（`b4751145`）继续有效，本轮只新增 2.0 分项工程候选（[当前验收记录](../reviews/2026-09-21-r20-contextual-acceptance.md) 第六节第 8 条、第七节）。
