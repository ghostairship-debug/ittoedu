# 权威关系、已激活路线与基线策略

## 1. 当前治理事实

Owner 已于 2026-08-24 决定立即稳定化。治理采用 **exactly-one-active**：根 `COURSEWARE_DEVELOPMENT_PLAN.md` 是唯一长期总纲，`docs/development-plan/` 是其下唯一激活的详细执行子计划；旧 dated plan、三份根评估和 `docs/tasks/editor-1.0/` 只保留历史证据，不再发出当前任务。

根入口继续保留：

- V9 Schema 软冻结；
- 已完成 T/P/Q/F/G 任务不可重做；
- 教师 `accepted` 前不得宣称发布；
- 两个课件 Skill 的路由；
- 当前详细执行子计划与统一任务协议入口。

这一激活决定不改写历史任务状态，也不代表教师已 `accepted` 或 Editor 1.0 已发布。ARCH-0A 对根入口的修改只是传播已经生效的决定，不是再次等待授权的前置。

## 2. ARCH-0A 治理落盘门禁

治理提交应准确记录：

1. 稳定性、可维护性与架构统一成为当前优先事项；
2. 本包是该稳定化路线的执行子计划；
3. 教师验收不再阻止稳定化施工；可以随后进行或与稳定化并行，但不得被伪造为已经通过；
4. 任何版本仍不得在未获教师确认时宣称 `accepted` 或正式发布；
5. 原任务包在新计划完全接管并保留证据前不得删除；
6. 所有阶段使用 `ARCH-*`，不复用历史车道编号；
7. V9 软冻结与 Skill 路由原样保留；
8. 本轮以“软件做减法”为原则：先减少重复真相、旁路和无消费者代码，不新建第三模式、新代码工作区入口或平行框架。

## 3. 推荐的治理关系

```text
AGENTS.md
  ├─ 产品与协议硬约束
  ├─ 课件创作 Skill 路由
  ├─ 当前激活计划指针
  └─ 任务执行协议指针

COURSEWARE_DEVELOPMENT_PLAN.md
  ├─ 唯一长期总纲、产品结果与发布状态
  └─ 指向唯一激活的详细稳定化子计划

本包
  ├─ 架构与阶段执行
  └─ 不自行覆盖协议和产品验收结论
```

不得创建第二份活跃总纲、第二份详细执行子计划或平行任务板。

## 4. 基线记录

ARCH-0A 必须记录：

- `git rev-parse HEAD`；
- 工作树是否干净；
- Node/npm/OS；
- `package-lock.json` 状态；
- 当前合同和能力索引 check 结果；
- 当前 `verify` 结果或已知失败；
- 三份代表工程；
- 核心编辑、保存、预览、导出截图/录屏；
- 一组性能与交互特征数据。

本计划整合开始时的工作树 HEAD 是 `dbe518ea2223b994534e9f02d228873f264ffe11`，产品 TypeScript 源码与其父提交 `690411d4a101b4020134712108262bddf08e0d2e` 一致；计划落盘后的新 HEAD 必须由 ARCH-0A 重新记录。`dbe518e` 并非纯文档提交：它同时刷新了 `artifacts/ai-capabilities/` 下三份生成物。因此基线必须分别记录“产品源码”和“生成能力/外部输入”，不得简写为“HEAD 仅增加文档”。

当前 `check:contracts`、`check:ai-capabilities` 和 TypeScript 类型检查已通过。当前 AI 能力快照报告外部组件目录 `available`、共 4 个包；这是本机当前外部状态，可随目录变化，不是持久合同或架构不变量。

## 5. 工作树卫生

基线前：

```bash
git status --short
git ls-files --others --exclude-standard
```

生成物存在未提交差异时：

- 先运行对应 `--check` 或生成器判断是确定性刷新还是异常；
- 合法刷新单独提交；
- 不明差异不得夹带进架构任务；
- 不未经确认回退用户修改。

## 6. 基线标签与回退点

建议建立：

```text
pre-architecture-stabilization-202608
```

或同等不可歧义 Tag，并保存代表工程。每个 ARCH 阶段有独立完成提交；出现跨模块系统性回归时回退到最近阶段点，而不是在失控分支上继续补丁。
