# G1E HANDOFF
- 范围：FlowWorkspace.tsx 中同步 `useEditorStore.flowTextEdit` 到局部 `editRef`/`edit` 状态与 restyleToken；flowWorkspace.test.tsx 增加单测验证 store 修改（如斜体 run）能驱动 `flow-inline-editor` innerHTML 重建。
- 合同是否变化：否
- 分支 / SHA：cursor/g1e-text-edit-sync-0ab9 / 待提交
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/flowWorkspace.test.tsx` (9 tests passed)
  - `git diff --check` (clean)
- 未验证（交给 T6）：完整 PropertiesTab 与纸面联动端到端集成、跨页面类型切换。
- 停下来的原因（若有）：无
- 下游：Lane G / 集成测试 (T6)
