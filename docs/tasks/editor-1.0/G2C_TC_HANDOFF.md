# G2C-TC HANDOFF
- 范围：tests/unit/flowBlockContextToolbar.test.tsx 中的 FlowTextEditSession stub 补齐必填字段，移除不存在的 cursor 字段与 as FlowTextEditSession 类型断言
- 合同是否变化：否
- 分支 / SHA：cursor/g2c-tc-fixtures-0ab9
- 允许列表外改动（必须空，除非重命名机械 import）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/flowBlockContextToolbar.test.tsx`: 1 test file passed, 5 tests passed
  - `npm run typecheck`: 0 errors
  - `git diff --check`: clean
- 未验证（交给 T6）：全量集成测试
- 停下来的原因（若有）：无
- 下游：车道 G 集成 / 父代理合入
