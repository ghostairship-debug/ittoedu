# G3B HANDOFF
- 范围：实现 Flow media 与 component 的 wrap 绕排（none / left / right）以及内容浮层的 paperSpace（paper 跟随稿纸滚动、viewport 固定视口），并把转浮层命令默认 paperSpace 设为 paper；PropertiesTab 提供 media wrap 下拉选择。
- 合同是否变化：否
- 分支 / SHA：cursor/g3b-wrap-paperspace-0ab9
- 允许列表外改动（必须空）：无
- 最小验证命令与结果：
  - `npx vitest run tests/unit/flowSurfaceHost.test.ts tests/unit/flowWorkspace.test.tsx` (2 test files passed, 31 tests passed)
  - `git diff --check` (clean)
- 未验证：全套桌面/e2e
- 停下来的原因（若有）：无
- 下游：Lane G / 集成测试 (T6)
